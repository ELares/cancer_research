# Calibration Status — the one honest accounting

This is the **single place** that states, for every simulation layer in the
suite, whether its parameters are calibrated to independent data, partially
anchored, only self-consistent, or uncalibrated placeholders. It exists because
the suite grew fast (the `sim-tme-3d` 3D track added more than a dozen "realism
layers" on top of the original 2D/single-cell engine), and the calibration
caveats for those
layers were scattered across module doc-comments, two READMEs, and
`3d_validation_report.md`. A reader deserves one table, not a scavenger hunt.

**The headline, stated plainly:** the simulation suite is **broad but mostly
uncalibrated**. A small core is anchored to literature or sensitivity-tested;
the rest is *mechanistic scaffolding* — it shows the *shape* of an effect
(direction, qualitative behavior, spatial structure), not a *quantitative*
prediction. Breadth of coverage is **not** evidence of depth (see the repo's
`CLAUDE.md` guiding principles). None of the uncalibrated 3D layers feed the
manuscript's quantitative claims; the manuscript's numbers come from the 2D
engine and are themselves labeled order-of-magnitude (see
`article/drafts/v1.md` §8.4).

This document is **handwritten interpretation**, kept in sync by hand. The
machine-checked authorities remain:
- `parameter_provenance.md` — per-parameter table for the **core** engine
  (`Params`, `SpatialParams`, `ImmuneParams`, `RecoveryRates`) + RSL3 PK +
  persister + photosensitizer PK, with literature citations and a `Grounded?`
  column.
- `targets.yaml` — the 8 machine-evaluated calibration/self-consistency targets
  (read by `calibrate.py`).
- `3d_validation_report.md` — what the 3D capstone's Q1–Q4 self-consistency
  checks actually produce, and the 2D-vs-3D magnitude differences.
- Source code (`ferroptosis-core/src/*.rs`) — authoritative for default values.

## Status legend

| Tier | Meaning |
|------|---------|
| **Calibrated** | At least one key parameter fit/anchored to an independent published measurement. |
| **Partially anchored** | Structure or a subset of parameters has a literature basis, but the headline magnitude is not fit to data. |
| **Self-consistency only** | A regression-guard that the model reproduces its *own* hard-coded physics; not an independent calibration. |
| **Uncalibrated (illustrative)** | Placeholder parameters chosen for plausible behavior. The mechanism/direction is the claim; the magnitude is not. |

## Core engine and 2D spatial work

| Layer | Module(s) | Status | Notes / what would calibrate it |
|-------|-----------|--------|--------------------------------|
| Core ferroptosis biochemistry | `biochem`, `params` | **Partially anchored** | Mix of literature-grounded and estimated rate constants (see `parameter_provenance.md` `Grounded?` column). Directionally robust: ±50% sensitivity held in 22/22 conditions (manuscript §5, Chapter sensitivity analysis). Magnitudes are estimates. |
| Single-cell RSL3 kill switch (in-vitro) | `biochem`/`params` (`lp_propagation`, `lp_rate`) | **Calibrated (in-vitro, held-out validated)** | #330: the single-cell RSL3 death rate is fit to CTRPv2 GPX4-inhibitor median dose-response (`scripts/calibrate_kill_switch.py`; target `analysis/calibration/ctrpv2_ferroptosis_curves.csv`). Fit on ML162 (RMSE 0.05), held-out validated on ML210 (RMSE 0.07, same GPX4-inhibition mechanism); the default in-vivo-tuned switch is ~11x worse (RMSE 0.57, far too RSL3-resistant for in-vitro cell-line data). Calibrated `lp_propagation=0.7`, `lp_rate=0.4`, µM-to-intensity scale `K=0.25` µM. IN-VITRO only: the in-vivo / spatial production defaults are unchanged, so this anchors the single-cell switch to in-vitro data without altering the production matrix. A distributional fit of the cell-line spread (an order of magnitude in EC50) to the model's phenotype heterogeneity is the documented next extension. See `analysis/calibration/kill-switch-calibration.md`. |
| GPX4 recovery kinetics | `params` / `RecoveryRates` | **Calibrated** | `targets.yaml: gpx4_recovery_rate` — consensus from multiple ferroptosis studies. |
| MUFA / PUFA protection | `biochem` | **Calibrated (composite)** | `targets.yaml: mufa_protection_factor` (18.6×) — model-derived composite of Dixon/Park (unpublished 2025 submission, no PMID) + Tesfay 2019 (PMID 31270077, SCD1 protects ovarian cancer from ferroptosis). Not a single measurement. |
| Persister FSP1/HDAC suppression | `biochem` | **Calibrated** | `targets.yaml: fsp1_hdac_persister` — Higuchi et al., Science Advances 2026 (PMID 41481741). |
| PDT optical attenuation | `physics`, `oxygen` | **Self-consistency only** | `targets.yaml: pdt_depth_attenuation` verifies the hard-coded `pdt_mu_eff` reproduces Jacques 2013 optics — checks the code, not biology. |
| SDT acoustic attenuation | `physics` | **Self-consistency only** | `targets.yaml: sdt_depth_attenuation` verifies `sdt_alpha` vs Cobbold 2007. |
| Exo-ROS kill threshold (source-independence) | #464 | `biochem`/`params` | **Partially anchored (form + order of magnitude)** | The model feeds SDT and PDT exogenous ROS into ONE bistable death switch with a single `death_threshold` and `sdt_ros == pdt_ros`, i.e. the dose-to-kill is a property of the cell/tissue, not the ROS source. #464 validates that design choice against Zhu et al. 2015 (PMID 25927018, PMC4410434), who measured the in-vivo reacted-singlet-oxygen necrosis threshold [1O2]rx as approximately PHOTOSENSITIZER-INDEPENDENT: Photofrin ~0.56, BPD ~0.72, mTHPC ~0.40 mM (~0.5 mM, within a factor of 1.8 across three chemically distinct sensitizers). So a single source-independent kill threshold is the right structure. SCOPE: the model is dimensionless, so this validates the FORM (single source-independent threshold) + order of magnitude, NOT a unit-rigorous calibration of `death_threshold=10.0`. The O2-DEPENDENCE form is only partly addressed: the model's `oxygen::o2_dependent_exo_factor` (#336) is LINEAR in O2 while Zhu's macroscopic model makes the singlet-O2 quantum yield a saturating (Michaelis) function of [O2]; the linear form is a first-order approximation and the precise oxygen-quenching constant is in the full text (flagged, NOT fabricated). `scripts/validate_pdt_threshold.py` (pure stdlib, runs in CI, drift-guards `sdt_ros`/`pdt_ros`/`death_threshold`); committed `analysis/calibration/pdt-threshold-validation.{md,json}`. Calibrate the absolute threshold + Michaelis O2 constant vs the Zhu 2015 full-text macroscopic-model parameters. |
| Tumor pharmacokinetics | `tumor_pk` | **Partially anchored to measured data (#334); per-tumor presets still estimated** | The per-tumor presets stay order-of-magnitude estimates (`parameter_provenance.md`), but #334 anchors the PK STRUCTURE + key constants to the only public ferroptosis-specific in-vivo PK with a paired plasma+tumor course: imidazole ketone erastin (IKE, a system-xc- inducer; Zhang 2019, PMID 30799221). Measured tissue:plasma partition **Kp = 0.90** (vs the presets' estimated 0.15 to 0.5), a ~2 h plasma to tumor delay, and slower tumor clearance (terminal t1/2 3.5 vs 1.83 h). A closed-form check shows the IKE plasma AUC/Cmax (2.11 h) is below the hard 1-compartment floor e*Tmax (3.67 h), proving multi-compartment disposition (a fast distribution phase) and bounding what summary NCA can identify. Sorafenib (clinical, system-xc-) adds a human-scale forward check (popPK Cavg,ss ~4.1 mg/L at 400 BID, in the clinical 3 to 5 range; Jain 2011, PMID 21392074). The canonical RSL3/ML tool compounds have NO usable in-vivo PK (erastin's instability is why IKE was made), so that gap is flagged not fabricated; per-tumor-type recalibration needs per-tumor measured PK that does not exist publicly. `scripts/calibrate_pk.py`; see `analysis/calibration/pk-calibration.md` + `calibration-targets-pk.md`. |
| Tissue drug penetration | `drug_transport` | **Form + reference-drug length validated vs measured (#335); ferroptosis-inducer length + binding-site barrier unvalidated** | Exponential Krogh-cylinder approximation across 3 tissue types. #335 validates it against published MEASURED penetration data: the exponential FORM is confirmed (Primeau 2005 PMID 16361566 and Tannock 2002 PMID 11895922 both report exponential decline of drug vs distance-from-vessel in vivo), and the doxorubicin transport reference (model λ=50 µm, half-distance 34.7 µm) sits WITHIN Tannock 2002's measured 25 to 75 µm and ~13% below Primeau 2005 / Minchinton 2006 (40 to 80 µm), i.e. right form and order of magnitude, conservative (shorter) end. A drift-guard ties the validated λ to the Rust presets. NOT validated: (1) the RSL3-like λ=100 µm (no ferroptosis inducer has published spatial penetration data, so it is an extrapolation), and (2) the binding-site barrier in #335's title, because the model has NO such mechanism (uptake is linear and lumped into λ, so penetration depth is dose-INDEPENDENT; a true binding-site barrier is saturable/dose-dependent, but a data-availability review (#335) concluded the mechanism is NOT warranted for the small molecules this model targets and is deliberately NOT added: the strong dose-dependent barrier is an ANTIBODY phenomenon (Fujimori 1990 PMID 2362198, Saga 1995 PMID 7568060, Thurber/Wittrup 2008 PMID 18541331 penetration ~ sqrt(dose)), it is physically WEAK for small molecules (a deep binding sink barely saturates at achievable doses, El-Kareh/Secomb), and no extractable small-molecule dose-resolved penetration-depth data exists to validate it; so #335's binding-site clause is resolved by evidence, not by building unvalidatable complexity). So the cross-tissue comparison (manuscript §6.1) stays directional, not quantitative. #315 adds an off-by-default ECM-tortuosity factor (`λ_eff = λ/√τ`; denser ECM shortens penetration, Netti 2000 PMID 10811131 / Provenzano 2012 PMID 22439937), `τ=1` for all shipped tissues (identity/byte-identical), uncalibrated. `scripts/validate_penetration.py`; see `analysis/calibration/penetration-validation.md`. |
| Photosensitizer PK | `photosensitizer_pk` | **Partially anchored** | Distribution-phase + inter-drug ROS-yield normalization closed via #203 with literature scaling; absolute cellular PK still estimated. |
| Immune ICD/DAMP cascade (2D) | `immune` | **Uncalibrated (illustrative); direction literature-anchored (#288)** | DAMP diffusion, T-cell kill rates estimated. The 104:1 SDT:RSL3 ratio (manuscript §7.2) is "a theoretical ceiling … not a quantitative prediction." Per #288, the *direction* (SDT ≫ RSL3 immune priming) is supported by verified literature (Wiernicki 35760796; Wang 34669472; Luo 35568916; Foglietta 38232641); the 2D 104:1 over-extrapolates because the model's saturating Michaelis-Menten DAMP→activation (Kd=50) is driven deep into saturation by the dense 2D kill field, while 3D volumetric dilution keeps it sub-saturating (~4:1, more consistent with the literature). The exact ratio stays uncalibratable. |

## 3D realism layers (`sim-tme-3d` track)

All off-by-default and byte-identical when off. Each was built to model a
*mechanism*; none has been fit to independent data. Where a parameter has a
literature anchor for its **structure** (e.g. vessel spacing), it is noted.
Two rows below are **not** biology realism layers and are listed only for
accounting completeness: the **vessel spatial index** (#268) is a pure
performance optimization with no parameters, and **`dose_schedule`** (#239) is a
second-wave PK module the 3D track merely consumes (manuscript §11.2 groups it
with the second wave, not the realism layers). So the table carries a few more
rows than the "more than a dozen" realism-layer count above.

| Layer | Issue | Module | Status | What would calibrate it |
|-------|-------|--------|--------|-------------------------|
| Explicit vasculature | #191 | `vasculature` | **Partially anchored** | Inter-vessel spacing (~150 µm well / ~400 µm poor) is from Vaupel; the random-uniform *placement* and Krogh λ are uncalibrated. Calibrate vs measured hypoxic fractions / vessel maps (micro-CT, pimonidazole). |
| Vessel spatial index | #268 | `vasculature` | **N/A (exact)** | Pure performance; bit-identical to brute force. No parameters. |
| Fractal vessel topology | #268 | `vasculature` | **Uncalibrated (illustrative); structure literature-anchored** | `VesselTopology::Fractal` / `place_vessels_fractal_3d` build a fractal-branching vessel tree whose chaotic-perfusion *structure* follows Baish & Jain 2000 (PMID 10919633; tumor vasculature is fractal/space-filling, D≈1.89). The branching *constants* are placeholders: BASE_ANGLE≈35°, LENGTH_RATIO 0.80, MIN_LEN 3.0, MAX_DEPTH 14, DEAD_END_PROB 0.12, n_trunks = round(points/30) (clamped [2,64]). At matched point-count it leaves a higher hypoxic fraction than random, but the magnitude is uncalibrated. Calibrate vs micro-CT vessel-tree morphometry (segment-length/branch-angle distributions, tortuosity). Off-by-default (topology=Random) so the matrix stays byte-identical. |
| 3D radial spheroid biochem | #197, #270, #333 | `spheroid` | **Zone geometry validated with a valid size range (#333); biochem gradients uncalibrated** | Zone *volumes* match the Browning 2021 (eLife, DOI 10.7554/eLife.73020) limiting structure (necrotic core 0.73 of radius → 0.39 of volume; rim begins 0.90 of radius). #333 validated these FIXED thresholds against the Browning size-resolved confocal data (994 spheroids): they match only for **R >= 300 µm (diameter >= 600 µm)**; below that real spheroids have essentially no necrotic core (eta ~ 0 for R < 300 µm) so the model OVER-PREDICTS the persister/necrotic core for small spheroids (the core emerges near R ~ 300 µm, the O2 diffusion length). The size-aware refinement is now delivered (#333, v0.51.0): `spheroid::SizeAwareZones` (opt-in, byte-identical default) ramps both zone thresholds from 0 (small, all-proliferating, no core) up to the fixed limiting fractions (rim onset 200→370 µm, core onset 280→400 µm, first-order fits to the committed Browning bins), cutting the size-aware mean boundary abs-err to **0.025 vs the fixed model's 0.338** on those bins while reducing exactly to the fixed thresholds at large R; the params are weakly constrained (4 bins) so read the corrected DIRECTION (small spheroids are mostly proliferating with no core), not the exact radii. The per-zone MUFA/GSH/iron gradient *strengths* remain placeholders, validated structure constrains geometry only, not gradient magnitude (calibrate vs spatially-resolved spheroid metabolomics). The size-resolved KILL leg is now CHARACTERIZED (#333, `scripts/validate_spheroid_kill.py` over a `sim-tme-3d --spheroid-size-sweep`): with FIXED thresholds the supply-gradient gives bigger-spheroids-resist-more (RSL3 kill 3.6%→0.19% over 144→540 µm, ~19x, in the measured cytotoxic 1.8-22x range, so the DIRECTION + order of magnitude is validated against generic cytotoxic/PDT data), and with SIZE-AWARE thresholds a falsifiable persister-targeting prediction emerges (RSL3 targets persisters, the persister core only appears above ~280 µm, so small all-proliferating spheroids RESIST RSL3 and vulnerability emerges with the core, OPPOSITE generic cytotoxics). The kill MAGNITUDE stays uncalibrated (RSL3-resistant in-vivo switch + placeholder gradients), and a ferroptosis-inducer multi-size kill experiment (the documented falsifiable prediction) is what would validate it. See `analysis/calibration/spheroid-kill-vs-size.md` + `spheroid-structure-validation.md`. |
| Patient-scale slab | #240 | `slab` | **Uncalibrated (illustrative)** | Krogh λ ~150 µm placeholder; the "<20 % kill at 4 mm depth" is *illustrative of the scale gap*, not a validated efficacy number. Calibrate vs depth-resolved kill in thick tissue / patient PK. |
| Slab + vasculature coupling | #272 | `slab`+`vasculature` | **Uncalibrated (illustrative)** | Inherits the slab λ and vessel placeholders; combine rule (element-wise max) is a first-order Krogh approximation. |
| Depth-graded slab phenotype | #272 | `slab` | **Uncalibrated (illustrative); structure literature-motivated** | `apply_depth_graded_cells_3d` re-assigns the slab's flat bulk mix to a layered rim→core gradient (proliferating/glycolytic at the vessel-proximal +z face, persister-like in the chronically supply-starved deep (−z) layers), the depth-axis analog of the spheroid's radial zones (#197). Thresholds are on the planar supply `exp(-depth/λ)` (NOT geometric volume fractions like the spheroid, because the slab models an *absolute* depth: a `patient_deep()` 4 mm slab is uniformly persister-like, which is correct). `SlabPhenotypeConfig::literature()` cut-points (`glycolytic_supply` 0.5 / `oxphos_supply` 0.15) are placeholder magnitudes encoding the documented direction (deep, supply-starved tissue is enriched for tolerant phenotypes), not fit to data, so read the direction, not the layer counts. Scope: (1) phenotype tracks the planar depth gradient only (internal vessels (#272 coupling) raise *delivered* drug dynamically downstream but do not reshape the chronic phenotype, a future refinement); (2) only the phenotype is depth-graded (no separate static GSH/iron gradient as in the spheroid, since the supply field already deprives deep cells dynamically). Off-by-default (the matrix never enters slab mode) ⇒ byte-identical. Calibrate vs depth-resolved phenotype/marker histology (Ki-67 proliferative rim, hypoxia/quiescence markers at depth). |
| GPX4-independent suppressors (DHODH, GCH1/BH4) | #338 | `biochem`/`params` | **Uncalibrated (illustrative); mechanism literature-anchored** | `Params.dhodh_rate` (a GPX4-independent CoQ10/ubiquinol repair term in parallel to FSP1, Mao et al. Nature 2021) and `Params.gch1_rate` (a BH4 radical-trapping quench term gating autocatalytic propagation, Kraft 2020 / Soula 2020) close the gap where the model had only GSH/GPX4 + FSP1, which overstated RSL3 monotherapy kill (cells escape GPX4 inhibition via DHODH/BH4 too). Both `0.0` by default ⇒ off ⇒ byte-identical; the rates are uncalibrated placeholders encoding the direction (the backups reduce ferroptosis; inhibiting them is the GPX4i+DHODHi combination). Calibrate vs DHODH/GCH1 knockdown ferroptosis-sensitivity data. |
| Kinetic / acute MUFA start | #339 | `biochem`/`params` | **Uncalibrated (illustrative); direction literature-anchored** | `Params.mufa_acute_start` (off by default, `None`). `invivo()` starts cells at the established MUFA steady state (M_ss ≈ 0.40), which assumes the ~48 to 72 h SCD1/MUFA enrichment is already complete and so overstates early ferroptosis resistance for acute day-0 dosing (Dixon/Park lineage). `Some(x)` instead starts the cell at a naive value `x` (e.g. 0.0) and lets the existing logistic `update_mufa_protection` accumulate protection over the run, reproducing the acute-versus-established-tumor difference (acute cells are less protected early and peroxidize more under RSL3). The accumulation RATE is the existing `scd_mufa_rate`, not fit to the 48 to 72 h timescale (a step-to-time mapping the suite lacks), so the DIRECTION is the result, not a precise hours figure. `None` default ⇒ byte-identical. First PR of #339; ether-lipid (FAR1/TMEM189) and MBOAT1/2 axes are follow-up PRs. Calibrate vs time-resolved MUFA lipidomics + SCD1-inhibition resensitization kinetics. |
| Ether-lipid (ether-PUFA) pool | #339 | `biochem`/`params` | **Uncalibrated (illustrative); direction literature-anchored** | `Params.ether_pufa_fraction` (off by default, `0.0`). Polyunsaturated ether phospholipids (ether-PUFA-PE, made via FAR1/AGPS + the peroxisomal pathway) are an extra pool of oxidizable membrane lipid: the peroxidizable PUFA term is scaled by `1 + ether_pufa_fraction`, so enabling it raises lipid-peroxide accumulation and the `0` limit is the FAR1/AGPS-null escape (ether-lipid loss confers resistance). This is the robust direction (Zou 2020 Nature PMID 32939090; Cui 2021 Cell Death Differ PMID 33731874) and is the OPPOSITE sign to the loose "shift to ether-PE = escape" framing: ether-PUFA PROMOTES ferroptosis and its LOSS is the escape. The plasmalogen/TMEM189 (PEDS1) vinyl-ether sub-step is deliberately NOT modeled because its sign is contested (protective in Cui 2021 / Zoeller 1999 PMID 10051451; dispensable in Zou 2020; sensitizing-on-loss in Perez 2022 PMID 36178986). The `fraction` magnitude is a placeholder. `0.0` default ⇒ ×1.0 ⇒ byte-identical. PR 2 of #339. Calibrate vs ether-lipid lipidomics + FAR1/AGPS-knockdown ferroptosis-sensitivity. |
| MBOAT1/2 MUFA enrichment | #339 | `biochem`/`params` | **Uncalibrated (illustrative); direction literature-anchored** | `Params.mboat_mufa_boost` (off by default, `0.0`). MBOAT1 (estrogen-receptor-regulated) and MBOAT2 (androgen-receptor-regulated) remodel phospholipids toward MUFA-PE and suppress ferroptosis independently of GPX4 (Liang et al. Cell 2023 PMID 37267948), so this is a constant additive MUFA protection layered onto the dynamic SCD1 `mufa_protection` at the peroxidation sites (`total_mufa_protection`), floored at `0` (enrichment is protective-only). A higher value (e.g. an AR-driven MBOAT2-high tumor) lowers ferroptosis, the opposite direction to the ether-PUFA pool. The boost magnitude is a placeholder. `0.0` default ⇒ byte-identical. PR 3 of #339 (closes the issue). Calibrate vs MBOAT1/2-knockdown / hormone-receptor-modulated ferroptosis-sensitivity (Liang 2023 datasets). |
| Phenotype-specific MUFA rate + cap | #363, #390 | `phenotype_mufa`/`cell`/`biochem` | **Uncalibrated (illustrative); DIRECTION genuinely uncertain** | Follow-up to #339, which used one GLOBAL `scd_mufa_rate` for the MUFA accumulation dynamics. The per-cell `Cell::mufa_rate` (`None` ⇒ global, byte-identical) lets SCD1/MUFA enrichment kinetics differ by phenotype, set via `phenotype_mufa::PhenotypeMufaConfig` (per-phenotype rate multiplier; `identity()` default ⇒ no per-cell rate ⇒ byte-identical) + `apply_phenotype_mufa_3d`, wired into sim-tme-3d as `Overrides.phenotype_mufa` (gated on non-identity). The library test (`biochem` `sim_cell_step_reads_per_cell_mufa_rate`) confirms a faster per-cell rate builds more `mufa_protection` by the same step (the "two phenotypes diverge" criterion, at the right readout). The DIRECTION is genuinely uncertain and so left configurable, not baked in: MUFA enrichment confers a ferroptosis-resistant state (Magtanong 2019 Cell Chem Biol PMID 30686757, ACSL3-dependent) and SCD1 (the desaturase that supplies endogenous MUFA) protects from ferroptosis (Tesfay 2019 Cancer Res PMID 31270077), AND drug-tolerant persisters are simultaneously GPX4-dependent / ferroptosis-vulnerable (Hangauer 2017 Nature PMID 29088702). `PhenotypeMufaConfig::literature()` (persister/OXPHOS faster than glycolytic) is an UNCALIBRATED placeholder hypothesis, not fit to data. Not in the C ABI (FFI maps `mufa_rate` to `None`). Reachable from the CLI via `--snapshot=phenotype-mufa` (spheroid context). Revived clonal-repopulation cells re-derive their per-phenotype rate + cap via `apply_phenotype_mufa_at_3d` (coherent with #266/#302, the analogue of the contact re-application). NOTE: the kill COUNT is an insensitive readout for the rate in the cap-limited spheroid (the bistable switch means dying cells are not MUFA-rescuable and survivors are not MUFA-dependent), so the divergence is asserted on `mufa_protection` (library) not on spheroid kills. Per the issue's "rate/cap", a per-phenotype CAP (steady-state) multiplier is now also delivered (#390): the `*_cap` fields on `PhenotypeMufaConfig` scale each tumor cell's effective `mufa_cap` MULTIPLICATIVELY (so a cap-mul `1.0` leaves the cap untouched, preserving a spheroid radial cap; applied AFTER the spheroid so the two compose), and a steady-state divergence test (`phenotype_caps_yield_different_mufa_steady_states`) confirms a 2x-cap persister plateaus higher than glycolytic. The cap direction is even more uncertain than the rate, so `literature()`'s `persister_cap`/`oxphos_cap` are placeholders. Both axes off-by-default identity ⇒ byte-identical. Calibrate vs time-resolved per-phenotype MUFA lipidomics + SCD1-inhibition resensitization kinetics. |
| ALOX isoform peroxidation + MCFA sensitization | #446 | `alox`/`biochem`/`params` | **Uncalibrated (illustrative); direction literature-anchored** | The core engine drives lipid peroxidation through ONE generic `lp_propagation`, implicitly assuming an average enzymatic-oxidation capacity. `alox::AloxConfig` (an arachidonate-lipoxygenase isoform activity/expression mix + an MCFA level) collapses to two off-by-default ADDITIVE boosts a consumer writes onto `Params`: `alox_propagation_boost` scales the propagation rate by `1 + boost` (clamped >= 0), so an ALOX15/12/5-high tumor peroxidizes faster (ALOX15 is the canonical ferroptosis-driving isoform; ALOX12/ALOX5 contribute at isoform-specific rates varying several-fold; lipoxygenase-driven ferroptosis, PNAS 2016 PMID 27506793) and an ALOX-poor tumor slower (down to `-1`, the ALOX-null limit with no enzymatic propagation), INDEPENDENT of the GPX4/GSH/FSP1 defenses; `mcfa_pufa_boost` is added to the oxidizable-PUFA augmentation alongside the ether-lipid pool (`ether_augmented_pufa`), modeling MCFA -> ACSL4/CD36 upregulation -> more oxidizable PUFA incorporation (Sci Rep 2024 s41598-024-55050-4; MCFA ferroptosis sensitization PMC11901882). A sim-tme-3d A/B (`alox_high_mcfa_raises_ferroptosis`) confirms an ALOX15-high + MCFA tumor out-kills the balanced baseline under RSL3 (and `identity()` is a no-op). Reachable via `--snapshot=alox`. The per-isoform activity weights + MCFA kinetics are UNCALIBRATED placeholders (isoform Kcat/Km vary ~10-fold and are not fit; absolute MCFA kinetics deferred to the experimental E-series), the directions are the result; per-cell stochastic ALOX heterogeneity is a deferred refinement (this models a per-condition ALOX phenotype). Both boosts `0.0` by default ⇒ ×1.0 ⇒ byte-identical (production matrix SHA unchanged), FFI defaults them so the C ABI/header is unchanged. Calibrate vs ALOX-isoform-stratified RNA-seq + isoform-knockdown / MCFA-exposure ferroptosis dose-response. |
| ACSL4-status biomarker stratification | #444, #462 | `acsl4`/`biochem`/`params` | **Partially anchored (TCGA prevalence + z→status bridge, #462); status→magnitude still uncalibrated** | ACSL4 ligates the PUFA ferroptosis requires into membranes, the single most discriminating pro-ferroptotic lipid-metabolism gene (Doll et al., Nat Chem Biol 2017, PMID 27842070; Yang et al., PNAS 2016, PMID 27506793). `acsl4::pufa_boost_from_status(status)` maps a tumor's relative ACSL4 expression (`1.0`=wild-type) to `Params.acsl4_status_boost` = `status-1` clamped at `-1`, folded into the oxidizable-PUFA augmentation (`ether_augmented_pufa`): `< 1` (ACSL4-low/negative) collapses the PUFA substrate ⇒ intrinsically ferroptosis-REFRACTORY through a mechanism distinct from GPX4/GSH/FSP1 (e.g. some HCC/AML subtypes), `> 1` (ACSL4-high) ⇒ sensitive. A sim-tme-3d A/B (`acsl4_status_stratifies_ferroptosis_sensitivity`) confirms ACSL4-high out-kills the ACSL4-normal baseline under RSL3 while ACSL4-negative kills LESS (the patient-stratification headline: ACSL4-high cancers respond, ACSL4-negative do not). Reachable via `--snapshot=acsl4-negative`. **#462 anchors the per-cancer-type prevalence** to cBioPortal TCGA PanCancer Atlas ACSL4 mRNA (32 studies, login-free REST API; `scripts/fetch_acsl4_prevalence.py`, committed `analysis/calibration/acsl4_prevalence_tcga.{csv,json}` + `acsl4-prevalence-calibration.md`): the within-cohort low-ACSL4 prevalence is 10.8–18.8% (z<−1, median 14.4%) / median 3.0% (z<−2) per cancer type, the real population prior for the refractory-leaning tail; and `acsl4::status_from_zscore(z)=max(0,1+z/2)` bridges a real ACSL4 z-score onto the status scalar, its integer-z points reproducing the shipped NEGATIVE/LOW/NORMAL/HIGH constants exactly. The LOAD-BEARING honest negative: bulk TCGA mRNA does NOT rank HCC/AML as ACSL4-low (lihc ranks HIGHEST on raw RSEM), so the Doll-2017 refractory phenotype is a protein/subtype property bulk mRNA misses and cross-study RSEM is batch-confounded — the layer is bounded to within-cohort-z stratification. STILL data-gated: the status→ferroptosis-MAGNITUDE mapping (the linear `status-1` boost) and the protein-level (RPPA/IHC) refractory-subtype prevalence; the cell-line ACSL4-status-vs-dose-response meta-analysis (#444) would calibrate the magnitude. `status_from_zscore` is library-only / byte-identical (nothing in the matrix calls it); `status=1.0`/`acsl4_status_boost=0.0` default ⇒ byte-identical, FFI defaults it so the C ABI is unchanged. |
| ESCRT-III membrane-repair brake (death execution) | #465 | `repair`/`biochem`/`params` | **Uncalibrated (illustrative); direction literature-anchored** | A genuinely new category: every other layer modulates the redox/lipid SUBSTRATE (whether a cell peroxidizes), but this acts on the death-EXECUTION step. A cell whose lipid peroxide has crossed `death_threshold` can be RESEALED by Ca2+-recruited ESCRT-III (CHMP5/CHMP6) membrane repair for a finite per-cell budget, so more repair capacity ⇒ slower/blocked execution ⇒ more ferroptosis resistance (Dai et al., BBRC 2020, PMID 31761326; CHMP5/CHMP6 knockdown sensitizes). Two off-by-default `Params` fields: `escrt_repair_rate` (per-step rescue probability when over threshold) + `escrt_repair_budget` (finite per-cell capacity, tracked on `CellState.escrt_budget_used`); the death check in `sim_cell_step` and `sim_cell` draws the RNG roll ONLY when `escrt_repair_rate > 0` and budget remains (so the default path makes no extra draw and is byte-identical). A rescued cell still runs its defenses next step, so ESCRT buys time for GSH/GPX4 to recover under transient stress while sustained GPX4 inhibition still kills once the budget is spent (a finite delay). sim-tme-3d wires it via `Overrides.escrt` + an `escrt_repair_reduces_rsl3_kill` A/B (ESCRT-high out-survives baseline under RSL3) + `--snapshot=escrt`. Rate/budget are uncalibrated placeholders; the direction (more repair ⇒ more resistance) is the result. `escrt_repair_rate=0.0` (default) ⇒ brake never fires ⇒ byte-identical (production matrix SHA unchanged); FFI defaults both to 0.0 so the C ABI is unchanged. Calibrate vs CHMP5/CHMP6-knockdown ferroptosis-sensitivity + membrane-resealing kinetics (Ca2+-imaging / annexin time-courses). |
| POR/CYB5R1 enzymatic O2-coupled H2O2 source | #466 | `biochem`/`oxygen`/`params` | **Uncalibrated (illustrative); direction literature-anchored** | POR (cytochrome P450 reductase) + CYB5R1 transfer electrons from NAD(P)H to O2 to make H2O2, the Fenton substrate, an enzymatic O2/NADPH-dependent oxidant source distinct from the static `basal_ros` and parallel to the ALOX enzymatic-propagation leg (Yan et al., 2021, PMID 33860083, POR/CYB5R1 catalyze lipid peroxidation to execute ferroptosis; Zou et al., Nat Chem Biol 2020). `Params.por_h2o2_rate` (off-by-default 0.0) is added to `total_ros` at all three biochem sites, and `oxygen::por_o2_factor(o2_supply, dependence)` is the O2-coupling helper. sim-tme-3d injects `rate·por_o2_factor(local_o2, dep)` into each tumor cell's `basal_ros` at setup (`Overrides.por`), so POR makes LESS H2O2 in the hypoxic core, tying the Fenton-feeding oxidant to O2 (unlike a uniform oxidant, it does NOT amplify the deep-core artifact). A `por_h2o2_raises_rsl3_kill` A/B confirms more POR ⇒ more RSL3 kill; `--snapshot=por` runs it fully O2-dependent (rim-weighted). The rate + O2-dependence are uncalibrated placeholders; the direction is the result. `por_h2o2_rate=0.0` (default) ⇒ byte-identical (production matrix SHA unchanged), FFI defaults it to 0.0 so the C ABI is unchanged. Calibrate vs POR/CYB5R1-knockdown vs overexpression ferroptosis-sensitivity + measured H2O2 flux vs O2. |
| 7-DHC/DHCR7 sterol radical-trapping defense | #467 | `biochem`/`params` | **Uncalibrated (illustrative); direction literature-anchored** | 7-dehydrocholesterol (7-DHC), a distal cholesterol-biosynthesis intermediate, is a potent endogenous membrane radical-trapping antioxidant that shields PUFA from peroxyl-radical autoxidation, gating the autocatalytic peroxidation chain (Freitas/Li et al., Nature 2024, '7-dehydrocholesterol dictates ferroptosis sensitivity,' PMID 38297130, DOI 10.1038/s41586-023-06983-9). `Params.dhc7_radical_trap` (off-by-default 0.0) is added to the GPX4-independent radical-trapping quench at both biochem sites (alongside `gch1_rate`/FSP1), so a higher 7-DHC pool raises the quench and LOWERS the propagation rate ⇒ ferroptosis RESISTANCE. DHCR7 consumes 7-DHC, so DHCR7-loss raises the pool (the modeled escape, resistance) and EBP/SC5D-low lowers it (sensitization). A sterol-pathway radical-trapping defense distinct from GPX4/GSH/FSP1/DHODH and from iron/MUFA. A `dhc7_radical_trap_reduces_rsl3_kill` A/B confirms a DHCR7-low tumor out-survives the baseline under RSL3; `--snapshot=dhc7`. The pool magnitude is an uncalibrated placeholder; the direction is the result. `dhc7_radical_trap=0.0` (default) ⇒ byte-identical (production matrix SHA unchanged), FFI defaults it to 0.0 so the C ABI is unchanged. Calibrate vs DHCR7-knockout/EBP-modulation ferroptosis-sensitivity + measured 7-DHC pool size vs RSL3 IC50. |
| Dynamic labile iron (NCOA4 ferritinophagy) | #340 | `biochem`/`oxygen`/`params` | **Uncalibrated (illustrative); direction literature-anchored** | The static-iron model holds `cell.iron` fixed. `Params.ferritinophagy_release` (off by default, `0.0`) + `biochem::ferritinophagy_iron_factor(step, params)` scale the Fenton iron by a time-dependent factor that ramps from `1.0` toward `1 + release` (time constant `ferritinophagy_tau` steps), representing NCOA4-mediated autophagy of ferritin releasing stored iron into the labile pool over the run (Mancias 2014 Nature PMID 24695223; Hou 2016 Autophagy PMID 27245739), so enabling it raises Fenton ROS and ferroptosis. A companion `oxygen::hypoxia_iron_factor(o2_supply, sensitivity)` helper (also off by default at `sensitivity=0`) lets a consumer raise iron release in hypoxic zones (HIF/TfR1 up), which **qualifies the model's hypoxia-leg framing** (§7.1): hypoxia can *increase* the Fenton/iron contribution even as it lowers the O2-dependent SDT exo-ROS (#336), so "hypoxia uniformly protects RSL3" is too strong. Release magnitude, `tau`, and hypoxia sensitivity are placeholders. **Now wired into sim-tme-3d (#365):** `Overrides.ferritinophagy_release` sets `Params.ferritinophagy_release`, and `Overrides.hypoxia_iron_sensitivity` applies `hypoxia_iron_factor(local_o2, sensitivity)` to each tumor cell's static `iron` using the same per-cell O2 supply that gates the SDT exo-ROS. The spatial A/B (`hypoxia_iron_raises_rsl3_kill_where_ros_is_available`) confirms the direction: enabling the coupling raises RSL3 ferroptosis kill several-fold (in one 3D config, grid 60 / λ=50 µm / 180 steps / both knobs at 2.0, overall 0.5%→2.9%, hypoxic-zone 0%→1.3%; at a more anoxic λ=40 the deep-zone rise is even larger). The deep-core rise is a **MODEL ARTIFACT** worth flagging: the Fenton ROS term is O2-independent (`iron × fenton_rate`, added to total ROS independently of basal ROS; the model modulates only `basal_ros`, NOT the Fenton rate), so the iron boost (largest where O2 is lowest) raises core Fenton ROS regardless of anoxia, whereas a real anoxic core would also lose the superoxide/SOD-derived H2O2 substrate the model does not couple to O2. So the robust result is the DIRECTION (hypoxia can raise the RSL3 iron contribution, qualifying "hypoxia uniformly protects RSL3"), not the deep-core magnitude. §7.1 reports this combined with the #336/#358 O2-dependent SDT leg (the asymmetry shrinks from both sides). The deep-core artifact is now addressable by the O2-dependent Fenton option (#383, next row). `release=0.0` and `sensitivity=0.0` ⇒ factors exactly `1.0` ⇒ byte-identical (production matrix SHA unchanged). Calibrate vs NCOA4/ferritinophagy-knockdown labile-iron + ferroptosis-sensitivity timecourses. |
| O2-dependent Fenton (H2O2 substrate) | #383 | `oxygen`/sim-tme-3d | **Uncalibrated (illustrative); direction biochemically grounded** | Corrects the deep-core MODEL ARTIFACT flagged in the #340/#365 row. The model's Fenton term (`iron × fenton_rate`) is O2-independent, so the #365 hypoxia-iron rise lets the model wrongly "rescue" the anoxic core. `oxygen::fenton_o2_factor(o2_supply, dependence) = (1−dep)+dep·o2_supply` (off by default, `dependence=0` ⇒ factor `1.0`) + sim-tme-3d `Overrides.fenton_o2_dependence` scale the effective Fenton flux DOWN where O2 is low, on the basis that the Fenton substrate H2O2 is itself O2-derived (superoxide → SOD → H2O2, the Haber-Weiss chain, Kehrer 2000 Toxicology PMID 10963860) and ferroptotic lipid peroxidation is an oxygen-consuming lipid-radical chain (reviewed in the ferroptosis Primer, Stockwell 2017 Cell PMID 28985560). Because `cell.iron` feeds ONLY the Fenton term, the consumer applies it by scaling `cell.iron`, where it composes multiplicatively with the #365 `hypoxia_iron_factor`: hypoxia raises the iron but gates the H2O2 substrate, so the NET deep-core Fenton can FALL. The spatial A/B (`o2_dependent_fenton_protects_the_anoxic_core_under_hypoxia_iron`, same grid 60 / λ=50 / 180-step config as the #365 test) confirms it: with hypoxia-iron on (the artifact regime that rescues the core) AND the Fenton fully O2-coupled (`dependence=1.0`), the deep hypoxic-zone kill drops by more than half back toward the un-rescued baseline. So the corrected picture is: hypoxia raises labile iron but starves the Fenton reaction of H2O2, net no deep-core rescue, while the iron rise can still raise kill in the oxygenated rim/mid-zone where some H2O2 remains. The `dependence` value (Type-I-like O2-independent vs O2-gated Fenton split) is an uncalibrated placeholder; the direction (anoxic core protected) is the result. `fenton_o2_dependence=0` ⇒ factor `1.0` ⇒ `cell.iron` unchanged ⇒ byte-identical (production matrix SHA unchanged). Calibrate vs pO2-resolved Fenton/•OH and lipid-peroxidation assays. |
| Immunosuppressive ferroptosis | #337 | `immune_spatial`/`params`/sim-tme-3d | **Uncalibrated (illustrative); direction literature-anchored** | The immune layer otherwise treats ferroptotic ICD as net pro-immune (DAMP -> DC -> CD8 kill). `SpatialImmuneConfig.ferro_immunosuppression_strength` (off by default, `0.0`) + `immune_spatial::ferroptotic_immunosuppression(local_damp, strength) = 1/(1 + strength·local_damp)` scale immune kill DOWN as the local ferroptotic-death/DAMP signal rises, modeling DC-suppressing factors co-released by ferroptotic cells: extracellular GPX4 binding DC ZP3 (Liu 2026 Cell PMID 41494530), oxidized lipids / PGE2 (Kim 2022 Nature PMID 36385526), so ferroptotic cells impede DC immunity (Wiernicki 2022 Nat Commun PMID 35760796; review Tang 2024 Immunol Rev PMID 37424139). Keyed on the SAME `local_damp` that drives pro-immune `dc_activation`, so the NET immune effect can FLIP sign at sustained/high death density (verified by the `immunosuppressive_ferroptosis_reduces_immune_kills` A/B test). Direction is timing-dependent (early ferroptotic cells can be immunogenic, Efimova 2020 PMID 33188036); the magnitude is a placeholder, the sign is the result. `strength=0.0` ⇒ multiplier `1.0` ⇒ byte-identical. Calibrate vs ferroptotic-cell DC-maturation / cross-presentation assays + serum-GPX4-vs-response data (Liu 2026). |
| Persister reversible-to-irreversible locking | #342 | `persister`/`params` | **Uncalibrated (illustrative); direction literature-anchored** | The persister model otherwise treats all persistence as freely reversible. `PersisterConfig.lock_rate` / `lock_threshold` / `exposure_decay` (all off by default, `0.0`) + `persister::step_with_locking` split the pool into a reverting `reversible` fraction and a non-reverting `locked` fraction: a sustained-exposure EMA (`cumulative·(1-exposure_decay) + drug`, steady state `avg_drug/exposure_decay`) gates locking, so once it crosses `lock_threshold` a fraction `lock_rate` of the reversible pool moves to `locked` each step. Because the EMA decays in drug-off windows, only SUSTAINED continuous dosing crosses the threshold (epigenetic locking ~10-14 days continuous in several systems; FSP1/HDAC-suppressed persister durability, Higuchi 2026 Sci Adv PMID 41481741), while intermittent dosing settles below it and never locks (verified by the `continuous_dosing_locks_persisters_but_intermittent_does_not` test). Thresholds/rates are placeholders. `lock_rate=0.0` ⇒ `step_with_locking` reduces EXACTLY to `step` ⇒ byte-identical. Now wired into sim-tme-3d (#342): the per-cell persister state (`CellState.persister_reversible`/`persister_locked`/`persister_cum_exposure`, all `0.0` for the inert default so the production matrix SHA is unchanged) advances via `step_with_locking`, a `persister_locked_mean` summary metric reports the irreversible sub-pool, and the spatial `continuous_dosing_locks_persisters_but_intermittent_does_not` sim test confirms continuous dosing locks a non-negligible fraction (`> 0.01`) while intermittent dosing locks nothing (the per-cell exposure EMA stays below threshold); the library unit test (`persister.rs`) additionally runs a full drug washout and confirms the locked fraction does NOT revert. ENTRY: there are now TWO entry routes. (1) the drug-driven acquisition (`acquisition_rate` scaled by the per-step `drug_intensity`); and (2) a non-drug STRESS-niche entry trigger (#377): `PersisterConfig.stress_entry_rate` (off by default `0.0`) + `persister::stress_entry(state, stress, cfg)` raise the REVERSIBLE pool from a hypoxic/nutrient-poor drug-sanctuary niche (the HIF1α-driven slow-cycling chemoresistant phenotype, Cuesta-Borràs 2023 Cell Rep PMID 37537841), DECOUPLED from drug (stress drives ENTRY, drug drives durability) so it never feeds the locking EMA or the GPX4 resistance. Wired into sim-tme-3d with the stress signal = the local hypoxia deficit `1 - o2_supply` (the same per-cell O2 field that gates SDT exo-ROS / the Fenton substrate), applied after `step_with_locking`; the `stress_niche_drives_nondrug_persister_entry` A/B confirms it raises the persister fraction under ZERO drug in the hypoxic zones above the stress-off baseline (which stays exactly 0). `stress_entry_rate=0` ⇒ no-op ⇒ byte-identical (production matrix SHA unchanged); the direction is the result, the rate is an uncalibrated placeholder. Calibrate vs time-to-irreversibility under continuous vs intermittent dosing (lineage tracing / drug-holiday reversibility assays) + hypoxia-induced drug-tolerance kinetics. |
| Persister OXPHOS-ROS suppression + HDAC rescue | #470 | `persister`/`params`/sim-tme-3d | **Uncalibrated (illustrative); direction literature-anchored** | A distinct MITOCHONDRIAL persister escape axis (separate from the `gpx4_resistance` and MUFA axes): drug-tolerant persisters survive GPX4 inhibition partly by DOWNREGULATING oxidative phosphorylation, a main source of the mitochondrial ROS / peroxidizable-substrate flux a GPX4 inhibitor acts on, so RSL3 has less to peroxidize and kills them LESS; HDAC inhibitors re-raise OXPHOS/ROS and synergize with GPX4i to kill persisters ("FSP1 and histone deacetylases suppress cancer persister cell ferroptosis", PMID 40909720). `PersisterConfig.oxphos_ros_suppression` (off by default, `0.0`, AND `0.0` in `enabled()` so existing persister runs are unaffected) + `PersisterConfig.hdac_inhibitor` (in `[0,1]`) + `persister::oxphos_ros_multiplier(fraction, cfg) = 1 - oxphos_ros_suppression·fraction·(1-hdac_inhibitor)` (clamped `[0,1]`): the multiplier interpolates from `1.0` at no persistence to `1 - oxphos_ros_suppression` at full persistence, and a full HDAC inhibitor restores it to `1.0`. sim-tme-3d applies it as a transient save/multiply/restore of the per-cell `basal_ros` around `sim_cell_step` (NOT durable: `basal_ros` is static while `persister_fraction` evolves per step), parallel to the #443 IFN-γ/ACSL4 arm. The `persister_oxphos_suppression_reduces_rsl3_kill_and_hdac_restores` A/B confirms OXPHOS-low persisters take fewer RSL3 kills while a full HDAC inhibitor AND `oxphos_ros_suppression=0` both reproduce the persister baseline EXACTLY. Reachable via `--snapshot=persister-oxphos` (RSL3 + persister + suppression). Magnitude is an uncalibrated placeholder; only the direction (OXPHOS-low ⇒ less RSL3 kill, HDACi reverses) is claimed. `oxphos_ros_suppression=0.0` ⇒ multiplier `1.0` ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI. Calibrate vs persister OXPHOS/mito-ROS measurements + HDACi-plus-GPX4i synergy dose-response. |
| Senescence program (bidirectional) | #341, #447 | `senescence`/(`Cell`) | **Uncalibrated (illustrative); direction contested/literature-anchored** | The `senescence` module marks a `fraction` of tumor cells (independent RNG) and scales their `iron`/`gpx4`/`nrf2`/`fsp1` per `SenescenceConfig` (off by default, `fraction=0`/all muls `1.0`). The ferroptosis direction is genuinely AXIS-DEPENDENT, so the program is bidirectional, not a single sign: senescent cells RESIST upstream (cystine/iron) triggers via compartmentalized iron + raised antioxidant capacity (Masaldan 2018 PMID 28888202; Feng 2024 PMID 38683121; Loo 2025 PMID 40731111; Machii 2026 PMID 42003248), but are a SENOLYTIC target under DIRECT GPX4 inhibition (the model's RSL3) because they are primed (high labile Fe2+) and GPX4-dependent (D'Ambrosio/Gil 2026 Nat Cell Biol PMID 42032311). This CORRECTS the issue's one-sided "resist" framing for the model's GPX4-inhibitor inducer. The unit test drives BOTH nets (iron-dominant ⇒ senolytic, defense-dominant ⇒ resist) under RSL3; magnitudes are placeholders, the axis structure is the result. `fraction=0` ⇒ byte-identical. Now wired into sim-tme-3d (#341): a `--snapshot=senescence` preset, a `senescent_fraction` summary metric, and the optional **SASP→immune coupling**: `SenescenceConfig.sasp_immune_mult` is a signed per-cell multiplier on a senescent cell's immune-kill probability, again bidirectional and STAGE-dependent: `> 1` is anti-tumor senescence immune surveillance but is a PRE-malignant-stage finding (Kang 2011 PMID 22080947), while `< 1` is immunosuppressive SASP that recruits myeloid cells antagonizing surveillance in ESTABLISHED tumors (Di Mitri 2014 PMID 25156255), the stage this therapy-induced-senescence module actually represents, so the net senescence-associated immune response flips with stage (Eggert 2016 PMID 27728804) and `literature()` defaults the arm to `< 1` (the established-tumor escape sign). The SASP is biologically a secreted, paracrine signal (it recruits/reprograms immune cells in the neighborhood and, for the suppressive arm, protects adjacent non-senescent cells, the Di Mitri 2014 IL-1RA mechanism). The cell-autonomous `sasp_immune_mult` captures only the senescent cell's OWN kill probability, so the neighbor/bystander reach is added by the **diffusing SASP field** (#376): `SenescenceConfig.sasp_field_strength` seeds a field at the senescent cells, the consumer diffuses it via `diffuse_damp_3d_step` (reusing the DAMP/suppressor-field infrastructure), and `immune_spatial::sasp_field_kill_multiplier` applies a SIGNED multiplier to EVERY exposed cell including adjacent non-senescent tumor cells (`> 0` immunosuppressive `1/(1+strength·field)` lowers neighbor kill, `< 0` surveillance `1+|strength|·field` raises it). A sim-tme-3d A/B confirms a FIELD-only config (cell-autonomous coupling and biochem held identity) still shifts net immune kills, so the bystander coupling is real; a library test confirms the field reaches a non-senescent Moore-neighbor. The field transport constants (replenish/diffusion/clearance) mirror the suppressor field's and are fixed placeholders; the single tunable knob is the signed strength. Off-by-default (both `sasp_immune_mult=1.0` AND `sasp_field_strength=0.0` ⇒ no coupling ⇒ byte-identical, production matrix SHA unchanged); the magnitudes are placeholders, the signed/stage/paracrine structure is the result. Calibrate vs senescent-vs-non-senescent ferroptosis dose-response under GPX4 inhibition vs cystine deprivation, and the SASP arm vs stage-resolved senescence-surveillance clearance data. #447 adds the CDK4/6-inhibitor-PRIMED combination (`Cdk46PrimingConfig` + `senescent_fraction_after_priming` + `cdk46_primed_senolytic_config`): CDK4/6i (palbociclib/ribociclib) induces senescence over a multi-day saturating induction toward `f_max`, then the senescent fraction RESOLVES over a recovery window after withdrawal, so longer priming recruits a larger iron-dominant senolytic-vulnerable population (a primed cell peroxidizes MORE under RSL3, confirmed by a `sim_cell` test) and the vulnerability window CLOSES on resolution, the #447 falsifiable prediction (CDK4/6i + ferroptosis-inducer is a near-term translational combination since both are approved/in-development). Kinetics are UNCALIBRATED placeholders grounded only in the broadly-reported ~14-21 day senescence-establishment timescale (the specific induction/recovery kinetics are not fit to any dataset); DIRECTION is the result, not the day-precise magnitudes. Off-by-default (`priming_days=0` ⇒ fraction 0 ⇒ identity ⇒ byte-identical). Calibrate vs CDK4/6i-induced senescence kinetics + post-senescence ferroptosis-sensitivity time-course. |
| Oxygen-dependent SDT exo-ROS | #336 | `oxygen` | **Uncalibrated (illustrative); direction clinically grounded** | `oxygen::o2_dependent_exo_factor` + sim-tme-3d `Overrides.sdt_o2_dependence` make a configurable Type II fraction of the SDT/PDT exogenous ROS yield scale with local O2 (`(1−dep)+dep·o2_supply`). This addresses the manuscript's single most contested assumption (§7.1: SDT modeled as O2-independent, an optimistic upper bound) model-side: the lead clinical sonodynamic agent SONALA-001 is Type II / O2-dependent (Sanai 2025, Sci Transl Med, PMID 41296829), with first-in-human glioma results showing only modest cell death, so the real SDT hypoxia advantage is likely far smaller than the O2-independent default. The `dependence` value itself is an uncalibrated knob (the actual Type I/Type II split for a given sonosensitizer is not measured here); calibrate vs sonosensitizer-specific O2-dependence assays (kill vs measured pO2). `sdt_o2_dependence=0` default ⇒ factor 1.0 ⇒ byte-identical. **Now reachable + quantified (#358):** the 2D `sim-tme` exposes it via a `--sdt-o2-dependence <f>` CLI flag (default `0.0`, byte-identical). Re-running the §7.1 hypoxia comparison at full O2-dependence (`1.0`) collapses gradient SDT overall kill from 87.8% to ~3.7% (the across-λ mean; 4.0% at λ = 120, the λ of the 87.8% upper bound; range 2.7% to 4.9% across λ = 80 to 150 μm) and the hypoxic-zone SDT kill from 86.6% to 0%. In the deep hypoxic zone SDT's advantage over RSL3 disappears entirely (both ~0%); in the overall metric a residual rim-driven gap remains (SDT ~3.7% to 4.0% vs RSL3 0.1%, from the still-oxygenated edge). The knob is threaded through the immune-on and O2-cycling gradient paths too, so a flagged run is internally consistent (off-by-default byte-identical). §7.1 now reports BOTH bounds (O2-independent upper, O2-dependent lower); the committed default output stays the O2-independent bound. A sim-tme unit test (`o2_dependent_sdt_collapses_hypoxic_kill`) guards both the knob-off identity and the collapse direction. | **#468 makes the Type I/Type II decomposition explicit** (no new parameter): `o2_dependent_exo_factor(o2, dep) = (1−dep) + dep·o2` already splits the exo-ROS yield into a Type I (oxygen-INDEPENDENT, hydroxyl/superoxide) arm `(1−dep)` and a Type II (oxygen-dependent singlet-oxygen) arm `dep·o2`, so `dep` IS the Type II fraction and `1−dep` the Type I fraction. A `type_i_sdt_retains_hypoxic_kill_where_type_ii_collapses` A/B (grid 60/λ=50/180) shows a Type-I-heavy sonosensitizer (`dep=0.3`) retains ~43% hypoxic-core SDT kill while a pure Type II agent (`dep=1.0`) collapses to exactly 0, the hypoxia-tolerant radical arm the §7.1 leg debates; `--snapshot=sdt-typei` is the complement of `--snapshot=sdt-o2dep`.
| 3D radial pH gradient | #190 | `ph` | **Uncalibrated (illustrative)** | The `ph_on` sim-tme-3d toggle: edge/core pH (7.4 → ~6.5) plus the iron-release and drug ion-trap sensitivities are placeholders. The manuscript's "53% pH-driven RSL3 reduction" (§7.4) rests on `ion_trap_sensitivity`, flagged there as the model's most uncertain parameter (RSL3 is a chloroacetamide with an uncharacterized pKa). Calibrate vs intracellular-RSL3-vs-pH measurement. |
| Stromal / CAF shielding | #189 | `stromal` | **Self-consistency only** | `targets.yaml: 3d_stromal_boundary_shielding`. The CAF GSH/MUFA boost reduces boundary RSL3 kill; sim-tme-3d produces ~51.5% shielding (ratio ≈ 0.485), nearly identical to 2D's 50%. The boost rates have no textbook CAF-biology source (estimates), and the target is a self-consistency check, not independent data. Upgrade to **Calibrated** vs CAF-coculture spheroid data (PMID 34373744, cited in `stromal.rs`). |
| Clonal heterogeneity + spatial expansion | #242, #266 | `clonal` | **Uncalibrated (illustrative)** | Per-subclone iron/GPX4/MUFA perturbations and `repopulation_rate` are placeholders. Calibrate vs single-cell resistance-marker distributions + lineage-tracing growth rates. |
| Cell-cell contact resistance | #270 | `contact` | **Uncalibrated (illustrative); mechanism literature-anchored** | The mechanism (dense contact ⇒ E-cadherin/Merlin/NF2 ⇒ YAP inhibition ⇒ ACSL4/TFRC down ⇒ ferroptosis resistance) is established (Wu 2019, PMID 31341276). The `ContactConfig::literature()` strengths (`lipid_strength` 0.4 / `iron_strength` 0.2, applied as `1 − strength·contact_fraction` on the durable PUFA/iron axes) are placeholder magnitudes encoding the documented direction, not fit to data. The resulting kill-rate change is **threshold-proximity-sensitive** (PUFA enters LP seeding AND autocatalytic propagation, so it scales steeply — read the direction, not the number), and the effect is **almost entirely the lipid/ACSL4 axis**; the iron/TFRC axis is near-inert under RSL3 (small additive Fenton term) and would matter more in Fenton-dominated conditions. Calibrate vs density-resolved ferroptosis-sensitivity assays (sparse vs confluent; NF2/YAP knockdown). Geometric (sphere/spheroid only — mutually-exclusive with the slab geometry); off-by-default ⇒ byte-identical. |
| Radial nutrient gradient | #270 | `nutrient` | **Uncalibrated (illustrative); direction literature-anchored** | A radial nutrient-availability field (`exp(-depth/λ)`, the O2 field's form) scales the durable antioxidant setpoint `cell.nrf2` DOWN toward the nutrient-starved core, so the core has less glucose-derived NADPH for GSH/GPX4 regeneration and is more ferroptosis-sensitive (Dixon 2012 PMID 22632970, the foundational GSH-defense mechanism; glucose metabolic reprogramming regulates ferroptosis, PMID 42190602). `NutrientConfig::literature()` `antioxidant_strength` 0.3 is a placeholder magnitude encoding ONE documented direction; the NET effect is genuinely context-dependent (energy stress also activates AMPK, which INHIBITS ferroptosis; glutaminolysis is REQUIRED for some ferroptosis, PMID 30581146). Calibrate vs depth-resolved glucose/NADPH/GSH in spheroids. Geometric (no RNG); off-by-default ⇒ byte-identical. |
| Drug-tolerant persisters | #241, #262 | `persister` | **Uncalibrated (illustrative)** | Acquisition/reversion rates are step-level placeholders (no published step-level kinetics). Target: Hangauer-style multi-cycle drug screen (fraction-surviving vs cycles) + off-drug reversion half-time — see `parameter_provenance.md`. |
| T-cell exhaustion | #243 | `immune_spatial` | **Uncalibrated (illustrative)** | `exhaustion_rate` placeholder. Calibrate vs longitudinal TIL cytotoxicity / exhaustion-marker time-courses. |
| Treg/MDSC suppressor field | #264 P2 | `immune_spatial` | **Uncalibrated (illustrative)** | Suppressor strength + diffusion length placeholders; niche sources heuristic. Calibrate vs spatial Treg/MDSC density + local kill suppression. |
| Multi-checkpoint brake | #264 P3 | `immune_spatial` | **Uncalibrated (illustrative)** | Per-axis (PD-1/CTLA-4/LAG-3/TIM-3) brake residuals + drug efficacies placeholders. Calibrate vs blockade-response dose-effect data per axis. |
| DC subset mix (cDC1/cDC2) | #264 P4 | `immune_spatial` | **Uncalibrated (illustrative); direction literature-anchored** | `DcSubsetConfig` collapses to one uniform anti-tumor priming-efficiency scalar (`cdc1_fraction·cdc1_eff + (1−cdc1_fraction)·cdc2_eff`) multiplied into the immune kill probability. A cDC1-poor tumor primes CD8 killing less efficiently (cDC1/Batf3 cross-presenting DCs are the rare, critical anti-tumor APCs: Broz et al., Cancer Cell 2014, PMID 25446897). The `literature()` mix (10% cDC1, cdc2_eff 0.3 ⇒ efficiency 0.37) is an UNCALIBRATED placeholder; only the direction (cDC1-poor ⇒ weaker priming ⇒ fewer immune kills) is claimed. Calibrate vs tumor DC-subset abundance + response data. Off-by-default (balanced ⇒ efficiency 1.0) byte-identical. |
| IFN-γ → System Xc⁻ coupling | #443 | `ifngamma`/`immune_spatial`/sim-tme-3d | **Uncalibrated (illustrative); direction literature-anchored** | The immune layer's pro-immune arm couples ferroptotic death → DAMP → CD8 priming, but the reverse molecular loop (activated CD8 IFN-γ sensitizing tumor cells back to ferroptosis) was missing. The `ifngamma` module adds it: an IFN-γ field is seeded from the local immune DAMP signal (`per_damp · local_damp` at DAMP-positive positions), diffused with the shared `diffuse_damp_3d_step` operator, and `system_xc_retention(local_ifn, ic50) = ic50/(ifn+ic50)` (in `(0,1]`) multiplies the per-cell GSH pool DOWN as IFN-γ rises, modeling IFN-γ transcriptional repression of System Xc⁻ (SLC7A11/SLC3A2) → cystine starvation → GSH loss → more ferroptosis (Wang et al., Nature 2019, PMID 31043744). The parallel ACSL4-upregulation arm (`acsl4_upregulation`, more oxidizable PUFA) is now also wired into sim-tme-3d (#443 PR 2): a transient save/multiply/restore around `sim_cell_step` raises each exposed cell's `lipid_unsat` for that step (NOT a durable mutation — `lipid_unsat` is static, so a persistent per-step multiply would compound, unlike the regenerating GSH pool), gated on `acsl4_strength>0`. Two A/Bs confirm direction: `ifngamma_couples_immune_activation_to_ferroptosis_sensitization` (both arms, RSL3 + immune) roughly doubles kill, and `ifngamma_acsl4_arm_alone_raises_ferroptosis` (ic50=∞ so the GSH arm is a no-op) confirms the lipid arm raises ferroptosis on its own. Magnitudes (`system_xc_ic50`, `acsl4_strength`, field transport constants) are placeholders, the Wang-2019 coupling direction is the result. Reachable via `--snapshot=ifngamma`. `IFNGammaConfig::disabled()` (the default) ⇒ retention `1.0` + boost `1.0` ⇒ byte-identical, production matrix SHA unchanged. Calibrate vs IFN-γ-dose vs SLC7A11/GSH-suppression + ACSL4/PUFA + ferroptosis-sensitivity co-culture data. |
| DC/effector-cell ferroptosis susceptibility | #469 | `immune_spatial`/sim-tme-3d | **Uncalibrated (illustrative); direction literature-anchored** | The immune layer otherwise treats dendritic cells only as an undying DAMP source plus a fixed priming scalar (#264 P4). `DcFerroptosisConfig` + `dc_ferroptosis_survival(local_lipid_ros, susceptibility, pdl1_protection) = 1/(1 + susceptibility·local_lipid_ros·(1−pdl1_protection))` add the missing coupling: a ferroptotic TME kills the effector DCs THEMSELVES, lowering priming. Loss of PD-L1 on tumor-associated DCs downregulates System Xc⁻/SLC7A11, raises their lipid peroxidation, and drives them into ferroptosis (Yao et al., Cell Reports 2024, PMID 39423128); PD-L1/checkpoint state protects them, so anti-PD-L1 blockade that strips the protection can paradoxically sensitize DCs to a ferroptotic TME. Keyed on the SAME `local_damp`/lipid-ROS that drives pro-immune `dc_activation`, so a ferroptosis-inducing TME can SUPPRESS its own immune amplification. Distinct from the #337 secreted-factor immunosuppression (there dying tumor cells release DC-suppressing factors; here the effector cells are directly ferroptosis-KILLED, checkpoint-protected). A `dc_ferroptosis_reduces_immune_kills_and_pdl1_protects` A/B confirms PD-L1-low vulnerable DCs lower net immune kills while full PD-L1 protection AND `susceptibility=0` both reproduce the baseline exactly. `susceptibility`/magnitude are placeholders, the PMID 39423128 direction is the result. `susceptibility=0.0` default ⇒ survival multiplier `1.0` ⇒ byte-identical (production matrix SHA unchanged). Calibrate vs DC viability / ferroptosis-marker vs local lipid-ROS + DC PD-L1 status data. |
| Time-varying dose schedules | #239 | `dose_schedule` | **Partially anchored** | Schedule *shapes* (bolus/infusion/multidose) are standard PK forms; the `FromPk` path inherits `tumor_pk`'s order-of-magnitude constants. |

### 3D self-consistency targets (in `targets.yaml`)

The three 3D targets (`3d_rsl3_o2_collapse_ratio`, `3d_immune_sdt_dominates`,
`3d_stromal_boundary_shielding`) are **self-consistency** — they regression-guard
the 3D model's own predictions, not measured values. `3d_validation_report.md`
records that, like-for-like, the 2D engine produces a **larger** immune ratio
than 3D (104:1 vs **4:1**) and a slightly more complete hypoxia collapse — i.e.
the 3D numbers are not "stronger," and the immune ratio in particular is
sensitive to grid size / volumetric DAMP dilution.

**Immune target — direction now literature-anchored (#288).** A #288 literature
review (every PMID verified against PubMed) established that the SDT:RSL3 immune
ratio's *exact value* cannot be calibrated — no published study runs SDT and an
RSL3-only arm against a shared immune readout, and the previously-named
"Nguyen 2019" source was a **phantom citation** (no such applicable study exists;
removed from `targets.yaml`). But the *direction* (ratio > 1) and *single-digit
magnitude-class* ARE supported by verified primary literature: the
RSL3/GPX4-inhibitor denominator is non- to weakly-immunogenic (Wiernicki 2022
PMID 35760796, 0% vaccination protection; with the genuine caveat that early
ferroptosis can be immunogenic — Efimova 2020 PMID 33188036), SDT drives
DC maturation + CD8 infiltration over controls (Wang 2021 PMID 34669472;
Luo 2022 PMID 35568916; pancreatic-spheroid SDT-ICD Foglietta 2024 PMID 38232641;
the precise fold-changes are figure-level, not abstract-level). The 2D 104:1
over-extrapolates for a model-internal reason: the immune layer's saturating
Michaelis-Menten DAMP→activation (Kd=50) is driven deep into saturation by the
dense 2D kill field, whereas 3D volumetric DAMP dilution keeps it sub-saturating
(the more literature-consistent ~4:1). So `3d_immune_sdt_dominates`
stays self-consistency for its *value* but is now literature-anchored for its
*direction and magnitude bound*.

## Calibration roadmap (priority order)

Calibration is **data-gated**: each item needs an independent measurement the
repo does not currently hold. Listed by leverage (how much it would change a
load-bearing claim) × tractability (how obtainable the data is):

1. **Immune coupling (2D + 3D).** ~~Needs a published SDT-immune-priming dataset.~~
   **DONE as far as the literature allows (#288):** the direction + magnitude-class
   are now literature-anchored (see the immune-target note above). What remains
   genuinely data-blocked is a *precise* calibrated value, which requires a study
   running SDT and RSL3 against a **shared** immune readout — none exists. The
   honest residual is that the exact ratio (3D ~4:1) is uncalibratable, not that
   the claim is ungrounded; the keystone direction is supported.
2. **Persister kinetics.** Direction is well-supported; only the rates are
   guessed. A single Hangauer-style multi-cycle screen would fit
   acquisition/reversion/`max_fraction`. Tractable, well-scoped.
3. **Vasculature → hypoxic fraction.** Spacing is anchored; validating that the
   field reproduces a *measured* hypoxic fraction (pimonidazole / micro-CT) would
   move it from "partially anchored" to calibrated and underwrite the #272
   patient-scale depth-collapse story.
4. **Slab depth-collapse magnitude.** The "<20 % at depth" number drives the
   in-vitro-vs-patient narrative; depth-resolved kill in thick tissue would make
   it quantitative rather than illustrative. Remaining `slab` refinements tracked
   under #240/#272 (all data-gated or opportunistic, none blocking): the
   depth-collapse calibration above; the depth-graded slab phenotype's supply
   cut-points (validate vs depth-resolved proliferation/quiescence histology); a
   *multi-face* supply (the current planar gradient is single-face +z; a real
   tumor chunk is perfused from several boundaries); and folding the hard-coded
   `VIRTUAL_TUMOR_MM` / `depth_offset_mm` placement constants into a derived
   geometry once a patient-scale tumor model is available. The slab+vasculature
   coupling (#272) and depth-graded phenotype (#272) have shipped; the rest are
   left until the matching data exists rather than guessed.
5. **Spheroid radial gradients, clonal perturbations, suppressor/checkpoint/
   exhaustion rates.** Lower individual leverage (each is one knob in a
   composite); calibrate opportunistically as spatially-resolved or longitudinal
   datasets surface.

Until an item lands, treat the corresponding layer's output as **directional /
illustrative**, exactly as the module doc-comments and the manuscript's §8.4
already state. When a target is upgraded to `calibration` in `targets.yaml`
(add `source_pmid` + the measured value), update its row here in the same PR.
