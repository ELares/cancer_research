# CLAUDE.md

## Author

Ezequiel Lares

## What This Repo Is For

This is an open cancer-research workspace. The point is to help people—not to be right about one hypothesis.

The repo exists to compare therapeutic mechanisms, evidence depth, resistant-state biology, pathway targets, and simulation-based ideas honestly, so that anyone who reads it can form their own informed view. If the evidence says a direction is weak, say so. If a new direction looks promising, surface it. Don't protect old framing at the expense of clarity.

## Guiding Principles

1. **Let the evidence lead.** The repo started around a PDT/SDT-persister-ferroptosis thesis. That's still worth evaluating, but it's one lane among many. Don't treat it as the default winner.

2. **Stay open.** New modalities, targets, pathways, and interpretations should be welcomed when the corpus or external literature supports them. The README invites anyone with curiosity to contribute—the codebase should reflect that same openness.

3. **Be honest about what we don't know.** Corpus-level non-detection is provisional, not proof of absence. Missing landmark papers distort field-level claims. Taxonomy choices inflate or deflate conclusions. Say so directly rather than burying caveats.

4. **Make it reproducible.** Scripts should be re-runnable. Analysis outputs should be regenerated, not hand-edited. Separate generated files from handwritten interpretation notes so it's clear what came from the pipeline and what came from a person.

5. **Keep it human.** This project matters because cancer takes people from their families. Technical rigor serves that mission—but so does making the work accessible, welcoming contributions, and not hiding behind jargon when plain language works.

6. **The work is more important than the paper.** Don't optimize for journal word limits or publication constraints. If a finding needs context, give context. If a decision needs explaining, explain it. Clarity and contribution to the scientific community matter more than fitting a format. A reader should understand why we made each decision, what the results yield, and why we believe they signal value.

## Current Workstreams

- manuscript drafting and revision (Parts I–V substantially complete: ~115 pages, 11 chapters + 3 appendices)
- corpus fetching, enrichment, tagging, and indexing
- evidence-tier audits and coverage caveats (gold-set evaluation: 46% exact, 96% precision, 55% recall on the production keyword tagger; an off-by-default MeSH-descriptor fallback (#346, `FERRO_MESH_EXPANSION=1`) lifts gold-set evidence-detection recall to 67.8% at 95.2% precision, see `analysis/evidence-gold-mesh-eval.md` — not applied to the production corpus, so the 55% figure remains the corpus reality)
- taxonomy and search refinement
- pathway-target and resistant-state analysis
- diagnostic-to-therapy chain extraction (6 chains, 129 articles mapped)
- tissue-of-origin analysis layer (5 tissue categories, 62% coverage)
- simulation work: ferroptosis biochemistry, drug penetration, calibration, photosensitizer PK (drug-light-interval scaling, saturating distribution phase, relative singlet-O₂ yield, FromStr-based clap CLI integration in sim-spatial), 3D spheroid scaffolding (TumorGrid3D #185, signed radial depth + 3D energy-physics dispatcher #186, 3D radial O₂ gradient + zone-census #187, 3D radial pH gradient + iron/ion-trap helpers #190, 3D CAF-shielded boundary detection #189, 3D spatial immune coupling + DAMP diffusion #188) for the #185–#197 spheroid-validation series; sim-tme-3d TME capstone (#195); 2D-math lift + `immune_3d`→`immune_spatial` rename + JSON schema_version (#220/#224); 3D trajectory snapshots + animated axial-slice GIF renderer (#193/#238); time-varying multi-dose pharmacokinetics (`dose_schedule` module — Constant/Bolus/MultiDose/Infusion/FromPk — wired into sim-tme-3d via `--dose-sweep` + the `--snapshot=multidose` preset, with the orphaned `tumor_pk` ODE finally bridged in via `FromPk`, #239); 3D performance and scalability work (`--bench` harness + within-condition rayon parallelism, byte-identical via position-independent per-cell RNG, 3.8x to 4.9x speedup on single large grids, dense 200³ measured at ~1.29 GB so sparse storage deferred to #254, #192); full-production byte-identity regression CI guarding sim-tme-3d's default-matrix `summary.json` SHA (#253); drug-tolerant persister cells (`persister` module + `PersisterConfig`, epigenetic ferroptosis tolerance acquired under drug exposure and reverting after clearance; off-by-default identity config keeps the matrix byte-identical, with a `--snapshot=persister` render overlay, #241; #262 replaces the acquire-xor-revert either-or with a competing-rate `step` so sustained sub-saturating drug reaches a sub-cap equilibrium, + persister calibration provenance); T-cell exhaustion (#243 Phase 1 — `immune_spatial::exhaustion_factor` + per-cell neighborhood `cumulative_kills`; sustained local killing suppresses further immune kills via `1/(1+rate·cumulative)`; `exhaustion_rate=0` default keeps the matrix byte-identical; #264 Phase 2 adds a Treg/MDSC suppressor field — a second diffusing field, replenished at perivascular-or-heuristic niche sources, that scales immune kill DOWN via `1/(1+strength·field)`; off-by-default byte-identical, anti-PD-1 Treg-depletion validation test + `--snapshot=suppressor` niche overlay); #264 Phase 3 generalizes the single PD-1 brake to a multi-checkpoint panel (PD-1/CTLA-4/LAG-3/TIM-3, each independently drug-modulated; combined brake 1−Π(1−residualᵢ)) to model anti-PD-1 + anti-CTLA-4 combinations, off-by-default byte-identical with a dual-blockade validation test + `--snapshot=checkpoint`); #264 Phase 4 adds the cDC1/cDC2 dendritic-cell subset mix (`DcSubsetConfig`: a cDC1-poor tumor primes anti-tumor CD8 killing less efficiently, collapsed to one uniform priming-efficiency scalar multiplied into the immune kill probability, Broz 2014 PMID 25446897; off-by-default balanced ⇒ efficiency 1.0 byte-identical, cDC1-poor A/B validation test + `--snapshot=dc-subsets`), completing the #264 Phases 2-4 immune-realism arc; cross-layer composition validation (#278 — pairwise-distinct realism-layer RNG-seed guard + multi-layer integration tests (clonal×suppressor×checkpoints, spheroid×vasculature) asserting deterministic composition + coherent per-layer metrics, plus a `--snapshot=combined-realism` kitchen-sink preset; all-off matrix stays byte-identical); clonal heterogeneity (#242 — `clonal` module: Voronoi subclone patches via an independent RNG + per-subclone iron/GPX4/MUFA perturbations, `summary.json` per-subclone kill reporting, K=1-identity default keeps the matrix byte-identical, `--snapshot=clonal` subclone overlay; #266 makes the GPX4 axis durable — `gpx4_mul` now scales the static `cell.nrf2` setpoint too, not just the initial `state.gpx4`, so a GPX4/antioxidant-low subclone stays differentiated for the whole run instead of relaxing to the shared setpoint; #266 item 3 adds spatial clonal expansion — dead tumor sites repopulated from living Moore-neighbors so resistant subclones grow their territory, with `subclone_kills.initial_tumor` reporting the shift; off-by-default (repopulation_rate=0) byte-identical); explicit 3D vasculature (#191 — `vasculature` module: random internal vessel seeds via an independent RNG + per-cell `exp(-dist_to_nearest_vessel/λ)` unified supply replacing the edge-distance O2 proxy and scaling drug delivery on all paths (SDT/PDT exo, RSL3 constant knockdown, dosed RSL3 availability), off-by-default byte-identical, `vascular_hypoxic_fraction` reporting + `--snapshot=vasculature` O2-supply overlay; #268 replaces the brute-force nearest-vessel scan with a `VesselIndex` uniform-grid spatial index — exact/byte-identical (Chebyshev-shell search with a provable early-stop, verified bit-for-bit vs brute force), ~O(cells) instead of O(cells×vessels), so vasculature scales to patient-size grids the #272 slab coupling needs; #268 also adds `VesselTopology::{Random,Fractal}` + `place_vessels_fractal_3d` — a fractal-branching vessel tree (trunks enter from the periphery and bifurcate inward via BFS with high, tumor-like angle/length jitter and occasional dead ends, per Baish & Jain 2000 PMID 10919633, capped at the same point-count target as the random network — matching raw point count, NOT effective coverage) producing hierarchical-but-chaotic perfusion with avascular gaps; at near-equal point count the fractal network leaves a higher hypoxic fraction than uniform-random seeding (a clustering-coverage effect: 1-cell-spaced branch points cover far less unique volume than scattered points — qualitative, not density-controlled), slab geometry ignores topology, off-by-default topology=Random byte-identical, no preset overlay yet); 3D spheroid radial biochemistry (#197 — `spheroid` module: radial phenotype re-assignment (glycolytic rim / OXPHOS mid / persister core) via an independent RNG + position-dependent MUFA/GSH/iron gradients, run under the new `Params::spheroid()` partial-MUFA context, off-by-default byte-identical, `--snapshot=spheroid` phenotype overlay; #270 makes the zone thresholds VOLUME-based (compare frac³) and grounds the defaults in the Browning 2021 eLife limiting structure (necrotic core 0.73 of radius → 0.39 of volume; rim begins 0.90 of radius), fixing the radial-threshold inversion that gave a ~4%-volume core / ~71%-volume rim — geometry now literature-grounded, biochem gradient strengths still placeholders; #270 makes the radial MUFA durable via a per-cell `Cell::mufa_cap` so the rim-vs-core spread persists instead of relaxing to the uniform M_ss; #270 item 3 adds the `contact` module — cell-cell contact-mediated ferroptosis resistance (E-cadherin → Merlin/NF2 → YAP inhibition → ACSL4/TFRC down, Wu 2019 PMID 31341276): `apply_contact_resistance_3d` scales each tumor cell's durable `lipid_unsat` (PUFA) and `iron` down with its tumor-26-neighbour fraction, so dense interior cells resist while sparse/surface cells stay sensitive (stronger in 3D); geometric/no-RNG, off-by-default identity byte-identical, integration-tested (RSL3 kills collapse to well under half at literature strengths — the magnitude is threshold-proximity-sensitive and almost entirely the PUFA/lipid axis, so the DIRECTION is the result, not the number; iron/TFRC is a minor contributor under RSL3), guarded mutually-exclusive with slab geometry; #302 closes the contact follow-ups: it re-applies contact resistance to clonally-repopulated cells (revived dead sites previously came back un-reduced, the opposite of the modeled biology, so `clonal(repopulation)` + `contact` now stay coherent via the new `apply_contact_resistance_at_3d` + `repopulate_dead_sites_3d` returning the revived indices), and adds the contact+spheroid composition test, `#[should_panic]` guard tests for the slab+contact / slab+spheroid invariants, the first `io`-module tests, and a `--snapshot=contact` overlay); patient-scale slab geometry (#240 — `slab` module + `TumorGrid3D::generate_slab`: an all-tumor block with a planar depth-graded `exp(-depth/λ)` supply (the 1-D analog of the radial O2 field, +z face vessel-proximal, −z reflective/no-flux) replacing O2 and scaling drug on all paths, `SlabConfig::patient_deep()`/`surface()` (10 mm virtual tumor), a `scale_interpretation` output string, off-by-default byte-identical, `--snapshot=slab` depth-gradient viz; addresses the in-vitro-vs-patient scale gap — a 4 mm-deep slab kills <20% of the spheroid under the same SDT, though the depth-collapse magnitude is an uncalibrated first-order Krogh approximation; #272 couples the slab with explicit vasculature (#191) — `place_vessels_in_slab_3d` scatters vessels uniform-in-box across the all-tumor block and the per-cell supply becomes `max(planar_depth, nearest_vessel)`, so internal vessels deliver drug to focal deep pockets and RAISE deep killing, i.e. a slab is less therapy-resistant at depth than the planar-only model implies wherever real vasculature reaches; off-by-default byte-identical, gated on both overrides, `--snapshot=slab-vessels` overlay)
- ferroptosis-core library packaging for external use
- news source authentication pipeline (fetch, extract claims, verify, score, index)
- broader strategy review of alternative therapies and biological bottlenecks
- operational maturity: Phase 2 complete — CI (#126), figure traceability (#127), archival release tooling (#131); workspace `cargo fmt --check` gate added to Rust CI (#209/#236); off-PR sim-tme-3d production byte-identity regression workflow (#253)
- manuscript integrity: Phase 3 complete — immune coupling confidence (#130), structural uncertainty qualifiers (#137), PRISMA corpus flow diagram (#135), retrieval bias subsection (#140)
- sensitivity analyses: weight-sensitivity (#128), taxonomy-sensitivity (#133), PRCC global sensitivity (#134), and O2 cycling (#138) complete — pre-registered, run, results in manuscript
- test expansion (#139) complete — 19 invariant/integration tests added (schema, weight monotonicity, tagging correctness)

## Current Repo State

- local full-text corpus: 4,830 records
- abstract-only archive: 5,585 records
- mechanism taxonomy, evidence tiers, pathway-targets, biology-process tags, and resistant-state scaffolding are all active
- evidence tagging is improved but still incomplete (gold-set measured); MECHANISM tagging recall is separately measured non-circularly vs MeSH (#412): 90.0% volume-weighted / 87.5% macro over 9 mechanisms with an independent MeSH leaf, with the newest/device/regulated-cell-death modalities flagged not-MeSH-measurable (analysis/mechanism-recall-report.md)
- tissue-of-origin and weighted-evidence layers are active
- diagnostic-therapy matching layer covers 6 chains across 4 modalities (radioligand, checkpoint, mRNA vaccine, oncolytic)
- manuscript: ~115 pages (book format), 11 chapters + 3 appendices, 24 figures, ~39,400 words
- simulation suite: 11 binaries (incl. sim-tumor-pk + sim-tme-3d) + ferroptosis-core library (MIT licensed, 26 modules including `photosensitizer_pk`, `oxygen`, `ph`, `stromal`, `immune_spatial`, `dose_schedule`, `persister`, `clonal`, `vasculature`, `spheroid`, `slab`, `contact`, `nutrient`, `phenotype_mufa`, `senescence`, and `reaction_diffusion`; v0.7.0 adds 3D radial-depth + 3D ROS-multiplier APIs alongside the 2D path #185–#186; v0.8.0 adds 3D radial O₂ field + zone-census #187; v0.9.0 adds 3D radial pH field + iron-release/ion-trap helpers #190; v0.10.0 adds 3D CAF-shielded boundary detection + adjacent-kill-rate #189; v0.11.0 adds 3D spatial immune coupling #188; sim-tme-3d capstone binary consumes all five #195; #220/#224 lift the TME config structs + 2D depth/pH/stromal helpers into the library and rename `immune_3d`→`immune_spatial`; v0.12.0 (#239) adds the `dose_schedule` time-varying-PK module + `biochem::exo_decay_factor`; v0.13.0 (#241) adds the `persister` drug-tolerant-persister module + `PersisterConfig`; v0.14.0 (#243) adds `immune_spatial::exhaustion_factor` (T-cell exhaustion) + `TumorGrid3D::coords` + `SpatialImmuneConfig::exhaustion_rate`; v0.15.0 (#242) adds the `clonal` module (Voronoi subclones + `ClonalConfig`/`SubclonePerturbation`); v0.16.0 (#191) adds the `vasculature` module (explicit vessel network + `VasculatureConfig`); v0.17.0 (#197) adds the `spheroid` module (radial phenotype/MUFA/GSH/iron + `SpheroidConfig`) + `Params::spheroid()`; v0.18.0 (#240) adds the `slab` module (patient-scale all-tumor block + planar depth-graded supply, `SlabConfig`, `scale_interpretation`, `KROGH_LAMBDA_UM`) + `TumorGrid3D::generate_slab`; v0.19.0 (#270) adds per-cell durable MUFA (`Cell::mufa_cap`, threaded into `update_mufa_protection`, set radially by the spheroid so position-dependent MUFA no longer relaxes to the uniform M_ss); v0.20.0 (#264 Phase 2) adds the Treg/MDSC immunosuppressor field to `immune_spatial` (`SuppressorConfig` + `suppressor_kill_multiplier` + `suppressor_source_mask_3d`); v0.21.0 (#264 Phase 3) adds the multi-checkpoint immune brake (`CheckpointPanel` + `Checkpoint`, `combined_brake` = 1 − Π(1 − brakeᵢ·(1−drug_effᵢ)) over PD-1/CTLA-4/LAG-3/TIM-3); v0.22.0 (#262) adds the persister competing-rate `step` (acquisition + reversion act simultaneously ⇒ sub-cap equilibrium under sustained sub-saturating drug); v0.23.0 (#266 item 3) adds spatial clonal expansion (`repopulate_dead_sites_3d` + `ClonalConfig::repopulation_rate`/`with_repopulation`); v0.24.0 (#272) adds slab+vasculature coupling (`vasculature::place_vessels_in_slab_3d` uniform-in-box vessel placement, so a patient-scale slab can carry internal vessels whose proximity supply combines element-wise-MAX with the planar depth gradient); v0.25.0 (#268) adds the `VesselIndex` uniform-grid spatial index for `vessel_supply_field`'s nearest-vessel lookup (exact/byte-identical to the former brute force, ~O(cells) instead of O(cells×vessels), so vasculature scales to patient-size grids — 100³/1M cells ≈ 105 ms); v0.26.0 (#268 + spheroid zone fix) adds `VesselTopology::{Random,Fractal}` + `place_vessels_fractal_3d` (a fractal-branching vessel tree — trunks enter from the periphery and bifurcate inward with high, tumor-like variability per Baish & Jain 2000 PMID 10919633, producing hierarchical-but-chaotic perfusion with avascular gaps, capped at the same point-count target as random (raw point count, NOT effective coverage — clustered 1-cell-spaced branch points cover far less unique volume than the same number of scattered points, so the higher hypoxic fraction is a clustering-coverage effect read qualitatively, not a density-controlled result); slab geometry ignores topology; off-by-default topology=Random keeps the matrix byte-identical) and makes `spheroid::radial_phenotype` volume-based (compares `frac.powi(3)` to volume-fraction thresholds with `literature()` defaults `glycolytic_frac:0.73`/`oxphos_frac:0.39` from Browning 2021 eLife so rim/mid/core zones reflect equal-volume shells, not equal-radius bands; spheroid layer is opt-in so byte-identical); v0.27.0 (#270) adds the `contact` module (cell-cell contact-mediated ferroptosis resistance — E-cadherin junctions → Merlin/NF2 → YAP inhibition → ACSL4/TFRC down, per Wu 2019 PMID 31341276; `ContactConfig` + `apply_contact_resistance_3d` scale the durable per-cell `lipid_unsat` (PUFA) and `iron` axes down in proportion to each cell's tumor-26-neighbour fraction, so dense interior cells resist while sparse/surface cells stay sensitive — stronger in 3D; geometric, no RNG, off-by-default identity keeps the matrix byte-identical); v0.28.0 (#302) adds `contact::apply_contact_resistance_at_3d` (per-cell re-application, used to keep `clonal(repopulation)` + `contact` coherent) and changes `clonal::repopulate_dead_sites_3d` to return the revived-site indices (so a consumer re-applies geometric per-cell setup layers to fresh cells); v0.29.0 (#270 item 3b) adds the `nutrient` module (radial nutrient gradient: glucose/glutamine availability `exp(-depth/λ)` scales the durable antioxidant setpoint `cell.nrf2` DOWN toward the starved core, less NADPH→GSH/GPX4 regeneration ⇒ more ferroptosis-sensitive per Dixon 2012 PMID 22632970 / glucose-metabolism PMID 42190602; ONE documented direction, net is context-dependent given AMPK/glutaminolysis; reuses `RadialDepthGeom`, off-by-default byte-identical, `--snapshot=nutrient`); v0.30.0 (#264 Phase 4) adds the DC-subset mix to `immune_spatial` (`DcSubsetConfig` + `priming_efficiency()`: cDC1/cDC2 composition collapses to one uniform anti-tumor priming-efficiency scalar the consumer multiplies into the immune kill probability; a cDC1-poor tumor primes CD8 killing less efficiently per Broz 2014 PMID 25446897, `balanced()` ⇒ efficiency 1.0 identity/byte-identical, `literature()` cDC1-poor placeholder, gated on immune_on + routed through the realism kill path so the default hot loop is untouched, `--snapshot=dc-subsets`); v0.31.0 (#315) adds an off-by-default ECM-tortuosity factor to `drug_transport` (`TissueParams::ecm_tortuosity`, `λ_eff = λ/√τ`: a denser desmoplastic matrix shortens drug penetration; `τ=1` identity for all shipped tissues so byte-identical, uncalibrated placeholder per Netti 2000 PMID 10811131 / Provenzano 2012 PMID 22439937); v0.32.0 (#272) adds the depth-graded slab phenotype to `slab` (`SlabPhenotypeConfig` + `apply_depth_graded_cells_3d` + `depth_phenotype` + the `layer_supply` helper factored out of `slab_supply_field`): the slab's flat bulk phenotype mix becomes a layered rim→core gradient (proliferating/glycolytic at the vessel-proximal +z face, persister-like in the chronically supply-starved deep (−z) layers), thresholded on the planar supply `exp(-depth/λ)` (NOT geometric volume fractions like the spheroid, since the slab models an absolute depth, so a `patient_deep()` 4 mm slab is uniformly persister-like, the intended behavior for a chunk drug/O2 never reach); independent per-cell RNG + off-by-default (gated on slab mode, which the matrix never enters) keeps the production matrix byte-identical, wired into the `slab`/`slab-vessels` presets, supply cut-points `glycolytic_supply:0.5`/`oxphos_supply:0.15` are placeholders pending depth-resolved histology; v0.33.0 (#336) adds the oxygen-dependent SDT/PDT exo-ROS option (`oxygen::o2_dependent_exo_factor` + sim-tme-3d `Overrides.sdt_o2_dependence`): the exogenous ROS yield can be made a configurable Type II fraction that scales with local O2 (`(1−dep)+dep·o2_supply`), so SDT loses efficacy in hypoxic zones like the clinical SONALA-001 agent (Sanai 2025 Sci Transl Med PMID 41296829, Type II/O2-dependent), addressing the manuscript's contested §7.1 hypoxia leg model-side; `sdt_o2_dependence=0` default ⇒ factor 1.0 ⇒ byte-identical; v0.34.0 (#338) adds the two missing GPX4-independent ferroptosis-suppressor axes to `biochem`/`params`: `Params.dhodh_rate` (DHODH/CoQ10 repair in parallel to FSP1, Mao 2021 Nature) and `Params.gch1_rate` (GCH1/BH4 radical-trapping quench gating autocatalytic propagation, Kraft/Soula 2020), so the model no longer overstates RSL3 monotherapy kill and can express GPX4i+DHODHi-style combinations; both `0.0` by default ⇒ byte-identical, FFI `ferro_params_to_params` defaults them so the C ABI/header is unchanged; v0.35.0 (#339 PR 1 of 3) adds an off-by-default kinetic/acute MUFA start to `biochem`/`params` (`Params.mufa_acute_start: Option<f64>`): `invivo()` starts cells at the established MUFA steady state (M_ss ≈ 0.40, assuming the ~48 to 72 h SCD1/MUFA enrichment is already complete), overstating early ferroptosis resistance for acute day-0 dosing; `Some(x)` instead starts a freshly dosed tumor at a naive value `x` (e.g. 0.0) and accumulates protection over the run via the existing logistic, reproducing the acute-versus-established difference (acute cells peroxidize more under RSL3); the accumulation rate is the existing `scd_mufa_rate`, uncalibrated against the 48 to 72 h timescale so the direction is the result not a precise hours figure; `None` default ⇒ byte-identical, FFI maps to `None` so the C ABI/header is unchanged; the ether-lipid (FAR1/TMEM189) and MBOAT1/2 axes are follow-up PRs of #339; v0.36.0 (#339 PR 2 of 3) adds the off-by-default ether-lipid axis to `biochem`/`params` (`Params.ether_pufa_fraction: f64`): polyunsaturated ether phospholipids (ether-PUFA-PE, made via FAR1/AGPS + the peroxisomal pathway) are an extra oxidizable-substrate pool that PROMOTES ferroptosis, so the new `ether_augmented_pufa` helper scales the peroxidizable PUFA term by `1 + ether_pufa_fraction` at all four `effective_unsat` sites; enabling it raises lipid-peroxide accumulation and the `0` limit is the FAR1/AGPS-null escape (ether-lipid loss confers resistance), the robust direction per Zou 2020 Nature PMID 32939090 / Cui 2021 Cell Death Differ PMID 33731874 and the OPPOSITE sign to the loose "shift to ether-PE = escape" framing; the contested plasmalogen/TMEM189 vinyl-ether sub-step is deliberately not modeled (protective in Cui 2021 / Zoeller 1999 PMID 10051451, dispensable in Zou 2020, sensitizing-on-loss in Perez 2022 PMID 36178986); `0.0` default ⇒ ×1.0 ⇒ byte-identical, FFI maps to `0.0` so the C ABI/header is unchanged; MBOAT1/2 is the remaining follow-up PR of #339; v0.37.0 (#339 PR 3 of 3, closes #339) adds the off-by-default MBOAT1/2 MUFA-enrichment boost to `biochem`/`params` (`Params.mboat_mufa_boost: f64`): MBOAT1 (estrogen-receptor-regulated) and MBOAT2 (androgen-receptor-regulated) remodel phospholipids toward MUFA-PE and suppress ferroptosis independently of GPX4 (Liang 2023 Cell PMID 37267948), modeled as a constant additive MUFA protection layered onto the dynamic SCD1 `mufa_protection` at the two live peroxidation sites via the new `total_mufa_protection` helper (floored at `0`, enrichment is protective-only), so a higher value (e.g. an AR-driven MBOAT2-high tumor) lowers ferroptosis, the OPPOSITE direction to the ether-PUFA pool; `0.0` default ⇒ byte-identical, FFI maps to `0.0` so the C ABI/header is unchanged; this completes the #339 lipid-remodeling escape trio (kinetic MUFA + ether-lipid + MBOAT); v0.38.0 (#340) replaces the static-iron assumption with an off-by-default dynamic labile-iron path: `Params.ferritinophagy_release` (+ ramp `ferritinophagy_tau`) and `biochem::ferritinophagy_iron_factor(step, params)` scale the Fenton iron at all three Fenton sites by a time-dependent factor that ramps from `1.0` toward `1 + release`, modeling NCOA4-mediated ferritinophagy releasing stored iron into the labile pool over the run (Mancias 2014 Nature PMID 24695223; Hou 2016 Autophagy PMID 27245739), so enabling it raises Fenton ROS and ferroptosis; a companion `oxygen::hypoxia_iron_factor(o2_supply, sensitivity)` helper lets a consumer raise iron release in hypoxic zones (HIF/TfR1 up), qualifying the §7.1 hypoxia-leg framing (hypoxia can RAISE the iron/Fenton contribution even as it lowers the O2-dependent SDT yield from #336, so "hypoxia uniformly protects RSL3" is too strong); `release=0.0` ⇒ factor exactly `1.0` for every step ⇒ byte-identical, FFI maps to `0.0` so the C ABI/header is unchanged; #365 wires both into sim-tme-3d (`Overrides.ferritinophagy_release` + `Overrides.hypoxia_iron_sensitivity` applying `hypoxia_iron_factor` to per-cell `cell.iron`), with a spatial A/B confirming hypoxia-iron raises RSL3 ferroptosis kill several-fold including in the deep hypoxic core (grid 60/λ=50: overall 0.5%→2.9%, hypoxic-zone 0%→1.3%); the deep-core rise is a flagged MODEL ARTIFACT (the Fenton term is O2-independent — `iron×rate` added to total ROS, model modulates only basal_ros not the Fenton rate — so a real anoxic core losing its H2O2 substrate is NOT captured), so the robust result is the direction (hypoxia can raise the RSL3 iron contribution, "hypoxia uniformly protects RSL3" too strong), not the deep-core magnitude; §7.1 reports this combined with the #358 O2-dependent SDT leg, off-by-default byte-identical (production matrix SHA unchanged); the two §7.1-leg viz presets landed as `--snapshot=sdt-o2dep` (#380, SDT O2-dependent hypoxic-core survival front) + `--snapshot=ferritinophagy` (#381, RSL3 iron-amplified ferroptosis front), both off-by-default byte-identical sim-tme-3d-only presets; v0.39.0 (#337) adds the off-by-default immunosuppressive arm of ferroptosis to `immune_spatial`/`params`/sim-tme-3d (`SpatialImmuneConfig.ferro_immunosuppression_strength` + `immune_spatial::ferroptotic_immunosuppression(local_damp, strength) = 1/(1 + strength·local_damp)`): the immune layer otherwise treats ferroptotic ICD as net pro-immune (DAMP -> DC -> CD8 kill), but ferroptotic cells co-release DC-suppressing factors (extracellular GPX4 binding DC ZP3, Liu 2026 Cell PMID 41494530; oxidized lipids / PGE2, Kim 2022 Nature PMID 36385526; ferroptotic cells impede DC immunity, Wiernicki 2022 Nat Commun PMID 35760796), so the new multiplier scales immune kill DOWN as the local ferroptotic-death/DAMP signal rises, keyed on the SAME `local_damp` that drives pro-immune `dc_activation` so the NET immune effect can FLIP sign at sustained/high death density (routed through the realism kill path so the default hot loop is untouched, `immunosuppressive_ferroptosis_reduces_immune_kills` A/B test); direction is timing-dependent (early ferroptotic cells can be immunogenic, Efimova 2020 PMID 33188036), magnitude is a placeholder and the sign is the result; `strength=0.0` default ⇒ multiplier `1.0` ⇒ byte-identical, not in the C ABI so the header is unchanged, no snapshot preset yet (reachable via `Overrides.immune`); v0.40.0 (#342) adds the off-by-default persister reversible-to-irreversible epigenetic-locking transition to `persister`/`params` (`PersisterConfig.lock_rate`/`lock_threshold`/`exposure_decay` + `persister::step_with_locking` + `PersisterState`): the persister model otherwise treats all persistence as freely reversible, but beyond a threshold of SUSTAINED drug exposure persistence becomes epigenetically locked / irreversible (FSP1/HDAC-suppressed durability, Higuchi 2026 Sci Adv PMID 41481741); a sustained-exposure EMA (`cumulative·(1-exposure_decay)+drug`, steady state `avg_drug/exposure_decay`) gates locking, so once it crosses `lock_threshold` a fraction `lock_rate` of the reversible pool moves into the non-reverting `locked` pool, and because the EMA decays in drug-off windows only continuous dosing locks while intermittent dosing settles below threshold (intermittent-vs-continuous A/B test); `lock_rate=0.0` default ⇒ `step_with_locking` reduces EXACTLY to `step` ⇒ byte-identical, not in the C ABI, library-only so far (sim-tme-3d consumer wiring is a follow-up); v0.41.0 (#341) adds the off-by-default `senescence` module (therapy-induced-senescence ferroptosis program): `SenescenceConfig` (`fraction`/`iron_mul`/`gpx4_mul`/`nrf2_mul`/`fsp1_mul`, identity at fraction 0) + `apply_senescence_program_3d` marks a fraction of tumor cells (independent RNG) and scales their iron/defenses; the ferroptosis direction is genuinely AXIS-dependent so the program is BIDIRECTIONAL not single-signed (CORRECTING the issue's one-sided "resist" framing): senescent cells RESIST upstream cystine/iron triggers via compartmentalized iron + raised antioxidant capacity (Masaldan 2018 PMID 28888202, Loo 2025 PMID 40731111) but are a SENOLYTIC target under DIRECT GPX4 inhibition (the model's RSL3) because they are primed/GPX4-dependent (D'Ambrosio/Gil 2026 Nat Cell Biol PMID 42032311); the unit test drives BOTH nets under RSL3 (iron-dominant ⇒ senolytic, defense-dominant ⇒ resist); `fraction=0` default ⇒ byte-identical, not in the C ABI; v0.42.0 (#341) wires senescence into sim-tme-3d (`--snapshot=senescence` preset + `senescent_fraction` summary metric) and adds the optional SASP→immune coupling: `SenescenceConfig.sasp_immune_mult` is a signed per-cell multiplier on a senescent cell's immune-kill probability, bidirectional and STAGE-dependent like the ferroptosis axis: `>1` anti-tumor senescence immune surveillance is the pre-malignant arm (Kang 2011 PMID 22080947), `<1` immunosuppressive SASP recruiting surveillance-antagonizing myeloid cells is the established-tumor arm (Di Mitri 2014 PMID 25156255; net stage-dependent, Eggert 2016 PMID 27728804), so `literature()` defaults to `<1` for the established therapy-induced-senescence stage the module models; the coupling is a cell-autonomous simplification of an inherently paracrine signal (neighbor/bystander effects deferred to a diffusing SASP-field follow-up); `1.0` default ⇒ no coupling ⇒ byte-identical (production matrix SHA unchanged; revived clonal-repopulation sites are cleared from the senescence mask to stay coherent; SASP-only A/B + preset-wiring + clonal-composition tests); v0.43.0 (#342) wires the persister epigenetic-locking transition into sim-tme-3d: the per-cell `PersisterState` is stored on `CellState` (`persister_reversible`/`persister_locked`/`persister_cum_exposure`, all `0.0`-inert default so the production matrix SHA is unchanged), advanced via `step_with_locking`, with a `persister_locked_mean` summary metric and a spatial `continuous_dosing_locks_persisters_but_intermittent_does_not` test (continuous dosing locks a non-negligible fraction, intermittent locks nothing; the library unit test separately runs a full washout to confirm the locked fraction does not revert); the drug-driven acquisition is the drug-exposure entry trigger, with a non-drug hypoxia/nutrient stress-niche entry trigger delivered in v0.47.0/#377; v0.44.0 (#383) adds the off-by-default O2-dependent Fenton option (`oxygen::fenton_o2_factor(o2_supply, dependence) = (1−dep)+dep·o2_supply` + sim-tme-3d `Overrides.fenton_o2_dependence`): the Fenton reaction needs O2-derived H2O2 (superoxide→SOD→H2O2, the Haber-Weiss chain; Kehrer 2000 Toxicology PMID 10963860 / ferroptotic LPO is an O2-consuming lipid-radical chain, reviewed in Stockwell 2017 Cell PMID 28985560), so it scales the effective Fenton flux DOWN where O2 is low by scaling `cell.iron` (its only Fenton consumer) at the consumer, composing multiplicatively with the #365 hypoxia_iron rise; this CORRECTS the #365 deep-core MODEL ARTIFACT (the O2-independent Fenton let hypoxia-iron wrongly "rescue" the anoxic core) — the spatial A/B (`o2_dependent_fenton_protects_the_anoxic_core_under_hypoxia_iron`, same grid 60/λ=50/180 config as the #365 test) confirms that with hypoxia-iron on AND the Fenton fully O2-coupled the deep hypoxic-zone kill drops by more than half back toward the un-rescued baseline, so the net deep-core effect is no rescue (hypoxia raises iron but starves H2O2 while the rim/mid-zone can still rise); §7.1 updated (the artifact is now addressable rather than only flagged); `fenton_o2_dependence=0` default ⇒ factor 1.0 ⇒ `cell.iron` unchanged ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI; v0.45.0 (#363) adds phenotype-specific SCD1/MUFA accumulation rates: a per-cell `Cell::mufa_rate: Option<f64>` (`None` ⇒ the global `scd_mufa_rate`, threaded into `update_mufa_protection`) + the new `phenotype_mufa` module (`PhenotypeMufaConfig` per-phenotype rate multiplier, `identity()` default; `apply_phenotype_mufa_rates_3d` sets each tumor cell's rate from `gc.phenotype`) wired into sim-tme-3d as off-by-default `Overrides.phenotype_mufa` (gated on non-identity); follow-up to #339 (which used one global `scd_mufa_rate`), so the acute-vs-established MUFA build-up now gets a phenotype-specific time constant; DIRECTION genuinely uncertain (MUFA enrichment resists ferroptosis Magtanong 2019 PMID 30686757 + SCD1 protects Tesfay 2019 PMID 31270077, but persisters are also GPX4-dependent/vulnerable Hangauer 2017 PMID 29088702) so `literature()` is an UNCALIBRATED placeholder, library test confirms a faster per-cell rate builds more `mufa_protection` (the "two phenotypes diverge" criterion at the right readout; the spheroid kill COUNT is an insensitive readout given the bistable switch); reachable via `--snapshot=phenotype-mufa` (spheroid context), and revived clonal-repopulation cells re-derive their rate via `apply_phenotype_mufa_rate_at_3d` (coherent with #266/#302); `mufa_rate=None`/identity ⇒ byte-identical (production matrix SHA unchanged), FFI maps it to `None` so the C ABI/header is unchanged; delivers the per-phenotype RATE (the per-phenotype CAP follows in v0.46.0/#390); v0.46.0 (#390) completes #363 with the per-phenotype SCD1/MUFA CAP (steady-state) multiplier: `PhenotypeMufaConfig` gains 5 `*_cap` fields + `cap_multiplier()`, and the apply fns are renamed `apply_phenotype_mufa_rates_3d`→`apply_phenotype_mufa_3d` / `_rate_at_3d`→`_at_3d` and take a new `base_max` (=`scd_mufa_max`) so they set BOTH the per-cell `mufa_rate` and scale the per-cell `mufa_cap` MULTIPLICATIVELY (cap-mul `1.0` ⇒ cap untouched, preserving a spheroid radial cap; applied AFTER the spheroid so the two compose); `phenotype_caps_yield_different_mufa_steady_states` confirms a 2x-cap persister plateaus higher than glycolytic, the cap direction is even more uncertain than the rate so `literature()`'s `*_cap` are placeholders; both axes off-by-default identity ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI; closes the #363 rate/cap pair; v0.47.0 (#377) adds the non-drug stress-niche persister entry trigger, completing #342's two-route entry: `PersisterConfig.stress_entry_rate` (off-by-default `0.0`) + `persister::stress_entry(state, stress, cfg)` raise the REVERSIBLE persister pool from a hypoxic/nutrient-poor drug-sanctuary niche, DECOUPLED from `drug_intensity` (stress drives ENTRY, drug drives durability) so it never feeds the locking EMA (#342) or the GPX4 resistance; wired into sim-tme-3d with the stress signal = the local hypoxia deficit `1 - o2_supply` (same per-cell O2 field as #336/#383), applied after `step_with_locking`, with a `stress_niche_drives_nondrug_persister_entry` A/B confirming hypoxic-zone persister entry under ZERO drug while the stress-off baseline stays exactly 0; `is_identity()` now also checks `stress_entry_rate` (a stress-only config is non-identity); `stress_entry_rate=0` ⇒ no-op ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI; v0.48.0 (#376) adds the diffusing SASP FIELD (the paracrine/bystander extension of #341's cell-autonomous `sasp_immune_mult`), completing #341: `SenescenceConfig.sasp_field_strength` (off-by-default `0.0`, SIGNED) + `immune_spatial::sasp_field_kill_multiplier(field, strength)` (`strength > 0` immunosuppressive `1/(1+strength·field)` LOWERS kill, `strength < 0` surveillance `1+|strength|·field` RAISES it, identity at field 0 or strength 0) — the consumer (sim-tme-3d) seeds a SASP field at the senescent cells (the senescence mask), diffuses it with `diffuse_damp_3d_step` (transport constants mirror the suppressor field's), and multiplies the signed factor into the immune kill probability of EVERY exposed cell, including adjacent NON-senescent tumor cells (the neighbor coupling #341 could not express; surveillance recruits CD4/monocytes per Kang 2011 PMID 22080947, immunosuppressive IL-1RA protects neighbors per Di Mitri 2014 PMID 25156255 / Eggert 2016 PMID 27728804); a library test confirms the field reaches a non-senescent Moore-neighbor and a sim-tme-3d FIELD-only A/B (cell-autonomous coupling + biochem held identity) confirms net immune kills still shift bidirectionally, so the bystander effect is real; `literature()` sets `sasp_field_strength=0.5` (immunosuppressive, the established-tumor arm) so the `--snapshot=senescence` preset now also exercises the field (and the dedicated `--snapshot=sasp-field` preset #398 writes a `sasp_field.npy` overlay of the quasi-steady diffused field, rendered as a "SASP field" panel by render_tme_3d_trajectory.py); `sasp_field_strength=0.0` default ⇒ multiplier `1.0` ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI; v0.49.0 (#331) adds the shared biochem parameter-override layer to `params` (`apply_param_overrides` — the single name→field mapping lifted out of the Python binding so the binding AND the simulation binaries perturb the SAME rate constants; `parse_param_overrides_json` + `overrides_from_env_value` + `param_overrides_from_env` read a `FERRO_PARAM_OVERRIDES` env var holding inline JSON or a file path), wired into `sim-tme` + `sim-combo-mech` off-by-default (env unset ⇒ empty overrides ⇒ no-op ⇒ byte-identical default run, confirmed by release-mode A/B; an override changes the hypoxia/immune and Bliss-synergy outputs respectively); enables driving global-sensitivity / calibration of the spatial+combo headline outputs (#331 follow-up to the single-cell Sobol #386) without recompiling; the binding refactor preserves its exact error message; not in the C ABI; v0.50.0 (#343 PR 1) adds the `reaction_diffusion` module (steady-state linear reaction-diffusion supply field `D∇²c − k·c = 0` over the same vessel network the exponential proxy uses, with vessel Dirichlet sources, tumor-only consumption `γ = (h/λ)²`, and finite-volume no-flux domain boundaries, solved by deterministic SOR; `ReactionDiffusionConfig` + `reaction_diffusion_solve`/`reaction_diffusion_supply_field` + `analytical_1d_slab`): the vasculature/oxygen proxies are monotonic in distance-to-nearest-source, but a real irregular vessel network superposes all nearby vessels while consumption draws the field back down, so the steady state has non-monotonic pockets the proxy averages away (addresses #343's biphasic-field gap); the diffusion-consumption length `λ = sqrt(D/k)` equals the proxy's `lambda_um`, so a single isolated source reproduces `exp(−dist/λ)` (the proxy is exactly the single-source limit) and divergence is the genuine multi-vessel/consumption effect; verified by an analytical self-consistency check against the closed-form 1-D reaction-diffusion cosh slab (the SOR discretization converges to the exact solution of its own continuous equation, max abs error < 0.01; `analytical_1d_slab` is evaluated in an overflow-safe subtracted-exponent form) + a two-source midpoint-enrichment test showing the proxy under-supplies multi-vessel pockets — the GENUINE external benchmark the issue asks for (proxy-vs-RD field/kill + a PhysiCell/published-PDE cross-check) and the proxy-adequacy documentation are the remaining #343 criteria delivered in PR 2; opt-in alternative supply field, nothing in the production matrix calls it ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI; #343 PR 2 wires it into sim-tme-3d (`Overrides.reaction_diffusion` swaps `vessel_supply_field`→`reaction_diffusion_supply_field` when explicit vasculature is on, gated on vasculature which is off the matrix ⇒ byte-identical; `--snapshot=reaction-diffusion` renders the RD field in the vessel_supply panel) + the proxy-vs-RD benchmark `analysis/reaction-diffusion-benchmark.md`, whose headline (in the λ ≲ vessel-spacing regime, λ=120µm here) is that the monotonic `exp(-dist/λ)` proxy reads optimistic — RD lower at ~100% of cells — DOMINATED BY 3D POINT-SOURCE GEOMETRY (the proxy is the planar `exp(-d/λ)`; a 3D point vessel's field is the Yukawa `exp(-r/λ)/r`, so a SINGLE isolated vessel already shows a 0.85→0.31 first-neighbour gap before any multi-vessel effect; cumulative consumption is mostly already in `λ=sqrt(D/k)` so it is NOT a separate omitted driver, and superposition/enrichment is small here), flipping a sparse tumor the proxy calls 62% hypoxic to 99% hypoxic; the DIRECTION flips with λ (depletion when λ ≲ spacing, enrichment when λ ≫ it, since `γ=(h/λ)²→0` ⇒ Laplace) and the magnitude depends on the single-voxel vessel representation + grid h, so the result is DIRECTIONAL not calibrated; the genuine external PhysiCell/BioFVM cross-check is honestly deferred (the analytical 1D self-consistency check stands in PR 1); current crate version 0.50.0; unit-test count tracked in CI / `cargo test --workspace`) + Python bindings + 219 Python tests (pipeline smoke + figure traceability + depth-kill physics-constant guard + flagship-figure data guard + quantitative-figure drift guards (Figs 21/22/23) + invariant/integration + calibrate-extractor + MeSH evidence-fallback + gold-set precision-floor regression (#346) + Bliss/sim-tme/penetration prior-predictive intervals (#332) + non-circular mechanism-recall (#412) + ferroptosis-python bindings)
- news authentication pipeline: 5 scripts (fetch, extract claims, verify against PubMed, score credibility, build claim-centric index) implementing the 3-tier source framework from analysis/news-source-criteria.md
- simulation calibration: 8 targets documented (5 original + 3 new 3D self-consistency targets from #196 covering hypoxia-RSL3 collapse, immune SDT/RSL3 ratio, stromal boundary shielding); evaluate script operational; 3D validation infrastructure in place (`3d_validation_report.md` + upgrade-to-calibration path); `simulations/calibration/CALIBRATION_STATUS.md` is the single honest per-layer accounting (calibrated / partially anchored / self-consistency / uncalibrated) + calibration roadmap, consolidating the previously-scattered caveats — the suite is broad but mostly uncalibrated (mechanistic scaffolding, excluded from the manuscript's quantitative claims)
- manuscript honesty pass (integrity audit): the 3D suite was absent from the manuscript + several §8.4 "the model cannot do X" caveats had gone stale (clonal/persister DO model adaptive evolution; suppressor/checkpoints DO model immune dampening; vasculature DOES replace the edge-O2 proxy — all uncalibrated); fixed by a consolidated §8.4 "3D simulation suite" honesty paragraph (names the suite, its uncalibrated status, the deliberate exclusion from quantitative claims, and the 104:1→4:1 immune-ratio shrinkage in 3D) + drift fixes (version 0.2.0→0.25.0, "ten modules"→21, missing sim-tme-3d/sim-tumor-pk binaries); `v1.md` edited then `v1.tex` regenerated (compiles, ~115 pp)
- manuscript scientific review (deep external+internal cross-check, `analysis/manuscript-scientific-review.md`): five-lens review (supporting lit, contradicting lit, math/physics, fresh-eyes coherence, figure gaps) with every new PMID personally verified. Math/physics confirmed sound (Beer-Lambert, Krogh, acoustic attenuation, Bliss 1.99×, ferroptosis ODEs); fixed the `iron_diffusion_coeff` mis-citation (was Jacques-2013-optics → tortuosity-reduced estimate). Key science: the central "physical ROS BYPASSES hypoxia" claim was over-extended vs the SDT field (SDT ROS is widely O2-dependent; lead clinical agent SONALA-001 is Type II/O2-dependent), so §7.1/§7.2/§7.5/§8.4/§10.1 were rebalanced (direction survives, "bypass"+magnitude do not) with 6 verified citations added on both sides (Hubbi/Dang 2026 PMID 41932308 supports the mechanism in PDAC but shows it combines GSH-defense + mito-ROS-suppression; Zou 2019 PMID 30962421 shows hypoxia SENSITIZES ccRCC to RSL3; Hayashi 2020 PMID 33288764 DAMP-balance≠quantity; Dai 2020 PMID 33311482 ferroptosis accelerates PDAC in vivo). Figure roadmap documented; figures not yet generated
- drug penetration module: 3 tissue types, exponential Krogh approximation
- drug combination modeling: 4 drugs, pairwise Bliss synergy scoring with pathway traces
- tumor microenvironment: oxygen gradients (edge-distance proxy, explicit internal vessel network #191, patient-scale planar depth gradient #240, or the steady-state reaction-diffusion field #343 that replaces the monotonic nearest-vessel proxy with a true diffusion+consumption solve), spatial immune zones (DAMP diffusion, T cell activation, anti-PD-1, T-cell exhaustion #243, Treg/MDSC suppressor field + multi-checkpoint PD-1/CTLA-4/LAG-3/TIM-3 brake #264), LP overshoot multiplier, CAF-mediated stromal protection (GSH/MUFA supply), pH gradient (iron release + drug ion trapping), cell-cell contact resistance (E-cadherin/NF2-YAP, dense cells lower PUFA/iron #270)
- some landmark papers are known to be missing from the local full-text archive
- content provenance manifest (PROVENANCE.yaml) documents asset licensing and redistribution rights
- pinned Python environment (requirements-lock.txt, 32 packages) and Rust toolchain (rust-toolchain.toml, 1.96.0)
- contributor guide (CONTRIBUTING.md), citation metadata (CITATION.cff), and pytest in tracked dependencies
- Python CI workflow (.github/workflows/python-test.yml): Linux on PR/push, macOS weekly
- Rust CI workflow (.github/workflows/cargo-test.yml): `cargo test --workspace` + `cargo fmt --all --check` gate on PR/push (fmt pinned to the 1.96.0 toolchain, #209/#236); the `test` job also builds `ferroptosis-ffi` and compiles + runs the C harness (`tests/test_ffi.c`) against the cdylib, verifying the PhysiCell C-ABI per-PR, plus a `git diff --exit-code` guard on the build.rs-regenerated cbindgen header so a stale committed `ferroptosis.h` fails CI (#295 item 4)
- sim-tme-3d production regression workflow (.github/workflows/sim-tme-3d-regression.yml): weekly + on-demand full 60³×180 run asserting `summary.json`'s SHA-256 against a checked-in hash on the pinned 1.96.0 toolchain (#253)
- figure traceability index (FIGURES.yaml) mapping all 28 figures to generators, inputs, and types (#285 quantitative simulation figures: hypoxia kill-collapse, Bliss synergy, treatment-timing window, depth-kill curves, and the flagship 2×2 resistance-mechanism asymmetry — manuscript figure 24)
- archival release tooling (.zenodo.json metadata template, scripts/generate_release_manifest.py for SHA256 manifest + filtered archive)

## What To Optimize For

- claims that are traceable and caveated
- taxonomy choices that do not inflate conclusions
- language that reflects uncertainty honestly
- outputs that help compare alternatives fairly
- maintainable scripts and reproducible reruns
- a tone that invites contribution rather than gatekeeping

## What To Avoid

- assuming the repo exists only to defend PDT/SDT
- writing stronger absence claims than the evidence model supports
- confusing patient-study signal with phase-labeled trial maturity
- treating broad process coverage as proof of therapeutic depth
- letting historical framing survive after the underlying analysis changed
- making the codebase feel closed or intimidating to newcomers

## Key Files

```text
README.md                                    repo-level framing and entry points
article/drafts/v1.{md,tex,pdf}               manuscript drafts
analysis/evidence-coverage-audit.md          evidence-tier coverage and guardrails
analysis/taxonomy-rerun-notes.md             taxonomy/query caveats after reruns
analysis/pathway-target-audit.md             pathway-target coverage
analysis/landmark-corpus-gaps.md             known missing papers that distort claims (VISION recovered #345)
scripts/landmark_coverage.py                  landmark-coverage guardrail + recovery (#345): per-mechanism membership check + fetch-and-add-as-abstract for absent field-definers
scripts/taxonomy_coverage.py                   expanded-taxonomy coverage (#347): 25-mechanism PubMed + local coverage report; cuproptosis/disulfidptosis added to config (scaffold; frozen 19-mechanism results untouched)
scripts/oa_bias_analysis.py                    open-access/journal bias on mechanism rankings (#348): per-mechanism OA-rate table + full-text-vs-abstract-only ranking sensitivity; regenerates §3.3.1's hand-derived shifts (immunotherapy 34.4→28.7% share, physical class 14.7→22.4%, bioelectric rank 14→3) into analysis/oa-bias-report.md
scripts/sobol_sensitivity.py                   Sobol global sensitivity + practical identifiability (#331): self-contained Saltelli/Jansen over the SAME 10 biochem rate constants + ranges as the PRCC (#134, the PRCC's 11 minus SDT-only sdt_ros, loaded from prcc-results.json) for the ferroptosis kill switch; total-effect ranking lp_propagation (ST=0.50), gpx4_rate (0.29), lp_rate (0.18) reproduces the PRCC top-three in order (~89% as first-order), interactions small (ΣS1≈1.05 confirms PRCC's 'no Sobol escalation' call); 6/10 params kill-rate-insensitive → flagged non-identifiable in parameter_provenance.md; analysis/sobol-sensitivity-report.md (per-headline-output sensitivity for the spatial/combo headlines is the #331 follow-up, see scripts/headline_sensitivity.py)
scripts/headline_sensitivity.py                per-headline Morris sensitivity (#331 PR2+PR3): screens which of the 11 PRCC rate constants drive a HEADLINE output the simulation binaries produce, perturbing them through the FERRO_PARAM_OVERRIDES hook (#331 PR1). Morris elementary effects (mu*/sigma) rather than Saltelli/Sobol because each binary run is far costlier than sim_batch, so a full variance design is infeasible; self-contained estimator (no SALib), analytically validated in tests; multi-headline (per-headline trajectory count: many for cheap sim-combo-mech, few for costly sim-tme; binary runs isolated in temp cwds for parallelism). (1) Bliss-synergy headline (RSL3+FSP1i synergy_score from sim-combo-mech): top drivers lp_propagation/gsh_scav_efficiency/gpx4_rate (the LP+GSH/GPX4 axis), sigma>mu* for every active param so the synergy is non-additive (interaction or single-param nonlinearity), structural zeros sdt_ros + rsl3_gpx4_inhib (no SDT in the pair; combo-mech bakes RSL3 GPX4-inhibition into a fixed DrugEffect) vs the empirical/below-resolution zero gpx4_degradation_by_ros. (2) Hypoxia kill-collapse headline (#331 PR3 — SDT-minus-RSL3 hypoxic-zone gap from sim-tme): sdt_ros is the single largest driver (SDT's hypoxic efficacy is primarily dose-driven, the model-side §7.1 confirmation), but the LP/GSH defenses (lp_rate/gsh_scav_efficiency/lp_propagation) also move the gap substantially because the SDT hypoxic kill (~0.87) is high-but-not-saturated, so SDT is dose-driven yet defense-MODULATED, not defense-independent (screen holds SDT O2-independent, the §7.1 upper bound). (3) Immune-amplification headline (#331 PR5 — SDT's POOL-DE-CONFOUNDED immune kill rate immune_kills/(total_tumor-ferroptosis_kills) at SDT/immune_on, screened from the SAME sim-tme runs as hypoxia via a multi-output Morris path): the raw immune_kills ratio MATCHES the headline (SDT>>RSL3 ~104:1 at default, the immune-coupling figure; an earlier "runs opposite" claim was a misread of override-contaminated output, corrected in PR4); the de-confounding controls for SDT clearing most cells by ferroptosis first and is bounded [0,1]. Top drivers lp_propagation/lp_rate (the LP-cascade), with sdt_ros close behind — the per-cell immune amplification rides on the SAME death switch (DAMP = ferroptotic-death density), NOT a separate immune axis; the informative CONTRAST with hypoxia (where sdt_ros led) is that SDT's hypoxic SURVIVAL is dose-driven but its per-cell immune AMPLIFICATION is death-density-driven. The immune-coupling params (DAMP diffusion, DC activation) are not in the PRCC biochemical set. All THREE headlines now screened, closing #331. Report: analysis/headline-sensitivity-report.md
scripts/uncertainty_intervals.py                prior-predictive uncertainty intervals on the kill switch (#332): uniform priors over the 11 PRCC rate-constant ranges (prcc-results.json) → Monte-Carlo (2000 draws) through `sim_batch` → 2.5/50/97.5 death-rate intervals per phenotype×treatment; the RSL3 rates span nearly [0,1] (Persister×RSL3 42.5% point — at the Figure 7 n=1M — but [1.6%,99.7%]) so they're least parameter-constrained, while the Glycolytic-RSL3 floor + Persister-SDT ceiling are robust (OXPHOS/Glycolytic-SDT wider); the NRF2-rescued persister stays resistant at point (0%) but its interval reaches ~38%; PRIOR-PREDICTIVE not a data-conditioned posterior (a true posterior needs the #330 GDSC data); analysis/uncertainty-intervals-report.md. The spatial/combination-headline extension has begun: scripts/headline_uncertainty.py
scripts/headline_uncertainty.py                 Bliss-synergy prior-predictive interval (#332 spatial extension): the same uniform PRCC priors propagated through the RSL3+FSP1i `synergy_score` from sim-combo-mech (reusing `headline_sensitivity.run_bliss` + the override hook), 300 Monte-Carlo draws → the Bliss headline (~1.99x point) spans a 95% prior-predictive interval ~[1.0x, 5.2x] (median ~1.35x), so the supra-additive DIRECTION is robust at the lower bound while the magnitude is not; PRIOR-PREDICTIVE (parameter, not structural; posterior blocked on #330); analysis/headline-uncertainty-report.md. `--headline sim-tme` extends it to the two spatial sim-tme headlines at a smaller 100-draw ensemble (sim-tme ~4 min/run): the hypoxia kill-collapse gap (SDT−RSL3 hypoxic kill) stays POSITIVE across its 95% interval ~[0.16, 1.00] (median 0.96, point 0.87) — collapse-asymmetry direction parameter-robust, magnitude wide — and SDT's de-confounded immune rate spans ~[0.009, 0.171] (median 0.042), robustly low-but-positive; reuses `run_sim_tme_observables`, defensive per-observable NaN partition (`_partition_tme`); analysis/headline-uncertainty-tme-report.md. `--headline penetration` completes the set via sim-tissue-pk (~4 s/run, the #331 override hook wired in off-by-default/byte-identical): the per-tissue vessel-wall RSL3 kill (the 40%→12.1%/2.6%/1.8% headline) carries very wide intervals at every tissue (well-vasc median 0.23, ~[0.00,0.93]; poorly-vasc 0.08, ~[0.00,0.83]; CNS/BBB 0.04, ~[0.00,0.77]) because the bistable RSL3 switch dominates, BUT the WITHIN-DRAW across-tissue ORDERING (well>=poorly>=CNS, same biochem applied at decreasing fixed vessel-wall concentration) held in 300/300 draws — penetration-gradient direction parameter-robust (data-backed per-draw, NOT inferred from the overlapping marginals), absolute magnitudes not; transport params held at fixed uncalibrated presets (their own ranges undocumented, not swept); analysis/headline-uncertainty-penetration-report.md. All four headline families now read as prior-predictive intervals; only the data-conditioned ABC posterior remains (blocked on #330)
scripts/mechanism_recall.py                    non-circular mechanism-tagging recall (#412): measures the MECHANISM keyword tagger's recall against expert-assigned MeSH descriptors as an INDEPENDENT reference label, removing the circularity that get_searchable_text would otherwise inject (MeSH is folded into the production matched text). Two guards: (1) re-runs the tagger on a LEAKAGE-FREE text (`get_searchable_text(..., include_metadata=False)` => title+abstract only, MeSH/disease/gene/drug annotations excluded — the load-bearing #412 fix in tag_articles.py), and (2) the headline uses only INDEPENDENT descriptors (a descriptor whose own string would trip the mechanism's keywords is classified LEAKY via the live `text_matches_keyword` matcher so it can never drift, and reported separately as near-tautological). Headline (independent pool >= N_MIN=30, frozen corpus): 9 measurable mechanisms, 90.0% volume-weighted / 87.5% macro recall (ADC 97.5%, synthetic-lethality 94.9%, electrochemical 93.7%, metabolic 92.9%, car-t 90.6%, immunotherapy 89.2%, sonodynamic 87.9%, bispecific 86.1%, epigenetic 54.7%). Epigenetic is the honest low: a mix of a real keyword-coverage gap (the field's "epigenetic modifications/regulation", EZH2/BET inhibitor terms are not in the keyword set — an actionable finding) and reference-scope breadth of the lone independent "DNA Methylation" leaf. Mechanisms whose only discriminative MeSH leaf is keyword-tautological (crispr/nanoparticle/oncolytic-virus/microbiome/etc.) or have no descriptor (ttfields/CAP/cuproptosis/disulfidptosis/radioligand) are reported NOT-MeSH-measurable, never 0%. Distinct from the evidence-tier 55% recall; does not touch the 96% evidence precision. Offline/deterministic: analysis/mechanism-recall-report.md + analysis/mechanism-recall.json
analysis/mesh-mechanism-map.yaml               handwritten MeSH-descriptor -> mechanism reference map for #412 (precision-first leaf descriptors per mechanism + an `unmeasurable:` block with reasons); the leak classifier splits each into independent/leaky at runtime
analysis/reaction-diffusion-benchmark.md     reaction-diffusion vs exponential-proxy supply benchmark (#343): in the λ ≲ vessel-spacing regime the proxy reads optimistic (dominated by 3D point-source geometry, not consumption), sparse 62%→99% hypoxic; direction flips with λ and magnitude is representation-dependent, so directional not calibrated; PhysiCell cross-check honestly deferred
analysis/manuscript-scientific-review.md     external+internal scientific review: verified support/contradiction, math check, reframe rationale, figure roadmap
analysis/research-roadmap-2026.md            deep web-grounded gap analysis + prioritized issue backlog (#330-#354): validation/calibration, model-biology gaps, corpus, contribution strategy
analysis/contribution-plan-2026.md           contribution plan (#353): venue split (corpus vs simulation), preregistration draft of the falsifiable predictions, collaborator experiment brief
analysis/ode-cross-validation.md             ODE cross-validation (#344): ferroptosis-core's bistable recover-or-collapse switch vs published models (Co 2024, Seidel 2026, Konstorum 2020); figure + script in simulations/calibration/
analysis/sbml-export.md                      SBML export (#351): deterministic single-cell core ODE -> SBML L3V2 (simulations/sbml/ferroptosis_core.xml), libSBML-validated + roadrunner round-trip; documents exportable vs not-exportable modules
simulations/calibration/CALIBRATION_STATUS.md per-layer calibration tiers + roadmap (the honest sim accounting)
MODEL_CARD.md                                model card: intended use, out-of-scope, assumptions/scope checklist, per-layer calibration/validation status (consolidates CALIBRATION_STATUS for external readers; #352)
corpus/INDEX.jsonl                           master index
scripts/                                     Python pipeline
simulations/                                 Rust simulation work
simulations/ferroptosis-python/              Python bindings (PyO3)
simulations/ferroptosis-ffi/                 C FFI bindings (PhysiCell integration)
tags/                                        precomputed tag indexes
article/book-outline.md                      frozen book outline and chapter contracts
article/AUTHORING.md                         writing rules and heading conventions
news/                                        news source scaffolding (issue #99)
PROVENANCE.yaml                              content provenance and redistribution rights
CONTRIBUTING.md                              contributor setup, testing, and PR guide
CITATION.cff                                 citation metadata (renders GitHub "Cite" button)
requirements-lock.txt                        pinned Python dependency versions
FIGURES.yaml                                 figure-to-script traceability index (28 figures)
.github/workflows/python-test.yml            Python CI (Linux PR/push, macOS weekly)
.github/workflows/cargo-test.yml             Rust CI (cargo test + cargo fmt --check gate)
.github/workflows/sim-tme-3d-regression.yml  sim-tme-3d production byte-identity regression (weekly + manual)
.github/workflows/living-review.yml          living-review (PRISMA-LSR) monthly incremental corpus update (#349): re-runs queries.txt vs PubMed, uploads a dated delta as an artifact (never commits)
scripts/living_review_update.py               living-review delta (#349): re-runs the committed mechanism queries (queries.txt) over a trailing PubMed window, diffs vs the FROZEN corpus, writes a dated incremental index (corpus/living/<date>/) + delta changelog (analysis/living-review/<date>.md) with mechanism-tagged new records + landmark detections; pure delta/changelog logic is unit-tested, frozen corpus never mutated; corpus/living/README.md documents the frozen-vs-living split + cadence
.zenodo.json                                 Zenodo deposit metadata template
scripts/generate_release_manifest.py         SHA256 manifest + filtered archive builder
```

## Search Conventions

Prefer fast repo-native inspection first:

```bash
rg "term" scripts analysis article
rg --files corpus/by-pmid | head
sed -n '1,120p' analysis/evidence-coverage-audit.md
```

## Writing Conventions

- every strong claim should be traceable to the corpus, analysis outputs, or external verification
- separate generated outputs from handwritten interpretation notes
- use coverage-aware language such as `not detected in the local keyword-derived analysis` where appropriate
- if a known taxonomy artifact or corpus gap applies, mention it directly rather than burying it
- keep the repo open to thesis revision rather than optimizing for rhetorical neatness
