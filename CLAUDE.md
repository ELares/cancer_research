# CLAUDE.md

## Author

Ezequiel Lares

## What This Repo Is For

This is an open cancer-research workspace. The point is to help people—not to be right about one hypothesis.

The repo exists to compare therapeutic mechanisms, evidence depth, resistant-state biology, pathway targets, and simulation-based ideas honestly, so that anyone who reads it can form their own informed view. If the evidence says a direction is weak, say so. If a new direction looks promising, surface it. Don't protect old framing at the expense of clarity.

## Guiding Principles

1. **Let the evidence lead.** The repo started around a PDT/SDT-persister-ferroptosis thesis. That's still worth evaluating, but it's one lane among many. Don't treat it as the default winner.

2. **Stay open.** New modalities, targets, pathways, and interpretations should be welcomed when the corpus or external literature supports them. The README invites anyone with curiosity to contribute—the codebase should reflect that same openness.

3. **Be honest about what we don't know.** Corpus-level non-detection is provisional, not proof of absence. Missing landmark papers distort field-level claims. Taxonomy choices inflate or deflate conclusions. Say so directly rather than burying caveats.

4. **Make it reproducible.** Scripts should be re-runnable. Analysis outputs should be regenerated, not hand-edited. Separate generated files from handwritten interpretation notes so it's clear what came from the pipeline and what came from a person.

5. **Keep it human.** This project matters because cancer takes people from their families. Technical rigor serves that mission—but so does making the work accessible, welcoming contributions, and not hiding behind jargon when plain language works.

6. **The work is more important than the paper.** Don't optimize for journal word limits or publication constraints. If a finding needs context, give context. If a decision needs explaining, explain it. Clarity and contribution to the scientific community matter more than fitting a format. A reader should understand why we made each decision, what the results yield, and why we believe they signal value.

## Current Workstreams

- manuscript drafting and revision (Parts I–V substantially complete: ~115 pages, 11 chapters + 3 appendices)
- corpus fetching, enrichment, tagging, and indexing
- evidence-tier audits and coverage caveats (gold-set evaluation: 46% exact, 96% binary evidence-presence precision, 55% recall on the production keyword tagger; an off-by-default MeSH-descriptor fallback (#346, `FERRO_MESH_EXPANSION=1`) lifts gold-set evidence-detection recall to 67.8% at 95.2% precision, see `analysis/evidence-gold-mesh-eval.md` — not applied to the production corpus, so the 55% figure remains the corpus reality)
- evidence tagger v2 (#TAGGER-V2, `FERRO_EVIDENCE_V2=1`, OFF by default so `corpus/INDEX.jsonl` and every manuscript number stay byte-identical): section-scoped full text (the tagger previously never read the 216 MB of stored full text, which is why recall sat at 55%; reading it *whole* lifts recall but drops precision 96%→89% because Introduction/Discussion cite OTHER studies' trial phases, so only SELF sections — Methods/Results — are read), expanded `theoretical`/`preclinical-invitro`/`clinical-other` vocabularies, an in-vivo reagent guard ("murine … **cell line**"), an opinion-pub-type veto (editorials inherited the tier of trials they merely discussed), a theoretical-dominance rule, and a dedicated evidence prose channel that keeps structured MeSH/PubTator metadata out of the keyword matcher. Measured: exact-label error falls 2.59x on a 170-record held-out set and 1.95x on the 77-record human-consensus subset; binary F1 0.705→0.966 with precision held at 96%. Annotator agreement between the human labels and an independent full-text relabel is only 77%, which is the practical ceiling for the task. Method, rejected alternatives, and caveats: `analysis/evidence-tagger-v2.md`; regenerate with `python scripts/evaluate_evidence_v2.py`. NOT applied to the production corpus — promotion checklist at the end of that document.
- taxonomy and search refinement
- pathway-target and resistant-state analysis
- diagnostic-to-therapy chain extraction (10 chains, 240 articles mapped; #441 added HER2/BRCA/EGFR/KRAS-G12C targeted-therapy chains, recomputed from frozen text so INDEX stays frozen)
- tissue-of-origin analysis layer (5 tissue categories, 62% coverage)
- simulation work: ferroptosis biochemistry, drug penetration, calibration, photosensitizer PK (drug-light-interval scaling, saturating distribution phase, relative singlet-O₂ yield, FromStr-based clap CLI integration in sim-spatial), 3D spheroid scaffolding (TumorGrid3D #185, signed radial depth + 3D energy-physics dispatcher #186, 3D radial O₂ gradient + zone-census #187, 3D radial pH gradient + iron/ion-trap helpers #190, 3D CAF-shielded boundary detection #189, 3D spatial immune coupling + DAMP diffusion #188) for the #185–#197 spheroid-validation series; sim-tme-3d TME capstone (#195); 2D-math lift + `immune_3d`→`immune_spatial` rename + JSON schema_version (#220/#224); 3D trajectory snapshots + animated axial-slice GIF renderer (#193/#238); time-varying multi-dose pharmacokinetics (`dose_schedule` module — Constant/Bolus/MultiDose/Infusion/FromPk — wired into sim-tme-3d via `--dose-sweep` + the `--snapshot=multidose` preset, with the orphaned `tumor_pk` ODE finally bridged in via `FromPk`, #239); 3D performance and scalability work (`--bench` harness + within-condition rayon parallelism, byte-identical via position-independent per-cell RNG, 3.8x to 4.9x speedup on single large grids, dense 200³ measured at ~1.29 GB so sparse storage deferred to #254, #192); full-production byte-identity regression CI guarding sim-tme-3d's default-matrix `summary.json` SHA (#253); drug-tolerant persister cells (`persister` module + `PersisterConfig`, epigenetic ferroptosis tolerance acquired under drug exposure and reverting after clearance; off-by-default identity config keeps the matrix byte-identical, with a `--snapshot=persister` render overlay, #241; #262 replaces the acquire-xor-revert either-or with a competing-rate `step` so sustained sub-saturating drug reaches a sub-cap equilibrium, + persister calibration provenance; #470 adds an OXPHOS-ROS suppression + HDAC-rescue mitochondrial escape axis (`oxphos_ros_multiplier` scales a persister cell basal/mito ROS DOWN so RSL3 kills it less, an HDAC inhibitor reverses it, PMID 41481741; off-by-default byte-identical, `--snapshot=persister-oxphos`)); T-cell exhaustion (#243 Phase 1 — `immune_spatial::exhaustion_factor` + per-cell neighborhood `cumulative_kills`; sustained local killing suppresses further immune kills via `1/(1+rate·cumulative)`; `exhaustion_rate=0` default keeps the matrix byte-identical; #264 Phase 2 adds a Treg/MDSC suppressor field — a second diffusing field, replenished at perivascular-or-heuristic niche sources, that scales immune kill DOWN via `1/(1+strength·field)`; off-by-default byte-identical, anti-PD-1 Treg-depletion validation test + `--snapshot=suppressor` niche overlay); #264 Phase 3 generalizes the single PD-1 brake to a multi-checkpoint panel (PD-1/CTLA-4/LAG-3/TIM-3, each independently drug-modulated; combined brake 1−Π(1−residualᵢ)) to model anti-PD-1 + anti-CTLA-4 combinations, off-by-default byte-identical with a dual-blockade validation test + `--snapshot=checkpoint`); #264 Phase 4 adds the cDC1/cDC2 dendritic-cell subset mix (`DcSubsetConfig`: a cDC1-poor tumor primes anti-tumor CD8 killing less efficiently, collapsed to one uniform priming-efficiency scalar multiplied into the immune kill probability, Broz 2014 PMID 25446897; off-by-default balanced ⇒ efficiency 1.0 byte-identical, cDC1-poor A/B validation test + `--snapshot=dc-subsets`), completing the #264 Phases 2-4 immune-realism arc; cross-layer composition validation (#278 — pairwise-distinct realism-layer RNG-seed guard + multi-layer integration tests (clonal×suppressor×checkpoints, spheroid×vasculature) asserting deterministic composition + coherent per-layer metrics, plus a `--snapshot=combined-realism` kitchen-sink preset; all-off matrix stays byte-identical); clonal heterogeneity (#242 — `clonal` module: Voronoi subclone patches via an independent RNG + per-subclone iron/GPX4/MUFA perturbations, `summary.json` per-subclone kill reporting, K=1-identity default keeps the matrix byte-identical, `--snapshot=clonal` subclone overlay; #266 makes the GPX4 axis durable — `gpx4_mul` now scales the static `cell.nrf2` setpoint too, not just the initial `state.gpx4`, so a GPX4/antioxidant-low subclone stays differentiated for the whole run instead of relaxing to the shared setpoint; #266 item 3 adds spatial clonal expansion — dead tumor sites repopulated from living Moore-neighbors so resistant subclones grow their territory, with `subclone_kills.initial_tumor` reporting the shift; off-by-default (repopulation_rate=0) byte-identical); explicit 3D vasculature (#191 — `vasculature` module: random internal vessel seeds via an independent RNG + per-cell `exp(-dist_to_nearest_vessel/λ)` unified supply replacing the edge-distance O2 proxy and scaling drug delivery on all paths (SDT/PDT exo, RSL3 constant knockdown, dosed RSL3 availability), off-by-default byte-identical, `vascular_hypoxic_fraction` reporting + `--snapshot=vasculature` O2-supply overlay; #268 replaces the brute-force nearest-vessel scan with a `VesselIndex` uniform-grid spatial index — exact/byte-identical (Chebyshev-shell search with a provable early-stop, verified bit-for-bit vs brute force), ~O(cells) instead of O(cells×vessels), so vasculature scales to patient-size grids the #272 slab coupling needs; #268 also adds `VesselTopology::{Random,Fractal}` + `place_vessels_fractal_3d` — a fractal-branching vessel tree (trunks enter from the periphery and bifurcate inward via BFS with high, tumor-like angle/length jitter and occasional dead ends, per Baish & Jain 2000 PMID 10919633, capped at the same point-count target as the random network — matching raw point count, NOT effective coverage) producing hierarchical-but-chaotic perfusion with avascular gaps; at near-equal point count the fractal network leaves a higher hypoxic fraction than uniform-random seeding (a clustering-coverage effect: 1-cell-spaced branch points cover far less unique volume than scattered points — qualitative, not density-controlled), slab geometry ignores topology, off-by-default topology=Random byte-identical, no preset overlay yet); 3D spheroid radial biochemistry (#197 — `spheroid` module: radial phenotype re-assignment (glycolytic rim / OXPHOS mid / persister core) via an independent RNG + position-dependent MUFA/GSH/iron gradients, run under the new `Params::spheroid()` partial-MUFA context, off-by-default byte-identical, `--snapshot=spheroid` phenotype overlay; #270 makes the zone thresholds VOLUME-based (compare frac³) and grounds the defaults in the Browning 2021 eLife limiting structure (necrotic core 0.73 of radius → 0.39 of volume; rim begins 0.90 of radius), fixing the radial-threshold inversion that gave a ~4%-volume core / ~71%-volume rim — geometry now literature-grounded, biochem gradient strengths still placeholders; #270 makes the radial MUFA durable via a per-cell `Cell::mufa_cap` so the rim-vs-core spread persists instead of relaxing to the uniform M_ss; #270 item 3 adds the `contact` module — cell-cell contact-mediated ferroptosis resistance (E-cadherin → Merlin/NF2 → YAP inhibition → ACSL4/TFRC down, Wu 2019 PMID 31341276): `apply_contact_resistance_3d` scales each tumor cell's durable `lipid_unsat` (PUFA) and `iron` down with its tumor-26-neighbour fraction, so dense interior cells resist while sparse/surface cells stay sensitive (stronger in 3D); geometric/no-RNG, off-by-default identity byte-identical, integration-tested (RSL3 kills collapse to well under half at literature strengths — the magnitude is threshold-proximity-sensitive and almost entirely the PUFA/lipid axis, so the DIRECTION is the result, not the number; iron/TFRC is a minor contributor under RSL3), guarded mutually-exclusive with slab geometry; #302 closes the contact follow-ups: it re-applies contact resistance to clonally-repopulated cells (revived dead sites previously came back un-reduced, the opposite of the modeled biology, so `clonal(repopulation)` + `contact` now stay coherent via the new `apply_contact_resistance_at_3d` + `repopulate_dead_sites_3d` returning the revived indices), and adds the contact+spheroid composition test, `#[should_panic]` guard tests for the slab+contact / slab+spheroid invariants, the first `io`-module tests, and a `--snapshot=contact` overlay); patient-scale slab geometry (#240 — `slab` module + `TumorGrid3D::generate_slab`: an all-tumor block with a planar depth-graded `exp(-depth/λ)` supply (the 1-D analog of the radial O2 field, +z face vessel-proximal, −z reflective/no-flux) replacing O2 and scaling drug on all paths, `SlabConfig::patient_deep()`/`surface()` (10 mm virtual tumor), a `scale_interpretation` output string, off-by-default byte-identical, `--snapshot=slab` depth-gradient viz; addresses the in-vitro-vs-patient scale gap — a 4 mm-deep slab kills <20% of the spheroid under the same SDT, though the depth-collapse magnitude is an uncalibrated first-order Krogh approximation; #272 couples the slab with explicit vasculature (#191) — `place_vessels_in_slab_3d` scatters vessels uniform-in-box across the all-tumor block and the per-cell supply becomes `max(planar_depth, nearest_vessel)`, so internal vessels deliver drug to focal deep pockets and RAISE deep killing, i.e. a slab is less therapy-resistant at depth than the planar-only model implies wherever real vasculature reaches; off-by-default byte-identical, gated on both overrides, `--snapshot=slab-vessels` overlay)
- ferroptosis-core library packaging for external use
- news source authentication pipeline (fetch, extract claims, verify, score, index)
- broader strategy review of alternative therapies and biological bottlenecks
- operational maturity: Phase 2 complete — CI (#126), figure traceability (#127), archival release tooling (#131); workspace `cargo fmt --check` gate added to Rust CI (#209/#236); off-PR sim-tme-3d production byte-identity regression workflow (#253)
- manuscript integrity: Phase 3 complete — immune coupling confidence (#130), structural uncertainty qualifiers (#137), PRISMA corpus flow diagram (#135), retrieval bias subsection (#140)
- sensitivity analyses: weight-sensitivity (#128), taxonomy-sensitivity (#133), PRCC global sensitivity (#134), and O2 cycling (#138) complete — pre-registered, run, results in manuscript
- test expansion (#139) complete — 19 invariant/integration tests added (schema, weight monotonicity, tagging correctness)

## Current Repo State

**THE CENSUS IS THE BASIS OF EVERY REPORTED NUMBER (#RETIRE-FROZEN, 2026-08-20).**
Owner directive: the 4,830-article framing is retired from the manuscript, the
README, the archival records and the front door. Every quantitative claim in the
manuscript's Chapters 3 and 4 is now computed over the census.

- census: **5,187,265** cancer articles — 4,403,994 MeSH-indexed (`corpus/atlas/records/`) plus 783,271 text-recovered (`corpus/atlas/records_unindexed/`), with an open-access full-text layer of 1,116,481 on the NAS
- labels are NLM's, not this project's: mechanisms and anatomical sites from MeSH descriptors, study design from publication types and check tags. 44.5% of the census carries no design-informative label, and that gap is BOUNDED — only 20.6% of it is reachable by open-access full text, whose median year is 11 years later than the pile's, so a full-text pass cannot correct the distribution (`analysis/census-fulltext-ceiling.md`)
- **TTFields and bioelectric modulation are NOT MEASURABLE at census scale** — MeSH has no descriptor for either. Report them as unmeasurable, never as zero: TTFields has FDA approval in two indications. Cold atmospheric plasma is unmeasurable for the OPPOSITE reason (`Plasma Gases` exists and is far too broad), and the two verdicts must stay apart
- retrieved corpus: 4,830 records, RETAINED as the method-comparison arm that separates a labelling effect from a selection effect, and not the basis of any reported figure. `scripts/corpus_dependency_audit.py` measures what still needs it: 19 of 41 consumers, all because they read this project's own `mechanisms`/`cancer_types`/`evidence_level` tags. "Needs full text" stopped meaning "needs the corpus"
- abstract-only archive: 5,593 files in `corpus/abstracts/by-pmid/`; superseded by the census for coverage purposes, retained for the tagger-evaluation work that was measured on it
- mechanism taxonomy, evidence tiers, pathway-targets, biology-process tags, and resistant-state scaffolding are all active
- evidence tagging is improved but still incomplete (gold-set measured); MECHANISM tagging recall is separately measured non-circularly vs MeSH (#412): 90.6% volume-weighted / 89.2% macro over 9 mechanisms with an independent MeSH leaf (after #418 closed the epigenetic keyword gap, 54.7%→69.8%), with the device/regulated-cell-death/very-recent modalities flagged not-MeSH-measurable (analysis/mechanism-recall-report.md)
- census generators, all registered in `tests/test_census_stream_registry.py` with a stated reason for the streams they read: `census_evidence_design` (study design from NLM labels), `census_mechanism_profile` (volume/trial share/site enrichment/partners per mechanism — the single source six manuscript sections read), `census_mechanism_growth` (the MATCHED denominator a growth claim needs), `census_mechanism_sites` (mechanism class by C04 site), `census_diagnostic_chains`, `census_oa_bias`, `census_fulltext_ceiling`, `corpus_dependency_audit`
- census figures (`scripts/generate_census_figures.py`) read ONLY committed analysis JSON, so they regenerate offline in seconds with no corpus, no census scan and no graphviz. Manuscript figures 1, 2, 10, 11, 12, 13 and 20 are census-derived
- the weighted evidence composite is RETIRED (its ranking moved seven places under a defensible reweighting); trial share from NLM publication types replaces it, with no editorial parameter
- tissue-of-origin is replaced by mechanism class against NLM's own C04 site assignment
- diagnostic-therapy matching layer covers 10 chains across 5 modalities (radioligand, checkpoint, mRNA vaccine, oncolytic, targeted therapy: HER2/BRCA/EGFR/KRAS-G12C #441); the audit is recomputed on the fly from the frozen corpus text (`scripts/diagnostic_therapy_audit.py`) so chains can be added without mutating the frozen `corpus/INDEX.jsonl`
- manuscript: ~115 pages (book format), 11 chapters + 3 appendices, 24 figures (FIGURES.yaml indexes 30 entries: 24 manuscript + 5 supplementary + 1 orphan), ~48,500 words
- simulation suite: 12 binaries (incl. sim-tumor-pk + sim-tme-3d + the large-n rare-event harness sim-scale) + ferroptosis-core library (MIT licensed, 32 modules including `photosensitizer_pk`, `oxygen`, `ph`, `stromal`, `immune_spatial`, `dose_schedule`, `persister`, `clonal`, `vasculature`, `spheroid`, `slab`, `contact`, `nutrient`, `phenotype_mufa`, `senescence`, `ifngamma`, `reaction_diffusion`, `alox`, `acsl4`, `repair`, `trigger_wave`, and `copper`; v0.7.0 adds 3D radial-depth + 3D ROS-multiplier APIs alongside the 2D path #185–#186; v0.8.0 adds 3D radial O₂ field + zone-census #187; v0.9.0 adds 3D radial pH field + iron-release/ion-trap helpers #190; v0.10.0 adds 3D CAF-shielded boundary detection + adjacent-kill-rate #189; v0.11.0 adds 3D spatial immune coupling #188; sim-tme-3d capstone binary consumes all five #195; #220/#224 lift the TME config structs + 2D depth/pH/stromal helpers into the library and rename `immune_3d`→`immune_spatial`; v0.12.0 (#239) adds the `dose_schedule` time-varying-PK module + `biochem::exo_decay_factor`; v0.13.0 (#241) adds the `persister` drug-tolerant-persister module + `PersisterConfig`; v0.14.0 (#243) adds `immune_spatial::exhaustion_factor` (T-cell exhaustion) + `TumorGrid3D::coords` + `SpatialImmuneConfig::exhaustion_rate`; v0.15.0 (#242) adds the `clonal` module (Voronoi subclones + `ClonalConfig`/`SubclonePerturbation`); v0.16.0 (#191) adds the `vasculature` module (explicit vessel network + `VasculatureConfig`); v0.17.0 (#197) adds the `spheroid` module (radial phenotype/MUFA/GSH/iron + `SpheroidConfig`) + `Params::spheroid()`; v0.18.0 (#240) adds the `slab` module (patient-scale all-tumor block + planar depth-graded supply, `SlabConfig`, `scale_interpretation`, `KROGH_LAMBDA_UM`) + `TumorGrid3D::generate_slab`; v0.19.0 (#270) adds per-cell durable MUFA (`Cell::mufa_cap`, threaded into `update_mufa_protection`, set radially by the spheroid so position-dependent MUFA no longer relaxes to the uniform M_ss); v0.20.0 (#264 Phase 2) adds the Treg/MDSC immunosuppressor field to `immune_spatial` (`SuppressorConfig` + `suppressor_kill_multiplier` + `suppressor_source_mask_3d`); v0.21.0 (#264 Phase 3) adds the multi-checkpoint immune brake (`CheckpointPanel` + `Checkpoint`, `combined_brake` = 1 − Π(1 − brakeᵢ·(1−drug_effᵢ)) over PD-1/CTLA-4/LAG-3/TIM-3); v0.22.0 (#262) adds the persister competing-rate `step` (acquisition + reversion act simultaneously ⇒ sub-cap equilibrium under sustained sub-saturating drug); v0.23.0 (#266 item 3) adds spatial clonal expansion (`repopulate_dead_sites_3d` + `ClonalConfig::repopulation_rate`/`with_repopulation`); v0.24.0 (#272) adds slab+vasculature coupling (`vasculature::place_vessels_in_slab_3d` uniform-in-box vessel placement, so a patient-scale slab can carry internal vessels whose proximity supply combines element-wise-MAX with the planar depth gradient); v0.25.0 (#268) adds the `VesselIndex` uniform-grid spatial index for `vessel_supply_field`'s nearest-vessel lookup (exact/byte-identical to the former brute force, ~O(cells) instead of O(cells×vessels), so vasculature scales to patient-size grids — 100³/1M cells ≈ 105 ms); v0.26.0 (#268 + spheroid zone fix) adds `VesselTopology::{Random,Fractal}` + `place_vessels_fractal_3d` (a fractal-branching vessel tree — trunks enter from the periphery and bifurcate inward with high, tumor-like variability per Baish & Jain 2000 PMID 10919633, producing hierarchical-but-chaotic perfusion with avascular gaps, capped at the same point-count target as random (raw point count, NOT effective coverage — clustered 1-cell-spaced branch points cover far less unique volume than the same number of scattered points, so the higher hypoxic fraction is a clustering-coverage effect read qualitatively, not a density-controlled result); slab geometry ignores topology; off-by-default topology=Random keeps the matrix byte-identical) and makes `spheroid::radial_phenotype` volume-based (compares `frac.powi(3)` to volume-fraction thresholds with `literature()` defaults `glycolytic_frac:0.73`/`oxphos_frac:0.39` from Browning 2021 eLife so rim/mid/core zones reflect equal-volume shells, not equal-radius bands; spheroid layer is opt-in so byte-identical); v0.27.0 (#270) adds the `contact` module (cell-cell contact-mediated ferroptosis resistance — E-cadherin junctions → Merlin/NF2 → YAP inhibition → ACSL4/TFRC down, per Wu 2019 PMID 31341276; `ContactConfig` + `apply_contact_resistance_3d` scale the durable per-cell `lipid_unsat` (PUFA) and `iron` axes down in proportion to each cell's tumor-26-neighbour fraction, so dense interior cells resist while sparse/surface cells stay sensitive — stronger in 3D; geometric, no RNG, off-by-default identity keeps the matrix byte-identical); v0.28.0 (#302) adds `contact::apply_contact_resistance_at_3d` (per-cell re-application, used to keep `clonal(repopulation)` + `contact` coherent) and changes `clonal::repopulate_dead_sites_3d` to return the revived-site indices (so a consumer re-applies geometric per-cell setup layers to fresh cells); v0.29.0 (#270 item 3b) adds the `nutrient` module (radial nutrient gradient: glucose/glutamine availability `exp(-depth/λ)` scales the durable antioxidant setpoint `cell.nrf2` DOWN toward the starved core, less NADPH→GSH/GPX4 regeneration ⇒ more ferroptosis-sensitive per Dixon 2012 PMID 22632970 / glucose-metabolism PMID 42190602; ONE documented direction, net is context-dependent given AMPK/glutaminolysis; reuses `RadialDepthGeom`, off-by-default byte-identical, `--snapshot=nutrient`); v0.30.0 (#264 Phase 4) adds the DC-subset mix to `immune_spatial` (`DcSubsetConfig` + `priming_efficiency()`: cDC1/cDC2 composition collapses to one uniform anti-tumor priming-efficiency scalar the consumer multiplies into the immune kill probability; a cDC1-poor tumor primes CD8 killing less efficiently per Broz 2014 PMID 25446897, `balanced()` ⇒ efficiency 1.0 identity/byte-identical, `literature()` cDC1-poor placeholder, gated on immune_on + routed through the realism kill path so the default hot loop is untouched, `--snapshot=dc-subsets`); v0.31.0 (#315) adds an off-by-default ECM-tortuosity factor to `drug_transport` (`TissueParams::ecm_tortuosity`, `λ_eff = λ/√τ`: a denser desmoplastic matrix shortens drug penetration; `τ=1` identity for all shipped tissues so byte-identical, uncalibrated placeholder per Netti 2000 PMID 10811131 / Provenzano 2012 PMID 22439937); v0.32.0 (#272) adds the depth-graded slab phenotype to `slab` (`SlabPhenotypeConfig` + `apply_depth_graded_cells_3d` + `depth_phenotype` + the `layer_supply` helper factored out of `slab_supply_field`): the slab's flat bulk phenotype mix becomes a layered rim→core gradient (proliferating/glycolytic at the vessel-proximal +z face, persister-like in the chronically supply-starved deep (−z) layers), thresholded on the planar supply `exp(-depth/λ)` (NOT geometric volume fractions like the spheroid, since the slab models an absolute depth, so a `patient_deep()` 4 mm slab is uniformly persister-like, the intended behavior for a chunk drug/O2 never reach); independent per-cell RNG + off-by-default (gated on slab mode, which the matrix never enters) keeps the production matrix byte-identical, wired into the `slab`/`slab-vessels` presets, supply cut-points `glycolytic_supply:0.5`/`oxphos_supply:0.15` are placeholders pending depth-resolved histology; v0.33.0 (#336) adds the oxygen-dependent SDT/PDT exo-ROS option (`oxygen::o2_dependent_exo_factor` + sim-tme-3d `Overrides.sdt_o2_dependence`): the exogenous ROS yield can be made a configurable Type II fraction that scales with local O2 (`(1−dep)+dep·o2_supply`), so SDT loses efficacy in hypoxic zones like the clinical SONALA-001 agent (Sanai 2025 Sci Transl Med PMID 41296829, Type II/O2-dependent), addressing the manuscript's contested §7.1 hypoxia leg model-side; `sdt_o2_dependence=0` default ⇒ factor 1.0 ⇒ byte-identical; v0.34.0 (#338) adds the two missing GPX4-independent ferroptosis-suppressor axes to `biochem`/`params`: `Params.dhodh_rate` (DHODH/CoQ10 repair in parallel to FSP1, Mao 2021 Nature) and `Params.gch1_rate` (GCH1/BH4 radical-trapping quench gating autocatalytic propagation, Kraft/Soula 2020), so the model no longer overstates RSL3 monotherapy kill and can express GPX4i+DHODHi-style combinations; both `0.0` by default ⇒ byte-identical, FFI `ferro_params_to_params` defaults them so the C ABI/header is unchanged; v0.35.0 (#339 PR 1 of 3) adds an off-by-default kinetic/acute MUFA start to `biochem`/`params` (`Params.mufa_acute_start: Option<f64>`): `invivo()` starts cells at the established MUFA steady state (M_ss ≈ 0.40, assuming the ~48 to 72 h SCD1/MUFA enrichment is already complete), overstating early ferroptosis resistance for acute day-0 dosing; `Some(x)` instead starts a freshly dosed tumor at a naive value `x` (e.g. 0.0) and accumulates protection over the run via the existing logistic, reproducing the acute-versus-established difference (acute cells peroxidize more under RSL3); the accumulation rate is the existing `scd_mufa_rate`, uncalibrated against the 48 to 72 h timescale so the direction is the result not a precise hours figure; `None` default ⇒ byte-identical, FFI maps to `None` so the C ABI/header is unchanged; the ether-lipid (FAR1/TMEM189) and MBOAT1/2 axes are follow-up PRs of #339; v0.36.0 (#339 PR 2 of 3) adds the off-by-default ether-lipid axis to `biochem`/`params` (`Params.ether_pufa_fraction: f64`): polyunsaturated ether phospholipids (ether-PUFA-PE, made via FAR1/AGPS + the peroxisomal pathway) are an extra oxidizable-substrate pool that PROMOTES ferroptosis, so the new `ether_augmented_pufa` helper scales the peroxidizable PUFA term by `1 + ether_pufa_fraction` at all four `effective_unsat` sites; enabling it raises lipid-peroxide accumulation and the `0` limit is the FAR1/AGPS-null escape (ether-lipid loss confers resistance), the robust direction per Zou 2020 Nature PMID 32939090 / Cui 2021 Cell Death Differ PMID 33731874 and the OPPOSITE sign to the loose "shift to ether-PE = escape" framing; the contested plasmalogen/TMEM189 vinyl-ether sub-step is deliberately not modeled (protective in Cui 2021 / Zoeller 1999 PMID 10051451, dispensable in Zou 2020, sensitizing-on-loss in Perez 2022 PMID 36178986); `0.0` default ⇒ ×1.0 ⇒ byte-identical, FFI maps to `0.0` so the C ABI/header is unchanged; MBOAT1/2 is the remaining follow-up PR of #339; v0.37.0 (#339 PR 3 of 3, closes #339) adds the off-by-default MBOAT1/2 MUFA-enrichment boost to `biochem`/`params` (`Params.mboat_mufa_boost: f64`): MBOAT1 (estrogen-receptor-regulated) and MBOAT2 (androgen-receptor-regulated) remodel phospholipids toward MUFA-PE and suppress ferroptosis independently of GPX4 (Liang 2023 Cell PMID 37267948), modeled as a constant additive MUFA protection layered onto the dynamic SCD1 `mufa_protection` at the two live peroxidation sites via the new `total_mufa_protection` helper (floored at `0`, enrichment is protective-only), so a higher value (e.g. an AR-driven MBOAT2-high tumor) lowers ferroptosis, the OPPOSITE direction to the ether-PUFA pool; `0.0` default ⇒ byte-identical, FFI maps to `0.0` so the C ABI/header is unchanged; this completes the #339 lipid-remodeling escape trio (kinetic MUFA + ether-lipid + MBOAT); v0.38.0 (#340) replaces the static-iron assumption with an off-by-default dynamic labile-iron path: `Params.ferritinophagy_release` (+ ramp `ferritinophagy_tau`) and `biochem::ferritinophagy_iron_factor(step, params)` scale the Fenton iron at all three Fenton sites by a time-dependent factor that ramps from `1.0` toward `1 + release`, modeling NCOA4-mediated ferritinophagy releasing stored iron into the labile pool over the run (Mancias 2014 Nature PMID 24695223; Hou 2016 Autophagy PMID 27245739), so enabling it raises Fenton ROS and ferroptosis; a companion `oxygen::hypoxia_iron_factor(o2_supply, sensitivity)` helper lets a consumer raise iron release in hypoxic zones (HIF/TfR1 up), qualifying the §7.1 hypoxia-leg framing (hypoxia can RAISE the iron/Fenton contribution even as it lowers the O2-dependent SDT yield from #336, so "hypoxia uniformly protects RSL3" is too strong); `release=0.0` ⇒ factor exactly `1.0` for every step ⇒ byte-identical, FFI maps to `0.0` so the C ABI/header is unchanged; #365 wires both into sim-tme-3d (`Overrides.ferritinophagy_release` + `Overrides.hypoxia_iron_sensitivity` applying `hypoxia_iron_factor` to per-cell `cell.iron`), with a spatial A/B confirming hypoxia-iron raises RSL3 ferroptosis kill several-fold including in the deep hypoxic core (grid 60/λ=50: overall 0.5%→2.9%, hypoxic-zone 0%→1.3%); the deep-core rise is a flagged MODEL ARTIFACT (the Fenton term is O2-independent — `iron×rate` added to total ROS, model modulates only basal_ros not the Fenton rate — so a real anoxic core losing its H2O2 substrate is NOT captured), so the robust result is the direction (hypoxia can raise the RSL3 iron contribution, "hypoxia uniformly protects RSL3" too strong), not the deep-core magnitude; §7.1 reports this combined with the #358 O2-dependent SDT leg, off-by-default byte-identical (production matrix SHA unchanged); the two §7.1-leg viz presets landed as `--snapshot=sdt-o2dep` (#380, SDT O2-dependent hypoxic-core survival front) + `--snapshot=ferritinophagy` (#381, RSL3 iron-amplified ferroptosis front), both off-by-default byte-identical sim-tme-3d-only presets; v0.39.0 (#337) adds the off-by-default immunosuppressive arm of ferroptosis to `immune_spatial`/`params`/sim-tme-3d (`SpatialImmuneConfig.ferro_immunosuppression_strength` + `immune_spatial::ferroptotic_immunosuppression(local_damp, strength) = 1/(1 + strength·local_damp)`): the immune layer otherwise treats ferroptotic ICD as net pro-immune (DAMP -> DC -> CD8 kill), but ferroptotic cells co-release DC-suppressing factors (extracellular GPX4 binding DC ZP3, Liu 2026 Cell PMID 41494530; oxidized lipids / PGE2, Kim 2022 Nature PMID 36385526; ferroptotic cells impede DC immunity, Wiernicki 2022 Nat Commun PMID 35760796), so the new multiplier scales immune kill DOWN as the local ferroptotic-death/DAMP signal rises, keyed on the SAME `local_damp` that drives pro-immune `dc_activation` so the NET immune effect can FLIP sign at sustained/high death density (routed through the realism kill path so the default hot loop is untouched, `immunosuppressive_ferroptosis_reduces_immune_kills` A/B test); direction is timing-dependent (early ferroptotic cells can be immunogenic, Efimova 2020 PMID 33188036), magnitude is a placeholder and the sign is the result; `strength=0.0` default ⇒ multiplier `1.0` ⇒ byte-identical, not in the C ABI so the header is unchanged, no snapshot preset yet (reachable via `Overrides.immune`); v0.40.0 (#342) adds the off-by-default persister reversible-to-irreversible epigenetic-locking transition to `persister`/`params` (`PersisterConfig.lock_rate`/`lock_threshold`/`exposure_decay` + `persister::step_with_locking` + `PersisterState`): the persister model otherwise treats all persistence as freely reversible, but beyond a threshold of SUSTAINED drug exposure persistence becomes epigenetically locked / irreversible (FSP1/HDAC-suppressed durability, Higuchi 2026 Sci Adv PMID 41481741); a sustained-exposure EMA (`cumulative·(1-exposure_decay)+drug`, steady state `avg_drug/exposure_decay`) gates locking, so once it crosses `lock_threshold` a fraction `lock_rate` of the reversible pool moves into the non-reverting `locked` pool, and because the EMA decays in drug-off windows only continuous dosing locks while intermittent dosing settles below threshold (intermittent-vs-continuous A/B test); `lock_rate=0.0` default ⇒ `step_with_locking` reduces EXACTLY to `step` ⇒ byte-identical, not in the C ABI, library-only so far (sim-tme-3d consumer wiring is a follow-up); v0.41.0 (#341) adds the off-by-default `senescence` module (therapy-induced-senescence ferroptosis program): `SenescenceConfig` (`fraction`/`iron_mul`/`gpx4_mul`/`nrf2_mul`/`fsp1_mul`, identity at fraction 0) + `apply_senescence_program_3d` marks a fraction of tumor cells (independent RNG) and scales their iron/defenses; the ferroptosis direction is genuinely AXIS-dependent so the program is BIDIRECTIONAL not single-signed (CORRECTING the issue's one-sided "resist" framing): senescent cells RESIST upstream cystine/iron triggers via compartmentalized iron + raised antioxidant capacity (Masaldan 2018 PMID 28888202, Loo 2025 PMID 40731111) but are a SENOLYTIC target under DIRECT GPX4 inhibition (the model's RSL3) because they are primed/GPX4-dependent (D'Ambrosio/Gil 2026 Nat Cell Biol PMID 42032311); the unit test drives BOTH nets under RSL3 (iron-dominant ⇒ senolytic, defense-dominant ⇒ resist); `fraction=0` default ⇒ byte-identical, not in the C ABI; v0.42.0 (#341) wires senescence into sim-tme-3d (`--snapshot=senescence` preset + `senescent_fraction` summary metric) and adds the optional SASP→immune coupling: `SenescenceConfig.sasp_immune_mult` is a signed per-cell multiplier on a senescent cell's immune-kill probability, bidirectional and STAGE-dependent like the ferroptosis axis: `>1` anti-tumor senescence immune surveillance is the pre-malignant arm (Kang 2011 PMID 22080947), `<1` immunosuppressive SASP recruiting surveillance-antagonizing myeloid cells is the established-tumor arm (Di Mitri 2014 PMID 25156255; net stage-dependent, Eggert 2016 PMID 27728804), so `literature()` defaults to `<1` for the established therapy-induced-senescence stage the module models; the coupling is a cell-autonomous simplification of an inherently paracrine signal (neighbor/bystander effects deferred to a diffusing SASP-field follow-up); `1.0` default ⇒ no coupling ⇒ byte-identical (production matrix SHA unchanged; revived clonal-repopulation sites are cleared from the senescence mask to stay coherent; SASP-only A/B + preset-wiring + clonal-composition tests); v0.43.0 (#342) wires the persister epigenetic-locking transition into sim-tme-3d: the per-cell `PersisterState` is stored on `CellState` (`persister_reversible`/`persister_locked`/`persister_cum_exposure`, all `0.0`-inert default so the production matrix SHA is unchanged), advanced via `step_with_locking`, with a `persister_locked_mean` summary metric and a spatial `continuous_dosing_locks_persisters_but_intermittent_does_not` test (continuous dosing locks a non-negligible fraction, intermittent locks nothing; the library unit test separately runs a full washout to confirm the locked fraction does not revert); the drug-driven acquisition is the drug-exposure entry trigger, with a non-drug hypoxia/nutrient stress-niche entry trigger delivered in v0.47.0/#377; v0.44.0 (#383) adds the off-by-default O2-dependent Fenton option (`oxygen::fenton_o2_factor(o2_supply, dependence) = (1−dep)+dep·o2_supply` + sim-tme-3d `Overrides.fenton_o2_dependence`): the Fenton reaction needs O2-derived H2O2 (superoxide→SOD→H2O2, the Haber-Weiss chain; Kehrer 2000 Toxicology PMID 10963860 / ferroptotic LPO is an O2-consuming lipid-radical chain, reviewed in Stockwell 2017 Cell PMID 28985560), so it scales the effective Fenton flux DOWN where O2 is low by scaling `cell.iron` (its only Fenton consumer) at the consumer, composing multiplicatively with the #365 hypoxia_iron rise; this CORRECTS the #365 deep-core MODEL ARTIFACT (the O2-independent Fenton let hypoxia-iron wrongly "rescue" the anoxic core) — the spatial A/B (`o2_dependent_fenton_protects_the_anoxic_core_under_hypoxia_iron`, same grid 60/λ=50/180 config as the #365 test) confirms that with hypoxia-iron on AND the Fenton fully O2-coupled the deep hypoxic-zone kill drops by more than half back toward the un-rescued baseline, so the net deep-core effect is no rescue (hypoxia raises iron but starves H2O2 while the rim/mid-zone can still rise); §7.1 updated (the artifact is now addressable rather than only flagged); `fenton_o2_dependence=0` default ⇒ factor 1.0 ⇒ `cell.iron` unchanged ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI; v0.45.0 (#363) adds phenotype-specific SCD1/MUFA accumulation rates: a per-cell `Cell::mufa_rate: Option<f64>` (`None` ⇒ the global `scd_mufa_rate`, threaded into `update_mufa_protection`) + the new `phenotype_mufa` module (`PhenotypeMufaConfig` per-phenotype rate multiplier, `identity()` default; `apply_phenotype_mufa_rates_3d` sets each tumor cell's rate from `gc.phenotype`) wired into sim-tme-3d as off-by-default `Overrides.phenotype_mufa` (gated on non-identity); follow-up to #339 (which used one global `scd_mufa_rate`), so the acute-vs-established MUFA build-up now gets a phenotype-specific time constant; DIRECTION genuinely uncertain (MUFA enrichment resists ferroptosis Magtanong 2019 PMID 30686757 + SCD1 protects Tesfay 2019 PMID 31270077, but persisters are also GPX4-dependent/vulnerable Hangauer 2017 PMID 29088702) so `literature()` is an UNCALIBRATED placeholder, library test confirms a faster per-cell rate builds more `mufa_protection` (the "two phenotypes diverge" criterion at the right readout; the spheroid kill COUNT is an insensitive readout given the bistable switch); reachable via `--snapshot=phenotype-mufa` (spheroid context), and revived clonal-repopulation cells re-derive their rate via `apply_phenotype_mufa_rate_at_3d` (coherent with #266/#302); `mufa_rate=None`/identity ⇒ byte-identical (production matrix SHA unchanged), FFI maps it to `None` so the C ABI/header is unchanged; delivers the per-phenotype RATE (the per-phenotype CAP follows in v0.46.0/#390); v0.46.0 (#390) completes #363 with the per-phenotype SCD1/MUFA CAP (steady-state) multiplier: `PhenotypeMufaConfig` gains 5 `*_cap` fields + `cap_multiplier()`, and the apply fns are renamed `apply_phenotype_mufa_rates_3d`→`apply_phenotype_mufa_3d` / `_rate_at_3d`→`_at_3d` and take a new `base_max` (=`scd_mufa_max`) so they set BOTH the per-cell `mufa_rate` and scale the per-cell `mufa_cap` MULTIPLICATIVELY (cap-mul `1.0` ⇒ cap untouched, preserving a spheroid radial cap; applied AFTER the spheroid so the two compose); `phenotype_caps_yield_different_mufa_steady_states` confirms a 2x-cap persister plateaus higher than glycolytic, the cap direction is even more uncertain than the rate so `literature()`'s `*_cap` are placeholders; both axes off-by-default identity ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI; closes the #363 rate/cap pair; v0.47.0 (#377) adds the non-drug stress-niche persister entry trigger, completing #342's two-route entry: `PersisterConfig.stress_entry_rate` (off-by-default `0.0`) + `persister::stress_entry(state, stress, cfg)` raise the REVERSIBLE persister pool from a hypoxic/nutrient-poor drug-sanctuary niche, DECOUPLED from `drug_intensity` (stress drives ENTRY, drug drives durability) so it never feeds the locking EMA (#342) or the GPX4 resistance; wired into sim-tme-3d with the stress signal = the local hypoxia deficit `1 - o2_supply` (same per-cell O2 field as #336/#383), applied after `step_with_locking`, with a `stress_niche_drives_nondrug_persister_entry` A/B confirming hypoxic-zone persister entry under ZERO drug while the stress-off baseline stays exactly 0; `is_identity()` now also checks `stress_entry_rate` (a stress-only config is non-identity); `stress_entry_rate=0` ⇒ no-op ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI; v0.48.0 (#376) adds the diffusing SASP FIELD (the paracrine/bystander extension of #341's cell-autonomous `sasp_immune_mult`), completing #341: `SenescenceConfig.sasp_field_strength` (off-by-default `0.0`, SIGNED) + `immune_spatial::sasp_field_kill_multiplier(field, strength)` (`strength > 0` immunosuppressive `1/(1+strength·field)` LOWERS kill, `strength < 0` surveillance `1+|strength|·field` RAISES it, identity at field 0 or strength 0) — the consumer (sim-tme-3d) seeds a SASP field at the senescent cells (the senescence mask), diffuses it with `diffuse_damp_3d_step` (transport constants mirror the suppressor field's), and multiplies the signed factor into the immune kill probability of EVERY exposed cell, including adjacent NON-senescent tumor cells (the neighbor coupling #341 could not express; surveillance recruits CD4/monocytes per Kang 2011 PMID 22080947, immunosuppressive IL-1RA protects neighbors per Di Mitri 2014 PMID 25156255 / Eggert 2016 PMID 27728804); a library test confirms the field reaches a non-senescent Moore-neighbor and a sim-tme-3d FIELD-only A/B (cell-autonomous coupling + biochem held identity) confirms net immune kills still shift bidirectionally, so the bystander effect is real; `literature()` sets `sasp_field_strength=0.5` (immunosuppressive, the established-tumor arm) so the `--snapshot=senescence` preset now also exercises the field (and the dedicated `--snapshot=sasp-field` preset #398 writes a `sasp_field.npy` overlay of the quasi-steady diffused field, rendered as a "SASP field" panel by render_tme_3d_trajectory.py); `sasp_field_strength=0.0` default ⇒ multiplier `1.0` ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI; v0.49.0 (#331) adds the shared biochem parameter-override layer to `params` (`apply_param_overrides` — the single name→field mapping lifted out of the Python binding so the binding AND the simulation binaries perturb the SAME rate constants; `parse_param_overrides_json` + `overrides_from_env_value` + `param_overrides_from_env` read a `FERRO_PARAM_OVERRIDES` env var holding inline JSON or a file path), wired into `sim-tme` + `sim-combo-mech` off-by-default (env unset ⇒ empty overrides ⇒ no-op ⇒ byte-identical default run, confirmed by release-mode A/B; an override changes the hypoxia/immune and Bliss-synergy outputs respectively); enables driving global-sensitivity / calibration of the spatial+combo headline outputs (#331 follow-up to the single-cell Sobol #386) without recompiling; the binding refactor preserves its exact error message; not in the C ABI; v0.50.0 (#343 PR 1) adds the `reaction_diffusion` module (steady-state linear reaction-diffusion supply field `D∇²c − k·c = 0` over the same vessel network the exponential proxy uses, with vessel Dirichlet sources, tumor-only consumption `γ = (h/λ)²`, and finite-volume no-flux domain boundaries, solved by deterministic SOR; `ReactionDiffusionConfig` + `reaction_diffusion_solve`/`reaction_diffusion_supply_field` + `analytical_1d_slab`): the vasculature/oxygen proxies are monotonic in distance-to-nearest-source, but a real irregular vessel network superposes all nearby vessels while consumption draws the field back down, so the steady state has non-monotonic pockets the proxy averages away (addresses #343's biphasic-field gap); the diffusion-consumption length `λ = sqrt(D/k)` equals the proxy's `lambda_um`, so a single isolated source reproduces `exp(−dist/λ)` (the proxy is exactly the single-source limit) and divergence is the genuine multi-vessel/consumption effect; verified by an analytical self-consistency check against the closed-form 1-D reaction-diffusion cosh slab (the SOR discretization converges to the exact solution of its own continuous equation, max abs error < 0.01; `analytical_1d_slab` is evaluated in an overflow-safe subtracted-exponent form) + a two-source midpoint-enrichment test showing the proxy under-supplies multi-vessel pockets — the GENUINE external benchmark the issue asks for (proxy-vs-RD field/kill + a PhysiCell/published-PDE cross-check) and the proxy-adequacy documentation are the remaining #343 criteria delivered in PR 2; opt-in alternative supply field, nothing in the production matrix calls it ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI; #343 PR 2 wires it into sim-tme-3d (`Overrides.reaction_diffusion` swaps `vessel_supply_field`→`reaction_diffusion_supply_field` when explicit vasculature is on, gated on vasculature which is off the matrix ⇒ byte-identical; `--snapshot=reaction-diffusion` renders the RD field in the vessel_supply panel) + the proxy-vs-RD benchmark `analysis/reaction-diffusion-benchmark.md`, whose headline (in the λ ≲ vessel-spacing regime, λ=120µm here) is that the monotonic `exp(-dist/λ)` proxy reads optimistic — RD lower at ~100% of cells — DOMINATED BY 3D POINT-SOURCE GEOMETRY (the proxy is the planar `exp(-d/λ)`; a 3D point vessel's field is the Yukawa `exp(-r/λ)/r`, so a SINGLE isolated vessel already shows a 0.85→0.31 first-neighbour gap before any multi-vessel effect; cumulative consumption is mostly already in `λ=sqrt(D/k)` so it is NOT a separate omitted driver, and superposition/enrichment is small here), flipping a sparse tumor the proxy calls 62% hypoxic to 99% hypoxic; the DIRECTION flips with λ (depletion when λ ≲ spacing, enrichment when λ ≫ it, since `γ=(h/λ)²→0` ⇒ Laplace) and the magnitude depends on the single-voxel vessel representation + grid h, so the result is DIRECTIONAL not calibrated; the genuine external PhysiCell/BioFVM cross-check is honestly deferred (the analytical 1D self-consistency check stands in PR 1); v0.51.0 (#333) adds size-aware spheroid zone thresholds (`spheroid::SizeAwareZones` + `apply_radial_cells_sized_3d` + `tumor_radius_um`): the persister-core and glycolytic-rim VOLUME thresholds ramp from 0 (a small, all-proliferating spheroid with NO necrotic core) up to the fixed Browning-2021 limiting fractions, reducing EXACTLY to the fixed `SpheroidConfig` thresholds at large radius (rim onset 200→370 µm, core onset 280→400 µm, first-order fits to the committed Browning size-binned medians, weakly constrained by 4 bins), fixing the structure leg's flagged finite-size error where the fixed thresholds over-predicted the core for R<300 µm (size-aware mean boundary abs-err 0.025 vs fixed 0.338 on the committed bins); opt-in (nothing constructs it by default) so the matrix and fixed spheroid path stay byte-identical, drift-guarded Python↔Rust (`scripts/validate_spheroid_structure.py size_aware_rust_constants`); the per-zone biochem gradient STRENGTHS remain uncalibrated placeholders and the size-resolved KILL leg stays data-blocked (no ferroptosis-inducer size-kill data), so this refines STRUCTURE only; v0.52.0 (#447) adds the CDK4/6-inhibitor-primed senescence to ferroptosis combination to `senescence` (`Cdk46PrimingConfig` + `senescent_fraction_after_priming` + `cdk46_primed_senolytic_config`): a senescence-INDUCING pre-treatment (palbociclib/ribociclib) raises the senescent fraction over a multi-day induction (saturating exponential toward `f_max`) and the fraction RESOLVES over a recovery window after withdrawal, feeding the existing iron-dominant senolytic-under-RSL3 program (D'Ambrosio/Gil 2026 PMID 42032311), so longer priming recruits a larger senolytic-vulnerable population (a primed cell peroxidizes MORE under RSL3) and the vulnerability window CLOSES as senescence resolves; kinetics UNCALIBRATED placeholders grounded only in the broadly-reported ~14-21 day senescence-establishment timescale (the specific induction/recovery kinetics are not fit to any dataset), DIRECTION is the result; off-by-default (`priming_days=0` ⇒ fraction 0 ⇒ identity SenescenceConfig ⇒ byte-identical), not in the C ABI; v0.53.0 (#443) adds the `ifngamma` module (IFN-γ → System Xc⁻ ferroptosis-sensitization coupling, the molecular RETURN arm of the immune-amplification thesis): the immune layer already couples ferroptotic death → DAMP → CD8 priming, but the reverse loop (activated CD8 IFN-γ sensitizing tumor cells BACK to ferroptosis) was missing; `IFNGammaConfig` + `system_xc_retention(local_ifn, ic50) = ic50/(ifn+ic50)` (in `(0,1]`, `1` at no IFN, → 0 as IFN rises) is the GSH-retention multiplier the consumer applies to the per-cell GSH pool, modeling IFN-γ transcriptional repression of System Xc⁻ (SLC7A11/SLC3A2) → cystine starvation → GSH loss → more ferroptosis (Wang et al., Nature 2019, PMID 31043744), plus a parallel `acsl4_upregulation` arm (more oxidizable PUFA, library-only — the per-cell static-lipid wiring is a deferred follow-up); sim-tme-3d seeds an IFN-γ field from the local immune DAMP signal (`per_damp · local_damp` at DAMP-positive cells), diffuses it with the shared `diffuse_damp_3d_step` operator (gated on immune_on), and multiplies `system_xc_retention` into `gc.state.gsh` each biochem step, with an `ifngamma_couples_immune_activation_to_ferroptosis_sensitization` A/B confirming the coupling measurably RAISES RSL3 kill (roughly doubles it at the placeholder config, 0.076 → 0.154); magnitudes (`system_xc_ic50`, field transport constants) are uncalibrated placeholders, the Wang-2019 DIRECTION is the result; `IFNGammaConfig::disabled()` (the default, ic50=∞) ⇒ retention `1.0` ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI; the #443 follow-up (PR 2, library API unchanged so no version bump) wires the parallel ACSL4 arm (`acsl4_upregulation`: more IFN-γ → more ACSL4 → more oxidizable PUFA) into the per-cell `lipid_unsat` axis as a save/multiply/restore around `sim_cell_step` (transient per-step boost, NOT a durable mutation — `lipid_unsat` is a static parameter so a persistent per-step multiply would compound, unlike the regenerating GSH pool; the transient form also composes cleanly with the contact/clonal layers that set `lipid_unsat`), gated on `acsl4_strength>0` (`0` ⇒ byte-identical), with an ACSL4-arm-alone A/B (ic50=∞ so only the lipid arm acts) confirming it raises ferroptosis independent of the GSH arm, plus the `--snapshot=ifngamma` preset (RSL3 + immune, both arms via `literature()`, no bespoke overlay since the IFN-γ field seeds from the run's evolving DAMP not a static mask); v0.54.0 (#446) adds the `alox` module (ALOX lipoxygenase-isoform-specific peroxidation rate + MCFA→ACSL4 PUFA sensitization): the core engine drives peroxidation through ONE generic `lp_propagation`, implicitly assuming an average enzymatic-oxidation capacity, but the rate depends on which arachidonate-lipoxygenase isoforms a tumor expresses (ALOX15 is the canonical ferroptosis driver; ALOX12/ALOX5 contribute at isoform-specific rates varying several-fold, PNAS 2016 PMID 27506793), and MCFA exposure upregulates ACSL4/CD36 → more oxidizable PUFA (Sci Rep 2024 s41598-024-55050-4; PMC11901882); `AloxConfig` (isoform activity/expression mix + MCFA level) collapses to two off-by-default ADDITIVE boosts a consumer writes onto `Params`: `alox_propagation_boost` (the propagation rate is ×`(1+boost)` clamped ≥0, so ALOX-high → faster propagation, ALOX-null → −1 → zero enzymatic propagation) and `mcfa_pufa_boost` (added to the oxidizable-PUFA augmentation alongside the ether-lipid pool in `ether_augmented_pufa`); both `0.0` by default ⇒ ×1.0 ⇒ byte-identical, FFI `ferro_params_to_params` defaults them so the C ABI/header is unchanged; sim-tme-3d wires them via off-by-default `Overrides.alox` (writes the two boosts onto `params` per condition, gated on non-identity) + an `alox_high_mcfa_raises_ferroptosis` A/B (ALOX15-high + MCFA out-kills the balanced baseline, identity is a no-op) + the `--snapshot=alox` preset; the per-isoform activity weights + MCFA kinetics are UNCALIBRATED placeholders (isoform Kcat/Km vary ~10-fold, absolute MCFA kinetics deferred to the E-series), the directions (ALOX-high ⇒ more ferroptosis; MCFA ⇒ more ferroptosis) are the result; per-cell stochastic ALOX heterogeneity is a deferred refinement (this models a per-condition ALOX phenotype); `AloxConfig::identity()` ⇒ both boosts 0 ⇒ byte-identical (production matrix SHA unchanged); v0.55.0 (#444) adds the `acsl4` module (ACSL4-status biomarker stratification): ACSL4 ligates the PUFA ferroptosis requires into membranes, so a tumor's ACSL4 expression status sets its oxidizable-PUFA baseline (ACSL4-high ⇒ sensitive; ACSL4-negative ⇒ intrinsically REFRACTORY via a collapsed PUFA substrate, a mechanism distinct from GPX4/GSH/FSP1, e.g. some HCC/AML subtypes; Doll 2017 Nat Chem Biol PMID 27842070); `acsl4::pufa_boost_from_status(status)` maps a status scalar (1.0=wild-type) to the new off-by-default `Params.acsl4_status_boost` (`status−1` clamped at −1) folded into `ether_augmented_pufa` alongside the ether/MCFA boosts; `ACSL4_NEGATIVE/LOW/NORMAL/HIGH` constants; sim-tme-3d `Overrides.acsl4_status` writes the boost per condition + `--snapshot=acsl4-negative` preset (RSL3 on a refractory ACSL4-negative tumor), with an `acsl4_status_stratifies_ferroptosis_sensitivity` A/B (ACSL4-high out-kills normal, ACSL4-negative kills LESS — the patient-stratification headline); the status→boost mapping is an UNCALIBRATED linear placeholder and the per-cancer-type ACSL4 prevalence + cell-line-status-vs-dose-response validation are DATA-GATED (TCGA/cBioPortal + a cell-line meta-analysis the repo lacks), NOT fabricated; `status=1.0`/`acsl4_status_boost=0.0` (default) ⇒ byte-identical (production matrix SHA unchanged), FFI defaults it so the C ABI/header is unchanged; v0.56.0 (#462) anchors the #444 ACSL4-status layer to real data (the first calibration of an issue-scan layer): `acsl4::status_from_zscore(z) = max(0, 1 + z/2)` is the calibrated bridge from a within-cohort ACSL4 mRNA z-score to the model status scalar, whose integer-z points reproduce the shipped constants EXACTLY (z=+1⇒ACSL4_HIGH, 0⇒NORMAL, −1⇒LOW, −2⇒NEGATIVE), so a real cBioPortal z-score maps onto the existing scale; `scripts/fetch_acsl4_prevalence.py` pulls the 32 TCGA PanCancer Atlas studies' ACSL4/GPX4/SLC7A11 mRNA via the login-free cBioPortal REST API (offline contract: run local, CI reads only the committed `analysis/calibration/acsl4_prevalence_tcga.{csv,json}`), giving the committed per-cancer-type low-ACSL4 prevalence prior (z<−1: 10.8–18.8%, median 14.4%; z<−2: median 3.0%, fairly uniform across types) that sets how many tumors fall in the refractory-leaning tail; the LOAD-BEARING honest negative result is that bulk TCGA mRNA does NOT rank HCC/AML as ACSL4-low (lihc ranks HIGHEST on raw RSEM, laml high), so the Doll-2017 ACSL4-low-refractory phenotype is a protein/subtype/functional property bulk mRNA medians do not capture (and cross-study RSEM is batch-confounded), which bounds the layer to within-cohort-z stratification not cross-type bulk-mRNA ranking; moves #444 from DATA-GATED to partially anchored (the status→z bridge + prevalence prior anchored; the status→magnitude mapping + protein-level refractory-subtype prevalence still data-gated); `status_from_zscore` is a new pure library API (byte-identical, nothing in the matrix calls it), 4 Python guards pin the prior + the negative finding offline; v0.57.0 (#465) adds the `repair` module (ESCRT-III membrane-repair brake on death EXECUTION, a genuinely new category): every other layer modulates the redox/lipid SUBSTRATE (WHETHER a cell peroxidizes), but ESCRT-III acts on the death-DECISION step, a cell whose lipid peroxide has already crossed `death_threshold` can be RESEALED (Ca2+→CHMP5/CHMP6 membrane repair) for a time, so more repair capacity ⇒ slower/blocked execution ⇒ more ferroptosis RESISTANCE (Dai 2020 BBRC PMID 31761326, CHMP5/CHMP6 knockdown sensitizes); two off-by-default `Params` fields drive it, `escrt_repair_rate` (per-step rescue probability when over threshold) and `escrt_repair_budget` (finite per-cell repair capacity, tracked on the new `CellState.escrt_budget_used`), with the decision helper `repair::escrt_rescue(rate, roll)`/`escrt_can_attempt(rate, used, budget)`; the death check in BOTH `sim_cell_step` and `sim_cell` draws the RNG roll ONLY when `escrt_repair_rate > 0` and budget remains (the `escrt_can_attempt` short-circuit), so the default path makes no extra draw and is byte-identical; a rescued cell still runs its defenses next step, so ESCRT buys time for GSH/GPX4 to recover under transient stress while sustained GPX4 inhibition still kills once the budget is spent (a finite delay); sim-tme-3d wires it via off-by-default `Overrides.escrt: Option<(rate, budget)>` + an `escrt_repair_reduces_rsl3_kill` A/B (ESCRT-high tumor out-survives the baseline under RSL3) + the `--snapshot=escrt` preset; the rate/budget are uncalibrated placeholders, the direction (more repair ⇒ more resistance) is the result; FFI defaults both to 0.0 so the C ABI/header is unchanged, `escrt_repair_rate=0.0` (default) ⇒ brake never fires ⇒ byte-identical (production matrix SHA unchanged); v0.58.0 (#466) adds the POR/CYB5R1 enzymatic O2-coupled H2O2 source: POR (cytochrome P450 reductase) and CYB5R1 transfer electrons from NAD(P)H to O2 to make H2O2, the Fenton substrate, an enzymatic O2/NADPH-dependent oxidant source distinct from the static `basal_ros` and parallel to the ALOX enzymatic-propagation leg (Yan 2021 PMID 33860083, POR/CYB5R1 catalyze lipid peroxidation to execute ferroptosis; Zou 2020 Nat Chem Biol); `Params.por_h2o2_rate` (off-by-default 0.0) is added to `total_ros` at all three biochem sites (more POR ⇒ more H2O2 ⇒ more Fenton ROS ⇒ more ferroptosis), with `oxygen::por_o2_factor(o2_supply, dependence)` (same `(1−dep)+dep·s` form as `o2_dependent_exo_factor`) the O2-coupling the spatial consumer applies; sim-tme-3d wires it via off-by-default `Overrides.por: Option<(rate, o2_dep)>` injecting `rate·por_o2_factor(local_o2, dep)` into each tumor cell's `basal_ros` at setup (so POR makes LESS H2O2 in the hypoxic core, unlike a uniform oxidant it does NOT amplify the deep-core artifact), with a `por_h2o2_raises_rsl3_kill` A/B + `--snapshot=por` preset (RSL3, fully O2-dependent so rim-weighted); rate is an uncalibrated placeholder, the direction is the result; `por_h2o2_rate=0.0` (default) ⇒ byte-identical (production matrix SHA unchanged), FFI defaults it to 0.0 so the C ABI is unchanged; v0.59.0 (#467) adds the 7-DHC/DHCR7 sterol radical-trapping defense: 7-dehydrocholesterol (7-DHC), a distal cholesterol-biosynthesis intermediate, is a potent endogenous membrane radical-trapping antioxidant that shields PUFA from peroxyl-radical autoxidation, gating the autocatalytic peroxidation chain (Freitas/Li, Nature 2024, '7-dehydrocholesterol dictates ferroptosis sensitivity,' PMID 38297130, DOI 10.1038/s41586-023-06983-9); `Params.dhc7_radical_trap` (off-by-default 0.0) is added to the GPX4-independent radical-trapping quench term (alongside `gch1_rate`/FSP1) at both biochem sites, so a higher 7-DHC pool raises the quench and LOWERS the propagation rate ⇒ ferroptosis RESISTANCE; the DIRECTION: DHCR7 consumes 7-DHC, so DHCR7-loss raises the pool (resistance, the modeled escape) and EBP/SC5D-low lowers it (sensitization); a sterol-pathway radical-trapping defense distinct from GPX4/GSH/FSP1/DHODH and from the iron/MUFA axes (the GCH1/BH4 analog but a separate sterol pool); sim-tme-3d wires it via off-by-default `Overrides.dhc7: Option<pool>` + a `dhc7_radical_trap_reduces_rsl3_kill` A/B (a DHCR7-low tumor out-survives the baseline under RSL3) + `--snapshot=dhc7` preset; the pool magnitude is an uncalibrated placeholder, the direction is the result; `dhc7_radical_trap=0.0` (default) ⇒ byte-identical (production matrix SHA unchanged), FFI defaults it to 0.0 so the C ABI is unchanged; #468 (no crate-version bump, a reframing not a new mechanism) makes explicit that the existing #336 `sdt_o2_dependence` knob ALREADY expresses the Type I (oxygen-INDEPENDENT) vs Type II (oxygen-dependent singlet-oxygen) SDT radical split, since `o2_dependent_exo_factor(o2, dep) = (1−dep) + dep·o2` decomposes into a Type I arm `(1−dep)` and a Type II arm `dep·o2` (so `dep` IS the Type II fraction, no redundant `type_i_fraction` param needed); the oxygen doc comment now names this, a `type_i_sdt_retains_hypoxic_kill_where_type_ii_collapses` A/B (grid 60/λ=50/180) demonstrates a Type-I-heavy sonosensitizer `dep=0.3` retains ~43% hypoxic-core SDT kill while pure Type II `dep=1.0` collapses to 0, and the `--snapshot=sdt-typei` preset is the hypoxia-tolerant complement of `--snapshot=sdt-o2dep`; v0.60.0 (#469) adds the off-by-default DC/effector-cell ferroptosis-susceptibility coupling to `immune_spatial` (`DcFerroptosisConfig` + `dc_ferroptosis_survival`): the immune layer otherwise treats dendritic cells only as an undying DAMP source plus a fixed priming scalar (#264 P4), but a ferroptotic TME can kill the effector DCs THEMSELVES (loss of PD-L1 on tumor-associated DCs downregulates System Xc-/SLC7A11, raises their lipid peroxidation, and drives them into ferroptosis, lowering T-cell priming, Yao et al. Cell Reports 2024 PMID 39423128), so `dc_ferroptosis_survival(local_lipid_ros, susceptibility, pdl1_protection) = 1/(1 + susceptibility*local_lipid_ros*(1-pdl1_protection))` is a per-cell DC-survival multiplier the consumer scales into the immune kill probability, keyed on the SAME local DAMP/lipid-ROS that drives pro-immune `dc_activation` so a ferroptosis-inducing TME can SUPPRESS its own immune amplification; distinct from the #337 secreted-factor immunosuppression (there the dying tumor cells release DC-suppressing factors, here the effector cells are directly ferroptosis-KILLED, checkpoint-state-protected); sim-tme-3d wires it via off-by-default `Overrides.dc_ferroptosis` (gated on immune_on + non-identity) with a `dc_ferroptosis_reduces_immune_kills_and_pdl1_protects` A/B (PD-L1-low vulnerable DCs lower net immune kills, full PD-L1 protection AND susceptibility=0 both reproduce the baseline exactly) + the `--snapshot=dc-ferroptosis` preset; susceptibility/magnitude are UNCALIBRATED placeholders, the PMID 39423128 direction is the result; `susceptibility=0.0` default => survival multiplier 1.0 => byte-identical (production matrix SHA unchanged), not in the C ABI; v0.61.0 (#470) adds the off-by-default persister OXPHOS-ROS suppression + HDAC-rescue axis to `persister`/`params` (`PersisterConfig.oxphos_ros_suppression` + `PersisterConfig.hdac_inhibitor` + `persister::oxphos_ros_multiplier(fraction, cfg) = 1 - oxphos_ros_suppression*fraction*(1-hdac_inhibitor)` clamped to [0,1]): a distinct MITOCHONDRIAL escape axis (separate from the existing GPX4-resistance + MUFA axes) where drug-tolerant persisters survive GPX4 inhibition partly by DOWNREGULATING oxidative phosphorylation, a main source of the mitochondrial ROS / peroxidizable-substrate flux a GPX4 inhibitor acts on, so RSL3 has less to peroxidize and kills persisters LESS, while an HDAC inhibitor re-raises OXPHOS/ROS and restores the kill ("FSP1 and histone deacetylases suppress cancer persister cell ferroptosis", PMID 41481741); the consumer scales a persister cell basal/mitochondrial ROS DOWN by the multiplier (1.0 at no persistence, 1-suppression at full persistence, restored to 1.0 by a full HDAC inhibitor); sim-tme-3d applies it as a transient save/multiply/restore of the per-cell `basal_ros` around `sim_cell_step` (NOT durable: `basal_ros` is a static parameter while `persister_fraction` evolves per step), parallel to the #443 IFN-gamma/ACSL4 arm, with a `persister_oxphos_suppression_reduces_rsl3_kill_and_hdac_restores` A/B (OXPHOS-low persisters take fewer RSL3 kills; full HDAC inhibitor AND suppression=0 both reproduce the persister baseline exactly) + the `--snapshot=persister-oxphos` preset (RSL3 + persister + OXPHOS suppression); `oxphos_ros_suppression=0.0` default (AND in `enabled()`, so the existing persister snapshot/tests are unaffected) => multiplier 1.0 => byte-identical (production matrix SHA unchanged), not in the C ABI; magnitude is an uncalibrated placeholder, the PMID 41481741 direction (OXPHOS-low persister => less RSL3 kill, HDACi reverses) is the result; v0.62.0 (#482) adds the `trigger_wave` module (propagating ferroptotic trigger-wave front) AND its measured-data calibration, the flagship "tie simulations to reality" leg: ferroptosis can PROPAGATE as a constant-speed lipid-peroxide/ROS front (each dying cell igniting its neighbours), measured by Co/Wu/Lee/Chen Nature 2024 PMID 38987590 at 5.52 um/min baseline, iron-tunable to 2.33 (DFO chelation) / 9.40 (iron loading) um/min (open code github.com/imb-lcd/ftw2024 + figshare 25762806); the module solves the 1-D bistable Nagumo reaction-diffusion equation `dL/dt = D*Lxx + k*L*(L-a)*(1-L)` with closed-form front speed `c = sqrt(D*k/2)*(1-2a)` where the autocatalytic peroxidation rate `k` scales with labile iron (Fenton, so `c ~ sqrt(iron)`) and the GPX4/GSH defense raises the ignition threshold `a` (slowing/halting the front); `front_speed` integrates the PDE (explicit Euler, CFL-guarded) and measures the L=0.5 front, `analytical_front_speed` is the closed form, and the numeric==analytic agreement is the self-consistency check (the trigger-wave analogue of `reaction_diffusion::analytical_1d_slab`); validated by `scripts/validate_trigger_wave.py` (pure-stdlib, CI): the baseline lands at 5.52 um/min (a one-point D*k calibration) and the iron-dose SHAPE `c ~ sqrt(iron)` reproduces the measured 2.33/5.52/9.40 at plausible iron fold-changes ~0.18/1.0/2.9 (the PREDICTED result, not fit), with a drift-guard tying the Python BASELINE to the Rust `baseline()` constants; this is the SPATIAL complement of the single-cell switch we already cross-validate (#344) and the first propagating-front capability in the suite; opt-in (nothing in the production matrix calls it) so byte-identical, not in the C ABI; committed `analysis/calibration/trigger_wave_measured_data.csv` + `trigger-wave-validation.{md,json}` + `calibration-targets-trigger-wave.md`; v0.63.0 (#483) adds the off-by-default vitamin-K / VKORC1L1 radical-trapping defense + warfarin inhibition to `biochem`/`params`, a SIXTH GPX4-independent ferroptosis-suppressor axis alongside `gch1_rate` (BH4), `dhc7_radical_trap` (7-DHC), and FSP1 (CoQ): VKORC1L1 reduces vitamin K to a radical-trapping antioxidant (vitamin K hydroquinone) that quenches phospholipid peroxyl radicals INDEPENDENT of GPX4/GSH and is a p53 transcriptional target (Yang et al. Cell Metab 2023 PMID 37467745; origin Mishima et al. Nature 2022 PMID 35922516); `Params.vitk_radical_trap` adds to the `antioxidant_quench` pool at both peroxidation sites (higher trap LOWERS propagation => ferroptosis RESISTANCE), and `Params.warfarin_vkor_inhibition` (in [0,1]) scales it DOWN via `Params::effective_vitk_radical_trap() = vitk_radical_trap*(1-warfarin_vkor_inhibition)`, so the FDA anticoagulant warfarin (a VKOR inhibitor) collapses the trap and DRIVES ferroptosis (the repurposable, p53-status-gated arm Yang 2023 used to suppress tumors in immunocompetent mice); sim-tme-3d wires it via off-by-default `Overrides.vitk: Option<(trap,warfarin)>` + a `--snapshot=vkorc1l1` preset (the defended no-warfarin resistant state) + a `warfarin_reverses_vkorc1l1_resistance` A/B (the VKORC1L1 trap lowers RSL3 kill, full warfarin restores it to the unprotected baseline EXACTLY) + a library `sim_cell_step_respects_vitk_radical_trap_and_warfarin` test; magnitude is an uncalibrated placeholder, the PMID 37467745 direction (VKORC1L1 protects, warfarin re-sensitizes) is the result; both fields `0.0` default => effective trap 0.0 => byte-identical (production matrix SHA unchanged), FFI maps both to 0.0 so the C ABI/header is unchanged; v0.64.0 (#484) adds the off-by-default PROM2 / MVB-exosome labile-iron EFFLUX path to `biochem`/`params`, an iron-EXPORT resistance axis (the OPPOSITE sign to the #340 ferritinophagy RELEASE): pro-ferroptotic stress induces Prominin-2 (PROM2), which packages ferritin-bound iron into multivesicular bodies secreted as exosomes, DEPLETING the labile iron pool and starving the Fenton reaction, driving EMT/metastatic ferroptosis resistance (Brown et al. Dev Cell 2019 PMID 31735663; PROM2-overexpression metastasis, Paris et al. Clin Transl Med 2024); `Params.prom2_iron_efflux` (in [0,1]) + `biochem::prom2_iron_factor(step, params)` scale the Fenton iron at all three Fenton sites by a time-dependent factor ramping from 1.0 toward `1 - prom2_iron_efflux` with the shared `ferritinophagy_tau` time constant (composing multiplicatively with `ferritinophagy_iron_factor`), so a PROM2-high cell exports iron over the run and resists; sim-tme-3d wires it via off-by-default `Overrides.prom2: Option<f64>` + a `--snapshot=prom2` preset + a `prom2_iron_efflux_reduces_rsl3_kill` A/B (a PROM2-high tumor takes fewer RSL3 kills, efflux=0 reproduces the baseline exactly) + library `prom2_iron_factor_drains_iron_and_default_is_identity` / `prom2_iron_efflux_lowers_rsl3_peak_lp` tests; magnitude is an uncalibrated placeholder, the PMID 31735663 direction (PROM2 iron export => less ferroptosis) is the result; `prom2_iron_efflux=0.0` default => factor exactly 1.0 for every step => byte-identical (production matrix SHA unchanged), FFI maps it to 0.0 so the C ABI/header is unchanged; v0.65.0 (#485) adds the `copper` module (copper-ionophore / ferroptosis-cuproptosis crosstalk), a metal-2 axis bridging the iron-driven engine to a copper death mode: copper ionophores (elesclomol, disulfiram) overload intracellular copper, which simultaneously enables ferroptosis AND cuproptosis by depleting GSH (copper binds glutathione) and driving GPX4 degradation, gated by the ATP7A/B copper-efflux transporter (an efflux-competent tumor exports copper and resists; Gao et al. Mol Oncol 2021 PMID 34390123, elesclomol degrades ATP7A; FIN+ionophore synergy PMID 37214358); `CopperConfig` (cu_gsh_depletion + cu_gpx4_degradation + atp7b_efflux) + `copper::gsh_retention`/`gpx4_retention(cfg) = 1 - depletion*(1-atp7b_efflux)` are per-step pool multipliers the consumer applies (pure module, the core engine never reads it so it stays byte-identical, like `ifngamma`); sim-tme-3d wires it via off-by-default `Overrides.copper: Option<CopperConfig>` (the GSH/GPX4 pools scaled DOWN each step, parallel to the #443 IFN-gamma System-Xc- arm) + a `--snapshot=copper` preset + a `copper_ionophore_raises_rsl3_kill_and_atp7b_efflux_protects` A/B (the ionophore raises RSL3 kill, full ATP7B efflux AND disabled() both reproduce the baseline exactly) + 3 library tests; lets the suite model elesclomol+RSL3 combinations and the cuproptosis previously only scaffolded in the taxonomy; magnitudes are uncalibrated placeholders (the CTRPv2 elesclomol/disulfiram dose-response + DepMap ATP7B expression #485 mentions could calibrate them), the PMID 34390123 direction is the result; `CopperConfig::disabled()` ⇒ retention 1.0 ⇒ byte-identical (production matrix SHA unchanged), not in the C ABI; v0.66.0 (#486) adds the off-by-default dietary-PUFA / lipid-droplet (DGAT) buffer to `biochem`/`params`, reusing the existing PUFA + pH + MUFA layers: exogenous n-3/n-6 polyunsaturated fatty acids add an oxidizable-substrate load that sensitizes to ferroptosis in proportion to double-bond count, BUT only AFTER the saturable triglyceride-storage sink (the lipid droplet, filled by DGAT) is exceeded, and DGAT inhibition makes the cytotoxicity emerge sooner (Dierge et al. Cell Metab 2021 PMID 34118189); `Params.dietary_pufa_supply` + `Params.lipid_droplet_buffer` fold `(dietary_pufa_supply - lipid_droplet_buffer).max(0)` (the dietary-PUFA EXCESS over the buffer) into the additive oxidizable-PUFA augmentation `ether_augmented_pufa` alongside the ether-lipid/MCFA/ACSL4-status boosts, so PUFA below the buffer is stored (no effect) and PUFA above it raises peroxidation; sim-tme-3d wires it via off-by-default `Overrides.dietary_pufa: Option<(supply,buffer)>` + a `--snapshot=dietary-pufa` preset (RSL3 + ph_on, so the existing pH layer provides the Dierge ACIDOSIS potentiation by composition, the acidic gradient lowers defenses while the dietary PUFA adds substrate) + a `dietary_pufa_above_dgat_buffer_raises_rsl3_kill` A/B (supply over the buffer raises RSL3 kill; supply at/below the buffer AND both-0.0 reproduce the baseline exactly) + a library `sim_cell_step_respects_dietary_pufa_dgat_buffer` test (buffered = no effect, excess raises LP, DGAT inhibition raises it more at fixed supply); magnitudes are uncalibrated placeholders (Dierge reports in-vivo n-3-rich-vs-MUFA-diet tumor-growth separation + DGATi synergy), the direction is the result; both fields `0.0` default => no excess => byte-identical (production matrix SHA unchanged), FFI maps both to 0.0 so the C ABI/header is unchanged; v0.67.0 (#502) adds System Xc-/SLC7A11 cystine import to the CORE engine (the most therapeutically important UPSTREAM ferroptosis node, so unlike the off-by-default distal realism layers it lives in the core): the NRF2-driven GSH resynthesis IS the cystine supply, scaled by `Params::xc_cystine_factor()` = `1 - erastin_xc_inhib*(1-transsulfuration_floor)`; `erastin_xc_inhib` is erastin's first-class drug input (peer to `rsl3_gpx4_inhib`) that starves cystine so GSH depletes under basal ROS and ferroptosis follows (Dixon 2012 PMID 22632970), and `transsulfuration_floor` is the minimal GPX4-independent cysteine backup (methionine->cysteine, erastin-resistant high-transsulfuration cells); both `0.0` default => factor exactly 1.0 => byte-identical (production matrix SHA unchanged, ×1.0 is exact in IEEE 754), FFI maps both to 0.0 so the C ABI/header is unchanged; calibrated against CTRPv2 erastin in `scripts/calibrate_erastin.py` (the core's SECOND data-anchored inducer mechanism after the GPX4i kill switch #330, RMSE ~0.10, a PARTIAL anchor: the fit captures the EC50 transition but has a basal-death floor at low dose and saturates at the top dose, the single-switch reconciliation with RSL3 deferred to the joint multi-inducer posterior #500); current crate version 0.67.0; unit-test count tracked in CI / `cargo test --workspace`) + Python bindings + 1171 Python tests (pipeline smoke + figure traceability + depth-kill physics-constant guard + flagship-figure data guard + quantitative-figure drift guards (Figs 21/22/23) + invariant/integration + calibrate-extractor + MeSH evidence-fallback + gold-set precision-floor regression (#346) + Bliss/sim-tme/penetration prior-predictive intervals + ABC posterior (#332) + non-circular mechanism-recall (#412) + CTRPv2 calibration target + in-vitro kill-switch fit (#330) + spheroid structure validation (#333) + embedding evidence leg (#411) + RD-vs-BioFVM cross-check (#408) + dashboard data layer (#354) + tumor-PK measured-data anchor (#334) + Krogh penetration validation (#335) + spheroid size-aware zone thresholds (#333) + spheroid kill-vs-size direction (#333) + gene-symbol ambiguity/FSP1 sense disambiguation (#ATLAS-AMBIG) + rare-event Poisson intervals + tail-resolution classification + ferroptosis-python bindings)
- news authentication pipeline: 5 scripts (fetch, extract claims, verify against PubMed, score credibility, build claim-centric index) implementing the 3-tier source framework from analysis/news-source-criteria.md
- simulation calibration: 8 targets documented (5 original + 3 new 3D self-consistency targets from #196 covering hypoxia-RSL3 collapse, immune SDT/RSL3 ratio, stromal boundary shielding); evaluate script operational; 3D validation infrastructure in place (`3d_validation_report.md` + upgrade-to-calibration path); `simulations/calibration/CALIBRATION_STATUS.md` is the single honest per-layer accounting (calibrated / partially anchored / self-consistency / uncalibrated) + calibration roadmap, consolidating the previously-scattered caveats — the suite is broad but mostly uncalibrated (mechanistic scaffolding, excluded from the manuscript's quantitative claims); #330/#333/#334/#335 add the first real-data-anchored legs (in-vitro kill switch vs CTRPv2, spheroid zone geometry vs Browning 2021, tumor-PK partition/structure vs IKE+sorafenib, Krogh penetration form + reference-drug length vs Primeau/Tannock); #462 anchors the ACSL4-status layer (#444) to cBioPortal TCGA per-cancer-type ACSL4 prevalence (the first calibration of an innovation-scan layer; `analysis/calibration/acsl4-prevalence-calibration.md`); #464 validates the PDT/SDT single source-independent kill-threshold design against Zhu 2015's measured photosensitizer-independent singlet-O₂ necrosis threshold (~0.5 mM; `analysis/calibration/pdt-threshold-validation.md`); #482 adds the flagship spatial-front leg: the new `trigger_wave` module (a propagating ferroptotic Nagumo front) is validated against Co 2024's measured ferroptotic trigger-wave speeds (5.52 um/min baseline, iron-tunable 2.33/9.40 via DFO/loading, PMID 38987590) reproducing the iron-dose response shape `c ~ sqrt(iron)` (`analysis/calibration/trigger-wave-validation.md`); #501 adds the calibrate-or-cut policy + layer-freeze rule (no new off-by-default axis lands without a named calibration target + a CALIBRATION_STATUS row, codified in CONTRIBUTING.md "Simulation layer policy"), and states the used-in-any-reported-number invariant explicitly (no off-by-default realism layer feeds any number the manuscript reports; the reported numbers come from the 2D core, guarded by #253/#499)
- manuscript honesty pass (integrity audit): the 3D suite was absent from the manuscript + several §8.4 "the model cannot do X" caveats had gone stale (clonal/persister DO model adaptive evolution; suppressor/checkpoints DO model immune dampening; vasculature DOES replace the edge-O2 proxy — all uncalibrated); fixed by a consolidated §8.4 "3D simulation suite" honesty paragraph (names the suite, its uncalibrated status, the deliberate exclusion from quantitative claims, and the 104:1→4:1 immune-ratio shrinkage in 3D) + drift fixes (version 0.2.0→0.25.0, "ten modules"→21, missing sim-tme-3d/sim-tumor-pk binaries); `v1.md` edited then `v1.tex` regenerated (compiles, ~115 pp)
- manuscript scientific review (deep external+internal cross-check, `analysis/manuscript-scientific-review.md`): five-lens review (supporting lit, contradicting lit, math/physics, fresh-eyes coherence, figure gaps) with every new PMID personally verified. Math/physics confirmed sound (Beer-Lambert, Krogh, acoustic attenuation, Bliss 1.99×, ferroptosis ODEs); fixed the `iron_diffusion_coeff` mis-citation (was Jacques-2013-optics → tortuosity-reduced estimate). Key science: the central "physical ROS BYPASSES hypoxia" claim was over-extended vs the SDT field (SDT ROS is widely O2-dependent; lead clinical agent SONALA-001 is Type II/O2-dependent), so §7.1/§7.2/§7.5/§8.4/§10.1 were rebalanced (direction survives, "bypass"+magnitude do not) with 6 verified citations added on both sides (Hubbi/Dang 2026 PMID 41932308 supports the mechanism in PDAC but shows it combines GSH-defense + mito-ROS-suppression; Zou 2019 PMID 30962421 shows hypoxia SENSITIZES ccRCC to RSL3; Hayashi 2020 PMID 33288764 DAMP-balance≠quantity; Dai 2020 PMID 33311482 ferroptosis accelerates PDAC in vivo). Figure roadmap documented; figures not yet generated
- drug penetration module: 3 tissue types, exponential Krogh approximation
- drug combination modeling: 4 drugs, pairwise Bliss synergy scoring with pathway traces
- tumor microenvironment: oxygen gradients (edge-distance proxy, explicit internal vessel network #191, patient-scale planar depth gradient #240, or the steady-state reaction-diffusion field #343 that replaces the monotonic nearest-vessel proxy with a true diffusion+consumption solve), spatial immune zones (DAMP diffusion, T cell activation, anti-PD-1, T-cell exhaustion #243, Treg/MDSC suppressor field + multi-checkpoint PD-1/CTLA-4/LAG-3/TIM-3 brake #264, IFN-γ → System Xc⁻ ferroptosis-sensitization return loop #443, DC/effector-cell ferroptosis susceptibility #469 where a ferroptotic TME kills the PD-L1-gated effector DCs themselves and so suppresses its own immune amplification), LP overshoot multiplier, CAF-mediated stromal protection (GSH/MUFA supply), pH gradient (iron release + drug ion trapping), cell-cell contact resistance (E-cadherin/NF2-YAP, dense cells lower PUFA/iron #270)
- some landmark papers are known to be missing from the local full-text archive
- content provenance manifest (PROVENANCE.yaml) documents asset licensing and redistribution rights
- pinned Python environment (requirements-lock.txt, 32 packages) and Rust toolchain (rust-toolchain.toml, 1.96.0)
- contributor guide (CONTRIBUTING.md), citation metadata (CITATION.cff), and pytest in tracked dependencies
- Python CI workflow (.github/workflows/python-test.yml): Linux on PR/push, macOS weekly
- Rust CI workflow (.github/workflows/cargo-test.yml): `cargo test --workspace` + `cargo fmt --all --check` gate on PR/push (fmt pinned to the 1.96.0 toolchain, #209/#236); the `test` job also builds `ferroptosis-ffi` and compiles + runs the C harness (`tests/test_ffi.c`) against the cdylib, verifying the PhysiCell C-ABI per-PR, plus a `git diff --exit-code` guard on the build.rs-regenerated cbindgen header so a stale committed `ferroptosis.h` fails CI (#295 item 4)
- sim-tme-3d production regression workflow (.github/workflows/sim-tme-3d-regression.yml): weekly + on-demand full 60³×180 run asserting `summary.json`'s SHA-256 against a checked-in hash on the pinned 1.96.0 toolchain (#253). This SHA gate is a CONTAINMENT invariant (the software did not change the default matrix), NOT scientific validation
- calibration-regression gate (#499, `tests/test_calibration_regression.py`, runs in Python CI per-PR): asserts the legs actually anchored to independent data still reproduce their committed held-out metrics, so a change cannot silently move a calibrated leg off its anchor (CTRPv2 kill-switch ML162 fit + ML210 held-out RMSE #330, tumor-PK partition Kp #334, trigger-wave 5.52 µm/min #482 live-recomputed, Krogh penetration lengths #335, spheroid size-aware zone geometry #333, PDT source-independent threshold #464); this guards the science, complementing the byte-identity gate which only guards the software
- figure traceability index (FIGURES.yaml) mapping all 28 figures to generators, inputs, and types (#285 quantitative simulation figures: hypoxia kill-collapse, Bliss synergy, treatment-timing window, depth-kill curves, and the flagship 2×2 resistance-mechanism asymmetry — manuscript figure 24)
- archival release tooling (.zenodo.json metadata template, scripts/generate_release_manifest.py for SHA256 manifest + filtered archive)

## What To Optimize For

- claims that are traceable and caveated
- taxonomy choices that do not inflate conclusions
- language that reflects uncertainty honestly
- outputs that help compare alternatives fairly
- maintainable scripts and reproducible reruns
- a tone that invites contribution rather than gatekeeping

## What To Avoid

- assuming the repo exists only to defend PDT/SDT
- writing stronger absence claims than the evidence model supports
- confusing patient-study signal with phase-labeled trial maturity
- treating broad process coverage as proof of therapeutic depth
- letting historical framing survive after the underlying analysis changed
- making the codebase feel closed or intimidating to newcomers

## Key Files

```text
README.md                                    repo-level framing and entry points
article/drafts/v1.{md,tex,pdf}               manuscript drafts
analysis/evidence-coverage-audit.md          evidence-tier coverage and guardrails
analysis/taxonomy-rerun-notes.md             taxonomy/query caveats after reruns
analysis/pathway-target-audit.md             pathway-target coverage
analysis/landmark-corpus-gaps.md             known missing papers that distort claims (VISION recovered #345)
scripts/landmark_coverage.py                  landmark-coverage guardrail + recovery (#345): per-mechanism membership check + fetch-and-add-as-abstract for absent field-definers
scripts/taxonomy_coverage.py                   expanded-taxonomy coverage (#347): 25-mechanism PubMed + local coverage report; cuproptosis/disulfidptosis added to config (scaffold; frozen 19-mechanism results untouched)
scripts/oa_bias_analysis.py                    open-access/journal bias on mechanism rankings (#348): per-mechanism OA-rate table + full-text-vs-abstract-only ranking sensitivity; regenerates §3.3.1's hand-derived shifts (immunotherapy 34.4→28.7% share, physical class 14.7→22.4%, bioelectric rank 14→3) into analysis/oa-bias-report.md
scripts/sobol_sensitivity.py                   Sobol global sensitivity + practical identifiability (#331): self-contained Saltelli/Jansen over the SAME 10 biochem rate constants + ranges as the PRCC (#134, the PRCC's 11 minus SDT-only sdt_ros, loaded from prcc-results.json) for the ferroptosis kill switch; total-effect ranking lp_propagation (ST=0.50), gpx4_rate (0.29), lp_rate (0.18) reproduces the PRCC top-three in order (~89% as first-order), interactions small (ΣS1≈1.05 confirms PRCC's 'no Sobol escalation' call); 6/10 params kill-rate-insensitive → flagged non-identifiable in parameter_provenance.md; analysis/sobol-sensitivity-report.md (per-headline-output sensitivity for the spatial/combo headlines is the #331 follow-up, see scripts/headline_sensitivity.py)
scripts/headline_at_fitted.py                   re-derives EVERY headline at the CTRPv2-fitted cascade and reports it beside the in-vivo default (the outstanding piece of work `analysis/calibration/in-vivo-prior-provenance.md` named as "achievable now and the cheaper of the two"). The expected output was a list of directions that survive crossing the bistable tipping point; the actual result is that BOTH fitted sets are INADMISSIBLE for these models — under either, the untreated Persister/Control population dies at ~99.96% against the manuscript's own stated constraint of under 2%, a factor of 50. Every headline then degenerates: the Bliss ratio is exactly 1.0 because both single arms saturate, and all three penetration tissues kill 100% so their ordering is trivially preserved. So the #332/#500 disjunction is no longer only an argument about parameter ranges — substituting the in-vitro values does not merely fail to condition the in-vivo models, it breaks their baseline-viability constraint outright. The one direction that survives is the hypoxia kill-collapse gap (0.866/0.606/0.498, positive at all three), read as weak evidence because a positive gap in a saturated regime is nearly guaranteed. Reuses headline_sensitivity's EXTRACTORS so the observables cannot drift, but supplies its own launcher because those build a full parameter vector from a design matrix while this overrides a handful and leaves the rest at default. `k_um` from the #330 fit deliberately does not transfer (a micromolar dose-scale, not a Params field). Guards in tests/test_headline_at_fitted.py derive the 2% threshold from the manuscript and refuse to let a degenerate arithmetic result be reported as a failed direction. analysis/headline-at-fitted-cascade.{md,json}
scripts/identifiability_report.py               consolidated practical-identifiability synthesis (#503): one reproducible accounting of which HEADLINE outputs are point-estimable vs directional-only, synthesizing the already-committed sensitivity/uncertainty analyses (Sobol #331, prior-predictive #332, ABC #332, headline Morris #331) rather than re-running them. Cross-checks the structural facts against the machine-readable analysis/prcc-results.json (11 swept params, the 6 non-identifiable named from the kill rate); states degrees-of-freedom=11, data-conditioned-headlines=0 (the only fit is the disjoint in-vitro switch), and a per-headline verdict (single-cell kill rate + immune ratio = directional_only; Bliss synergy + hypoxia asymmetry + penetration gap = direction-robust-magnitude-not; NONE point-estimable). Writes analysis/identifiability-report.{md,json}; guarded by tests/test_identifiability_report.py. Manuscript Chapter 5 points to it. Directly addresses the 2026 review practical-identifiability critique
scripts/headline_sensitivity.py                per-headline Morris sensitivity (#331 PR2+PR3): screens which of the 11 PRCC rate constants drive a HEADLINE output the simulation binaries produce, perturbing them through the FERRO_PARAM_OVERRIDES hook (#331 PR1). Morris elementary effects (mu*/sigma) rather than Saltelli/Sobol because each binary run is far costlier than sim_batch, so a full variance design is infeasible; self-contained estimator (no SALib), analytically validated in tests; multi-headline (per-headline trajectory count: many for cheap sim-combo-mech, few for costly sim-tme; binary runs isolated in temp cwds for parallelism). (1) Bliss-synergy headline (RSL3+FSP1i synergy_score from sim-combo-mech): top drivers lp_propagation/gsh_scav_efficiency/gpx4_rate (the LP+GSH/GPX4 axis), sigma>mu* for every active param so the synergy is non-additive (interaction or single-param nonlinearity), structural zeros sdt_ros + rsl3_gpx4_inhib (no SDT in the pair; combo-mech bakes RSL3 GPX4-inhibition into a fixed DrugEffect) vs the empirical/below-resolution zero gpx4_degradation_by_ros. (2) Hypoxia kill-collapse headline (#331 PR3 — SDT-minus-RSL3 hypoxic-zone gap from sim-tme): sdt_ros is the single largest driver (SDT's hypoxic efficacy is primarily dose-driven, the model-side §7.1 confirmation), but the LP/GSH defenses (lp_rate/gsh_scav_efficiency/lp_propagation) also move the gap substantially because the SDT hypoxic kill (~0.87) is high-but-not-saturated, so SDT is dose-driven yet defense-MODULATED, not defense-independent (screen holds SDT O2-independent, the §7.1 upper bound). (3) Immune-amplification headline (#331 PR5 — SDT's POOL-DE-CONFOUNDED immune kill rate immune_kills/(total_tumor-ferroptosis_kills) at SDT/immune_on, screened from the SAME sim-tme runs as hypoxia via a multi-output Morris path): the raw immune_kills ratio MATCHES the headline (SDT>>RSL3 ~104:1 at default, the immune-coupling figure; an earlier "runs opposite" claim was a misread of override-contaminated output, corrected in PR4); the de-confounding controls for SDT clearing most cells by ferroptosis first and is bounded [0,1]. Top drivers lp_propagation/lp_rate (the LP-cascade), with sdt_ros close behind — the per-cell immune amplification rides on the SAME death switch (DAMP = ferroptotic-death density), NOT a separate immune axis; the informative CONTRAST with hypoxia (where sdt_ros led) is that SDT's hypoxic SURVIVAL is dose-driven but its per-cell immune AMPLIFICATION is death-density-driven. The immune-coupling params (DAMP diffusion, DC activation) are not in the PRCC biochemical set. All THREE headlines now screened, closing #331. Report: analysis/headline-sensitivity-report.md
scripts/uncertainty_intervals.py                prior-predictive uncertainty intervals on the kill switch (#332): uniform priors over the 11 PRCC rate-constant ranges (prcc-results.json) → Monte-Carlo (2000 draws) through `sim_batch` → 2.5/50/97.5 death-rate intervals per phenotype×treatment; the RSL3 rates span nearly [0,1] (Persister×RSL3 42.5% point — at the Figure 7 n=1M — but [1.6%,99.7%]) so they're least parameter-constrained, while the Glycolytic-RSL3 floor + Persister-SDT ceiling are robust (OXPHOS/Glycolytic-SDT wider); the NRF2-rescued persister stays resistant at point (0%) but its interval reaches ~38%; PRIOR-PREDICTIVE not a data-conditioned posterior (a true posterior needs the #330 GDSC data); analysis/uncertainty-intervals-report.md. The spatial/combination-headline extension has begun: scripts/headline_uncertainty.py
scripts/headline_uncertainty.py                 Bliss-synergy prior-predictive interval (#332 spatial extension): the same uniform PRCC priors propagated through the RSL3+FSP1i `synergy_score` from sim-combo-mech (reusing `headline_sensitivity.run_bliss` + the override hook), 300 Monte-Carlo draws → the Bliss headline (~1.99x point) spans a 95% prior-predictive interval ~[1.0x, 5.2x] (median ~1.35x), so the supra-additive DIRECTION is robust at the lower bound while the magnitude is not; PRIOR-PREDICTIVE (parameter, not structural; posterior blocked on #330); analysis/headline-uncertainty-report.md. `--headline sim-tme` extends it to the two spatial sim-tme headlines at a smaller 100-draw ensemble (sim-tme ~4 min/run): the hypoxia kill-collapse gap (SDT−RSL3 hypoxic kill) stays POSITIVE across its 95% interval ~[0.16, 1.00] (median 0.96, point 0.87) — collapse-asymmetry direction parameter-robust, magnitude wide — and SDT's de-confounded immune rate spans ~[0.009, 0.171] (median 0.042), robustly low-but-positive; reuses `run_sim_tme_observables`, defensive per-observable NaN partition (`_partition_tme`); analysis/headline-uncertainty-tme-report.md. `--headline penetration` completes the set via sim-tissue-pk (~4 s/run, the #331 override hook wired in off-by-default/byte-identical): the per-tissue vessel-wall RSL3 kill (the 40%→12.1%/2.6%/1.8% headline) carries very wide intervals at every tissue (well-vasc median 0.23, ~[0.00,0.93]; poorly-vasc 0.08, ~[0.00,0.83]; CNS/BBB 0.04, ~[0.00,0.77]) because the bistable RSL3 switch dominates, BUT the WITHIN-DRAW across-tissue ORDERING (well>=poorly>=CNS, same biochem applied at decreasing fixed vessel-wall concentration) held in 300/300 draws — penetration-gradient direction parameter-robust (data-backed per-draw, NOT inferred from the overlapping marginals), absolute magnitudes not; transport params held at fixed uncalibrated presets (their own ranges undocumented, not swept); analysis/headline-uncertainty-penetration-report.md. All four headline families now read as prior-predictive intervals; only the data-conditioned ABC posterior remains (blocked on #330)
scripts/mechanism_recall.py                    non-circular mechanism-tagging recall (#412): measures the MECHANISM keyword tagger's recall against expert-assigned MeSH descriptors as an INDEPENDENT reference label, removing the circularity that get_searchable_text would otherwise inject (MeSH is folded into the production matched text). Two guards: (1) re-runs the tagger on a LEAKAGE-FREE text (`get_searchable_text(..., include_metadata=False)` => title+abstract only, MeSH/disease/gene/drug annotations excluded, the load-bearing #412 fix in tag_articles.py), and (2) the headline uses only INDEPENDENT descriptors (a descriptor whose own string would trip the mechanism's keywords is classified LEAKY via the live `text_matches_keyword` matcher so it can never drift, and reported separately as near-tautological). Headline (independent pool >= N_MIN=30): 9 measurable mechanisms, 90.6% volume-weighted / 89.2% macro recall (ADC 97.5%, synthetic-lethality 94.9%, electrochemical 93.7%, metabolic 92.9%, car-t 90.6%, immunotherapy 89.2%, sonodynamic 87.9%, bispecific 86.1%, epigenetic 69.8%). Epigenetic started as the honest low (54.7%): a mix of a real keyword-coverage gap and reference-scope breadth of the lone independent "DNA Methylation" leaf. #418 closed the keyword-coverage half by adding precision-first terms ("epigenetic regulation/modification", "ezh2 inhibitor", "enhancer of zeste", "dnmt") to config.py, lifting epigenetic recall to 69.8% (the live leakage-free tagger; INDEX.jsonl stays frozen so the manuscript's 19-mechanism counts are unchanged, hence the report's production column still reads 54.7%). Mechanisms whose only discriminative MeSH leaf is keyword-tautological (crispr/nanoparticle/oncolytic-virus/microbiome/etc.) or have no descriptor (ttfields/CAP/cuproptosis/disulfidptosis/radioligand) are reported NOT-MeSH-measurable, never 0%. Distinct from the evidence-tier 55% recall; does not touch the 96% evidence precision. Offline/deterministic: analysis/mechanism-recall-report.md + analysis/mechanism-recall.json
scripts/atlas_discovery_headroom.py        ASKS WHETHER ANY SEED-SPECIFIC SIGNAL ADDS ANYTHING OVER A CANDIDATE-ONLY PRIOR, which the leaderboard structurally cannot: it compares rankers in ISOLATION, and losing alone is not the same as adding nothing (a signal can be weaker than a baseline and still be complementary). `popularity` is the ONLY ranking whose score is a function of the candidate alone -- `deg_c` does not vary with the seed -- so the published table already reads as a seed-BLIND score beating every seed-aware one. Blends each method's within-seed percentile into a degree-only benchmark (which reproduces the published popularity precision exactly, 15.5%) and tests the per-seed change through the same paired bootstrap, decision rule fixed in the docstring BEFORE the result, Holm-corrected. RESULT: NO METHOD ADDS MEASURABLE PRECISION -- three are indistinguishable from zero, abc and jaccard significantly HURT. So on this metric the candidate prior exhausts what is measurable, which makes 'the shipped ranker is bad' and 'the metric cannot see discovery' INDISTINGUISHABLE on this evidence: a statement about the evaluation, not a defence of any ranker. A 5-weight sweep confirms it is not an artifact of the mixture -- 2 of 25 cells are positive at all and NONE survives the decision rule (the largest, resource_alloc at w=0.1, is +0.060 hits of 20 with an interval spanning zero). THE SWEEP'S FIRST VERSION READ SIGNS RATHER THAN TESTING THEM and called those cells worth pursuing; every positive cell now goes through the same bootstrap, guarded. Runs offline in seconds off the committed dump
scripts/atlas_discovery_dump.py            dumps EVERY discovery candidate with its features and its outcome, so re-ranking questions are offline and instant instead of a multi-minute run over a 2.2GB graph. 289,217 rows over the same split/seeds/pools the precision table uses, ~6.5MB gzipped, committed. It RECOMPUTES the pool features (rank_all returns orderings, not the rows behind them) and therefore does not ask to be trusted: for every seed it asserts its own ranking reproduces rank_all's ordering EXACTLY for all seven methods, ties included, and refuses to write otherwise. Scoring top-20 from the dump reproduces every committed precision to machine precision -- which is how the STALE-BUILD problem surfaced: the committed evaluation had been computed on a graph with 1,406,960 pairs before the split against the current 1,565,428 (the disambiguation corrections it consumes had grown), so every ranker read ~2 points low. Regenerating moved abc 12.3->10.1 and popularity 17.2->15.5 and left the direction and the paired decision unchanged. Both artifacts now carry `pairs_before` as a BUILD FINGERPRINT, because split year and k are not one and a guard comparing only those passed while two builds were quoted side by side
scripts/atlas_discovery_degree_bias.py     MEASURES WHAT THE DISCOVERY RANKERS ACTUALLY SELECT (#ATLAS-LBD-DEGREE), turning the evaluation's hand-written "degree correction" column into a number. `L` = mean degree of a ranker's top-20 over the mean degree of the pool it drew from, so L=1 is degree-neutral and popularity is the ceiling by construction. Measured on the SAME split, seeds and pools the precisions use (not the current graph -- quoting a selectivity from one build beside a precision from another is what let comention_regression invert its own finding): popularity 7.4x, adamic_adar 5.6x, bridges 5.5x, resource_alloc 4.3x, abc 2.3x, random 1.0x (the control, calibrating the measure), jaccard 0.1x (over-corrects BELOW neutral). Rank correlation between L and precision@20 is 1.00 over ALL 7 methods on the current build. THE CONTRIBUTION IS THE MAGNITUDE, NOT THE OBSERVATION -- atlas-discovery-eval.md already said the harder the correction the worse it does, and the measured ordering CONFIRMS its hand-written one (bridges and adamic_adar swap, within 0.1x). What the magnitude adds: the methods span the whole range 0.1x-7.4x with precisions tracking almost perfectly, so there is no gap for a ranker that corrects substantially AND scores well. CONSEQUENCE FOR THE CONCLUSION NEXT DOOR: 'good candidate generator, bad ranker' -- the first half stands, the second does not follow, because on this metric a degree-correcting ranker and a bad ranker are indistinguishable. HARD LIMIT, established by two independent adversarial critiques: whether hub-selection is WRONG is NOT identifiable from this corpus at any sample size -- a latent importance drives degree, exposure and real biology together, and every available exposure measure is either the outcome through the same extractor or the treatment itself
scripts/atlas_discovery_eval.py                gives the literature-based-discovery layer the HIT RATE it never had, and the answer is a NEGATIVE RESULT about this repo's own work (#ATLAS-LBD-EVAL). Method = TIME SPLIT: rebuild the graph as it stood before year Y, rank the absent 2-hop candidates, and count a prediction as a hit if the literature FIRST asserts that pair in Y or later (so a hit is a new statement, not a rediscovery). THE COMPARISON THAT MATTERS is not ABC-vs-random (almost anything beats random on a clustered co-occurrence graph) but ABC vs ranking the SAME candidate set by popularity, so only the ordering differs. Result at 200 seeds, precision@20: abc 10.1%, popularity 15.5%, random 2.8% -- the shipped ranking is measurably WORSE than asking which candidate is already famous, paired-bootstrap decided (-1.09 hits of 20, 95% CI [-1.41,-0.79], abc behind on 93 of 200 seeds) and reproduced at split years 2015/2018/2021. THESE MAGNITUDES MOVED once (abc 12.3->10.1, popularity 17.2->15.5) when the artifact was regenerated on a graph 11% larger -- the disambiguation corrections it consumes had grown since -- so the artifact carries `pairs_before` as a BUILD FINGERPRINT now: split year and k are NOT one, and a downstream analysis quoted these precisions beside a quantity computed on the newer graph while a guard comparing only split and k passed. The DIRECTION and the paired decision were unchanged by the rebuild. WHAT SURVIVES: both rankings beat random ~6x, so the candidate SET (2-hop bridging) is genuinely informative -- atlas_discovery.py is a GOOD CANDIDATE GENERATOR and a BAD RANKER, and its docstring now carries this measurement because it previously claimed to correct for popularity. Caches the pmid->year map (2.2GB of census records) to corpus/atlas/records/.pmid-years.pkl so extra split years are nearly free
scripts/atlas_evidence_check.py                checks the evidence tagger against NLM's OWN publication types (#ATLAS-EVCHECK), an independent expert label over 3,659 frozen records where the gold set had only 100. TWO QUESTIONS THE GOLD SET IS TOO SMALL FOR, both answered better than the manuscript implies: (1) does it ever file a TRIAL as PRECLINICAL -- the most consequential error possible -- and the answer is 0 of 1,288; (2) is the untagged pile hiding evidence -- 6 of 1,963 untagged records are trials (0.3%). THIS REFRAMES THE 57.8% UNTAGGED, which reads as a hole in the corpus: 73.8% of it is REVIEWS/EDITORIALS/COMMENTS, literature that HAS no evidence tier because it reports other studies' evidence, and the v2 rebuild added an opinion-pub-type veto for exactly that reason -- so leaving them untagged is the tagger WORKING. The genuine missed-primary residue is 13.9% of records checked, not 57.8%. HONEST SCOPE LIMIT: publication types are NOT evidence tiers; NLM has no preclinical in-vivo-vs-in-vitro concept, so this validates the CLINICAL BOUNDARY and the UNTAGGED PILE only -- and the in-vivo/in-vitro split is exactly where the gold set found the tagger weakest, so that stays measured only there. MISSION.md + evidence-v2-corpus-delta.md now carry the reframing
scripts/generate_census_figures.py            THE CENSUS LAYER'S FIRST FIGURE (fig28), because the finding that bounds the widest set of manuscript claims had no picture. Panel A: how much of the full cancer census the frozen corpus captures PER MECHANISM, on a LOG axis (a linear one collapses everything under 10% into the axis) -- the spread is 213-FOLD, from epigenetic 0.20% to mrna-vaccine 41.86%, so every relative prevalence the manuscript computes over the corpus inherits that unevenness. Panel B: what that does to the central corpus claim -- pharmacological:physical is 9.1:1 by the manuscript's method and 17.6:1 on the census, because the corpus over-samples physical modalities 3.3x (4.53% vs 1.36% capture), so the claim SURVIVES and the manuscript UNDERSTATES its own case. THE CLASS DEFINITION IS LOAD-BEARING AND WAS CAUGHT IN-FLIGHT: computing "pharmacological" as everything-that-is-not-physical reports 12.5:1 -> 25.0:1 by sweeping in delivery platforms and genetic tools (nanoparticle, crispr, oncolytic-virus, mrna-vaccine) that are neither a drug class nor a physical modality; the committed analysis uses a CURATED 7-mechanism list, so `atlas_landscape.PHARMACOLOGICAL` was hoisted to module level and is IMPORTED rather than restated (a hand-written list beside the real one is exactly how the discrepancy arose). A mechanism with no census articles is EXCLUDED, not plotted as 0% -- 0/0 shown as zero would read as "never captured", a different claim. Separate script from generate_figures.py because it reads only committed atlas JSON and runs offline in seconds, while that one loads the whole corpus plus gitignored simulation outputs. 5 guards recompute the figure's numbers from the JSON and assert they still match `analysis/atlas-landscape.md`, so figure and prose cannot drift apart
analysis/census-findings.md                    THE ANSWER TO 'what did the census teach us' (#CENSUS-FINDINGS), ASSEMBLED BY scripts/census_findings.py from the committed JSON of every analysis so it cannot drift. Five things it established: (1) the frozen corpus is a 213-FOLD non-uniform sample (41.9% capture on mrna-vaccine, 0.20% on epigenetic), so any relative prevalence computed on it inherits that; (2) the manuscript's volume claim goes 9.1:1 -> 17.6:1, but THOSE ARE NOT ON COMPARABLE LABELS (the same articles under MeSH give 5.3:1, so labelling moves it 0.58x and census selection 3.3x) and whether that is an UNDERSTATEMENT depends on the restriction -- the three the sibling publishes are 6.43:1, 8.04:1 and 16.08:1, and only the first two fall below 9.1:1, so the inversion holds under two readings of three and the three STRADDLE 9.1:1 rather than settling it; the manuscript separately OVER-BROADENED its maturity claim (HIFU 7.10% clinical beats CAR-T 6.64%, so physical modalities are not a maturity class); (3) the thesis's sonodynamic leg sits on ~30 papers against 479 for the resistance leg; (4) the two planning docs name different keystone experiments; (5) HO-1/TP53/TFRC/KEAP1 are the top literature-weighted unmodelled mechanisms. AND A 'WHAT THE CENSUS DID NOT SUPPORT' SECTION, because a findings page that only lists wins is marketing: LBD does not work as built, the replication 'collapse' was my own censoring artifact, and the entity collisions reach 1.28% of relation rows rather than the 50.5% containment figure. Plus the integrity failures (3 broken module citations, 55.7% bogus news verifications, FSP1->spastic-paraplegia) with the observation that NONE were computational errors -- every one was a true statement describing something narrower than what it was used for
scripts/atlas_taxonomy_reach.py                MEASURES THE DENOMINATOR EVERY MECHANISM SHARE IS A SHARE OF (#730). The repo reports per-mechanism capture spanning 0.20%-41.86% and treats that 213-fold spread as the headline caveat; the spread is CONDITIONED ON HAVING A TAG and nobody had measured the tagger's field of view. The PUBLISHED 5.98% was the wrong instrument: it looped the low-level `text_matches_keyword` over title+abstract, while PRODUCTION calls `match_mechanisms`, which opens with a cancer-context gate and reads MeSH too. Measured on the same 1-in-20 sample: raw loop 5.98%, production-on-title+abstract 5.73%, PRODUCTION 6.80% (95% CI 6.70-6.91) -- the gate costs 0.25 points and the MeSH channel adds 1.08. A verifier's control shows 6.80% is not merely a lower bound: on the frozen corpus, blanking the gene/drug/disease annotation layer changes the tag count by zero. ALSO FIXED, and the mechanism is sharper than the audit claimed: BOTH ranked tables were ALPHABETICAL because `json.dumps(sort_keys=True)` reordered the stored dicts -- so `--render-only` produced a different document from the documented command, the committed .md was a second-generation render, and the default invocation would have silently rewritten it. The table headed "most common MeSH descriptors" OMITTED the most common one (`neoplasms`, 12.1%) and the mechanism table omitted `nanoparticle`, the SECOND-LARGEST. Now stored as ordered pairs, with a guard asserting render() is invariant to key order. Three more: the pub-type table summed to ~104% (no `break`, 8,399 records in several buckets) while presented as a breakdown; two SUMS were published as shares of overlapping sets and are measured UNIONS now; and "demographic check-tags excluded" also dropped STUDY-DESIGN descriptors (`cell line, tumor`, `retrospective studies`, `prognosis`) which are exactly what answers the section's question -- split and reported separately. Reports the keyword tagger and the MeSH leaf map SEPARATELY, because the map is PRECISION-FIRST BY DESIGN -- it drops bare `Immunotherapy` as non-discriminative so it can measure recall non-circularly -- and reading its 3.64% as a coverage failure would criticise a precision instrument for not being a coverage one. THE FALSIFIER CAME BACK AGAINST THE COMFORTABLE ANSWER: reviews and case reports are only 31.6% of the unlabelled mass (the 34.3% this entry used to quote summed two OVERLAPPING buckets), 63.4% is primary research, and `antineoplastic agents` + chemotherapy protocols sit on 11.1% of it as a UNION (11.4% was the sum of two overlapping rows) -- therapy papers the taxonomy has no name for, not literature outside its remit. Two self-inflicted errors caught here: a hand-rolled line parser missed 5 of 31 map descriptors (multi-line lists), and the first descriptor table read `humans 92%, female 45.6%` -- all true, all MeSH check-tags, all silent on subject
scripts/scope_audit.py                        COUNTS WHAT THE PROJECT'S WORK IS ACTUALLY ABOUT (#731), because the corpus is broad and the work is not and a reader could only tell by counting files. MEASURED 2026-08-17: 14 ferroptosis-biology analyses, 1 other therapy (radioligand), 103 method of 118; 8 of 8 preregistered predictions; and 29 of 32 engine modules MENTION ferroptosis or physical-ROS, 19 in code, only 15 in PRODUCTION code. The figures this entry carried until then -- 13/1/95 and `33 of 33` -- were stale and vacuous respectively. THAT GUARD CLAIM WAS FALSE and is retracted: the guards pinned the .md to the .json and the two went STALE TOGETHER -- nine analyses landed (this campaign's own), the shipped row said 13/1/95 against a live 14/1/103, the README carried the stale row on the front door, and every guard in the file passed (7 test functions, 17 assertions -- an earlier draft of this entry said fifteen guards, which was not checked). A real regenerate-and-diff gate exists now. THREE MORE DEFECTS, all confirmed by independent recomputation: `engine modules 33 of 33` was `f"{n} of {n}"` -- the same dict key on both sides, from a file count that never opened a file -- and the sentence it supported was FALSE, since `reaction_diffusion.rs`, `spheroid.rs` and `vasculature.rs` contain zero matches of this file's own regex (measured: 29 of 32 mention it anywhere, only 19 in non-comment code). The headline sentence was UNCONDITIONAL, printing "every falsifiable commitment ... concerns ferroptosis" even when fed a doctored table showing half the predictions otherwise; it is derived now. And the `1 other therapy` is a FILENAME MARKER: therapy matches on the stem only while ferroptosis matches stem OR body, so the count moves on a rename and an empty file named for a therapy is filed as one -- the body route gives 29 instead of 1, which is NOT the corrected number either (most are instrument analyses citing a therapy as an example), so the page now states both and says neither rule measures subject. Also: the 47.6% mechanism share was described as a share of TAGGED articles in BOTH this analysis and the README, and it is the share of the CORPUS -- three non-interchangeable denominators (47.6% of 4,830 records, 53.6% of 4,284 tagged, 34.4% of 6,678 tags), now derived from INDEX.jsonl rather than typed. TWO ERRORS IN THE AUDIT, BOTH FLATTERING, which is the direction an audit of one's own narrowness must never be wrong in: the matcher was `ferroptos`, which does not match "ferroptotic", so P5 counted as NON-ferroptosis; and classification tested vocabulary before subject, so census analyses using the FSP1 collision as their worked example were filed as ferroptosis BIOLOGY, inflating that bucket 13->39 and overstating the file's own argument 3x
scripts/atlas_ingest_sensitivity.py           WHAT THE MESH QUALIFIER AXIS ADDS (#722). `atlas_baseline.py:308` reads DescriptorName and never QualifierName -- zero hits repo-wide -- so the census carries one of MeSH's two axes, and `tests/test_atlas.py`'s parser fixture is built from DescriptorName only, making the one existing ingest test structurally incapable of failing on it. Measured on 8 randomly sampled shards (a PREFIX would sample only the oldest literature), 25,809 cancer articles, both arms seeing the SAME articles: qualifier-only gain surgery +14.0 pts, drug therapy +10.2, imaging +5.2, RADIOTHERAPY +2.4. THE CROSS-MODALITY ORDERING IS WITHDRAWN, AND THE CONTROL IS RUN RATHER THAN ARGUED -- the descriptor arm recomputed as every descriptor NLM puts under the node each modality names (families 29/21/582/192, fetched to corpus/atlas/mesh/tree-*.tsv). IT INVERTS: proxy arm surgery > drug therapy > imaging > radiotherapy, family arm DRUG THERAPY > surgery > radiotherapy > imaging, and the modality published as the sharpest case is the one whose proxy covers least of its own family (4 entries against 582; its descriptor recall goes 0.101 -> 0.798). Every gain stays positive so the LICENSING survives, but the arms disagree by up to 9.4 points. The per-shard counts are committed too, so the interval resamples the eight near-single-year blocks instead of treating 25,809 articles as independent draws: surgery 11.70-15.57, drug therapy 7.46-13.06, imaging 3.81-6.70, radiotherapy 1.78-3.28. AND THE ERA COVERAGE IS MEASURED TOO, from the committed records rather than a re-download: four decades holding 25.1% of the census contribute FOUR of the 25,809 sampled articles (1950s/60s/80s/90s), while the 1940s are over-represented 14.7x and the 2010s 2.4x. So 'sampled across the whole chronological range' was FALSE as written and is corrected -- and the sampling RATIONALE was wrong too: file order is only broadly chronological, the oldest literature sits in the MIDDLE of the range, and a prefix samples the mid-1970s rather than the oldest. The decision to spread stands on a different reason (a prefix is a contiguous block of near-single-year shards, so it samples one era whichever era that is). Original reason: each row's gain is measured against a hand-written descriptor proxy of 4-7 entries standing in for a MeSH family of tens to hundreds, while the qualifier arm is a WHOLE closed axis -- so ranking the rows ranks proxy incompleteness as much as the axis, and 'the sharpest case is surgery' was a statement about how short the surgery list is. The within-modality gains stand and the issue's premise stands; the ordering and the prioritisation drawn from it do not. Also withdrawn: 'and nowhere the ingest looks' (true only against those four descriptors, not against the ingest, which stores every DescriptorName), and 'it says which those are' (no materiality threshold appears on the page). A LABELLING gap, not a selection gap: membership is decided on descriptor UIs before any qualifier is read. Supersedes an earlier 2.5x text-vs-descriptor claim that used ASYMMETRIC RULES and failed a control -- angiogenesis, with no qualifier form at all, has descriptor recall of the SAME LOW ORDER as radiotherapy's -- three sites said LOWER, which the only figures the repo carries (5.9% against 2.7%) contradict, and those figures are computed nowhere here
scripts/atlas_untagged_partner.py             WHAT HAPPENS TO A MODALITY THE TAXONOMY CANNOT NAME (#723), and the answer is not that it goes missing. Radiotherapy is titled in 88 frozen-corpus articles and substantially discussed in 907 (18.8%); only 13 of the 88 are untagged and 75 carry ANOTHER modality's tag (immunotherapy 45, ttfields 11). Same for chemotherapy (140 of 166) and surgery (17 of 24), with IMMUNOTHERAPY THE DOMINANT ABSORBER IN ALL THREE -- so part of its 47.6% corpus share is combination papers whose other half is invisible. BOTH READINGS THIS PAGE CARRIED WERE WRONG, IN OPPOSITE DIRECTIONS. The first presented a structural certainty as an empirical finding. The second WITHDREW it against the corpus-wide tagged rate (88.7%) -- a denominator measuring a DIFFERENT EVENT, since for these three "carries a tag" IS "filed under another modality" while for the corpus it is overwhelmingly "carries its OWN tag". The EVENT-MATCHED control is a title-anchored set for a modality that HAS a lane: those are filed entirely under others 6.1% of the time (152 of 2,480) against 85.2/84.3/70.8% -- a factor of 12-14. So the claim is TRUE BY CONSTRUCTION (no lane exists, so any tag is necessarily another's), and what the page contributes is the size of the affected set and that contrast. The multi-tag argument is DROPPED as invalid: a paper about radiotherapy AND a checkpoint inhibitor can carry at most ONE modality tag, so the hypothesis predicts enrichment for exactly one, and the column cannot test it either way. VOCABULARY VINTAGE: the frozen INDEX predates #MECH-PRECISION, and re-running the PRODUCTION matcher on PRODUCTION text with only the vocabulary changed drops `bioelectric` (22, chemotherapy) and `frequency-therapy` (2, surgery) while every other partner is UNTOUCHED (immunotherapy 58->58, nanoparticle 27->27). An earlier version built title+abstract from INDEX.jsonl, WHICH HAS NO ABSTRACT FIELD, so it matched titles alone, then published three caveats -- one bolded -- claiming the frozen tagger read full text, which it also never did (`include_full_text=False`). Caught by its own guards: sort_keys reordered the partner-tag dict ALPHABETICALLY, reporting `antibody-drug-conjugate` as radiotherapy's dominant partner instead of `immunotherapy`
scripts/atlas_modality_ratio.py               DOES THE MANUSCRIPT'S CENTRAL CORPUS CLAIM SURVIVE A SYMMETRIC CLASS DEFINITION? (#724) Both curated classes omit their largest real member -- PHYSICAL has no radiotherapy, PHARMACOLOGICAL no cytotoxic chemotherapy -- and for the same reason: neither has a mechanism tag. Five partitions give 1.32:1 to 3.93:1 against the reported 17.6:1 -- BUT THAT SPREAD IS ONE UNDECLARED VARIABLE: how much operative SURGERY each admits to the physical class. Its share runs 0.0% to 62.8% and ranks PERFECTLY INVERSELY with the ratio (Spearman -1.00); held out, the spread collapses 2.99x -> 1.18x with every partition near 3.7:1. THE HOLD-OUT RULE IS ITSELF A JUDGEMENT, the same kind of thing the page just found, so it is not one regex: five rules disagreeing about NAMED descriptor sets all land between 1.18x and 1.23x. The stem list LEAKS `Radiosurgery`/`Cryosurgery`/`Electrosurgery`/`Ultrasonic Surgical Procedures` (ENERGY, not removal -- holding Radiosurgery out removes RADIOTHERAPY mass) plus 9 transplantation descriptors, MISSES `Reoperation`/`Castration`/`Curettage`/`Eye Enucleation`/`Pelvic Exenteration`, and 5 of its 17 stems match nothing at all; and the REPLACEMENT list is a judgement too -- 4 of its 10 are looser than operative removal (vertebroplasty injects cement, MeSH Castration covers the chemical kind, Orthopedic Procedures is an umbrella, Limb Salvage names a goal). THE CONTROL WAS WRONG TWICE BEFORE IT WORKED, and the sequence is the lesson. v1 matched removed mass PER PARTITION, which pins `phys - removed` everywhere so every held-out ratio is fixed before a descriptor is chosen -- no power at all, and its '15 of 20' was greedy-draw overshoot that moved between 9 and 18 across seeds. v2 withdrew it and leaned on the named families widening the spread as 'the part the mass argument cannot explain' -- ALSO WRONG: their masses are near-uniform (radiotherapy 86,328-131,818, energy 20,890-54,435), and spreading the same total EVENLY across all five, with no descriptors at all, already gives 5.58x of radiotherapy's 5.78x and 3.47x of energy's 3.96x. v3 is the control that discriminates, and it was cheap all along (seconds, off the incidence table the scan already builds): match the TOTAL, not the partition, so the ALLOCATION is free. 0 of 1,000 random descriptor sets removing surgery's total mass reach 1.18x (min 1.62). And the exact version -- reassign surgery's OWN five masses across the partitions, all 120 permutations -- gives 2 of 48 feasible, against 54 of 120 for radiotherapy and 120 of 120 for energy. Surgery's masses run 0 to 353,184, and that total CANNOT be spread evenly at all -- an equal share is 225,980 articles and `mechanism-of-action` holds fewer physical articles than that in total, which is itself the point. An intermediate version reported 3.15x there, silently computed over four of the five partitions receiving 80% of "the identical total" under a column header saying otherwise. So it is not the amount, it is that the amount lands where the partitions differ. The `pharm/(phys - articles that drop out)` identity is ALGEBRA not a measurement, and a first attempt to 'verify' it computed one expression twice and reported max error 0 over 25 cells (substituting a constant for `_remaining` still said `holds: True`); the invariant that CAN fail is checked instead. THREE PROVENANCE CLAUSES WITHDRAWN: "checked by an independent reviewer", "required to reproduce its own count", and "a single STATED principle" -- modality-partitions.json holds only two member lists, and carried a leaked section header counted as a physical member. THE SURGERY RECALL THIS PAGE LEANED ON WAS A PROPERTY OF A FOUR-ENTRY LIST: #722 now recomputes its descriptor arm as the whole MeSH family and surgery goes 0.101 -> 0.798, the largest move of the four, so the figure quoted as the reason the bias might run DOWN was an artifact of a list rather than a measurement of indexing. The conclusion is unchanged and is now stronger than 'not established': THE TWO ARMS POINT IN OPPOSITE DIRECTIONS. Correcting each class by its own recall multiplies the ratio by recall_physical/recall_pharmacological -- x0.19 on the proxy arm (sharply DOWN) and x1.24 on the family arm (UP) -- so the direction FLIPS with a choice about the descriptor list rather than merely lacking a measurement, which is why no correction is applied. Both columns ship side by side, and the guard requires the flip to be DERIVED and to stop being claimed if the arms ever agree. THE QUALIFIER CAVEAT, BOTH VERSIONS: the original "moves the ratio further DOWN" was invalid (it compared percentage POINTS), and its replacement was ALSO one-sided -- correcting by the drug-therapy/radiotherapy pair gives UP ~5%, while #722's OWN HEADLINE ROW, surgery at recall 0.101 against drug therapy's 0.547, points sharply DOWN and this physical class is up to 63% surgery. All four rows are reported now, from raw counts not the rounded percentages; NEITHER correction is licensed and the FLOOR argument is MEASURED rather than asserted -- a proxy's recall bounds a class's only where the class CONTAINS the proxy, true of the pharmacological side in 5 of 5 partitions and the physical side in 3 of 5 (counting only real DescriptorNames: #722's `neoplasms/surgery` is a descriptor/qualifier composite that can never be a class member). THE 17.6:1 COMPARATOR is 65% two mechanisms; restricting BOTH classes to atlas_landscape's PRECISE set gives 6.43:1, which is 1.63x ABOVE the held-out partition range -- an earlier draft hand-wrote "within the range" and it was FALSE. And "that script's own PRECISE set" has TWO readings: the page intersects it with the curated PHARMACOLOGICAL list, while atlas_landscape itself uses PRECISE MINUS PHYSICAL for its maturity table, which gives 8.04:1; both are reported. PRECISE was written for that script's MATURITY question, and two excluded mechanisms satisfy its own stated criterion (`immunotherapy` -> Immune Checkpoint Inhibitors, `synthetic-lethality` -> PARP Inhibitors); restoring them gives 16.08:1 instead. 6.43:1 is also BELOW the manuscript's own 9.1:1, which INVERTS the "manuscript understates its own case ~2x" claim carried in v1.md / atlas-landscape.md / census-findings.md / CLAUDE.md; the page states that arithmetic and leaves the four sites as an owner decision. THE DIRECTION SURVIVES AND THE MAGNITUDE DOES NOT: the order-of-magnitude gap is a property of a PHYSICAL class holding three tags while radiotherapy, brachytherapy, hyperthermia, ablation and phototherapy sit outside it. Partitions are the committed INPUT with ratios STRIPPED, and the loader's refusal is exercised on a doctored dict rather than grepped for
scripts/atlas_descriptor_recall.py           TWO MODALITY DESCRIPTORS, TWO DIFFERENT RECALLS, AND A RATIO THAT DIVIDED THEM. `manuscript-vs-census.md` reported the manuscript's Section 8.2 PDT:SDT ratio as 2.93:1, recomputed it at 4.75:1 on the census, and concluded the claim SURVIVES *UNDERSTATED BY THE MANUSCRIPT*. That verdict is WITHDRAWN. It divides two MeSH descriptor counts, and measured over the 13,346 ferroptosis-indexed census articles with ONE text rule applied to both arms, `Photochemotherapy` recalls 80.2% of ferroptosis-PDT papers while `Ultrasonic Therapy` recalls 46.0% of ferroptosis-SDT papers -- comparably PRECISE (96.1% / 90.6%), 1.74x apart in RECALL. So 4.75:1 is substantially a measurement of indexing practice. Symmetrically the ratio is 2.89:1 against the manuscript's 2.93:1 -- but BOTH ratios are loosely determined ([2.17,3.85] and [3.24,6.95]) and an earlier draft's "reproduces to within 1.4%" was withdrawn after review as ~20x more precise than the counts support (P(exceeds) about 0.46, a coin flip quoted to 3 s.f.). WHAT SURVIVES is stated on intervals: the manuscript's figure lies INSIDE the symmetric interval so a symmetric rule CANNOT DISTINGUISH census from manuscript (which withdraws the understatement verdict without establishing agreement), and the PAIRED inflation descriptor-over-symmetric is 1.64x CI [1.02,2.65], excluding 1 -- far better determined because both routes share their articles. WHY IT SURVIVED: the existing symmetry check tested PRECISION (90.8% vs 87.5% on-modality, genuinely symmetric) and never RECALL; and the four-variant sensitivity sweep varies DESCRIPTOR SETS, all of which inherit the same gap, so it structurally cannot detect a descriptor-versus-text recall problem. ALSO INVERTS A CAVEAT CARRIED IN NINE SITES (a 2-review sweep found 4 the first pass missed, incl. v1.md itself and tests/test_atlas.py, which PINNED the error) (`Ultrasonic Therapy` broader => SDT an over-estimate => ratios are LOWER bounds): the breadth is real but is 3 records of 32, the recall shortfall is 34, so the count is an UNDER-estimate and ratios against it are UPPER bounds. WHY THE TWO DESCRIPTORS DIFFER is its own finding: PDT has a dedicated MeSH descriptor and SDT has none, so it is indexed under a broader physical-modality term when indexed at all -- the [[atlas-untagged-partner]] mechanism (a modality with no name of its own is recorded under someone else's) appearing one layer up, in MeSH rather than in this project's taxonomy. Does NOT claim the manuscript is wrong (its figure is reproduced) nor that the text rule is truth (it has errors both ways; its value is that it is applied IDENTICALLY to both arms)
scripts/atlas_thesis_rank.py                  WHERE THE THESIS SITS AMONG ALL 166 MODALITIES, NOT THE SIX IT NAMES (#725). `atlas_thesis_position.py` enumerates a hand-written list, which cannot surface a leg nobody thought of. Ranked over the committed partition universe: antineoplastic agents 954, immunotherapy 370, cisplatin 251, photochemotherapy 152 (rank 6), ULTRASONIC THERAPY 32 (ranks 20-22 of 166 ranked, a 3-way tie), HIFU 1 (ranks 122-166, a 45-WAY TIE). A rank inside a tie is not determined by the data and the table says so now: removing one descriptor that matched NOTHING from the universe silently moved HIFU 160 -> 161, because `Counter.most_common()` preserves set-iteration order, so the ordering is fixed by (count, name) and the SPAN is published rather than one index of it -- an intermediate version printed '140 (tied with 44 others)', which is worse than the bug it replaced since it implies ranks 140-184 in a 166-row table. The .md also said 'each of 415 modality descriptors, ranked' while ranking 166 -- the other 249 have no intersection. The largest intersection is 29.8x the sonodynamic leg in RAW COUNT, so the unexamined precedent is chemotherapy not radiotherapy -- BUT THAT RATIO DIVIDES AN UMBRELLA PHARMACOLOGIC-ACTION DESCRIPTOR BY A SPECIFIC TECHNIQUE, and normalising each by its census-wide prevalence REVERSES the direction: `antineoplastic agents` sits on 353,001 census records and ferroptosis carries it at 0.89x its own base rate (DEPLETED), while `ultrasonic therapy` sits on 2,513 and ferroptosis is 4.20x ENRICHED in it. Absolute attention is larger; relative attention on THIS PAIR is not, and publishing only the raw ratio published the more flattering of two answers. SCOPED DELIBERATELY, because a reviewer showed the generalised form is FALSE: at CLASS level (the #724 maximally-inclusive partition) ferroptosis is 0.80x enriched in the pharmacological class against 0.20x in the physical one, i.e. 4x MORE enriched in pharmacological modalities, so `ultrasonic therapy` is an outlier inside a depleted class. Two further controls the first version omitted: ~26% of the enrichment is ERA (MeSH Ferroptosis is a 2020 descriptor; 100% of the subject's literature postdates 2019 against 23.8% of the census, and both arms restricted gives 3.09x not 4.20x -- this repo's own atlas_recent_window lesson unapplied), and the replacement measure carries a breadth dependence of the OPPOSITE sign (Spearman rho -0.38 between descriptor size and enrichment over the 166 ranked), so neither the raw ratio nor the normalised one is a clean measure and the pair must be read together. Withdraws this issue's own "~10x radiotherapy" claim, which reproduced under no descriptor set because it compared a wide text stem against a narrow descriptor. Attention, not endorsement. THE OVER-ESTIMATE CAVEAT IS WITHDRAWN AND INVERTED: `Ultrasonic Therapy` IS broader, but measured that is the smaller effect (precision 90.6%, 3 records of 32) against a recall of 46.0% (34 ferroptosis-SDT papers carry no such descriptor), so 32 is an UNDER-estimate by ~2x and 29.8x an UPPER bound -- see [[atlas-descriptor-recall]]; TTFields and CAP were named unmeasurable rather than shown as zero -- and the CAP HALF WAS FALSE: `Plasma Gases` carries 474 census records and 9 ferroptosis intersections, MORE than the HIFU leg this report does rank, so a hand-written no-descriptor list was refuted by the census it sits beside. Every such claim now names its candidate descriptor and is CHECKED. Also fixed: `counts.get(desc, 0)` printed 0 under a column headed COUNT for `drug resistance, neoplasm` (real intersection 479, published by the sibling artifact from the same build and called "the strong one") because it is legitimately outside a MODALITY universe -- absence and zero rendered identically
scripts/engine_time_audit.py                  WHAT ONE ENGINE STEP IS WORTH IN REAL TIME (#727). THE FIRST VERSION'S HEADLINE WAS FALSE AND IS RETRACTED HERE: it said "nothing states it, searched all 33 modules", and `tumor_pk.rs:354` says `Time points in minutes (one per simulation step)` inside the directory that scan already walked. The detector required the STEP-FIRST word order ("one step is N minutes") and a NUMBER before the unit, so the unit-first phrasing a step declaration is naturally written in, and a bare `Time step (min)`, were both invisible -- the instrument was structurally blind to the thing it existed to find, and the blindness shipped as an absence. EXACTLY ONE module prices a step in wall-clock time (`tumor_pk`, 1 min/step, reaching `sim-tumor-pk` + `sim-tme-3d` at 180 steps = 3.0 h), so the issue's "four conflicting bindings" was wrong AND the previous withdrawal ("there are NONE") was wrong -- there is one. A SECOND FALSE HEADLINE was caught by fresh-eyes review before merge and is also retracted here: the draft said the 3.0 h span meant "the model cannot represent either outcome of its most testable prediction". FALSE -- `cell.rs` carries the four P3 defenses' recovery half-times IN DAYS (gsh 1, gpx4 3, nrf2 5, fsp1 7), `sim-window` sweeps them to 28 days at P3's own timepoints, and fig26 is a published manuscript figure; the draft compared one binary's INNER kill assay against a prediction scored on the OUTER recovery axis, an asymmetric comparison. Worst part: the refutation was ALREADY in this script's own artifact (`orphan_timescale_fields["cell.rs"]` -> `sim-window`) and the renderer printed only the EMPTY caller lists, dropping its own counter-example -- the same failure it was retracting, one level up. Both sides are printed now. THE FINDING THAT REPLACES IT is better: P3 states "FSP1 and GSH first, GPX4 and NRF2 later" and the engine's defaults CONTRADICT it -- fsp1 is the SLOWEST at 7d against gpx4 at 3d -- a real scoreable disagreement visible only BECAUSE the model represents the quantity. Also corrected: `trigger_wave`'s `dt_min` is a CFL-constrained INTEGRATOR timestep, not a step binding, and pooling it manufactured a "50x disagreement"; "6 declarations" was 6 regex hits of 2 conventions (`\bdt_min\b` matches a field 3x); `_production_steps` selected the production binary BY TYPE ANNOTATION (`usize` only), so sim-tme-3d's `u32` count was never read; and A SECOND REVIEWER then found `TIME_UNIT` STILL required a number before the unit -- the same blindness ONE FUNCTION OVER from the one just fixed -- so `cell.rs` and `trigger_wave.rs` scored zero real-time mentions and were missing from the table headed "which modules carry real time" while the same page said cell.rs carries half-times in days. Also: sim-tme-3d's 3.0 h was a GREP artifact (its default matrix uses `DoseSchedule::Constant`; the PK solver is reachable only under `--dose-sweep`), now checked by TRANSITIVE reachability from main excluding `std::env::args()` guards and `#[cfg(test)]` -- a ONE-LEVEL check still got it wrong because the call sits one function inside the guarded one, and a doc comment naming the symbol counted as a call. Still retracted: "all composed into one 180-step loop". Does not choose or reconcile a duration. 22/24 mutations caught (13 of them the two reviewers' own, none reused), the 2 survivors being deliberate CONTROLS where a legitimate source change SHOULD move the report; running those controls is what exposed a guard of mine that had gone too BROAD (a bare-`s` rule matching `1.0 - s` in oxygen.rs's linear formula, failing on a valid tree). A real regenerate-and-diff freshness gate now exists, because every other guard compared the md to the json and the two go stale TOGETHER
scripts/engine_selectivity.py                 WHAT EACH MODALITY KILLS AND WHAT IT SPARES (#728), which the engine could always produce and never published while "toxicity" appears once in 48,900 manuscript words. Two properties nothing else records: SDT AND PDT ARE BIT-IDENTICAL across every phenotype (`sdt_ros` and `pdt_ros` both default to 5.0; they differ only in depth physics), so a single-cell contrast between them compares a modality with itself. And RSL3 KILLS EXACTLY ZERO of 20,000 non-tumour cells -- the `Stromal` parameters were chosen to encode ferroptosis resistance, so a ratio here RESTATES THE PROJECT'S SELECTIVITY ASSUMPTION RATHER THAN TESTING IT, THE FINGERPRINT READING IS WITHDRAWN: a control in the SAME ROW refutes it -- RSL3 also kills exactly zero `Glycolytic` cells, a TUMOUR phenotype -- so the exactness is a property of the RSL3 path against a resistant parameterisation, not evidence about how `Stromal` was chosen. What survives without it: the ratio is UNDEFINED at a zero denominator, and one computed against parameters encoding the assumption would restate it rather than test it. ALSO: the 14.7x/12.8x were single-seed points with no interval, and `sim_batch` draws cell i from `seed + 2i` and its sim-RNG from `seed + 2i + 1`, so a run consumes `seed .. seed+2n-1` -- EVEN offsets for cells, ODD for simulation. THE OVERLAP DEPENDS ON PARITY, which a first draft of this entry got wrong by saying any gap under 2n shares almost every cell: an EVEN gap d shares n - d/2 (so +2 differs in one cell of 20,000 and OFTEN, not always, returns the same count), while an ODD gap shares NONE and is already independent. Space by 2n regardless. Across 8 such seeds the ranges are 14.61-15.80x and 12.75-13.79x against single-seed points of 14.7/12.8 -- and the spread now EXCLUDES the point's own seed, because a first version drew it from a set the point was a member of. Also derived rather than asserted: `Stromal` is the most ferroptosis-resistant phenotype on 6 of 7 parameter axes (parsed from cell.rs), so the 'resistance is encoded in the parameters' half of the criticism STANDS -- only the inference from the exact zero is withdrawn. NOT a therapeutic index, which the issue's original title claimed: `Stromal` models CAFs -- tumour-resident, parameterised for shielding -- and converting them into a normal-tissue proxy is a category error
scripts/atlas_site_coverage.py                GATES THE BURDEN QUESTION BEFORE ANSWERING IT (#729). A literature-per-death ratio is only as good as the site assignment underneath it, and that had never been measured: 57.8% of the census is assignable to one of 18 major sites, spread 63,046 (thyroid) to 375,499 (breast), with 8.2% assigned to more than one. THE THREE THINGS IT USED TO SAY WITHOUT MEASURING THEM ARE NOW MEASURED, and each was wrong. (1) A row read `carry no MeSH at all | 0 | 0.0%` and COULD NOT HAVE READ ANYTHING ELSE -- atlas_baseline admits a record only on a DescriptorName match, so the branch is dead and it measured the ADMISSION RULE while reading as a property of the literature. Deleted; what is ACTUALLY excluded is stated instead: 783,271 MeSH-less text-matched cancer records in a second stream (over both, assignability is 49.1% not 57.8%) and 200,758 adjacent-basis records carrying NO C04 descriptor, which no C04 string can ever match (over the C04 core alone, 60.6%). (2) `a deep subtree walk would raise assignability, at the cost of a mapping nobody can audit` was asserted in FOUR places and computed in none. Measured: 57.8% -> 67.0%, +405,299 articles. And the audit half is false -- the repo already commits its entire cancer definition as a 704-descriptor TSV, so the resolved site map is committed too (`analysis/site-descriptor-map.tsv`, 233 descriptors, each row carrying the tree root that placed it). The real cost is a REVIEW burden, not an audit impossibility. (3) THE DEPTH WAS NEVER UNIFORM, which is the defect that actually matters because the per-site column is what a burden ratio divides into mortality: a 6-descriptor CAP produced ONE descriptor for stomach/ovary/bladder/thyroid and four for brain/CNS. THE FIRST FIX REPRODUCED THE DEFECT ONE FUNCTION OVER -- a hand-written per-site NAME regex that put `ganGLIOn cysts` and `paraganGLIOma` under brain/CNS, the benign salivary `adenoLYMPHOMA` under lymphoma, a lung disease under cervix/uterus, and merged 63,620 plasma-cell myeloma records into lymphoma, which MOVED THE HEADLINE RANK. There is no rule now: the deep list is every C04 descriptor at or beneath the tree nodes THE SHALLOW LIST ALREADY OCCUPIES, so NLM decides membership and the only judgement left is the shallow list, which was already published (tree numbers fetched into `corpus/atlas/mesh/c04-tree-numbers.tsv`; the SPARQL already bound `?t` and discarded it). Deep/shallow then runs head and neck 2.72x, leukaemia 2.04x, lymphoma 1.67x, brain/CNS 1.45x against prostate/stomach/oesophagus/bladder 1.00x, 13 of 18 sites change rank, and assignability is 57.8% -> 67.0% (+405,299, within 0.1 points of the audit's independently-derived 67.06%). AND THE TREE DOES NOT DRAW THIS PAGE'S BOUNDARIES: MeSH `Head and Neck Neoplasms` SUBSUMES oesophagus and thyroid, which are listed separately here, so the deep column double-counts across the page's own list and its ranks are partly a statement about MeSH -- derived and stated rather than shipped as a rank-1 headline. (4) THE REMAINDER WAS NARRATED: `much cancer literature is about biology, methods or cancer in general rather than a site` is wrong for a large share -- of the 1,857,050 unassigned, 405,299 (21.8%) are the SAME 18 sites at greater depth, 476,436 (25.7%) carry the generic `Neoplasms` descriptor, 200,758 (10.8%) carry no C04 descriptor at all, and the LARGEST bucket is the 41.7% that is none of those three -- its size is stated rather than left implicit, and its commonest descriptors are named. The exact multipliers are LIST-DEPENDENT, so the list is committed and both variants are published; a guard regenerates the map from the rules and diffs it, requires every deep list to CONTAIN its shallow one, and refuses any descriptor absent from the committed C04 file. Still deliberately computes no ratio, and a guard forbids mortality terms as IDENTIFIERS -- three attempts, because the word GLOBOCAN legitimately appears in the prose that disclaims using it
scripts/oxygen_form_check.py                  THE ENGINE'S O2 DEPENDENCE IS LINEAR AND RADIOBIOLOGY'S IS A HYPERBOLA (#726), which withdraws that issue's premise and strengthens its conclusion. `o2_dependent_exo_factor` is `(1-d) + d*s`; the Alper-Howard-Flanders OER is `(3p+3)/(p+3)`. At 7.5 mmHg the engine gives 0.125 against the OER's 0.810, a 6.5-fold underestimate, and at anoxia 0.000 against 0.333 -- a fully anoxic cell is still killed, about three times less efficiently. THE DISAGREEMENT IS LARGEST BELOW 10 mmHg, exactly where P4, the project's self-declared weakest leg, lives. So adopting the measured form is not a renaming but a real improvement to the parameter the project is least sure about, with eighty years of measurements as the calibration target the layer-freeze policy demands. Changes nothing: a functional-form change moves committed byte-identity gates, and a guard fails if the script writes into simulations/
scripts/atlas_recent_window.py                WHAT THE NEWEST LITERATURE DOES AND WHAT IT ONLY APPEARS TO DO. Of 597 MeSH descriptors rising significantly against the whole census, 193 (32.3%) are flat or lower against 2025 alone -- comparing a 2026 window to fifty years measures the ERA, not the topic. The share is not a constant and the report shows it: 8.4% against 2022, 32.3% against 2025, 44.9% against the partly-indexed 2026, so anyone quoting one number is quoting their comparator. Only 47 of the 193 are demonstrably FALLING, so the defensible verb is "no longer demonstrably rising". Also measures how the UN-INDEXED POOL resolves, and the first answer was wrong twice over. It said resolution 'collapses within ~2 years' and quoted 2026 38.0%, 2025 10.9%, 2024 0.777%, 2019 exactly 0 -- OMITTING THE ONE ROW THAT FALSIFIES IT, 2023 at 2.907%, printed directly beneath the sentence and 3.7x the two-year-old cohort. A second falsifier was hidden entirely by a 12-row truncation: 2001 at 2.312%, a 25-year-old cohort resolving 3x faster than the two-year-old one. So 'a cohort not indexed by then generally never will be' is WITHDRAWN -- resolution is concentrated in the two newest cohorts and is a few percent beyond them, but it is NOT monotone in age, and one update window bounds a per-window rate rather than a lifetime. The supporting sentence was also arithmetically impossible: '29 of the cohorts shown resolve at under 1%' when only 12 are shown (9 of those are), because the count ran over all 33 cohorts while the table printed rows[:12]; and its 'highest of them at 0.777%' was the MAXIMUM OF A SET SELECTED FOR BEING BELOW 1%, so it could not have exceeded 1% whatever the data did. Every cohort is committed now, so each count is checkable against the artifact

scripts/atlas_site_coverage.py                GATES THE BURDEN QUESTION BEFORE ANSWERING IT (#729). A literature-per-death ratio is only as good as the site assignment underneath it, and that had never been measured: 57.8% of the census is assignable to one of 18 major sites, spread 63,046 (thyroid) to 375,499 (breast), with 8.2% assigned to more than one. THE THREE THINGS IT USED TO SAY WITHOUT MEASURING THEM ARE NOW MEASURED, and each was wrong. (1) A row read `carry no MeSH at all | 0 | 0.0%` and COULD NOT HAVE READ ANYTHING ELSE -- atlas_baseline admits a record only on a DescriptorName match, so the branch is dead and it measured the ADMISSION RULE while reading as a property of the literature. Deleted; what is ACTUALLY excluded is stated instead: 783,271 MeSH-less text-matched cancer records in a second stream (over both, assignability is 49.1% not 57.8%) and 200,758 adjacent-basis records carrying NO C04 descriptor, which no C04 string can ever match (over the C04 core alone, 60.6%). (2) `a deep subtree walk would raise assignability, at the cost of a mapping nobody can audit` was asserted in FOUR places and computed in none. Measured: 57.8% -> 67.0%, +405,299 articles. And the audit half is false -- the repo already commits its entire cancer definition as a 704-descriptor TSV, so the resolved site map is committed too (`analysis/site-descriptor-map.tsv`, 233 placements over 225 distinct descriptors -- 8 sit in more than one of these sites -- each row carrying the tree root that placed it, and a guard now CHECKS that root places it, since falsifying 95 of the 233 left every test green). The real cost is a REVIEW burden, not an audit impossibility. (3) THE DEPTH WAS NEVER UNIFORM, which is the defect that actually matters because the per-site column is what a burden ratio divides into mortality: a 6-descriptor CAP produced ONE descriptor for stomach/ovary/bladder/thyroid and four for brain/CNS. THE FIRST FIX REPRODUCED THE DEFECT ONE FUNCTION OVER -- a hand-written per-site NAME regex that put `ganGLIOn cysts` and `paraganGLIOma` under brain/CNS, the benign salivary `adenoLYMPHOMA` under lymphoma, a lung disease under cervix/uterus, and merged 63,620 plasma-cell myeloma records into lymphoma, which MOVED THE HEADLINE RANK. There is no rule now: the deep list is every C04 descriptor at or beneath the tree nodes THE SHALLOW LIST ALREADY OCCUPIES, so NLM decides membership and the only judgement left is the shallow list, which was already published (tree numbers fetched into `corpus/atlas/mesh/c04-tree-numbers.tsv`; the SPARQL already bound `?t` and discarded it). Deep/shallow then runs head and neck 2.72x, leukaemia 2.04x, lymphoma 1.67x, brain/CNS 1.45x against prostate/stomach/oesophagus/bladder 1.00x, 13 of 18 sites change rank, and assignability is 57.8% -> 67.0% (+405,299, within 0.1 points of the audit's independently-derived 67.06%). AND THE TREE DOES NOT DRAW THIS PAGE'S BOUNDARIES: MeSH `Head and Neck Neoplasms` SUBSUMES oesophagus, thyroid, parathyroid, trachea and eyelid, so 53% of head-and-neck's deep gain is descriptors the same table counts as separate rows. STATING THAT WAS NOT ENOUGH -- it reached rank 1 entirely because of the disagreement, so a site whose subtree contains another of these sites now gets NO deep rank at all, only `n/a (subsumes ...)`. The subtree also carries what the page called only 'benign and precursor' entities and is in fact experimental models and veterinary disease too: leukaemia's includes three mouse cell lines, avian and bovine leukosis and feline leukaemia. THE FIRST ATTEMPT AT THAT SCOPE NOTE OVERREACHED IN THE OPPOSITE DIRECTION -- a `syndrome|disease` alternation made the page tell readers to strip `Hodgkin Disease`, which the shallow SITES list itself uses to DEFINE lymphoma, along with Sezary syndrome and Bowen's disease; 15 of the 25 flagged placements were human cancers. A guard now refuses any shallow-list member. And the deep-rank suppression was HALF-APPLIED: the merged site kept rank 1 and shifted every other site by one, so the bolded rank changes were partly artifacts of the rank the page had just declared meaningless -- `breast` rendered 1->2 while actually not moving, and `brain/CNS`'s real move was hidden. Ranked over the 17 rankable sites it is 11 that change, not 12 of 18. AND A LIVE DEFECT THIS REPO HAD ALREADY FIXED ONCE (#730): `sort_keys=True` reordered the stored ranking alphabetically, so `--render-only` regenerated a DIFFERENT report -- every rank cell a self-identity and the rank-change clause GONE. Ordered pairs now, with a guard asserting render() is invariant to key order. (4) THE REMAINDER WAS NARRATED: `much cancer literature is about biology, methods or cancer in general rather than a site` is wrong for a large share -- of the 1,857,050 unassigned, 405,299 (21.8%) are the SAME 18 sites at greater depth, 476,436 (25.7%) carry the generic `Neoplasms` descriptor, 200,758 (10.8%) carry no C04 descriptor at all, and the LARGEST bucket is the 41.7% that is none of those three -- and its 'commonest descriptors' pointed at a counter accumulated over ALL unassigned records, including the two buckets the same sentence had just excluded. Counted over the residue alone it is largely CANCER AT A SITE THE LIST DOES NOT COVER (bone 45,185, multiple myeloma 42,185, neuroblastoma 24,864), which is bullet one's correction one level out. The exact multipliers are LIST-DEPENDENT, so the list is committed and both variants are published; a guard regenerates the map from the rules and diffs it, requires every deep list to CONTAIN its shallow one, and refuses any descriptor absent from the committed C04 file. Still deliberately computes no ratio, and a guard forbids mortality terms as IDENTIFIERS -- three attempts, because the word GLOBOCAN legitimately appears in the prose that disclaims using it
scripts/oxygen_form_check.py                  THE ENGINE'S O2 DEPENDENCE IS LINEAR AND RADIOBIOLOGY'S IS A HYPERBOLA (#726), which withdraws that issue's premise and strengthens its conclusion. `o2_dependent_exo_factor` is `(1-d) + d*s`; the Alper-Howard-Flanders OER is `(3p+3)/(p+3)`. At 7.5 mmHg the engine gives 0.125 against the OER's 0.810, a 6.5-fold underestimate, and at anoxia 0.000 against 0.333 -- a fully anoxic cell is still killed, about three times less efficiently. THE DISAGREEMENT IS LARGEST BELOW 10 mmHg, exactly where P4, the project's self-declared weakest leg, lives. So adopting the measured form is not a renaming but a real improvement to the parameter the project is least sure about, with eighty years of measurements as the calibration target the layer-freeze policy demands. Changes nothing: a functional-form change moves committed byte-identity gates, and a guard fails if the script writes into simulations/
scripts/atlas_recent_window.py                WHAT THE NEWEST LITERATURE DOES AND WHAT IT ONLY APPEARS TO DO. Of 597 MeSH descriptors rising significantly against the whole census, 193 (32.3%) are flat or lower against 2025 alone -- comparing a 2026 window to fifty years measures the ERA, not the topic. The share is not a constant and the report shows it: 8.4% against 2022, 32.3% against 2025, 44.9% against the partly-indexed 2026, so anyone quoting one number is quoting their comparator. Only 47 of the 193 are demonstrably FALLING, so the defensible verb is "no longer demonstrably rising". Also measures how the UN-INDEXED POOL resolves, and the first answer was wrong twice over. It said resolution 'collapses within ~2 years' and quoted 2026 38.0%, 2025 10.9%, 2024 0.777%, 2019 exactly 0 -- OMITTING THE ONE ROW THAT FALSIFIES IT, 2023 at 2.907%, printed directly beneath the sentence and 3.7x the two-year-old cohort. A second exhibit surfaced by publishing every cohort, 2001 at 2.312%, is NOT the same kind of evidence and an intermediate version read it as if it were: essentially all of that cohort's resolutions come from ONE journal's back-file while essentially none of its other journals acquired anything, so as a cohort rate it says the opposite of what it appears to. The withdrawal is carried by the adjacent-pair inversion alone, and the honest generalisation is that late resolution arrives in lumpy per-journal batches, not that cohorts keep acquiring indexing at a low rate. So 'a cohort not indexed by then generally never will be' is WITHDRAWN -- resolution is concentrated in the two newest cohorts and is a few percent beyond them, but it is NOT monotone in age, and one update window bounds a per-window rate rather than a lifetime. The supporting sentence was also arithmetically impossible: '29 of the cohorts shown resolve at under 1%' when only 12 are shown (9 of those are), because the count ran over all 33 cohorts while the table printed rows[:12]; and its 'highest of them at 0.777%' was the MAXIMUM OF A SET SELECTED FOR BEING BELOW 1%, so it could not have exceeded 1% whatever the data did. Every cohort is committed now, so each count is checkable against the artifact
tests/test_generator_guards_pin_source.py     META-GUARD: a test that reads only a generated artifact CANNOT FAIL when its generator changes, because the artifact is committed and does not move until someone re-runs it. Found SIX times in one day, every time by a mutation sweep rather than the suite -- a hardcoded winner where a maximum was derived, a deleted check-tag filter, a refusal replaced with `if False:`, a dropped caveat, a ranking reverted to a sort_keys-reordered dict, a finding deleted from a renderer. BLOCKING for the analyses where the pattern was verified by mutation, deliberately NOT suite-wide: that flags dozens of files at roughly half precision, and this repo already learned from audit_heading_only_assertions.py that such a gate trains exemption-adding rather than fixing. The suite-wide sweep is advisory in scripts/audit_generator_guards.py
MISSION.md                                     the census mission and, more importantly, its stated limits (~21% of cancer articles are open-access full text and the rest is legally out of reach; scale without measured error rates is worse than a small clean corpus; a literature census generates hypotheses, not cures)
corpus/atlas/                                  the CENSUS surface, the third alongside the frozen corpus and the living review. Frozen = the immutable snapshot every manuscript number comes from; living = NEW records found after the freeze; atlas = the whole cancer literature. Bulk is gitignored; the small committed artifacts are the MeSH cancer definition and the reports
scripts/atlas_baseline.py                      ingests the census from the PubMed annual baseline (1,334 gzipped XML files, ~40M records). E-utilities CANNOT do this: esearch over neoplasms[mh] reports all 4,276,707 hits but paging caps at retstart 9,999 even with WebEnv history (verified -- retstart=10000 returns an error document). Cancer is defined as MeSH tree C04 (704 descriptors from NLM's SPARQL endpoint) UNION nine adjacent experimental-context descriptors, because a C04-only census misses foundational mechanism papers: BOTH founding FSP1 papers (Doll 2019, Bersuker 2019, Nature) are absent from C04, and 62% of ALL ferroptosis papers are outside it. Records matched only by the extension carry cancer_basis="adjacent" so the C04 core stays separable
scripts/atlas_unindexed.py                     recovers what MeSH has not indexed yet. The unindexed share climbs monotonically with recency (0.0% in 1975 files -> 37.6% by file 959), so a pure MeSH census loses exactly the recent literature. Text-matched into a SEPARATE stream tagged source="text-match"; the matcher's own accuracy is measured against MeSH truth on indexed articles: precision 75.7%, recall 95.6%
scripts/atlas_fulltext.py                      pulls the open-access full text via PMC's bulk tarballs (28 packages, not ~900k per-article fetches) to the NAS. Every record carries its licence class, since oa_comm and oa_noncomm differ on REDISTRIBUTION even though both are readable
scripts/atlas_relations.py                     ingests PubTator3 bulk: normalized entities (NCBI Gene, MeSH) and TYPED relations over a fixed predicate vocabulary. 10,700,928 relations over 2,129,080 cancer PMIDs
scripts/atlas_graph.py                         builds and queries the relation index (739,383 aliases, 214,199 identifiers, 2,888,766 entity pairs; ~2 min); those figures move whenever the relation layer is re-ingested, so read them with the build. QUERY BY IDENTIFIER, NOT SYMBOL -- see the entity audit
scripts/atlas_entity_audit.py                  checks the symbols this project queries against NCBI's own record. STALENESS AUDIT COMPLETE (all four graph-derived generators re-run after the disambiguation corrections grew 441->1,269): this one regenerates BYTE-IDENTICAL, so the FSP1 finding is intact. The audit's honest tally is that a last-commit-predates-the-input heuristic flagged 7 artifacts and only 2 had a real problem -- atlas-emergence-error genuinely stale, atlas-contradictions carrying an unrecorded total that had drifted ~50% -- while atlas-model-gaps and this one were fine. A timestamp is a prompt to check, not a finding; regenerate-and-diff is the test, and it is cheap. 1 mismatch in 53, and it is the worst possible one: PubTator3 maps 357 mentions of `FSP1` and 90 of "ferroptosis suppressor protein 1" to gene 51062 = ATL1 (atlastin GTPase 1). The real FSP1 is 84883 (AIFM2). That collision sits directly under the manuscript's headline GPX4+FSP1 Bliss-synergy claim. This script REPORTS it and does not fix it; atlas_ambiguity.py + atlas_disambiguate.py now do, and it calls resolve_majority so it can keep reporting the vote the blocklist refuses
scripts/atlas_model_gaps.py                    asks the LITERATURE which ferroptosis mechanisms to model next (#ATLAS-GAPS). ferroptosis-core has ~30 off-by-default layers, each added because the author read a paper -- a reading list, not a priority order. 13,346 census articles carry MeSH `Ferroptosis` and PubTator annotates their genes, so the field's attention is countable against engine coverage. TOP UNMODELLED: HO-1/HMOX1 (1,583), TP53 (1,497), TFRC (1,482), KEAP1 (768) -- all genuine ferroptosis regulators with no parameter handle. THREE CORRECTIONS WITHOUT WHICH THE LIST IS NONSENSE, all caught in-flight: (1) ORTHOLOGS -- ranked raw, GPX4 takes 3 of the top 11 slots as separate human/mouse/rat identifiers and 2 read as unmodelled; species ambiguity is the BENIGN class from atlas-ambiguity.md and a prevalence question wants them merged. (2) METHOD GENES -- GAPDH ranks 11th and beta-actin 17th as Western-blot LOADING CONTROLS, and caspase-3 is measured in ferroptosis papers precisely to show the death is NOT apoptotic; excluded by name WITH the reason recorded. (3) 'MODELLED' must be read from params.rs + the module list, NOT the 20 hand-written module CLAIMS -- comparing against CLAIMS reports NRF2 and NCOA4 as gaps when params.rs carries `nrf2_gsh_rate` and `ferritinophagy_release`. Ranks ATTENTION not importance; a gap is not a defect; and the layer-freeze policy still requires a named calibration target before any new axis lands
scripts/atlas_prediction_position.py           measures how much literature each PREREGISTERED PREDICTION sits on (#ATLAS-PREDPOS), because two planning documents name DIFFERENT primary experiments and neither designation cited evidence: PREREGISTRATION.md's Sequencing calls E1 (testing P4, hypoxia) 'the keystone' while analysis/p1-wetlab-protocol.md calls P1 'the highest-leverage falsifiable prediction in the program'. Ferroptosis-intersecting census articles per leg: P1/P3 (persister/drug-resistance) 479, P4 (hypoxia) 64, P5 (ICD) 41, P7 (pH) 37, P6 (CAF) 22, P2/P8 (spheroid size) 15. EXPLICITLY NOT A QUALITY RANKING -- a sparse leg is where novelty lives and P4 is deliberately the contested one, which is what preregistration is FOR. The load-bearing asymmetry is INTERPRETABILITY OF A NEGATIVE: P4 also depends on SDT (32 ferroptosis-intersecting articles total), so a negative P4 could mean the mechanism is wrong OR that nobody has worked out how to run the experiment; P1 rests on GPX4/FSP1 parallel independence, which is established, so a negative P1 is just a negative result. Recommendation is to KEEP BOTH and state the basis -- keystone-because-most-decisive and keystone-because-most-novel point at different experiments. PREREGISTRATION.md now carries the table
scripts/atlas_thesis_position.py               MEASURES WHERE THIS PROJECT'S OWN THESIS SITS IN THE LITERATURE (#ATLAS-THESIS), which the frozen corpus structurally could not: it contains NO ferroptosis query and NO photodynamic-therapy query, the two topics the simulation half is built on, so no amount of analysis of it could say how much work exists on the thesis. MeSH carries a `Ferroptosis` descriptor, so the intersections are countable across 4,403,994 census articles. THE FIELD: 13,346 ferroptosis-indexed cancer articles, growing 176 (2019) -> 4,496 (2025), ~25x in six years. THE LEGS: ferroptosis x drug-resistance 479, x photodynamic 177, x ultrasonic(SDT) **32**. So the RESISTANCE leg is supported by a literature and the SONODYNAMIC leg -- the thesis's own central mechanism -- by roughly thirty papers in the entire indexed cancer literature, with PDT (the cautionary precedent) 6x better established than the thing it is a precedent for. REPORTED IN BOTH DIRECTIONS: a thin intersection is NOT a refutation, it is what an under-explored direction looks like and is precisely the manuscript's claim -- but it is also the difference between 'few people have tried this' and 'this is well supported', so the honest framing is that the SDT leg is a hypothesis with a handful of papers, not a literature, and the simulation work carries more of the argument than the citation count suggests. The SDT count is an UNDER-estimate, not an over-estimate as this entry previously said: `Ultrasonic Therapy` is broader (precision 90.6%) but recalls only 46.0% of ferroptosis-SDT papers, so the breadth is 3 records and the shortfall is 34. v1.md carries the count
scripts/atlas_replication.py                   the FOURTH mining pillar in MISSION.md -- 'the replications that never happened' -- and the only one that could not be attempted on 4,830 articles (a claim looks unreplicated in a small sample whether or not anyone replicated it) (#ATLAS-REPL). For every entity pair: how many DISTINCT papers assert it and when it was first asserted; a pair asserted once, long enough ago that a follow-up would have appeared, is ORPHANED. 22.7% of 2,370,011 eligible pairs acquired a second paper within 5 years; 1,547,746 orphans, 8,916 touching this project's focus genes. THE METHODOLOGICAL TRAP, caught and fixed: scoring whether a cohort was EVER replicated gave a clean monotonic decline that looked like a collapse in cancer science and was pure CENSORING -- a 1950 pair has had 76 years to acquire a second paper and a 2021 pair 5. That series is now COMPUTED and stored (`cohorts_censored_ever`, 60.1% -> 13.4%) rather than remembered: it was two numbers written into prose from a development run that stored nothing, which census_findings.py then re-quoted, so neither document could check it. ITS HONEST LIMIT, established by proof rather than by reading: for the NEWEST complete cohort the two series are IDENTICAL by construction (a pair first asserted at latest-W can only be replicated inside its own window), so the whole contrast is carried by the OLD end. Scoring every cohort on the SAME 5-year window from its own first assertion flattens it to 27.5% (1985-99) -> 24.4% (2000-14) -> 17.1% (2017+), and the last figure is flagged an UPPER BOUND because MeSH lag hides recent second papers. LOAD-BEARING CAVEAT: this measures CO-ASSERTION not experimental replication -- a paper citing rather than testing counts as a replication (inflates), the pair key carries no direction so a CONTRADICTION counts as one, and a genuine replication using different entities is invisible. It bounds ATTENTION, not truth
scripts/atlas_landscape.py                     RECOMPUTES THE MANUSCRIPT'S CENTRAL CORPUS CLAIM AT CENSUS SCALE (#ATLAS-LANDSCAPE). The claim -- immunotherapy dominates, physical modalities comparatively under-studied -- is computed over 4,830 articles from 33 keyword queries, a design that cannot separate "under-studied" from "we queried it narrowly". Three-way design so METHOD and SCALE are separable: A frozen+keyword (what the manuscript reports), B frozen+MeSH (same articles, independent expert labels), C census 4,403,994+MeSH (same labels, articles this project did not select). THE LOAD-BEARING COMPARISON IS B vs C, because it holds descriptor scope constant -- a cross-mechanism RANK is only as good as its least precise descriptor and several are broad (75% of `epigenetic` comes from `DNA Methylation`, carried by any paper MEASURING methylation, so its census rank-1 is a SCOPE ARTIFACT not a discovery). CAPTURE (frozen share of census per mechanism) spans 0.20%-41.86%, a 213-FOLD spread: the frozen corpus is a wildly uneven sample, so any relative prevalence computed on it inherits that. RESULT: pharmacological:physical is 9.1:1 by the manuscript's method but 17.6:1 on the census, because the corpus OVER-SAMPLES physical modalities 3.3x (4.53% vs 1.36% capture) -- exactly what a corpus built from queries about them would do. So the claim SURVIVES and the manuscript UNDERSTATES its own case by ~2x; v1.md now carries the recomputation. Two bugs caught in this script before trusting it: MeSH read from INDEX.jsonl (which has NO MeSH field) gave a silent all-zero column that read as a finding, and a case mismatch zeroed mRNA-vaccine
scripts/atlas_pipeline.sh                      THE ATLAS PIPELINE IN DEPENDENCY ORDER, because the order is load-bearing and running it wrong FAILS SILENTLY. The chain that built the census lived in a gitignored log dir and covered only baseline/coverage/fulltext/relations/graph; every quality layer since had to be run by hand. The dependencies: `atlas_relations` REBUILDS THE ENTITY FILES, which invalidates the ambiguity scan; `atlas_ambiguity` produces the blocklist `atlas_disambiguate` + `atlas_comention` consume; `atlas_disambiguate` produces the per-paper corrections `atlas_graph --build` APPLIES, so the graph must be rebuilt AFTER it, not before. Build the graph first and you get an index carrying stale corrections that looks completely normal. Four phases: `census` (baseline->relations, hours), `quality` (ambiguity->domain-sense->disambiguate->graph->impact->co-mention --rebuild), `mine` (the analyses reading the graph), `audit` (citation + news integrity). Two tests pin the ordering invariants. NOT a cron job -- several steps take hours and `verify_news_claims.py` is DELIBERATELY excluded from the audit phase because re-running it rewrites 44 claim statuses + credibility scores, a data change to review on its own rather than a pipeline side effect
scripts/atlas_ambiguity.py                     measures how many OTHER entities collide like FSP1 did, across ALL THREE entity types, and stops the graph guessing (#ATLAS-AMBIG). `atlas_graph.build_index` resolves a surface form by MAJORITY VOTE (`alias[s].most_common(1)`), always returning exactly one identifier with no signal that anything was discarded, so a wrong answer is indistinguishable from a right one. GENES ARE THE OUTLIER by ~10x: 27.7% of gene mentions sit on a contested surface form vs 2.0% chemical / 2.7% disease, and the reason is structural (MeSH is a curated vocabulary with one preferred term per concept; NCBI itself lists `FSP1` as an official alias of THREE genes). That contested share is NOT an error rate -- contested forms split THREE ways: species ambiguity (human/mouse GAPDH, benign), hierarchical granularity (Glioblastoma nests under Glioma in the MeSH tree: lossy, not wrong), and genuine SENSE COLLISION (damaging). Pooling them overstates the damage several-fold. The damaging class hits the field's most-queried concepts: `ER`->EREG (epiregulin) not ESR1 across 54,293 mentions, `COX-2`->mitochondrial cytochrome c oxidase not PTGS2, `p21`->the H3P16 PSEUDOGENE not CDKN1A, `PC`->Prostatic Neoplasms vs Pyruvate Carboxylase Deficiency vs Pancreatic Neoplasms, and `NO`->NOBELIUM not nitric oxide. 346-entry blocklist; `atlas_graph.resolve` returns None rather than a plausible-looking wrong entity so an analysis fails loudly, `resolve_majority` is the explicit unblocked path for the entity audit (whose job is to REPORT the vote), and `DOMAIN_SENSE` carries opt-in cancer-domain senses each with the reason it is safe -- never applied silently. Volume rank alone is insufficient (FSP1 ranks 2181st), so the symbols this project queries are ALWAYS checked. HONEST LIMIT: MeSH Supplementary Concept Records (C-prefixed) have no tree number, so 285 chemical / 136 disease forms are UNRESOLVED rather than guessed, and the MeSH benign-vs-sense split rests on ~a quarter of the forms; the contested RATE (the genes-are-the-outlier headline) is computed over every form and does not have this problem. `--render-only` rebuilds the report from the committed JSON (the scan is ~40 min, almost all sequential MeSH lookups)
scripts/atlas_ambiguity_impact.py              answers the question a READER of the graph has -- how much of it should I discount? (#ATLAS-AMBIG). The obvious answer is wrong by ~38x: 50.5% of relation rows touch a contested identifier, which sounds severe and measures CONTAINMENT not error (ESR1 is contested because the string `ER` is ambiguous, but most ESR1 edges come from papers that wrote `estrogen receptor` in full, where nothing was ever in doubt). The measurement that means something asks, per PAPER and per IDENTIFIER, whether an ambiguous surface form was the ONLY route to that assignment: a paper writing both `ER` and `estrogen receptor` corroborates itself and the collision cost nothing. Result: of 18,440,811 contested-identifier assignments, 93.0% never touch an ambiguous form, 3.1% are corroborated, and 3.9% (726,186) are AT RISK; **1.28% of the 10,700,928 relation rows rest on an at-risk assignment**. It is an UPPER BOUND not an estimate (the vote is sometimes right -- PSA->KLK3 already correct). THE PRACTICAL LESSON is the asymmetry: diffuse damage is ~1% and below the extractor's own ~79.6 F1 error, but damage to a query about a SPECIFIC ambiguous entity can approach 100%, as it did for GPX4-FSP1 (zero correct typed relations before the fix). Both numbers are always reported together and a test enforces it
scripts/atlas_domain_sense.py                  MEASURES the curated `atlas_ambiguity.DOMAIN_SENSE` claims instead of asserting them (#ATLAS-AMBIG). Those five entries were written from domain knowledge, and ONE WAS WRONG: the `psa` note claimed the majority vote returns NPEPPS when it returns KLK3 (the correct answer), and the repo's OWN committed scan already said so -- a curated claim nothing checks is exactly the assertion this project is supposed to avoid. Method: sample cancer papers carrying the symbol as a gene annotation and count how many DECLARE each candidate sense in their own words ("estrogen receptor (ER)"); papers declaring zero or several are excluded, not guessed. Result: 5/5 curated senses confirmed dominant (er->ESR1 98.7% of 451 declaring with epiregulin declared ZERO times; cox-2->PTGS2 541/541; psa->KLK3 528/528; p62->SQSTM1 96.2%; p21->CDKN1A 89.6%, the lowest because RAS p21 + a histone pseudogene are real minority senses), and the MAJORITY VOTE IS WRONG IN 4 OF 5 -- not merely noisy but wrong in a consistent direction, picking the sense the cancer literature essentially never means. KEY INTERPRETIVE POINT: this makes FSP1 the UNUSUAL case, not the typical one -- its senses are genuinely balanced (110 AIFM2 vs 132 S100A4) so it needs the per-paper classifier, while these five have a defensible curated default. Every DOMAIN_SENSE justification now carries its measured share; committed analysis/atlas-domain-sense-validation.{md,json}, guarded by 4 tests incl. one asserting every curated sense is validated and vice versa
scripts/atlas_disambiguate.py                  resolves the FSP1 sense collision PER PAPER (#ATLAS-AMBIG). `FSP1` is an official NCBI alias for THREE human genes -- AIFM2 84883 (Ferroptosis Suppressor Protein 1), S100A4 6275 (Fibroblast-Specific Protein 1), ATL1 51062 (Familial Spastic Paraplegia 1) -- so it has no single right answer and the blanket remap to AIFM2 would CORRUPT the S100A4 papers, which are the plurality. Gold set = the 242 of 875 papers that DECLARE their expansion (a label independent of anything the classifier reads). On it PubTator3 is 34.2% accurate (95% CI 40.8-53.6%) and the error shape is the finding: of 110 papers spelling out `ferroptosis suppressor protein 1`, NOT ONE resolves to AIFM2 (99 go to ATL1) and 76 (69%) carry no correct AIFM2 edge from any mention, so the ferroptosis biology is absent, not merely mislabelled. The contextual layer reaches 96.2% (95% CI 94.4-98.8%, abstaining on 13). The load-bearing design decision is ANTI-LEAKAGE: the gold label IS the expansion phrase, so every label-defining phrase is masked out before a single feature is read (`ferroptosis` counts only OUTSIDE `ferroptosis suppressor protein 1`); without it the number is circular. 1,269 corrections; the payoff lands on the manuscript's own headline -- GPX4+AIFM2 co-mention papers go 98 -> 257 (+159, +162%), i.e. the collision was hiding 62% of the census's evidence for the GPX4+FSP1 Bliss-synergy claim
scripts/build_label_source.py                 THE OFFLINE AUTHORITY-NAME TABLE the co-mention work needed and did not have (#628): identifier -> the names NLM and NCBI give it, committed as `analysis/comention/authority-labels.tsv.gz` (29,195 identifiers, 112,237 names, 501 KB) so CI and every downstream check read a FILE rather than a live API, matching the CTRPv2/cBioPortal offline contract. MeSH labels AND ENTRY TERMS from NLM's SPARQL endpoint (entry terms matter enormously: MeSH says `Breast Neoplasms` where papers say `breast cancer`, and without them the authority rule leaves 260 of 670 cancer descriptors unreachable); gene symbol + description + aliases from NCBI esummary, so `xCT` resolves as a name of SLC7A11 and `PHGPx` of GPX4. Also covers the top 3,000 identifiers the alias map CANNOT reach -- without them the table can only describe entities the layer already finds, which makes "is this entity unreachable by its own name?" unanswerable by construction. TWO ENDPOINT TRAPS: dumping every MeSH concept of a class and paginating by offset returns HTTP 500 (an unfiltered ORDER BY over ~30k rows), so it asks for the identifiers it needs via a VALUES block; and a 400-id gene batch overflows the request URI, silently losing 475 genes to a 414 reported only as "unresolved"
scripts/comention_name_check.py               ASKS WHETHER EACH ALIAS FORM IS A NAME OF WHAT IT RESOLVES TO, at corpus scale (#628). Only 54.0% of the layer's census mentions sit on a form that is a name of its identifier; the FORM-level figure is 59.6% and the gap runs the unfavourable way, because non-name forms are used MORE (generic English words are common words). THE DISCRIMINATOR AND ITS BOUNDS: requiring the form to be an authority name removes ~100% of span-bearing false positives at 100% kept precision, but ONLY for MeSH -- gene symbols are already specific strings, so the gene subset is 75% precise BEFORE filtering against 29% for MeSH, and on genes the rule removes no false positives while still costing true ones. TWO COSTS THE FIRST VERSION MISSED, both found by a 9-agent pre-flight before a 3-hour rebuild: (1) it scored canon-form-OR-matched-span, but a filter inside `build_alias_map` sees only the FORM, where the true-positive cost is 54.3% not 39%; (2) the strict comparison deletes 75.3% of MeSH tree C04's census mass and leaves 260 cancer descriptors unreachable -- `Neoplasms`, `Breast Neoplasms`, `Lung Neoplasms` among them. Normalising (plural stemming + cancer/tumour/carcinoma -> neoplasm + stopwords, since MeSH stores the INVERTED `Cancer of Breast`) takes C04 retention 24.7% -> 73.2% and the TP cost 54.3% -> 34.8% at unchanged precision. THE ORDER OF MEASUREMENT MATTERED: stopwords measured BEFORE entry terms were worth 1.0 point and looked not worth having; measured after, 3.3 -- a normalisation is only as good as the reference it compares against. Also reports why 91% of annotated identifiers are unreachable, and finds that class near-benign (57.5% their name is never used as a form, 37.7% an ortholog holds it, 4.8% "a different entity" which is mostly MeSH-vs-gene redundancy like Prostate-Specific Antigen -> KLK3); the `Apoptosis` lockout (every apoptosis co-mention filed under Malformations of Cortical Development) is an OUTLIER, not a symptom
scripts/comention_judge_prep.py               makes the co-mention judging sample REPRODUCIBLE (#628). The 140 verdicts behind the precision measurement came from a seed and a span-recovery step that lived only in one session's shell history -- the verdicts were committed, the scaffold they were judged on was not, so nobody could re-derive them or draw a comparable sample. Samples deterministically from a NAMED seed, attaches the two things a verdict cannot be checked without (the span that actually fired, recovered with the build's own longest-match-with-consumption matcher rather than an n-gram scan; and the NLM/NCBI label, since the index's `canon` is the most frequent surface form and therefore circular), and takes `--exclude` so a HELD-OUT draw is disjoint by construction -- which is how #628's criterion 2 was answered. Sorts the pool before shuffling, because the dump is written in shard-traversal order and an unsorted shuffle would make the draw depend on how the file was produced rather than on the seed. Emits an empty `verdict` column; nothing in it assigns a verdict
scripts/comention_regression.py               BOTH ENDS OF THE COMPARISON ARE NOW PINNED, after a latent bug that would have destroyed this finding: the BEFORE side was a constant but the AFTER side was read live from `atlas-comention-audit.json`, which was correct exactly until that artifact was regenerated for a DIFFERENT build -- which happened when the authority filter shipped (#646). From then on, re-running this generator recomputed the HISTORICAL #617 regression against the FILTERED layer and reported precision RISING 10.9 points where the finding is that it FELL 9. Nothing failed and nothing warned; the document rewrote its own conclusion and would have shipped on the next regeneration. Found by accident while regenerating for an unrelated reason. The post-#617 counts were recovered from the audit as committed at ed6b10ec (#633), the generator now prints a note when the live artifact describes a different build, and `tests/test_regression_doc_is_historical.py` pins the property. MEASURES WHETHER THIS REPO'S OWN #617 FIX WORKED, AND THE ANSWER IS NO (#ATLAS-COMENT-REG). #617 found the full-text co-mention layer at 55.5% precision, traced it to `usable_alias` exempting every MULTI-WORD form from the specificity test it applied to single tokens (132 of 152 measured false positives were multi-word), and fixed it by DELETING the single-token shape proxy for two measured filters (census support + share of an identifier's mentions). A fresh uniform sample after the rebuild says precision FELL ~9 points, 50.6% -> 41.6%. THE MECHANISM: the layer emits 1.33x more mentions per sentence, PubTator corroborates fewer (44.3% -> 32.5%), and the ABSTRACT-VISIBLE stratum nearly TRIPLED (8.2% -> 23.3%). ALL THREE STRATA ARE NOW HAND-JUDGED at the IDENTIFIER level (does the sentence discuss what the identifier DENOTES, not merely contain the matched string): corroborated 90.0% (36/40, the 92.5% assumption holds), body-only 20.0% (8/40, the 30.8% assumption was optimistic by a third and it carries the most volume), abstract-visible 15.0% (9/60). RETUNING WILL NOT FIX IT: over the judged sample the two replacement filters do not separate true from false at all (support 77-355,572 for FPs vs 297-100,758 for TPs), so it is a design failure not a threshold. FOUR ERRORS FOUND IN THIS WORK'S OWN MEASUREMENTS, each by checking rather than assuming: (1) the `share` filter divided a form's cross-sense total by one identifier's total, so it exceeded 1 (274% for `as`) and was biased to ADMIT ambiguous generic words -- fixed via `alias_ident_support`, which measurably does NOT rescue the filter (cuts 0 of 37 FPs); (2) the first hand-judging asked whether the sentence contained the matched STRING, so `apoptosis`->Malformations of Cortical Development and `node`->Sick Sinus Syndrome scored correct; (3) the audit sample recorded `canon[identifier]`, the corpus-wide dominant form, and NEVER the span that fired, so the real keys are `left`, `primary`, `activity`, `reaction` -- the build now records `matched_spans`; (4) the report asserted both that body-only was "the layer doing the job it exists for" AND its retraction, three screens apart. THE TRANSFERABLE LESSON: the #617 justification was true about the sample it was measured on and wrong about the population it was applied to -- the FPs were multi-word BECAUSE that channel was unfiltered, so closing it moved the pressure to the channel just opened. A filter justified by an error distribution it then CHANGES must be re-measured. A DISCRIMINATOR THAT DOES SEPARATE (#628): requiring an identifier's dominant corpus name to equal its NLM authority label removes 96% of FPs against 60% of TPs -- but it is an IDENTIFIER-LEVEL whitelist not a per-mention filter, it removes EVERY gene (NCBI Gene ids have no MeSH descriptor), and it was selected on the same sample, so it is not a recommendation. THE FILTER IS NOW IMPLEMENTED, off by default behind `FERRO_COMENTION_AUTHORITY=1` (evidence-tagger-v2 precedent): MeSH-only, normalised, applied AFTER the blocklist and the DOMAIN_SENSE redirect so a curated correction is never undone (all five survive anyway, since they target genes), and FAIL-OPEN -- with no label table it warns and passes everything through, because silently emptying the MeSH half of the map would be far worse than doing nothing. Measured on the alias map: forms 44,287 -> 32,842, MeSH forms halved, non-MeSH unchanged, mentions 62.1M -> 47.1M. The rebuild is what settles pair-level numbers and a sample of the NEW layer to judge. The reference-list confound is measured and does NOT move the headline (41.1% prose-only vs 41.6% reported), and it runs the opposite way to the worry -- structural text is MORE precise in the corroborated stratum, since a reference title naming two entities usually names them correctly. PROMOTED TO ON BY DEFAULT (#651; `FERRO_COMENTION_AUTHORITY=0` disables it, and the guard checks the hatch WORKS rather than assuming). THE MEASUREMENT THAT DECIDED IT WAS NOT PRECISION: the layer's purpose is RECALL (the complement to the abstract-level graph) and the filter costs 27.6% of true positives, so halving it would be a poor trade -- except the recall it actually SUPPLIES goes UP. All 20 module-support pairs are flat or higher, 32,886 -> 34,058 co-mentions, because removing a MeSH alias frees the tokens it consumed and a gene alias matches there; the only reader is 19/20 gene-gene so the whole recall cost falls on uses that do not exist. FINAL NUMBERS: precision 41.6% -> 88.0% on the filtered layer's own sample and 88.4% from a BLIND PANEL of three judges who never saw the first verdicts (hostile borderline bound 80.1%); recall cost 34.8% on MeSH and 0% on genes; pairs 15,482,176 -> 8,265,855; clean A/B loses 21 gene-gene pairs of 3.3M. THREE PROCESS LESSONS WORTH MORE THAN THE NUMBERS: (1) a 9-agent PRE-FLIGHT before the 3-hour rebuild STOPPED the version I had recommended -- the strict comparison deletes 75% of MeSH tree C04's census mass, leaving `Neoplasms`/`Breast Neoplasms`/`Lung Neoplasms` unreachable, which precision-on-samples cannot see; (2) the first before/after was CONFOUNDED because the graph index was rebuilt between builds, making a 40,050-pair gene-gene loss look like the filter's doing when the clean A/B shows 21 -- always READ each build's alias-map size from its own log rather than assuming; (3) the blind panel found EIGHT errors in my judging, five generous and three harsh, which nearly cancelled -- a judge can be wrong 8 times in 90 and still land within a point if the errors are symmetric, which is a fact about the judging rather than a defence of it. TWO CRITERION HOLES the panel found, for anyone re-judging: hypernym-vs-fragment is not tiebroken when the containing term IS an instance of the class (`folinic acid`), and the salt-suffix case was decided inconsistently between `sodium deoxycholate` and `parecoxib sodium`. No manuscript FIGURE depends on this layer, but Section 8.4 now leans on its OUTPUT: the cross-measure check that stops the module-support analysis naming which uncorroborated claims are genuine runs on this layer's co-mention counts, so a rebuild that moved them could move that caveat
scripts/atlas_retraction_exposure.py        BOUNDS HOW MUCH OF THE CANCER RELATION GRAPH RESTS ON RETRACTED WORK -- the first census analysis whose subject is the literature rather than this project. Retracted papers stay indexed and machine-extracted graphs keep the assertions they contributed, so anyone building on a PubTator-derived graph inherits them silently. PubMed's `Retracted Publication` type was stored per record by atlas_baseline.py all along and NOTHING read it. Measured: 6,796 retracted of 4,403,994 census records (0.154%); 40,499 of 10,700,928 relation rows (0.385%); 32,294 of 2,902,827 entity pairs TOUCHED (1.131%); and 7,373 pairs whose ONLY support is retracted (0.258%). Reports containment and damage separately because this repo has already been burned by the difference -- atlas_ambiguity_impact found 50.5% containment against 1.28% at risk, a 38x gap; here it is 4.4x. THE DENOMINATOR THAT MATTERS: 70.2% of all pairs rest on a SINGLE paper, which is the population a retraction can strand, so the rate against that denominator is 0.368% -- and that 70% is the more alarming figure and has nothing to do with retraction, it is what a machine-extracted relation graph looks like. `inhibit` carries 1.8x the tainted share of `associate`, offered as a hypothesis not a result (small counts, no control for field/year/journal). Every count is a LOWER bound (retraction indexing lags) and bounds withdrawn SUPPORT, not truth. The retraction-family types are DISCOVERED by pattern rather than named, because the first version guessed `Retraction of Publication` -- a string PubMed does not use -- and reported zero notices across 4.4M records, which reads as a finding; the real type is `Retraction Notice`. analysis/atlas-retraction-exposure.{md,json}
scripts/atlas_variant_drug_map.py       PARSES THE VARIANT LAYER NOBODY HAD EVER PARSED (#ATLAS-VARIANT). `relations.tsv.gz` carries `ProteinMutation`/`DNAMutation`/`SNP` entities whose identifier holds the variant inline (`RS#:121434568;HGVS:p.L858R;CorrespondingGene:1956` is EGFR L858R) over 228,501 rows. Those entities are NOT absent from this repo -- they are graph nodes like any other and `analysis/atlas-emergence.md` already ships a row reading `adagrasib -- RS#:121913530;HGVS:p.G12C;CorrespondingGene:3845` -- what nothing here had ever done is PARSE the fields, so the identifier passed through whole as an opaque label and nothing could group two spellings of one variant or say which gene a mutation belongs to. (An earlier draft claimed the layer was 'absent from every other analysis'; a fresh-eyes review showed that was FALSE and that the guard meant to protect it was checking a different file and sat green while the claim was wrong.) Builds the drug-by-variant slice (24,415 Chemical-to-variant rows -> 13,952 (drug, gene, variant) pairs) resolved through the committed authority table (#628). The top of the table is textbook precision oncology and is the sanity check that the extraction works: osimertinib/EGFR-T790M, vemurafenib/BRAF-V600E, sotorasib/KRAS-G12C, imatinib/ABL1-T315I, osimertinib/EGFR-C797S. THE IDENTIFIER IS NOT THE VARIANT, and for each of the two defects the OBVIOUS correction is wrong. (1) COLLAPSING AN RSID is wrong because rs121913529 covers KRAS `p.G12D`, `p.G12V` AND `p.G12A` -- one multi-allelic codon-12 site, three substitutions, different drug programs -- and the merged row would still look plausible. THREE threshold-free tests, failing any refuses the whole rsid: every spelling must PARSE, each representation class must agree INTERNALLY, and a protein spelling must agree with a coding one ACROSS classes since codon = (coding position + 2) // 3 relates them. Measured: 1,539 collapse, 915 refused for disagreeing spellings, 430 for a spelling that cannot be checked, 63 for a coding position in a different codon, and 36 where no position can be checked against the residue at all (a genomic coordinate or an intronic/UTR offset). THOSE LAST TWO ARE COUNTED APART because a third review pass found the report quoting every cross-class refusal as a codon FAILURE when 36 of them were never evaluated, and mislabelling a protein-versus-GENOMIC refusal as protein-versus-coding; the honest pair is 1,526 agrees / 63 fails, i.e. the relation holds in 96.0% of the cases it can decide, which UNDERSTATED its own case as originally written. Refusing on a genomic coordinate is not over-cautious: four would be wrongly ACCEPTED if one were read as a codon, purely by coordinate coincidence. THE FIRST TWO VERSIONS WERE BOTH WRONG AND BOTH SHIPPED AS THE SAFETY PROPERTY: v1 tested PROTEIN forms only and swept the rest onto the winner (would have moved 4,527 rows across 337 rsids onto keys they do not belong to -- under rs1801131 a 17-row `p.E429A` captured all 742 rows including a 710-row `c.1298A>C` plus `c.1286A>C` and `c.1298A>T`, a different position and a different allele); v2 added internal agreement but still could not see ACROSS classes, so rs1494558 merged `p.I66T` with `c.412G>A` (codon 138) and rs2297518 merged a missense with a promoter position. (2) READING THE MAJORITY AS CORRECT is wrong in BOTH directions across the 69 digit-drop twins: TP53 `p.R72P` and ERBB2 `p.I655V` are real rs-backed polymorphisms whose LONGER twins are the typos, while EGFR `p.T790M` is real and `p.T90M` the typo. THE RSID ADJUDICATES, being assigned independently of the extracted string -- where exactly one side carries rsids that side is canonical (32 pairs), and where neither or both do, OR where a form is one digit from TWO canonicals (KRAS `p.G1V` reaches both `p.G12V` and `p.G13V`), it ABSTAINS (37). The collision case first let dict-write order decide by ASCII sort and SHIPPED A VERDICT IN THE JSON THE CODE HAD NOT APPLIED; a guard now pins corrections_applied == adjudicated. THE ONE CASE THAT MATTERS: PubTator emits JAK2 `p.V61F`, the canonical MPN driver V617F with a digit missing, on 3,759 rows against 624 for `p.V617F`, so the unreconciled map read V617F at a seventh of its support; the verdict rests on evidence not counts, since `p.V617F` carries rs77375493 on 624 of its 624 rows, `p.V61F` carries none, and both show the identical Polycythemia Vera, Neoplasms, Primary Myelofibrosis profile in the same rank order. The 3 other majority-moving corrections rest on a SINGLE rsid-bearing row and the table says so. EVERY FIGURE IS DERIVED INCLUDING THE WORKED EXAMPLES, which name whichever case the data says is largest -- v1 hand-wrote them and shipped 'rs77375493 on 620 of 624 rows' when the truth is 624 of 624, and the FIX for that then hand-wrote the over-merge size into the docstring where it went stale within one commit; guards now grep the generator for hardcoded literals and re-derive the featured example's share and fraction word from an INDEPENDENT word list (calling the generator's own helper made the guard compare it to itself). WHAT IT IS NOT, stated before the table: CIViC/OncoKB/COSMIC curate directionality, evidence level and expert review and are the correct source for any clinical question; this measures ATTENTION not actionability, since `associate` carries no direction so 'confers resistance' and 'predicts response' land in the same bucket. 88% of pairs rest on a single paper; a variant with no CorrespondingGene is keyed SEPARATELY and never fused, since `p.G12C` alone could be KRAS, NRAS or HRAS; a bare rsid keeps its `rs` prefix (`113488022` in a variant column reads as a codon position); NLM's `[OBSOLETE]` prefix is stripped for display since it marks a retired DESCRIPTOR and '[OBSOLETE] avapritinib' reads as a withdrawn medicine; the gene table reports rows AND occurrences so 'variant-touching rows' can be checked rather than trusted. The `.tsv.gz` uses `mtime=0` so an unchanged input regenerates BYTE-IDENTICAL. analysis/atlas-variant-drug-map.{md,json,tsv.gz}, 29 guards in tests/test_variant_drug_map.py, all mutation-verified (31/31 planted mutations caught, after two review passes found 6 that survived)
scripts/atlas_combination_gaps.py       WHAT THE CO-TREATMENT LAYER CAN AND CANNOT SUPPORT (#ATLAS-COMBO), and the clearest record in this repo of plausible explanations surviving until something measured them: THREE successive claims here were refuted by review or by measurement. 224,146 `cotreat` rows over 40,878 pairs joined to the gene and variant layers. THE COUNTS ARE NOT NEW (atlas-retraction-exposure.md and atlas-emergence.md already ship them; 'nothing had read them' was the SECOND such overclaim in two PRs, and a guard now compares the NUMBER against the sibling file). (1) `cotreat` IS NOT CO-ADMINISTRATION: alectinib and crizotinib are sequential ALK inhibitors never given together and carry 86 cotreat rows; 1,387 pairs corpus-wide carry more `compare` rows than `cotreat`. The corpus's own `compare` predicate discriminates: a COMMITTED 37-pair panel of independently-known truth locates the boundary at (2.98, 3.77], so the 3.0 threshold sits inside a measured gap. An earlier version cited a 36-pair sweep THIS REPO HAD NEVER RUN, in three places, to justify that same threshold; the panel now exists. AND THE RATIO IS NOT A RATIO WHERE `compare` IS ZERO -- `cotreat/max(compare,1)` returns the raw count, so the flag called 17,467 rows with NO comparison evidence 'compare-dominated'; there are three verdict states now and the ratio is null where there is nothing to divide by. (2) PARTNER-AGENT CLASS PREDICTS WHAT THE VARIANT JOIN RECOVERS, AND IS NOT THE MECHANISM. v1 claimed an absence carries NO information (refuted by its own top row). v2 claimed the split was by BIOMARKER class -- wrong, and wrong because TWO PANEL LABELS WERE MINE TO ASSIGN AND I ASSIGNED BOTH TO MAKE THE SPLIT CLEAN (FLAURA2 enrols the same biomarker as MARIPOSA; CAPItello-291 is a three-gene alteration list). Over 14 regimens biomarker class does NOT separate (7/12 vs 1/2, +0.08) while partner class does (7/7 vs 1/7, +0.86) -- 3 of the 14 are major trials WITHOUT a combination approval (NEJ009, AG221-005, KRYSTAL-7), all three in the not-both-targeted arm and all three missing, so each widens the partner separation and narrows the biomarker one; named rather than dropped. v3's MECHANISM was then refuted TOO, by measurement: it asserted the extractor cannot tie chemo/HMA/checkpoint/endocrine agents to a substitution, and it can -- pemetrexed IS tied to EGFR L858R in 4 papers, azacitidine to IDH1 R132H in 1, pembrolizumab to KRAS G12C in 2, and 4 of 6 misses have BOTH drugs tied to the variant somewhere. What is thin is the VOLUME on one side (tens-to-hundreds vs nought-to-4), so a single paper carrying both links is rare not impossible. The honest statement: both drugs must be tied to one variant IN ONE PAPER, and partner class predicts that without causing it. PANELS RESOLVE BY NAME AND AN UNRESOLVABLE NAME RAISES (v1 hardcoded MeSH ids from memory; MESH:C571179 is eravacycline, not vemurafenib, and each wrong id shipped as a FALSE MISSED). Gene layer 24,864 triples (23,068 single-paper). WHAT NEITHER LAYER CAN SAY: no outcome; A GENE IS NOT A TARGET; PubTator's own gene assignment is read so the atlas_ambiguity blocklist DOES NOT APPLY, and the collision examples are DERIVED from the authority table at render time (`MEK`->MAP2K7, `Met`->SLTM, `PGP`->PGP, `PSA`->KLK3) because a hand-written one was FALSE -- `NP`->Neptunium was shipped under the words 'verified in the authority table itself' and Neptunium's alias list contains no `NP`; and GENE-LAYER PRECISION IS NOT MEASURED AT ALL (an earlier version reported a 40-row sample THIS REPOSITORY NEVER RAN). analysis/atlas-combination-gaps.{md,json,tsv.gz}, 20 guards in tests/test_combination_gaps.py; a 49-agent audit of the merged PR produced 39 findings, 37 confirmed, 8 high
scripts/manuscript_vs_census.py         TESTS THE MANUSCRIPT'S OWN CLAIMS AGAINST THE CENSUS (#MS-CENSUS), and BOTH SURVIVE -- one of them understated by the manuscript, which is reported as prominently as a failure would be. The manuscript's numbers came from a keyword-retrieved corpus or from live PubMed queries run once; the census is the whole indexed cancer literature with expert MeSH labels, so claims the frozen corpus could not test now are testable. THE SCOPE DIFFERENCE DECIDES EVERY ROW: the census is cancer-restricted (C04 + adjacent) and the manuscript's modality counts were UNRESTRICTED PubMed, so absolute counts differ several-fold BY CONSTRUCTION and only ratios and directions are comparable. SECTION 8.2 (the thesis chapter's only quantitative table, carrying its central self-correction that SDT is not the unique ferroptosis-ICD modality): manuscript ratio PDT:SDT = 2.93:1, census = 4.75:1 (152 vs 32) -- the claim HOLDS on independent expert labels over the whole cancer literature AND the manuscript understates it. What the table cannot test is stated rather than folded in: TTFields has NO MeSH descriptor (untestable, not a zero agreeing with the manuscript), IRE, HIFU fall below the 10-article floor, and the ICD column is not measurable at census scale at all. SECTION 3.7 (the growth story): the corpus grew x30.71 while the cancer literature grew x1.1 and its open-access share x1.67 -- and since the corpus was retrieved ONCE with fixed queries, OA availability is the only year-dependent retrieval effect, so neither confound accounts for it (x18.39 beyond availability). The claim HOLDS and the census strengthens it by removing both confounds. A SCOUTING AGENT HAD REPORTED THIS CLAIM AS NOT SURVIVING, by comparing the corpus's growth against the field's; verifying it produced the opposite result, the third time in that session a subagent headline ran 'your work is broken' and was wrong. A REVIEW THEN FOUND THE HEADLINE WAS A STATIC STRING that could not flip: forcing the comparison to fail produced a document whose section body said the ratio was SMALLER while its opening line still said the claim survived, and the guard written to keep the outcome prominent asserted the fixed words 'survive'/'understated', PINNING THE ENDORSEMENT IN PLACE. The headline is generated from the verdicts now and the guard checks agreement with them. The same review found the verdict over-extending past what a count ratio can establish (the ICD leg is unmeasurable and the depth-penetration argument is tissue optics), and found two things the analysis NAMED and never MEASURED, both now measured: the ratio ranges 3.23 to 6.31 across four defensible descriptor sets and EVERY one exceeds the manuscript's, and the relative over-estimation is symmetric (90.8% of PDT records vs 87.5% of SDT are on-modality-and-tumour, a 3.3-point gap; filtering gives 4.93 against the raw 4.75), so the verdict is earned by measurement rather than assertion. THE GUARDS PIN WHAT WOULD MAKE A CONFIRMATION HOLLOW rather than the conclusion: the verdicts are FUNCTIONS handed inputs where the answer must be False, because the first version recomputed each verdict from the same JSON fields the generator wrote and hardcoding it to True was invisible while the claim genuinely held -- the exact 'a test that can only print survives is not a test' failure, committed inside the test written to prevent it. Also pinned: the quoted manuscript figures are checked against v1.md so an edit there fails rather than silently changing what is tested. analysis/manuscript-vs-census.{md,json}, 8 guards in tests/test_manuscript_vs_census.py, mutation-verified 12/12; the script is enrolled in tests/test_no_handwritten_figures_in_generators.py
scripts/atlas_module_support.py                asks, for each ferroptosis realism layer, how many DISTINCT cancer articles assert its entity relation and whether the module's own cited PMID is among them. 11 of 20 corroborated (was 9 until the #ATLAS-AMBIG fix: `fsp1` AIFM2-GPX4 read 0 because every GPX4-FSP1 relation in the census had been filed under ATL1; it now reads 6). THE HEADLINE HAS TWO DENOMINATORS and quoting only the first flatters the layers: three of the twenty claims (`system_xc`, `erastin`, `fsp1`) are CORE-ENGINE mechanisms from the manuscript's Chapter 5, not off-by-default layers, and they are three of the four best-corroborated rows -- restricted to the layers proper it is 8 of 17, not 11 of 20. Neither is a survey of the library (author-chosen proxy pairs over a subset of modules). A ZERO IS USUALLY ABOUT EXPOSURE, NOT ABOUT THE CLAIM, and reporting the shortfall flat hid that: a pair can only be asserted if BOTH entities are written about, and the weaker entity's partner count across these claims runs 6 to 2,792. Every corroborated claim has a weaker entity of at least 108; 7 of the 9 zeros fall below that (Spearman rho = 0.86, tau 0.70, permutation p < 1e-5; the association RISES to 0.88 when every GPX4 pair is dropped, and an independent mention-count measure gives 0.83 with the identical split, so it is not a hub artifact). WHAT A HOSTILE STATS REVIEW KILLED, and why the section now names NO exceptions: (1) 'the census could not have corroborated them whatever the biology is' is FALSE -- the line is a sample minimum, not a detectability limit, and 45% of all 2,888,766 asserted pairs sit below it; (2) the line rests on ONE row (gch1, a single article under the weakest predicate) and a bootstrap returns the two-exception answer only ~65% of the time; (3) DECISIVELY, running the same procedure on the pair-level co-mention column already in the same table INVERTS the correlation (rho about -0.9) and yields a DISJOINT exception set, so which zeros look 'genuine' is an artifact of the measure chosen -- the first draft named copper + hdac_persister without checking the column beside them. Also fixed: `_entity_degree` double-counted self-pairs via `for i in key` over a 2-tuple, inflating AIFM2 (which has one) and so two of the twenty claims. Now emits `atlas-module-support.json` so `census_findings.py` quotes the artifact rather than copying prose. Its limit matters as much: PubTator has no biological-process entity type, so "X induces ferroptosis" is not expressible and every claim collapses to a gene-gene or gene-chemical pair
scripts/atlas_contradictions.py                finds pairs the literature asserts in BOTH directions (7,222) and chemicals asserted both to treat and to cause a disease (9,251). THOSE TWO TOTALS USED TO EXIST ONLY IN STDOUT -- copied from a terminal into this file and stored in no artifact, so nothing could check them and nothing noticed when they drifted ~50% (4,667/6,764) as the disambiguation corrections grew and merged more entities. The committed top-500 lists are BYTE-IDENTICAL across that change (the biggest conflicts are stable while the tail grows), so the artifact could not have revealed it either. The generator now writes both totals. Ranked by the WEAKER side so a 12-vs-9 split outranks 50-vs-1. 4 of 20 simulation-module claims sit on a contested edge while citing a single paper that does not mention the disagreement
scripts/atlas_emergence_error.py               bounds the emergence layer's SAMPLING error (#ATLAS-EMERG-ERR). atlas_emergence.py computes 'share of support since 2021' from the index's 60-PMID SAMPLE, and a THRESHOLD (>=80%) applied to an unbiased estimate is NOT unbiased -- a pair whose true share sits near 80% lands on either side by the draw. Compares against the EXACT share from every dated asserting paper: 89.5% of examined pairs carry no more papers than the sample holds so their share is exact (reported separately -- pooling them would flatter the estimator with cases where it does no estimating); on the 10.5% genuinely estimated, median abs share error 0.022 / p95 0.085 and the threshold decision is 88.1% precise at 91.3% recall; across ALL examined pairs 99.0% precision / 99.3% recall, the figure a reader actually gets. Error GROWS with support (median 0.018 at 61-120 asserting papers -> 0.045 above 2,000), confirming it is sampling error rather than the SYSTEMATIC failure that preceded it (the index once kept a LEXICOGRAPHIC prefix = the oldest PMIDs, reading a true 26.6% recent share as 0.0%). Limit stated: MeSH indexing lag under-represents the newest literature, biasing every recent share DOWN, estimate and exact alike
scripts/news_verification_audit.py             measures whether the news pipeline's `verified` PMIDs support their claims (#NEWS-VERIFY), and now also whether the FIX worked. THE ORIGINAL MEASUREMENT: news/NEWS_INDEX.jsonl marked 44 claims verification_status="verified" with linked_pmids as the evidence (TWO reached the manuscript as "Verified: PMID:X" footnotes), and 55.7% of the 212 resolved claim->PMID pairs shared NO content word with the article they verified. The diagnostic was numeric clustering -- 66 of 175 identifiers shared the prefix `4202` -- and PubMed IDs are assigned sequentially as records are INDEXED, so the linker was matching on WHEN a record was indexed, not what it says (clearest case: an electric-fields/brain-cancer claim "verified" against freshwater fish biodiversity and a speech-language-pathology survey). THE RE-RUN (#615): verify_news_claims.py's two root causes were fixed (sentence-initial capitals are no longer proper nouns; supports_claim() requires >=2 shared content words), plus two the re-run itself exposed -- a claim with NO extractable term hit an early `return claim` and KEPT its stale verdict (13 bogus "verified" survived the first pass that way), and a claim flipped to unverified kept its verification_source + linked_pmids, so withdrawn evidence stayed inside the audit's own denominator. Re-verified over ALL 38 articles (`--force`; the documented `--all` skips articles whose factual claims are all already verified, which was 20 of 38 and held 10 of the bogus verdicts). RESULT: 44 verified -> 14 (30 withdrawn, 0 gained), no-content-word share 55.7% -> 1.9%, prefix clustering 66/175 -> 3/45, idempotent on re-run. READ IT AS WITHDRAWAL NOT REPAIR: the denominator fell 212 -> 53 pairs, and 19 of the 33 surviving pairs clear the two-word bar on oncology BOILERPLATE alone (cancer/trial/phase/patients/united states), which the report now counts explicitly. UNFIXED and stated: supports_claim() gates only the PubMed fallback -- the corpus path runs FIRST and accepts 2 term substrings in a title, so a weak corpus match short-circuits the search (applying supports_claim to it would drop only 1 of 10, i.e. the bar not the gate is the limit); and query construction still emits `This Phase United States`-style queries that return recently indexed records. Stated as a band not a verdict: title-only overlap can miss a genuine supporting paper and can credit an unrelated one. Re-running the linker stays OUT of atlas_pipeline.sh -- it is a reviewable data change and depends on PubMed's live index
scripts/atlas_citation_audit.py                checks whether each module's CITED PAPER is the paper it claims (#ATLAS-CITE). atlas_module_support.py exists because every layer rests on 'one or two papers the author read, cited by PMID' -- it measures how many OTHER articles assert the relation and NEVER checked the citation itself. THREE WERE BROKEN: `dhodh` cited 33864050 = a Nature NEWS ITEM on US fetal-tissue policy, `prom2` cited 31761539 = a Theriogenology paper on embryo VITRIFICATION, `gch1` cited 31919077 = a PMID that does not resolve. A broken citation reads in the support table as `cited-absent`, which looks like an extraction gap and is not. Corrected to 33981038 (Mao 2021 Nature, DHODH-mediated ferroptosis defence), 31735663 (Brown 2019 Dev Cell, Prominin2 iron export) and 31989025 (Kraft 2020 ACS Cent Sci, GTP Cyclohydrolase 1/BH4) -- each found by PubMed search and verified by EXACT TITLE against the mechanism the module documents, never guessed; the prom2 error had propagated into 3 Rust doc comments + CALIBRATION_STATUS + CLAUDE.md (all comment-only, no behaviour change). TRIAGE MATTERS: the check is entity-mention, which has false positives, so a paper mentioning neither entity but plainly IN THE FIELD is reported separately as a naming mismatch, not an error (Dixon 2012 defines System Xc- without ever writing `SLC7A11`; alox + system_xc are both this). Two tests pin the corrected PMIDs and assert no broken citation returns
scripts/atlas_contradiction_quality.py         gives the contradiction layer the ERROR BOUND it never had (#ATLAS-CONTRA-Q). atlas_contradictions.py names extraction error as a caveat and never measured it; two failure modes are now bounded with OPPOSITE answers. (1) WITHIN-PAPER self-contradiction (one paper extracted as asserting a pair both ways = inconsistency, not disagreement) happens to 1 paper in 115,024 -- effectively absent, so the conflicts really are between studies. (2) ENTITY AMBIGUITY manufacturing disagreement is real: merging two entities under one identifier merges two literatures, and pairs involving a measured sense collision are 1.45x more likely to be flagged contradictory (95% CI 1.37-1.53, 2000-resample bootstrap). THE CONFOUND IS RULED OUT, and this is the load-bearing part: colliding identifiers are contested BECAUSE they are heavily mentioned, so the crude ratio could have been pure popularity -- stratifying by directional-assertion count and pooling Mantel-Haenszel leaves it at 1.45x vs a crude 1.47x, it holds inside EVERY stratum, and it RISES with assertion count (1.36x->1.88x), the direction conflation predicts. Stated as an ASSOCIATION not an attribution (it does not license subtracting 45% of ambiguous conflicts), and the ambiguity measurement covers only top forms so contaminated-but-unmeasured pairs count as clean, biasing the ratio DOWN
analysis/atlas-coverage.md                     the frozen corpus measured against the census: it holds 0.086% of the cancer literature; 20.9% of the census has a PMC id (the ceiling on any full-text claim) against the frozen corpus's 98.7% open access
scripts/evidence_sections.py                   section-scoped full-text splitter (#TAGGER-V2): splits an article's `## Full Text` blob into SELF (Methods/Results/Trial design/...), CITED (Introduction/Discussion/Conclusion) and DROP (References/Funding/Ethics) sections, so the evidence tagger reads only what the authors assert about their OWN study. Implements the rule analysis/evidence-labeling-guidelines-v2.md already states ("not in cited references or background discussion"). Headings survive as bare blank-line-surrounded lines and are found in 98.7% of a 600-article sample; unrecognised headings INHERIT the enclosing class so Methods subsections ("Calcein assay") stay SELF without enumeration. Median SELF fraction of full text 46%. `has_methods_section()` is exposed as a diagnostic only -- measured at 26% precision as a lone none-applicable predictor, so it must never be used alone
scripts/evaluate_evidence_v2.py                evidence tagger v2 evaluation (#TAGGER-V2): runs the real production path under FERRO_EVIDENCE_V2 off/on over three label sets and writes analysis/evidence-v2-eval.md. DEV (100 v1 human labels, TUNED on, so optimistic), HELDOUT (170 records never inspected during development, labelled by the independent full-text relabel -- the generalization test, 2.59x error reduction), CONSENSUS (the 77 records where the human label and the relabel AGREE -- the conservative headline, 1.95x). Reports Wilson intervals; offline and deterministic
analysis/evidence-tagger-v2.md                 how the v2 gain was obtained, what was REJECTED and why (six clinical phrases removed for firing more on preclinical than clinical records; a "no Methods section implies review" rule at 26% precision; anchored subject-verb branch predicates that measured WORSE at 1.54x consensus), and the caveats that bound the numbers (77% annotator agreement; the held-out labels share an "ignore Discussion" methodology with the tagger, so consensus is the defensible figure). Ends with the promotion checklist -- this layer is deliberately NOT applied to the production corpus
analysis/evidence-gold-set-v3-fulltext.csv     270-record full-text relabel of the evidence gold set. MACHINE labels, not human: an independent LLM pass read each article's Methods/Results and applied the committed guidelines, blocked from every existing gold file and from the frontmatter `evidence_level`. Agreement with the 100 human v1 labels is 77.0%, concentrated on `theoretical` and the in-vitro/in-vivo boundary -- i.e. the same classes the tagger finds hardest -- so treat ~77% as the practical annotator ceiling and neither column as ground truth
tests/test_evidence_v2.py                      v2 guards: flag-OFF byte identity vs the frozen corpus; a consensus-subset error-reduction floor; section-splitter unit tests incl. the PMID 40700574 case (a mouse study whose Discussion cites a phase 3 trial); the opinion-pub-type veto; the in-vivo reagent guard; and channel separation -- that the evidence flag does not move mechanism/cancer/pathway tags (it once moved 14.3% of them) and that prose outranks a contradicting MeSH descriptor while MeSH still fills prose silence
analysis/mesh-mechanism-map.yaml               handwritten MeSH-descriptor -> mechanism reference map for #412 (precision-first leaf descriptors per mechanism + an `unmeasurable:` block with reasons); the leak classifier splits each into independent/leaky at runtime
analysis/reaction-diffusion-benchmark.md     reaction-diffusion vs exponential-proxy supply benchmark (#343): in the λ ≲ vessel-spacing regime the proxy reads optimistic (dominated by 3D point-source geometry, not consumption), sparse 62%→99% hypoxic; direction flips with λ and magnitude is representation-dependent, so directional not calibrated; PhysiCell cross-check honestly deferred
analysis/manuscript-scientific-review.md     external+internal scientific review: verified support/contradiction, math check, reframe rationale, figure roadmap
analysis/research-roadmap-2026.md            deep web-grounded gap analysis + prioritized issue backlog (#330-#354): validation/calibration, model-biology gaps, corpus, contribution strategy
analysis/contribution-plan-2026.md           contribution plan (#353): venue split (corpus vs simulation), preregistration draft of the falsifiable predictions, collaborator experiment brief
analysis/p1-wetlab-protocol.md               detailed P1 wet-lab protocol (#496): the runnable, isolatable GPX4i+FSP1i dose-matrix synergy test in an FSP1-low persister line (cell line + FSP1-status check, RSL3/ML162 by iFSP1/brequinar checkerboard, viability + C11-BODIPY + Fer-1 rescue, replicates/power, Chou-Talalay CI + Bliss analysis, pre-stated CI>0.8 falsification), with the rationale that P1 depends on the FEWEST contested assumptions (only GPX4/FSP1 parallel independence, Doll/Bersuker 2019 + Hangauer 2017). A PROTOCOL not a result: no wet-lab has been run; executing it needs a collaborator. The single biggest credibility step (first test of a prediction vs biology the model was not built on)
analysis/collaborator-brief.md               one-page collaborator/reviewer recruitment brief (#498): the ask (ferroptosis/redox biologist OR computational oncologist OR external reviewer), what the model integrates (corpus + the data-anchored legs #330/#502/#500/#333/#334/#335/#482), the P1-P8 falsifiable predictions one-liners, the E1-E6 cheapest-first experiment menu, what a co-author gains (authorship on a registered falsification-first program), and SELECTION CRITERIA (not named individuals, to avoid fabrication/privacy; identifying specific candidates is the human step). PREREGISTRATION.md Part 1 now states each P1-P8 with a quantitative model output + a numeric falsification threshold (#497, the qualitative P2/P4/P6/P7 tightened); the OSF DOI filing remains the human step
analysis/manuscript-split-outline.md         two-paper split outline (#504): operationalizes the contribution-plan venue split into section-by-section outlines for Paper A (reproducible OA keyword-scoping corpus map, lead claim = apparent gaps are search/taxonomy/OA artifacts not biology) and Paper B (embeddable ferroptosis engine led ONLY by the data-anchored legs #330/#333/#334/#335/#464/#482 + the honesty infrastructure, with the 30+ off-by-default realism layers demoted to ONE supplementary table); each paper leads with its defended claim, the book becomes background; venue choice + submission are human actions
analysis/prisma-scr-protocol.md              retrospective PRISMA-ScR scoping protocol (#505): reframes the corpus honestly as automated keyword scoping of OA literature, NOT a systematic review and not yet fully PRISMA-ScR-compliant (no a-priori protocol, no dual screening, no critical appraisal, named item-by-item); PCC research question, eligibility-as-applied, search strategy (queries.txt + documented date windows), single-automated-pipeline selection, three-axis charting, the Figure 20 (#135) flow tied to the real counts (10,415→4,830 full-text + 5,586 abstract-only), the two-recall-types table (mechanism-presence ~90% #412 vs evidence-tier 55%, never conflated), and a 20-item checklist applicability map; the lone manuscript "systematic literature search" over-claim (§3.2) is corrected to "automated keyword-driven scoping search"
analysis/ode-cross-validation.md             ODE cross-validation (#344): ferroptosis-core's bistable recover-or-collapse switch vs published models (Co 2024, Seidel 2026, Konstorum 2020); figure + script in simulations/calibration/
analysis/arbatskiy-cross-validation.md       structural cross-validation + borrowable-constant accounting vs the Arbatskiy 2024 73-ODE/93-species ferroptosis model (#471; IJMS 25:11782, DOI 10.3390/ijms252111782, PMID 39519341, CC BY). Confirms our GSH/GPX4->L-OOH + Fenton/ferritinophagy topology matches an independently-built 73-ODE model; maps which repo constants the CC BY supp tables S1-S7 could constrain (lp_rate/lp_propagation/gpx4_rate/gsh_scav/Fenton/ferritinophagy) vs which stay repo-specific (DHODH/GCH1/FSP1 are NOT in their model; their only GPX4-independent trap is alpha-tocopherol); structural not numeric (compartmental molar state + on-request model + MDPI-403 supp block a 1:1 transplant); ~120 min Fenton fast-phase is a units-free ramp cross-check for ferritinophagy_tau. No production-default change
analysis/talkington-immune-params.md         immune-parameter borrowing from the Talkington & Kearsley 2025 multiscale ICB model (#472; Comput Syst Oncol 5(2):e70007, DOI 10.1002/cso2.70007, PMID 41322398, CC BY). Documents the verified Table 1 values (kill rate r2=1.101e-7 day^-1 cell^-1 mouse-cited, exhaustion threshold n=1e4 assumed, ICB efficiency I in [0,1] swept with an 80-90%-blockade optimum) and maps them onto our immune kill probability / exhaustion_rate (#243) / multi-checkpoint residual (#264): dimensionless quantities (n, I) port cleanly, the dimensional kill rate does NOT (needs p=1-exp(-r2*local_density*dt); a raw well-mixed rate double-counts our spatial locality, the same effect behind the 104:1->4:1 3D immune-ratio shrink). Opt-in anchor, NOT swapped into defaults; mapping math encoded+tested in scripts/icb_param_map.py
analysis/sbml-export.md                      SBML export (#351): deterministic single-cell core ODE -> SBML L3V2 (simulations/sbml/ferroptosis_core.xml), libSBML-validated + roadrunner round-trip; documents exportable vs not-exportable modules
scripts/fetch_calibration_data.py              CTRPv2 ferroptosis dose-response fetch (#330 data leg): pulls the DepMap-redistributed CTRPv2 (Harmonized CTD^2 25Q2, `CTRPResponseCurves.csv`, md5 36115a26..., login-free DepMap download API) since GDSC2 lacks the ferroptosis inducers, md5-verifies, filters to ML162/ML210/erastin/CIL55/CIL56 and writes the committed per-cell-line 4-param-logistic target `analysis/calibration/ctrpv2_ferroptosis_curves.csv` (3,021 curves) + `ctrpv2_ferroptosis_summary.json`. Empirical GPX4i target: median EC50 ~0.5-0.75 µM, kill ceiling ~0.89-0.98, EC50 spread ~10x across cell lines. The raw 45MB file is NOT committed; CI never downloads (offline contract); the derived CSV is the reproducible artifact. The model fit + held-out validation is the #330 follow-up leg. See analysis/calibration/calibration-targets-ctrpv2.md
scripts/calibrate_kill_switch.py               CTRPv2 kill-switch fit (#330 fit leg): tunes the single-cell RSL3 kill switch (`lp_propagation`/`lp_rate` cascade + a µM->`rsl3_gpx4_inhib` scale K via `dose/(dose+K)`) to the in-vitro GPX4-inhibitor median dose-response, via `ferroptosis_core.sim_batch` grid search. Fit on ML162 (RMSE 0.05), held-out validated on ML210 (RMSE 0.07, same GPX4i mechanism, generalizes); the default in-vivo-tuned switch is ~11x worse (RMSE 0.57, far too RSL3-resistant for in-vitro data, e.g. Glycolytic never dies under RSL3). Calibrated lp_propagation=0.7/lp_rate=0.4/K=0.25µM. IN-VITRO only (production defaults unchanged); the cell-line-spread→phenotype-heterogeneity distributional fit is the next extension; erastin (system-xc-) reported as a cross-mechanism contrast (RMSE 0.42, expectedly worse). Needs the compiled extension (not run in CI); the committed analysis/calibration/kill-switch-calibration.{md,json} are the checked-in result. Distinct from the #330 data leg (scripts/fetch_calibration_data.py)
scripts/calibrate_pk.py                        tumor-PK measured-data anchor (#334): anchors the `tumor_pk` plasma+tumor disposition to the only public ferroptosis-specific in-vivo PK with a PAIRED plasma+tumor course, imidazole ketone erastin (IKE, system-xc- inducer; Zhang 2019, PMID 30799221), plus a sorafenib human popPK forward check (Jain 2011, PMID 21392074). Pure Python + scipy (runs in CI, no compiled extension). Headline: measured tissue:plasma partition Kp=0.90 (vs presets' estimated 0.15-0.5), ~2 h plasma→tumor delay, slower tumor clearance (t1/2 3.5 vs 1.83 h). Structural finding: the IKE plasma AUC/Cmax (2.11 h) is below the closed-form 1-compartment floor e·Tmax (3.67 h), PROVING multi-compartment disposition (a fast distribution phase) and bounding what summary NCA identifies; the plasma curve is anchored exactly to Tmax/Cmax/terminal-t1/2 and the 1-cmt AUC over-prediction (+109%) is reported as that finding, not hidden. The tumor rates derive from measured Kp + t1/2 so tumor Tmax/Cmax are out-of-fit predictions (delay DIRECTION captured, Tmax magnitude over-predicted, same omitted-fast-phase root cause). Sorafenib Cavg,ss ~4.1 mg/L at 400 BID lands in the clinical 3-5 range. RSL3/ML tool-compound PK genuinely absent (erastin's instability is why IKE was made), flagged not fabricated; per-tumor presets NOT recalibrated (no public per-tumor PK). Exports a normalized FromPk series (#239 bridge). Committed analysis/calibration/pk-calibration.{md,json}; target analysis/calibration/{pk_measured_data.csv,calibration-targets-pk.md}
scripts/validate_penetration.py                Krogh drug-penetration validation (#335): validates the `drug_transport` exponential penetration `C(r)=C0*exp(-r/lambda)` (lambda=sqrt(D/k_total)) against PUBLISHED MEASURED data. Pure Python stdlib (runs in CI). Validates (1) the exponential FORM (Primeau 2005 PMID 16361566 + Tannock 2002 PMID 11895922 both report exponential drug-vs-distance-from-vessel decline in vivo) and (2) the doxorubicin transport-reference length (model lambda=50 um, half-distance 34.7 um) which sits WITHIN Tannock 2002's measured 25-75 um and ~13% below Primeau 2005 / Minchinton 2006 (40-80 um), i.e. right form + order of magnitude, conservative end. A drift-guard parses drug_transport.rs and asserts the validated lambda equals the Rust presets. Honest scope: the RSL3-like lambda=100 um is UNVALIDATED (no ferroptosis inducer has spatial penetration data), and the binding-site barrier in #335's title is NOT a model feature (uptake is linear/lumped into lambda => penetration depth is dose-INDEPENDENT; a true binding-site barrier is saturable/dose-dependent, but a #335 data-availability review found it is an ANTIBODY phenomenon (Fujimori 1990, Saga 1995, Thurber/Wittrup 2008 penetration~sqrt(dose)), physically WEAK for the small molecules this model targets (deep binding sink barely saturates, El-Kareh/Secomb), and unvalidatable from public small-molecule data, so it is deliberately NOT added, resolving #335's binding-site clause by evidence not by building unvalidatable complexity). Committed analysis/calibration/penetration-validation.{md,json}; target analysis/calibration/penetration_measured_data.csv
scripts/calibrate_erastin.py                   System Xc-/erastin core calibration (#502 fit leg): tunes the new core `erastin_xc_inhib` dose-response (Hill `dose^h/(dose^h+K)` -> System Xc- inhibition, run under `Treatment::Control` so erastin acts ONLY by starving GSH resynthesis) to the CTRPv2 erastin median viability(dose) via `ferroptosis_core.sim_batch` grid search; the core's SECOND data-anchored inducer mechanism after the GPX4i kill switch (#330). Fit RMSE ~0.10 (a PARTIAL anchor: nails the ~4.6 uM EC50 transition, over-predicts low-dose death via the hot cascade's basal floor, saturates at the top dose), with a shared-switch check showing the #330 RSL3 cascade does NOT cleanly serve both inducers (the single-switch reconciliation is the joint multi-inducer posterior #500) and a mechanism-specificity check (erastin=0 reproduces the Control baseline). Needs the compiled extension (not run in CI); committed analysis/calibration/erastin-calibration.{md,json} are the checked-in result, guarded offline by tests/test_calibration_regression.py. Distinct from the GPX4i fit scripts/calibrate_kill_switch.py
scripts/calibration_feasibility.py         asks whether a calibration target EXISTS for the four unmodelled ferroptosis mechanisms atlas_model_gaps.py surfaced (#616), instead of writing four layers. The layer-freeze policy (CONTRIBUTING.md) requires a named target before any new off-by-default axis lands, so the question is about DATA. THE ANSWER IS NO, AND THE REASON APPLIES RETROACTIVELY: cBioPortal mRNA z-scores are standardised WITHIN each study, so the fraction of tumors below z=-1 is ~15.87% for any gene under approximate normality -- and measured across the 32 TCGA PanCancer Atlas studies, all seven genes land within two points of it (ACSL4 14.4%, GPX4 14.5%, SLC7A11 16.0%, HMOX1 14.9%, TP53 14.1%, TFRC 15.2%, KEAP1 13.8%). That is the normal distribution being recovered, not biology, so the route cannot separate a gene whose low-expression tail matters from one with no ferroptosis role, and it anchors NONE of the four. THIS CORRECTS #462's READING of the same figure: acsl4-prevalence-calibration.md called it "the real, committed population prior" for the refractory-leaning tail, and although it already noted the by-construction caveat it had no CONTROL -- the six other genes are that control, and they show the number belongs to the z-score, not to ACSL4 (the status_from_zscore BRIDGE is unaffected; it maps a scale). WHAT SURVIVES: TP53's DEEP tail, 4.3% below z=-2 against an expected 2.28% and the others' 1.5-2.1%, with much the widest inter-cancer spread -- consistent with real TP53 deletion rather than standardisation, and the one gene where an expression-derived prior would carry information. Two of the four are also already absorbed by an existing parameter (TFRC by ferritinophagy_release, KEAP1 by nrf2_gsh_rate). Recorded as four "Layers proposed and NOT built" rows in CALIBRATION_STATUS.md; a cell-line expression matrix joinable to the committed CTRPv2 curves would unblock all four at once, and the DepMap catalogue that served it now returns HTML rather than the documented CSV, so it is blocked on ACCESS not method. Committed analysis/calibration/calibration-feasibility.{md,json}, guarded by 7 tests incl. mutation-checked ones that fail if a gene stops matching the normal or if the old #462 framing returns
scripts/abc_posterior.py                       data-conditioned ABC posterior on the kill switch (#332): ABC rejection over in-vitro-spanning priors (lp_propagation/lp_rate/k_um/gpx4_rate), accepting the draws closest to the CTRPv2 ML162 dose-response, posterior-predictive-checked on held-out ML210 (coverage: 3/7 strict, 7/7 within a 0.05 viability tolerance, the gap reflecting single-cell-vs-cell-line-median curves). The LOAD-BEARING finding: the in-vivo PRCC prior ranges (lp_propagation [0.05,0.2], lp_rate [0.03,0.12]) lie ENTIRELY below the in-vitro posterior (lp_propagation 0.38..0.79..0.95, lp_rate 0.28..0.65..0.95), i.e. the in-vivo priors and in-vitro data are DISJOINT, so (1) the data-conditioned posterior is in-vitro-only (credible bands on the #330 point fit) and (2) the in-vivo/spatial HEADLINE claims cannot be conditioned on in-vitro data and stay prior-predictive (the existing headline_uncertainty.py/uncertainty_intervals.py intervals); conditioning them needs an in-vivo ferroptosis dataset (the remaining #332 dependency). Needs the compiled extension (not CI); committed analysis/calibration/abc-posterior.{md,json}. Distinct from the prior-predictive headline intervals (already shipped)
scripts/abc_joint_posterior.py                  joint multi-inducer in-vitro posterior (#500): extends the #332 single-inducer ABC to condition the SHARED single-cell switch (`lp_propagation`/`lp_rate`/`gpx4_rate`/`gsh_scav_efficiency`) JOINTLY on TWO inducer mechanisms at once, the GPX4 inhibitor ML162 (via `rsl3_gpx4_inhib`) AND the System Xc- inhibitor erastin (via the #502 `erastin_xc_inhib`, Hill dose-map, run under `Treatment::Control`), both from CTRPv2; ABC over in-vitro-spanning priors, accepted by the JOINT distance (ML162 RMSE + erastin RMSE), giving a POSTERIOR with credible intervals (not a tuned point) per parameter + a `posterior_width_frac_of_prior` identifiability column (only the erastin Hill/potency are indistinguishable from the prior; the LP cascade is well determined, corrected after the original 0.6 unconstrained-threshold was measured against an uninformative null and found to fire on well-constrained parameters (scripts/abc_posterior_information.py)), a held-out posterior-predictive check on ML210 (never in the fit; 5/7 points inside the 95% band, PP RMSE ~0.11 = generalization not training fit), and a posterior-predictive PNG. The LOAD-BEARING result re-confirms the #332 disjunction for the joint fit: the in-vitro posterior (lp_propagation ~0.40-0.99, lp_rate ~0.39-0.98) lies ENTIRELY above the in-vivo PRCC priors, so it conditions only the IN-VITRO switch magnitudes (manuscript Ch.5 now reports those CrIs) while the in-vivo/spatial HEADLINES stay prior-predictive (conditioning them needs an in-vivo ferroptosis dataset that maps onto those headlines (one that does not exist publicly; the in-vivo ferroptosis readouts that DO exist measure different observables)). Needs the compiled extension (not run in CI); committed analysis/calibration/joint-posterior.{md,json} + joint-posterior-predictive.png, guarded offline by tests/test_calibration_regression.py (disjunction + held-out coverage). Builds on scripts/calibrate_kill_switch.py (#330) + scripts/calibrate_erastin.py (#502)
scripts/validate_spheroid_structure.py         spheroid zone-geometry validation (#333): fetches Browning 2021 (eLife, PMID 34842141; github.com/ap-browning/Spheroids `AllConfocalData.csv`, 994 spheroids) size-resolved confocal structure (outer radius R + boundary fractions phi/eta), bins by R, and compares to the spheroid module's FIXED radial thresholds (rim 0.90, core 0.73 of radius = the large-spheroid limiting structure). Finding: the fixed thresholds match the data only for R >= 300 µm (diameter >= 600 µm); below that real spheroids have NO necrotic core (eta~0 for R<300; necrotic core present in 0.8%/45%/100% of <200/200-300/300-400 µm spheroids), so the model OVER-PREDICTS the persister/necrotic core for small spheroids (the necrotic core emerges near R~300 µm, the O2 diffusion length). Validates zone GEOMETRY + a documented valid size range; does NOT calibrate the per-zone biochem gradient strengths (need metabolomics) and does not cover size-resolved KILL data (the remaining #333 piece). A drift-guard test ties the Python thresholds to the Rust glycolytic_frac/oxphos_frac, and (#333 v0.51.0) it also evaluates the size-aware thresholds (size_aware_rust_constants drift-guard, mean boundary err 0.025 vs fixed 0.338). The size-resolved KILL leg is characterized separately in scripts/validate_spheroid_kill.py. Raw not committed (offline contract); committed analysis/calibration/spheroid-structure-validation.{md,json}
scripts/validate_spheroid_kill.py              spheroid RSL3 kill-vs-size: directional validation + a falsifiable prediction (#333 kill leg). Reads the committed analysis/calibration/spheroid_kill_vs_size.csv (written by `sim-tme-3d --spheroid-size-sweep`, a new off-by-default sweep mode that runs RSL3 across grid sizes 16-60 with fixed vs size-aware zone thresholds; pure-stdlib script runs in CI). (1) With FIXED thresholds the supply-gradient gives bigger-spheroids-resist-more: RSL3 kill 3.6%→0.19% over 144→540 µm radius (~19x), in the measured cytotoxic 1.8-22x range (West 1989 PDT; Eilenberger 2021 DOX/CIS), so the DIRECTION + order of magnitude is validated against generic-cytotoxic data. (2) With SIZE-AWARE thresholds a falsifiable persister-targeting prediction emerges: RSL3 preferentially kills persisters and the persister core only appears above ~280 µm, so small all-proliferating spheroids RESIST RSL3 (≈0 kill) and vulnerability emerges with the core, OPPOSITE generic cytotoxics, untestable without ferroptosis-inducer size-kill data. Kill MAGNITUDE uncalibrated (RSL3-resistant in-vivo switch + placeholder gradients); this characterizes the kill leg as far as public data allows (not a magnitude calibration). Committed analysis/calibration/spheroid-kill-vs-size.{md,json}
scripts/validate_trigger_wave.py            ferroptotic trigger-wave validation (#482, the flagship tie-to-reality leg): validates the new `trigger_wave` module (a 1-D bistable Nagumo reaction-diffusion front, `dL/dt=D*Lxx+k*L*(L-a)*(1-L)`, closed-form speed `c=sqrt(D*k/2)*(1-2a)`, `k~iron` so `c~sqrt(iron)`) against Co/Wu/Lee/Chen Nature 2024 (PMID 38987590; open code github.com/imb-lcd/ftw2024 + figshare 25762806) measured ferroptotic trigger-wave speeds: baseline 5.52 um/min, iron chelation DFO 2.33, iron loading 9.40. Pure-stdlib (runs in CI): the baseline is a one-point D*k calibration to 5.52, and the iron-dose SHAPE `c~sqrt(iron)` reproduces 2.33/5.52/9.40 at plausible iron fold-changes ~0.18/1.0/2.9 (the PREDICTED result), plus a numeric==analytic self-consistency solve and a drift-guard tying the Python BASELINE to the Rust trigger_wave.rs `baseline()`. Spatial complement of the single-cell switch (#344); committed analysis/calibration/trigger_wave_measured_data.csv (target) + trigger-wave-validation.{md,json} + calibration-targets-trigger-wave.md
scripts/embed_gold_set.py                      SPECTER embedding RECOMPUTE for the #411 semantic leg (option B dependency story): embeds the v2 evidence gold set's title+abstract (allenai-specter, 768-d) and writes the embedding VECTORS to analysis/evidence-gold-embeddings.npz (committed, ~816KB). Needs sentence-transformers/torch (run LOCALLY in a venv), NEVER added to requirements-lock.txt; CI + the eval read the committed vectors with numpy only, so the offline/pinned-deps contract holds
scripts/embed_evidence_leg.py                  semantic-retrieval evidence-detection leg (#411, the deferred second leg of #346): an OFFLINE leave-one-out k-NN over the committed SPECTER vectors that flags a record evidence-bearing when its title+abstract neighbours are predominantly evidence-bearing, combined as keyword OR MeSH OR embedding (reuses retag_gold_set._predict/_binary for the keyword+MeSH legs). Evaluated on the BALANCED v2 gold set (270 records, 32% positive, NOT v1's 87% which degenerates a majority-vote k-NN to always-positive): recovers 19/28 of the MeSH-residual false-negatives, lifting binary recall 67.8%→89.7% with NO precision drop (35.3%→39.8% on v2; the embedding's own precision exceeds the keyword baseline's). v2 recall reproduces the documented v1 55.2%/67.8% (shared positives); v2's low absolute precision is from its hard negatives, not the manuscript's v1 96%. Binary detection only (not a level); off-by-default/gold-set-validated (production recall unchanged). analysis/evidence-gold-embedding-eval.{md,json}
scripts/validate_rd_vs_biofvm.py               reaction-diffusion external cross-check vs BioFVM (#408, the deferred PhysiCell leg of #343): runs the SAME matched scenario (Dirichlet vessel sources c=1, no-flux, uniform decay, λ=√(D/k)) in the Rust `reaction_diffusion` finite-volume SOR solver AND in BioFVM (PhysiCell's independent LOD/Thomas microenvironment solver, run to steady state), comparing field shape, decay length, and magnitude. Result: the solvers AGREE, identical shape (log-field Pearson r 0.9997→1.0 single + 1.0 two-source), identical effective decay length (55.1µm single / 59.0µm two-source, both solvers; shorter than planar λ=100 because a 3D point source is the Yukawa exp(-r/λ)/r, reproduced by BOTH so it is real geometry not a Rust artifact), magnitude ratio converging to 1 as BioFVM's dt→0 (2.82→1.42→1.08 at dt 0.02→0.005→0.001, the residual being BioFVM's LOD operator-splitting error, not a disagreement). LOCAL tool (needs a compiled BioFVM driver, source + build at simulations/calibration/biofvm/; Rust side is the `rd_field_dump` example); CI reads only the committed analysis/calibration/rd-biofvm-crosscheck.{md,json}. Upgrades the RD module from analytical-self-consistency-only to externally cross-checked
simulations/calibration/biofvm/                BioFVM cross-check driver (#408): biofvm_rd_check.cpp (the matched-scenario driver) + README.md (build incantation, incl. the macOS Apple-clang workarounds: empty omp.h stub since BioFVM calls no omp_ functions, + explicit -isysroot/-isystem for libc++). BioFVM itself is not vendored (own license); the README documents cloning it
scripts/dashboard.py                           interactive Streamlit dashboard (#354): Corpus tab (sidebar filters on mechanism/cancer/evidence/year, summary metrics, mechanism + evidence-tier bar charts, per-year trend, top-10×10 mechanism×cancer matrix heatmap, filtered article table) + Simulation-sweep tab (live `ferroptosis_core.sim_batch` death-rate sweep over a chosen PRCC rate constant × phenotype × treatment when the extension is built, else degrades to the committed prior-predictive intervals, the issue's "committed outputs first, live runs optional"). Launch: `pip install -r requirements-dashboard.txt && streamlit run scripts/dashboard.py`. Streamlit/pandas are UI-only deps NOT in requirements-lock; the offline core is untouched
scripts/dashboard_data.py                      pure data layer for the dashboard (#354): stdlib-only load_index / value_counts / year_histogram / mechanism_cancer_matrix / filter_records / summary_stats over corpus/INDEX.jsonl, factored out of the Streamlit app so the logic is unit-tested in CI WITHOUT streamlit (tests/test_dashboard_data.py). requirements-dashboard.txt holds the optional UI deps
simulations/calibration/CALIBRATION_STATUS.md per-layer calibration tiers + roadmap (the honest sim accounting)
MODEL_CARD.md                                model card: intended use, out-of-scope, assumptions/scope checklist, per-layer calibration/validation status (consolidates CALIBRATION_STATUS for external readers; #352)
corpus/INDEX.jsonl                           master index
scripts/                                     Python pipeline
simulations/                                 Rust simulation work
simulations/ferroptosis-python/              Python bindings (PyO3)
simulations/ferroptosis-ffi/                 C FFI bindings (PhysiCell integration)
tags/                                        precomputed tag indexes
article/book-outline.md                      frozen book outline and chapter contracts
article/AUTHORING.md                         writing rules and heading conventions
news/                                        news source scaffolding (issue #99)
PROVENANCE.yaml                              content provenance and redistribution rights
CONTRIBUTING.md                              contributor setup, testing, and PR guide
CITATION.cff                                 citation metadata (renders GitHub "Cite" button)
requirements-lock.txt                        pinned Python dependency versions
FIGURES.yaml                                 figure-to-script traceability index (28 figures)
tests/test_pdf_extraction.py                 PDF text-extraction drift gate: `fetch_articles.py` + `recover_fulltext.py` turn PDFs into corpus text (and `search_books.py` reads them the same way), all via `page.get_text("text")`, and NONE had coverage -- so a PyMuPDF bump could change the corpus silently (nothing raises when whitespace/ligatures/reading order shift; the corpus just becomes heterogeneous). Found during the 1.27.2.3 -> 1.28.0 bump (MuPDF 1.27.2 -> 1.29.0) and MEASURED rather than left as a worry: the REAL `extract_text_from_pdf_bytes` returns byte-identical text under both versions in isolated venvs (1,522 chars, same SHA-256), and the suite passes under both. TWO VACUITY TRAPS THE MUTATION CHECK CAUGHT, both worth knowing: (1) the first fixture was 241 chars, UNDER the extractors' own `>= 1000` floor, so production code discarded it and the tests were asserting against a REIMPLEMENTATION of the call sites -- deleting `if text:` from the real fetch_articles left every test green; (2) with the fixture lengthened, the empty page was LAST, where the skip is undetectable because the trailing separator is absorbed by the extractor's own closing `.strip()` -- same mutation, still green. Only with the empty page in the MIDDLE do 4 guards fire. Fixture is authored byte-by-byte from stdlib (scripts/make_extraction_fixture.py, registered in PROVENANCE.yaml) and deliberately NOT built with PyMuPDF, since a change affecting both writing and reading could cancel. Pins the fixture SHA (so 'regenerate until green' is blocked), the real extractors' agreement with each other, and an INVERSE coverage check (every fitz-importing module must be accounted for, so a new extractor cannot appear unguarded). NOTABLE PROPERTY PINNED: extraction follows CONTENT-STREAM order, not visual reading order (causally confirmed: flipping only the draw order at identical coordinates flips the output, and sort=True gives the opposite). SCOPE LIMIT: base-14 Helvetica ASCII only -- no embedded/subset fonts, ligatures, CID encodings, columns or tables, which is where real-paper risk concentrates, so green here does NOT license 'this bump is safe for the corpus'
tests/test_no_vacuous_assertions.py          META-GATE (narrow, blocking): no test may assert something built only from literal constants in its own file, which compares the test to itself. AST scan allowing str/list methods ON those constants, since the shape is `CONST.count(x) == 1` and a naive no-calls scan misses it. MEASURED: ZERO instances -- and review established the motivating line was written in a DRAFT this session and replaced before commit, so this class was never merged here; the docstring says so rather than implying repo history. The detector VALIDATES ITSELF against planted samples, because a scan returning zero because it is BROKEN looks identical to a clean suite. Review also caught that its allowlist test was DORMANT (ALLOW is empty, so the loop ran nothing and passed for that reason) -- an empty-loop test being exactly this file's subject; the checker is now a function exercised against synthetic entries
scripts/audit_heading_only_assertions.py   ADVISORY (non-blocking) sibling that finds the shape which ACTUALLY recurred: `assert "<literal>" in <doc>` where the literal appears ONLY on a heading line, so the generator's unconditional title satisfies a guard meant to check the section's content. 13 hits, precision about half -- the rest are legitimate structural checks or string functions over synthetic input -- so it PRINTS rather than blocks, because a gate at 50% precision trains people to add exemptions. Four genuinely weak guards were strengthened from it (news-verification residual, replication censored series, authority-result cost, rebuild-compare clean-A/B answer), each verified to fail on an emptied section. The live case worth remembering: `assert "did NOT support" in md` matched only a heading whose whole section sits behind an `if`, and the test survived solely on its sibling assertions
tests/test_calibration_regression.py         calibration-regression gate (#499, runs in Python CI): re-asserts the data-anchored legs' committed held-out metrics (kill-switch ML162/ML210 RMSE #330, tumor-PK Kp #334, trigger-wave 5.52 µm/min #482 live-recomputed, penetration #335, spheroid geometry #333, PDT threshold #464) so a change cannot silently move a calibrated leg off its anchor; guards the science (the byte-identity SHA gate only guards the software)
.github/workflows/python-test.yml            Python CI (Linux PR/push, macOS weekly)
.github/workflows/cargo-test.yml             Rust CI (cargo test + cargo fmt --check gate)
.github/workflows/sim-tme-3d-regression.yml  sim-tme-3d production byte-identity regression (weekly + manual)
.github/workflows/living-review.yml          living-review (PRISMA-LSR) monthly incremental corpus update (#349): re-runs queries.txt vs PubMed, uploads a dated delta as an artifact (never commits)
scripts/living_review_triage.py           READS a living-review delta and says what a human should look at, because the workflow uploads one every month, never commits by design, and NOBODY HAD OPENED ONE -- two windows had accumulated, 1,855 new records in the August one with 24 landmark detections. A monitoring mechanism whose output nobody opens is not monitoring anything. THE CONFOUND IT EXISTS TO PREVENT FIRED ON THE FIRST DELTA TRIAGED: comparing the window's mechanism mix against the frozen corpus reports `cuproptosis` at 0.0% frozen vs 4.0% in the window, which reads as explosive emergence and is a TAGGING-VINTAGE ARTIFACT -- the frozen INDEX was tagged with an older mechanism vocabulary, and 38 of its records already name cuproptosis (33 under `pathway_targets`), 87 for disulfidptosis. The corpus RECOGNISED the concepts; only the `mechanisms` list is stale (#347 added them as a scaffold with frozen results deliberately untouched). So every mechanism is classified COMPARABLE / NOT-COMPARABLE against the frozen TAG VOCABULARY before any share is compared, and the not-comparable ones are never reported as movement. SECOND TRAP, in the triage itself: the frozen index carries NO abstract and NO mesh_terms, so an initial search of `abstract` returned 5 mentions instead of 38 -- a missing field reading as a small count. It searches the tag axes that actually hold the evidence. Reports high-evidence records separately (a maturity shift beats a volume shift) and states plainly that a five-week window against a decade is not a base rate
scripts/living_review_update.py               living-review delta (#349): re-runs the committed mechanism queries (queries.txt) over a trailing PubMed window, diffs vs the FROZEN corpus, writes a dated incremental index (corpus/living/<date>/) + delta changelog (analysis/living-review/<date>.md) with mechanism-tagged new records + landmark detections; pure delta/changelog logic is unit-tested, frozen corpus never mutated; corpus/living/README.md documents the frozen-vs-living split + cadence
.zenodo.json                                 Zenodo deposit metadata template
scripts/generate_release_manifest.py         SHA256 manifest + filtered archive builder
```

## Search Conventions

Prefer fast repo-native inspection first:

```bash
rg "term" scripts analysis article
rg --files corpus/by-pmid | head
sed -n '1,120p' analysis/evidence-coverage-audit.md
```

## Writing Conventions

- every strong claim should be traceable to the corpus, analysis outputs, or external verification
- separate generated outputs from handwritten interpretation notes
- use coverage-aware language such as `not detected in the local keyword-derived analysis` where appropriate
- if a known taxonomy artifact or corpus gap applies, mention it directly rather than burying it
- keep the repo open to thesis revision rather than optimizing for rhetorical neatness
