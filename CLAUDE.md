# CLAUDE.md

## Author

Ezequiel Lares

## What This Repo Is For

This is an open cancer-research workspace. The point is to help people—not to be right about one hypothesis.

The repo exists to compare therapeutic mechanisms, evidence depth, resistant-state biology, pathway targets, and simulation-based ideas honestly, so that anyone who reads it can form their own informed view. If the evidence says a direction is weak, say so. If a new direction looks promising, surface it. Don't protect old framing at the expense of clarity.

## Guiding Principles

1. **Let the evidence lead.** The repo started around a PDT/SDT-persister-ferroptosis thesis. That's still worth evaluating, but it's one lane among many. Don't treat it as the default winner.

2. **Stay open.** New modalities, targets, pathways, and interpretations should be welcomed when the corpus or external literature supports them. The README invites anyone with curiosity to contribute—the codebase should reflect that same openness.

3. **Be honest about what we don't know.** Corpus-level non-detection is provisional, not proof of absence. Missing landmark papers distort field-level claims. Taxonomy choices inflate or deflate conclusions. Say so directly rather than burying caveats.

4. **Make it reproducible.** Scripts should be re-runnable. Analysis outputs should be regenerated, not hand-edited. Separate generated files from handwritten interpretation notes so it's clear what came from the pipeline and what came from a person.

5. **Keep it human.** This project matters because cancer takes people from their families. Technical rigor serves that mission—but so does making the work accessible, welcoming contributions, and not hiding behind jargon when plain language works.

6. **The work is more important than the paper.** Don't optimize for journal word limits or publication constraints. If a finding needs context, give context. If a decision needs explaining, explain it. Clarity and contribution to the scientific community matter more than fitting a format. A reader should understand why we made each decision, what the results yield, and why we believe they signal value.

## Current Workstreams

- manuscript drafting and revision (Parts I–V substantially complete: ~115 pages, 11 chapters + 3 appendices)
- corpus fetching, enrichment, tagging, and indexing
- evidence-tier audits and coverage caveats (gold-set evaluation: 46% exact, 96% binary evidence-presence precision, 55% recall on the production keyword tagger; an off-by-default MeSH-descriptor fallback (#346, `FERRO_MESH_EXPANSION=1`) lifts gold-set evidence-detection recall to 67.8% at 95.2% precision, see `analysis/evidence-gold-mesh-eval.md` — not applied to the production corpus, so the 55% figure remains the corpus reality)
- evidence tagger v2 (#TAGGER-V2, `FERRO_EVIDENCE_V2=1`, OFF by default so `corpus/INDEX.jsonl` and every manuscript number stay byte-identical): section-scoped full text (the tagger previously never read the 216 MB of stored full text, which is why recall sat at 55%; reading it *whole* lifts recall but drops precision 96%→89% because Introduction/Discussion cite OTHER studies' trial phases, so only SELF sections — Methods/Results — are read), expanded `theoretical`/`preclinical-invitro`/`clinical-other` vocabularies, an in-vivo reagent guard ("murine … **cell line**"), an opinion-pub-type veto (editorials inherited the tier of trials they merely discussed), a theoretical-dominance rule, and a dedicated evidence prose channel that keeps structured MeSH/PubTator metadata out of the keyword matcher. Measured: 1,539 collapse, 915 refused for disagreeing spellings, 430 for a spelling that cannot be checked, 63 for a coding position in a different codon, and 36 where no position can be checked against the residue at all (a genomic coordinate or an intronic/UTR offset). THOSE LAST TWO ARE COUNTED APART because a third review pass found the report quoting every cross-class refusal as a codon FAILURE, when 36 of them were never evaluated, and mislabelling a protein-versus-GENOMIC refusal as protein-versus-coding; the honest pair is 1,526 agrees / 63 fails, i.e. the relation holds in 96.0% of the cases it can decide, which UNDERSTATED its own case as originally written. Refusing on a genomic coordinate is not over-cautious: four would be wrongly ACCEPTED if one were read as a codon, purely by coordinate coincidence. THE FIRST TWO VERSIONS WERE BOTH WRONG AND BOTH SHIPPED AS THE SAFETY PROPERTY: v1 tested PROTEIN forms only and swept the rest onto the winner (would have moved 4,527 rows across 337 rsids onto keys they do not belong to -- under rs1801131 a 17-row `p.E429A` captured all 742 rows including a 710-row `c.1298A>C` plus `c.1286A>C` and `c.1298A>T`, a different position and a different allele); v2 added internal agreement but still could not see ACROSS classes, so rs1494558 merged `p.I66T` with `c.412G>A` (codon 138) and rs2297518 merged a missense with a promoter position. The cross-class codon relation is ALSO the strongest evidence the collapses are right: it AGREES for 1,539 rsids and fails for 99. (2) READING THE MAJORITY AS CORRECT is wrong in BOTH directions across the 69 digit-drop twins: TP53 `p.R72P` and ERBB2 `p.I655V` are real rs-backed polymorphisms whose LONGER twins are the typos, while EGFR `p.T790M` is real and `p.T90M` the typo. THE RSID ADJUDICATES, being assigned independently of the extracted string -- where exactly one side carries rsids that side is canonical (32 pairs), and where neither or both do, OR where a form is one digit from TWO canonicals (KRAS `p.G1V` reaches both `p.G12V` and `p.G13V`), it ABSTAINS (37). The collision case first let dict-write order decide by ASCII sort and SHIPPED A VERDICT IN THE JSON THE CODE HAD NOT APPLIED; a guard now pins corrections_applied == adjudicated. THE ONE CASE THAT MATTERS: PubTator emits JAK2 `p.V61F`, the canonical MPN driver V617F with a digit missing, on 3,759 rows against 624 for `p.V617F`, so the unreconciled map read V617F at a seventh of its support; the verdict rests on evidence not counts, since `p.V617F` carries rs77375493 on 624 of its 624 rows, `p.V61F` carries none, and both show the identical Polycythemia Vera, Neoplasms, Primary Myelofibrosis profile in the same rank order. The 3 other majority-moving corrections rest on a SINGLE rsid-bearing row and the table says so. EVERY FIGURE IS DERIVED INCLUDING THE WORKED EXAMPLES, which name whichever case the data says is largest -- v1 hand-wrote them and shipped 'rs77375493 on 620 of 624 rows' when the truth is 624 of 624, and the FIX for that then hand-wrote the over-merge size into the docstring where it went stale within one commit; guards now grep the generator for hardcoded literals and re-derive the featured example's share and fraction word from an INDEPENDENT word list (calling the generator's own helper made the guard compare it to itself). WHAT IT IS NOT, stated before the table: CIViC/OncoKB/COSMIC curate directionality, evidence level and expert review and are the correct source for any clinical question; this measures ATTENTION not actionability, since `associate` carries no direction so 'confers resistance' and 'predicts response' land in the same bucket. 88% of pairs rest on a single paper; a variant with no CorrespondingGene is keyed SEPARATELY and never fused, since `p.G12C` alone could be KRAS, NRAS or HRAS; a bare rsid keeps its `rs` prefix (`113488022` in a variant column reads as a codon position); NLM's `[OBSOLETE]` prefix is stripped for display since it marks a retired DESCRIPTOR and '[OBSOLETE] avapritinib' reads as a withdrawn medicine; the gene table reports rows AND occurrences so 'variant-touching rows' can be checked rather than trusted. The `.tsv.gz` uses `mtime=0` so an unchanged input regenerates BYTE-IDENTICAL. analysis/atlas-variant-drug-map.{md,json,tsv.gz}, 29 guards in tests/test_variant_drug_map.py, all mutation-verified (31/31 planted mutations caught, after two review passes found 6 that survived)
scripts/atlas_module_support.py                asks, for each ferroptosis realism layer, how many DISTINCT cancer articles assert its entity relation and whether the module's own cited PMID is among them. 11 of 20 corroborated (was 9 until the #ATLAS-AMBIG fix: `fsp1` AIFM2-GPX4 read 0 because every GPX4-FSP1 relation in the census had been filed under ATL1; it now reads 6). THE HEADLINE HAS TWO DENOMINATORS and quoting only the first flatters the layers: three of the twenty claims (`system_xc`, `erastin`, `fsp1`) are CORE-ENGINE mechanisms from the manuscript's Chapter 5, not off-by-default layers, and they are three of the four best-corroborated rows -- restricted to the layers proper it is 8 of 17, not 11 of 20. Neither is a survey of the library (author-chosen proxy pairs over a subset of modules). A ZERO IS USUALLY ABOUT EXPOSURE, NOT ABOUT THE CLAIM, and reporting the shortfall flat hid that: a pair can only be asserted if BOTH entities are written about, and the weaker entity's partner count across these claims runs 6 to 2,792. Every corroborated claim has a weaker entity of at least 108; 7 of the 9 zeros fall below that (Spearman rho = 0.86, tau 0.70, permutation p < 1e-5; the association RISES to 0.88 when every GPX4 pair is dropped, and an independent mention-count measure gives 0.83 with the identical split, so it is not a hub artifact). WHAT A HOSTILE STATS REVIEW KILLED, and why the section now names NO exceptions: (1) 'the census could not have corroborated them whatever the biology is' is FALSE -- the line is a sample minimum, not a detectability limit, and 45% of all 2,840,563 asserted pairs sit below it; (2) the line rests on ONE row (gch1, a single article under the weakest predicate) and a bootstrap returns the two-exception answer only ~65% of the time; (3) DECISIVELY, running the same procedure on the pair-level co-mention column already in the same table INVERTS the correlation (rho about -0.9) and yields a DISJOINT exception set, so which zeros look 'genuine' is an artifact of the measure chosen -- the first draft named copper + hdac_persister without checking the column beside them. Also fixed: `_entity_degree` double-counted self-pairs via `for i in key` over a 2-tuple, inflating AIFM2 (which has one) and so two of the twenty claims. Now emits `atlas-module-support.json` so `census_findings.py` quotes the artifact rather than copying prose. Its limit matters as much: PubTator has no biological-process entity type, so "X induces ferroptosis" is not expressible and every claim collapses to a gene-gene or gene-chemical pair
scripts/atlas_contradictions.py                finds pairs the literature asserts in BOTH directions (7,068) and chemicals asserted both to treat and to cause a disease (9,094). THOSE TWO TOTALS USED TO EXIST ONLY IN STDOUT -- copied from a terminal into this file and stored in no artifact, so nothing could check them and nothing noticed when they drifted ~50% (4,667/6,764) as the disambiguation corrections grew and merged more entities. The committed top-500 lists are BYTE-IDENTICAL across that change (the biggest conflicts are stable while the tail grows), so the artifact could not have revealed it either. The generator now writes both totals. Ranked by the WEAKER side so a 12-vs-9 split outranks 50-vs-1. 4 of 20 simulation-module claims sit on a contested edge while citing a single paper that does not mention the disagreement
scripts/atlas_emergence_error.py               bounds the emergence layer's SAMPLING error (#ATLAS-EMERG-ERR). atlas_emergence.py computes 'share of support since 2021' from the index's 60-PMID SAMPLE, and a THRESHOLD (>=80%) applied to an unbiased estimate is NOT unbiased -- a pair whose true share sits near 80% lands on either side by the draw. Compares against the EXACT share from every dated asserting paper: 89.5% of examined pairs carry no more papers than the sample holds so their share is exact (reported separately -- pooling them would flatter the estimator with cases where it does no estimating); on the 10.5% genuinely estimated, median abs share error 0.022 / p95 0.085 and the threshold decision is 88.1% precise at 91.3% recall; across ALL examined pairs 99.0% precision / 99.3% recall, the figure a reader actually gets. Error GROWS with support (median 0.018 at 61-120 asserting papers -> 0.045 above 2,000), confirming it is sampling error rather than the SYSTEMATIC failure that preceded it (the index once kept a LEXICOGRAPHIC prefix = the oldest PMIDs, reading a true 26.6% recent share as 0.0%). Limit stated: MeSH indexing lag under-represents the newest literature, biasing every recent share DOWN, estimate and exact alike
scripts/news_verification_audit.py             measures whether the news pipeline's `verified` PMIDs support their claims (#NEWS-VERIFY), and now also whether the FIX worked. THE ORIGINAL MEASUREMENT: news/NEWS_INDEX.jsonl marked 44 claims verification_status="verified" with linked_pmids as the evidence (TWO reached the manuscript as "Verified: PMID:X" footnotes), and 55.7% of the 212 resolved claim->PMID pairs shared NO content word with the article they verified. The diagnostic was numeric clustering -- 66 of 175 identifiers shared the prefix `4202` -- and PubMed IDs are assigned sequentially as records are INDEXED, so the linker was matching on WHEN a record was indexed, not what it says (clearest case: an electric-fields/brain-cancer claim "verified" against freshwater fish biodiversity and a speech-language-pathology survey). THE RE-RUN (#615): verify_news_claims.py's two root causes were fixed (sentence-initial capitals are no longer proper nouns; supports_claim() requires >=2 shared content words), plus two the re-run itself exposed -- a claim with NO extractable term hit an early `return claim` and KEPT its stale verdict (13 bogus "verified" survived the first pass that way), and a claim flipped to unverified kept its verification_source + linked_pmids, so withdrawn evidence stayed inside the audit's own denominator. Re-verified over ALL 38 articles (`--force`; the documented `--all` skips articles whose factual claims are all already verified, which was 20 of 38 and held 10 of the bogus verdicts). RESULT: 44 verified -> 14 (30 withdrawn, 0 gained), no-content-word share 55.7% -> 1.9%, prefix clustering 66/175 -> 3/45, idempotent on re-run. READ IT AS WITHDRAWAL NOT REPAIR: the denominator fell 212 -> 53 pairs, and 19 of the 33 surviving pairs clear the two-word bar on oncology BOILERPLATE alone (cancer/trial/phase/patients/united states), which the report now counts explicitly. UNFIXED and stated: supports_claim() gates only the PubMed fallback -- the corpus path runs FIRST and accepts 2 term substrings in a title, so a weak corpus match short-circuits the search (applying supports_claim to it would drop only 1 of 10, i.e. the bar not the gate is the limit); and query construction still emits `This Phase United States`-style queries that return recently indexed records. Stated as a band not a verdict: title-only overlap can miss a genuine supporting paper and can credit an unrelated one. Re-running the linker stays OUT of atlas_pipeline.sh -- it is a reviewable data change and depends on PubMed's live index
scripts/atlas_citation_audit.py                checks whether each module's CITED PAPER is the paper it claims (#ATLAS-CITE). atlas_module_support.py exists because every layer rests on 'one or two papers the author read, cited by PMID' -- it measures how many OTHER articles assert the relation and NEVER checked the citation itself. THREE WERE BROKEN: `dhodh` cited 33864050 = a Nature NEWS ITEM on US fetal-tissue policy, `prom2` cited 31761539 = a Theriogenology paper on embryo VITRIFICATION, `gch1` cited 31919077 = a PMID that does not resolve. A broken citation reads in the support table as `cited-absent`, which looks like an extraction gap and is not. Corrected to 33981038 (Mao 2021 Nature, DHODH-mediated ferroptosis defence), 31735663 (Brown 2019 Dev Cell, Prominin2 iron export) and 31989025 (Kraft 2020 ACS Cent Sci, GTP Cyclohydrolase 1/BH4) -- each found by PubMed search and verified by EXACT TITLE against the mechanism the module documents, never guessed; the prom2 error had propagated into 3 Rust doc comments + CALIBRATION_STATUS + CLAUDE.md (all comment-only, no behaviour change). TRIAGE MATTERS: the check is entity-mention, which has false positives, so a paper mentioning neither entity but plainly IN THE FIELD is reported separately as a naming mismatch, not an error (Dixon 2012 defines System Xc- without ever writing `SLC7A11`; alox + system_xc are both this). Two tests pin the corrected PMIDs and assert no broken citation returns
scripts/atlas_contradiction_quality.py         gives the contradiction layer the ERROR BOUND it never had (#ATLAS-CONTRA-Q). atlas_contradictions.py names extraction error as a caveat and never measured it; two failure modes are now bounded with OPPOSITE answers. (1) WITHIN-PAPER self-contradiction (one paper extracted as asserting a pair both ways = inconsistency, not disagreement) happens to 1 paper in 115,024 -- effectively absent, so the conflicts really are between studies. (2) ENTITY AMBIGUITY manufacturing disagreement is real: merging two entities under one identifier merges two literatures, and pairs involving a measured sense collision are 1.45x more likely to be flagged contradictory (95% CI 1.37-1.53, 2000-resample bootstrap). THE CONFOUND IS RULED OUT, and this is the load-bearing part: colliding identifiers are contested BECAUSE they are heavily mentioned, so the crude ratio could have been pure popularity -- stratifying by directional-assertion count and pooling Mantel-Haenszel leaves it at 1.45x vs a crude 1.47x, it holds inside EVERY stratum, and it RISES with assertion count (1.36x->1.88x), the direction conflation predicts. Stated as an ASSOCIATION not an attribution (it does not license subtracting 45% of ambiguous conflicts), and the ambiguity measurement covers only top forms so contaminated-but-unmeasured pairs count as clean, biasing the ratio DOWN
analysis/atlas-coverage.md                     the frozen corpus measured against the census: it holds 0.086% of the cancer literature; 20.9% of the census has a PMC id (the ceiling on any full-text claim) against the frozen corpus's 98.7% open access
scripts/evidence_sections.py                   section-scoped full-text splitter (#TAGGER-V2): splits an article's `## Full Text` blob into SELF (Methods/Results/Trial design/...), CITED (Introduction/Discussion/Conclusion) and DROP (References/Funding/Ethics) sections, so the evidence tagger reads only what the authors assert about their OWN study. Implements the rule analysis/evidence-labeling-guidelines-v2.md already states ("not in cited references or background discussion"). Headings survive as bare blank-line-surrounded lines and are found in 98.7% of a 600-article sample; unrecognised headings INHERIT the enclosing class so Methods subsections ("Calcein assay") stay SELF without enumeration. Median SELF fraction of full text 46%. `has_methods_section()` is exposed as a diagnostic only -- measured at 26% precision as a lone none-applicable predictor, so it must never be used alone
scripts/evaluate_evidence_v2.py                evidence tagger v2 evaluation (#TAGGER-V2): runs the real production path under FERRO_EVIDENCE_V2 off/on over three label sets and writes analysis/evidence-v2-eval.md. DEV (100 v1 human labels, TUNED on, so optimistic), HELDOUT (170 records never inspected during development, labelled by the independent full-text relabel -- the generalization test, 2.59x error reduction), CONSENSUS (the 77 records where the human label and the relabel AGREE -- the conservative headline, 1.95x). Reports Wilson intervals; offline and deterministic
analysis/evidence-tagger-v2.md                 how the v2 gain was obtained, what was REJECTED and why (six clinical phrases removed for firing more on preclinical than clinical records; a "no Methods section implies review" rule at 26% precision; anchored subject-verb branch predicates that measured WORSE at 1.54x consensus), and the caveats that bound the numbers (77% annotator agreement; the held-out labels share an "ignore Discussion" methodology with the tagger, so consensus is the defensible figure). Ends with the promotion checklist -- this layer is deliberately NOT applied to the production corpus
analysis/evidence-gold-set-v3-fulltext.csv     270-record full-text relabel of the evidence gold set. MACHINE labels, not human: an independent LLM pass read each article's Methods/Results and applied the committed guidelines, blocked from every existing gold file and from the frontmatter `evidence_level`. Agreement with the 100 human v1 labels is 77.0%, concentrated on `theoretical` and the in-vitro/in-vivo boundary -- i.e. the same classes the tagger finds hardest -- so treat ~77% as the practical annotator ceiling and neither column as ground truth
tests/test_evidence_v2.py                      v2 guards: flag-OFF byte identity vs the frozen corpus; a consensus-subset error-reduction floor; section-splitter unit tests incl. the PMID 40700574 case (a mouse study whose Discussion cites a phase 3 trial); the opinion-pub-type veto; the in-vivo reagent guard; and channel separation -- that the evidence flag does not move mechanism/cancer/pathway tags (it once moved 14.3% of them) and that prose outranks a contradicting MeSH descriptor while MeSH still fills prose silence
analysis/mesh-mechanism-map.yaml               handwritten MeSH-descriptor -> mechanism reference map for #412 (precision-first leaf descriptors per mechanism + an `unmeasurable:` block with reasons); the leak classifier splits each into independent/leaky at runtime
analysis/reaction-diffusion-benchmark.md     reaction-diffusion vs exponential-proxy supply benchmark (#343): in the λ ≲ vessel-spacing regime the proxy reads optimistic (dominated by 3D point-source geometry, not consumption), sparse 62%→99% hypoxic; direction flips with λ and magnitude is representation-dependent, so directional not calibrated; PhysiCell cross-check honestly deferred
analysis/manuscript-scientific-review.md     external+internal scientific review: verified support/contradiction, math check, reframe rationale, figure roadmap
analysis/research-roadmap-2026.md            deep web-grounded gap analysis + prioritized issue backlog (#330-#354): validation/calibration, model-biology gaps, corpus, contribution strategy
analysis/contribution-plan-2026.md           contribution plan (#353): venue split (corpus vs simulation), preregistration draft of the falsifiable predictions, collaborator experiment brief
analysis/p1-wetlab-protocol.md               detailed P1 wet-lab protocol (#496): the runnable, isolatable GPX4i+FSP1i dose-matrix synergy test in an FSP1-low persister line (cell line + FSP1-status check, RSL3/ML162 by iFSP1/brequinar checkerboard, viability + C11-BODIPY + Fer-1 rescue, replicates/power, Chou-Talalay CI + Bliss analysis, pre-stated CI>0.8 falsification), with the rationale that P1 depends on the FEWEST contested assumptions (only GPX4/FSP1 parallel independence, Doll/Bersuker 2019 + Hangauer 2017). A PROTOCOL not a result: no wet-lab has been run; executing it needs a collaborator. The single biggest credibility step (first test of a prediction vs biology the model was not built on)
analysis/collaborator-brief.md               one-page collaborator/reviewer recruitment brief (#498): the ask (ferroptosis/redox biologist OR computational oncologist OR external reviewer), what the model integrates (corpus + the data-anchored legs #330/#502/#500/#333/#334/#335/#482), the P1-P8 falsifiable predictions one-liners, the E1-E6 cheapest-first experiment menu, what a co-author gains (authorship on a registered falsification-first program), and SELECTION CRITERIA (not named individuals, to avoid fabrication/privacy; identifying specific candidates is the human step). PREREGISTRATION.md Part 1 now states each P1-P8 with a quantitative model output + a numeric falsification threshold (#497, the qualitative P2/P4/P6/P7 tightened); the OSF DOI filing remains the human step
analysis/manuscript-split-outline.md         two-paper split outline (#504): operationalizes the contribution-plan venue split into section-by-section outlines for Paper A (reproducible OA keyword-scoping corpus map, lead claim = apparent gaps are search/taxonomy/OA artifacts not biology) and Paper B (embeddable ferroptosis engine led ONLY by the data-anchored legs #330/#333/#334/#335/#464/#482 + the honesty infrastructure, with the 30+ off-by-default realism layers demoted to ONE supplementary table); each paper leads with its defended claim, the book becomes background; venue choice + submission are human actions
analysis/prisma-scr-protocol.md              retrospective PRISMA-ScR scoping protocol (#505): reframes the corpus honestly as automated keyword scoping of OA literature, NOT a systematic review and not yet fully PRISMA-ScR-compliant (no a-priori protocol, no dual screening, no critical appraisal, named item-by-item); PCC research question, eligibility-as-applied, search strategy (queries.txt + documented date windows), single-automated-pipeline selection, three-axis charting, the Figure 20 (#135) flow tied to the real counts (10,415→4,830 full-text + 5,586 abstract-only), the two-recall-types table (mechanism-presence ~90% #412 vs evidence-tier 55%, never conflated), and a 20-item checklist applicability map; the lone manuscript "systematic literature search" over-claim (§3.2) is corrected to "automated keyword-driven scoping search"
analysis/ode-cross-validation.md             ODE cross-validation (#344): ferroptosis-core's bistable recover-or-collapse switch vs published models (Co 2024, Seidel 2026, Konstorum 2020); figure + script in simulations/calibration/
analysis/arbatskiy-cross-validation.md       structural cross-validation + borrowable-constant accounting vs the Arbatskiy 2024 73-ODE/93-species ferroptosis model (#471; IJMS 25:11782, DOI 10.3390/ijms252111782, PMID 39519341, CC BY). Confirms our GSH/GPX4->L-OOH + Fenton/ferritinophagy topology matches an independently-built 73-ODE model; maps which repo constants the CC BY supp tables S1-S7 could constrain (lp_rate/lp_propagation/gpx4_rate/gsh_scav/Fenton/ferritinophagy) vs which stay repo-specific (DHODH/GCH1/FSP1 are NOT in their model; their only GPX4-independent trap is alpha-tocopherol); structural not numeric (compartmental molar state + on-request model + MDPI-403 supp block a 1:1 transplant); ~120 min Fenton fast-phase is a units-free ramp cross-check for ferritinophagy_tau. No production-default change
analysis/talkington-immune-params.md         immune-parameter borrowing from the Talkington & Kearsley 2025 multiscale ICB model (#472; Comput Syst Oncol 5(2):e70007, DOI 10.1002/cso2.70007, PMID 41322398, CC BY). Documents the verified Table 1 values (kill rate r2=1.101e-7 day^-1 cell^-1 mouse-cited, exhaustion threshold n=1e4 assumed, ICB efficiency I in [0,1] swept with an 80-90%-blockade optimum) and maps them onto our immune kill probability / exhaustion_rate (#243) / multi-checkpoint residual (#264): dimensionless quantities (n, I) port cleanly, the dimensional kill rate does NOT (needs p=1-exp(-r2*local_density*dt); a raw well-mixed rate double-counts our spatial locality, the same effect behind the 104:1->4:1 3D immune-ratio shrink). Opt-in anchor, NOT swapped into defaults; mapping math encoded+tested in scripts/icb_param_map.py
analysis/sbml-export.md                      SBML export (#351): deterministic single-cell core ODE -> SBML L3V2 (simulations/sbml/ferroptosis_core.xml), libSBML-validated + roadrunner round-trip; documents exportable vs not-exportable modules
scripts/fetch_calibration_data.py              CTRPv2 ferroptosis dose-response fetch (#330 data leg): pulls the DepMap-redistributed CTRPv2 (Harmonized CTD^2 25Q2, `CTRPResponseCurves.csv`, md5 36115a26..., login-free DepMap download API) since GDSC2 lacks the ferroptosis inducers, md5-verifies, filters to ML162/ML210/erastin/CIL55/CIL56 and writes the committed per-cell-line 4-param-logistic target `analysis/calibration/ctrpv2_ferroptosis_curves.csv` (3,021 curves) + `ctrpv2_ferroptosis_summary.json`. Empirical GPX4i target: median EC50 ~0.5-0.75 µM, kill ceiling ~0.89-0.98, EC50 spread ~10x across cell lines. The raw 45MB file is NOT committed; CI never downloads (offline contract); the derived CSV is the reproducible artifact. The model fit + held-out validation is the #330 follow-up leg. See analysis/calibration/calibration-targets-ctrpv2.md
scripts/calibrate_kill_switch.py               CTRPv2 kill-switch fit (#330 fit leg): tunes the single-cell RSL3 kill switch (`lp_propagation`/`lp_rate` cascade + a µM->`rsl3_gpx4_inhib` scale K via `dose/(dose+K)`) to the in-vitro GPX4-inhibitor median dose-response, via `ferroptosis_core.sim_batch` grid search. Fit on ML162 (RMSE 0.05), held-out validated on ML210 (RMSE 0.07, same GPX4i mechanism, generalizes); the default in-vivo-tuned switch is ~11x worse (RMSE 0.57, far too RSL3-resistant for in-vitro data, e.g. Glycolytic never dies under RSL3). Calibrated lp_propagation=0.7/lp_rate=0.4/K=0.25µM. IN-VITRO only (production defaults unchanged); the cell-line-spread→phenotype-heterogeneity distributional fit is the next extension; erastin (system-xc-) reported as a cross-mechanism contrast (RMSE 0.42, expectedly worse). Needs the compiled extension (not run in CI); the committed analysis/calibration/kill-switch-calibration.{md,json} are the checked-in result. Distinct from the #330 data leg (scripts/fetch_calibration_data.py)
scripts/calibrate_pk.py                        tumor-PK measured-data anchor (#334): anchors the `tumor_pk` plasma+tumor disposition to the only public ferroptosis-specific in-vivo PK with a PAIRED plasma+tumor course, imidazole ketone erastin (IKE, system-xc- inducer; Zhang 2019, PMID 30799221), plus a sorafenib human popPK forward check (Jain 2011, PMID 21392074). Pure Python + scipy (runs in CI, no compiled extension). Headline: measured tissue:plasma partition Kp=0.90 (vs presets' estimated 0.15-0.5), ~2 h plasma→tumor delay, slower tumor clearance (t1/2 3.5 vs 1.83 h). Structural finding: the IKE plasma AUC/Cmax (2.11 h) is below the closed-form 1-compartment floor e·Tmax (3.67 h), PROVING multi-compartment disposition (a fast distribution phase) and bounding what summary NCA identifies; the plasma curve is anchored exactly to Tmax/Cmax/terminal-t1/2 and the 1-cmt AUC over-prediction (+109%) is reported as that finding, not hidden. The tumor rates derive from measured Kp + t1/2 so tumor Tmax/Cmax are out-of-fit predictions (delay DIRECTION captured, Tmax magnitude over-predicted, same omitted-fast-phase root cause). Sorafenib Cavg,ss ~4.1 mg/L at 400 BID lands in the clinical 3-5 range. RSL3/ML tool-compound PK genuinely absent (erastin's instability is why IKE was made), flagged not fabricated; per-tumor presets NOT recalibrated (no public per-tumor PK). Exports a normalized FromPk series (#239 bridge). Committed analysis/calibration/pk-calibration.{md,json}; target analysis/calibration/{pk_measured_data.csv,calibration-targets-pk.md}
scripts/validate_penetration.py                Krogh drug-penetration validation (#335): validates the `drug_transport` exponential penetration `C(r)=C0*exp(-r/lambda)` (lambda=sqrt(D/k_total)) against PUBLISHED MEASURED data. Pure Python stdlib (runs in CI). Validates (1) the exponential FORM (Primeau 2005 PMID 16361566 + Tannock 2002 PMID 11895922 both report exponential drug-vs-distance-from-vessel decline in vivo) and (2) the doxorubicin transport-reference length (model lambda=50 um, half-distance 34.7 um) which sits WITHIN Tannock 2002's measured 25-75 um and ~13% below Primeau 2005 / Minchinton 2006 (40-80 um), i.e. right form + order of magnitude, conservative end. A drift-guard parses drug_transport.rs and asserts the validated lambda equals the Rust presets. Honest scope: the RSL3-like lambda=100 um is UNVALIDATED (no ferroptosis inducer has spatial penetration data), and the binding-site barrier in #335's title is NOT a model feature (uptake is linear/lumped into lambda => penetration depth is dose-INDEPENDENT; a true binding-site barrier is saturable/dose-dependent, but a #335 data-availability review found it is an ANTIBODY phenomenon (Fujimori 1990, Saga 1995, Thurber/Wittrup 2008 penetration~sqrt(dose)), physically WEAK for the small molecules this model targets (deep binding sink barely saturates, El-Kareh/Secomb), and unvalidatable from public small-molecule data, so it is deliberately NOT added, resolving #335's binding-site clause by evidence not by building unvalidatable complexity). Committed analysis/calibration/penetration-validation.{md,json}; target analysis/calibration/penetration_measured_data.csv
scripts/calibrate_erastin.py                   System Xc-/erastin core calibration (#502 fit leg): tunes the new core `erastin_xc_inhib` dose-response (Hill `dose^h/(dose^h+K)` -> System Xc- inhibition, run under `Treatment::Control` so erastin acts ONLY by starving GSH resynthesis) to the CTRPv2 erastin median viability(dose) via `ferroptosis_core.sim_batch` grid search; the core's SECOND data-anchored inducer mechanism after the GPX4i kill switch (#330). Fit RMSE ~0.10 (a PARTIAL anchor: nails the ~4.6 uM EC50 transition, over-predicts low-dose death via the hot cascade's basal floor, saturates at the top dose), with a shared-switch check showing the #330 RSL3 cascade does NOT cleanly serve both inducers (the single-switch reconciliation is the joint multi-inducer posterior #500) and a mechanism-specificity check (erastin=0 reproduces the Control baseline). Needs the compiled extension (not run in CI); committed analysis/calibration/erastin-calibration.{md,json} are the checked-in result, guarded offline by tests/test_calibration_regression.py. Distinct from the GPX4i fit scripts/calibrate_kill_switch.py
scripts/calibration_feasibility.py         asks whether a calibration target EXISTS for the four unmodelled ferroptosis mechanisms atlas_model_gaps.py surfaced (#616), instead of writing four layers. The layer-freeze policy (CONTRIBUTING.md) requires a named target before any new off-by-default axis lands, so the question is about DATA. THE ANSWER IS NO, AND THE REASON APPLIES RETROACTIVELY: cBioPortal mRNA z-scores are standardised WITHIN each study, so the fraction of tumors below z=-1 is ~15.87% for any gene under approximate normality -- and measured across the 32 TCGA PanCancer Atlas studies, all seven genes land within two points of it (ACSL4 14.4%, GPX4 14.5%, SLC7A11 16.0%, HMOX1 14.9%, TP53 14.1%, TFRC 15.2%, KEAP1 13.8%). That is the normal distribution being recovered, not biology, so the route cannot separate a gene whose low-expression tail matters from one with no ferroptosis role, and it anchors NONE of the four. THIS CORRECTS #462's READING of the same figure: acsl4-prevalence-calibration.md called it "the real, committed population prior" for the refractory-leaning tail, and although it already noted the by-construction caveat it had no CONTROL -- the six other genes are that control, and they show the number belongs to the z-score, not to ACSL4 (the status_from_zscore BRIDGE is unaffected; it maps a scale). WHAT SURVIVES: TP53's DEEP tail, 4.3% below z=-2 against an expected 2.28% and the others' 1.5-2.1%, with much the widest inter-cancer spread -- consistent with real TP53 deletion rather than standardisation, and the one gene where an expression-derived prior would carry information. Two of the four are also already absorbed by an existing parameter (TFRC by ferritinophagy_release, KEAP1 by nrf2_gsh_rate). Recorded as four "Layers proposed and NOT built" rows in CALIBRATION_STATUS.md; a cell-line expression matrix joinable to the committed CTRPv2 curves would unblock all four at once, and the DepMap catalogue that served it now returns HTML rather than the documented CSV, so it is blocked on ACCESS not method. Committed analysis/calibration/calibration-feasibility.{md,json}, guarded by 7 tests incl. mutation-checked ones that fail if a gene stops matching the normal or if the old #462 framing returns
scripts/abc_posterior.py                       data-conditioned ABC posterior on the kill switch (#332): ABC rejection over in-vitro-spanning priors (lp_propagation/lp_rate/k_um/gpx4_rate), accepting the draws closest to the CTRPv2 ML162 dose-response, posterior-predictive-checked on held-out ML210 (coverage: 3/7 strict, 7/7 within a 0.05 viability tolerance, the gap reflecting single-cell-vs-cell-line-median curves). The LOAD-BEARING finding: the in-vivo PRCC prior ranges (lp_propagation [0.05,0.2], lp_rate [0.03,0.12]) lie ENTIRELY below the in-vitro posterior (lp_propagation 0.38..0.79..0.95, lp_rate 0.28..0.65..0.95), i.e. the in-vivo priors and in-vitro data are DISJOINT, so (1) the data-conditioned posterior is in-vitro-only (credible bands on the #330 point fit) and (2) the in-vivo/spatial HEADLINE claims cannot be conditioned on in-vitro data and stay prior-predictive (the existing headline_uncertainty.py/uncertainty_intervals.py intervals); conditioning them needs an in-vivo ferroptosis dataset (the remaining #332 dependency). Needs the compiled extension (not CI); committed analysis/calibration/abc-posterior.{md,json}. Distinct from the prior-predictive headline intervals (already shipped)
scripts/abc_joint_posterior.py                  joint multi-inducer in-vitro posterior (#500): extends the #332 single-inducer ABC to condition the SHARED single-cell switch (`lp_propagation`/`lp_rate`/`gpx4_rate`/`gsh_scav_efficiency`) JOINTLY on TWO inducer mechanisms at once, the GPX4 inhibitor ML162 (via `rsl3_gpx4_inhib`) AND the System Xc- inhibitor erastin (via the #502 `erastin_xc_inhib`, Hill dose-map, run under `Treatment::Control`), both from CTRPv2; ABC over in-vitro-spanning priors, accepted by the JOINT distance (ML162 RMSE + erastin RMSE), giving a POSTERIOR with credible intervals (not a tuned point) per parameter + a `posterior_width_frac_of_prior` identifiability column (only the erastin Hill/potency are indistinguishable from the prior; the LP cascade is well determined, corrected after the original 0.6 unconstrained-threshold was measured against an uninformative null and found to fire on well-constrained parameters (scripts/abc_posterior_information.py)), a held-out posterior-predictive check on ML210 (never in the fit; 5/7 points inside the 95% band, PP RMSE ~0.11 = generalization not training fit), and a posterior-predictive PNG. The LOAD-BEARING result re-confirms the #332 disjunction for the joint fit: the in-vitro posterior (lp_propagation ~0.40-0.99, lp_rate ~0.39-0.98) lies ENTIRELY above the in-vivo PRCC priors, so it conditions only the IN-VITRO switch magnitudes (manuscript Ch.5 now reports those CrIs) while the in-vivo/spatial HEADLINES stay prior-predictive (conditioning them needs an in-vivo ferroptosis dataset that maps onto those headlines (one that does not exist publicly; the in-vivo ferroptosis readouts that DO exist measure different observables)). Needs the compiled extension (not run in CI); committed analysis/calibration/joint-posterior.{md,json} + joint-posterior-predictive.png, guarded offline by tests/test_calibration_regression.py (disjunction + held-out coverage). Builds on scripts/calibrate_kill_switch.py (#330) + scripts/calibrate_erastin.py (#502)
scripts/validate_spheroid_structure.py         spheroid zone-geometry validation (#333): fetches Browning 2021 (eLife, PMID 34842141; github.com/ap-browning/Spheroids `AllConfocalData.csv`, 994 spheroids) size-resolved confocal structure (outer radius R + boundary fractions phi/eta), bins by R, and compares to the spheroid module's FIXED radial thresholds (rim 0.90, core 0.73 of radius = the large-spheroid limiting structure). Finding: the fixed thresholds match the data only for R >= 300 µm (diameter >= 600 µm); below that real spheroids have NO necrotic core (eta~0 for R<300; necrotic core present in 0.8%/45%/100% of <200/200-300/300-400 µm spheroids), so the model OVER-PREDICTS the persister/necrotic core for small spheroids (the necrotic core emerges near R~300 µm, the O2 diffusion length). Validates zone GEOMETRY + a documented valid size range; does NOT calibrate the per-zone biochem gradient strengths (need metabolomics) and does not cover size-resolved KILL data (the remaining #333 piece). A drift-guard test ties the Python thresholds to the Rust glycolytic_frac/oxphos_frac, and (#333 v0.51.0) it also evaluates the size-aware thresholds (size_aware_rust_constants drift-guard, mean boundary err 0.025 vs fixed 0.338). The size-resolved KILL leg is characterized separately in scripts/validate_spheroid_kill.py. Raw not committed (offline contract); committed analysis/calibration/spheroid-structure-validation.{md,json}
scripts/validate_spheroid_kill.py              spheroid RSL3 kill-vs-size: directional validation + a falsifiable prediction (#333 kill leg). Reads the committed analysis/calibration/spheroid_kill_vs_size.csv (written by `sim-tme-3d --spheroid-size-sweep`, a new off-by-default sweep mode that runs RSL3 across grid sizes 16-60 with fixed vs size-aware zone thresholds; pure-stdlib script runs in CI). (1) With FIXED thresholds the supply-gradient gives bigger-spheroids-resist-more: RSL3 kill 3.6%→0.19% over 144→540 µm radius (~19x), in the measured cytotoxic 1.8-22x range (West 1989 PDT; Eilenberger 2021 DOX/CIS), so the DIRECTION + order of magnitude is validated against generic-cytotoxic data. (2) With SIZE-AWARE thresholds a falsifiable persister-targeting prediction emerges: RSL3 preferentially kills persisters and the persister core only appears above ~280 µm, so small all-proliferating spheroids RESIST RSL3 (≈0 kill) and vulnerability emerges with the core, OPPOSITE generic cytotoxics, untestable without ferroptosis-inducer size-kill data. Kill MAGNITUDE uncalibrated (RSL3-resistant in-vivo switch + placeholder gradients); this characterizes the kill leg as far as public data allows (not a magnitude calibration). Committed analysis/calibration/spheroid-kill-vs-size.{md,json}
scripts/validate_trigger_wave.py            ferroptotic trigger-wave validation (#482, the flagship tie-to-reality leg): validates the new `trigger_wave` module (a 1-D bistable Nagumo reaction-diffusion front, `dL/dt=D*Lxx+k*L*(L-a)*(1-L)`, closed-form speed `c=sqrt(D*k/2)*(1-2a)`, `k~iron` so `c~sqrt(iron)`) against Co/Wu/Lee/Chen Nature 2024 (PMID 38987590; open code github.com/imb-lcd/ftw2024 + figshare 25762806) measured ferroptotic trigger-wave speeds: baseline 5.52 um/min, iron chelation DFO 2.33, iron loading 9.40. Pure-stdlib (runs in CI): the baseline is a one-point D*k calibration to 5.52, and the iron-dose SHAPE `c~sqrt(iron)` reproduces 2.33/5.52/9.40 at plausible iron fold-changes ~0.18/1.0/2.9 (the PREDICTED result), plus a numeric==analytic self-consistency solve and a drift-guard tying the Python BASELINE to the Rust trigger_wave.rs `baseline()`. Spatial complement of the single-cell switch (#344); committed analysis/calibration/trigger_wave_measured_data.csv (target) + trigger-wave-validation.{md,json} + calibration-targets-trigger-wave.md
scripts/embed_gold_set.py                      SPECTER embedding RECOMPUTE for the #411 semantic leg (option B dependency story): embeds the v2 evidence gold set's title+abstract (allenai-specter, 768-d) and writes the embedding VECTORS to analysis/evidence-gold-embeddings.npz (committed, ~816KB). Needs sentence-transformers/torch (run LOCALLY in a venv), NEVER added to requirements-lock.txt; CI + the eval read the committed vectors with numpy only, so the offline/pinned-deps contract holds
scripts/embed_evidence_leg.py                  semantic-retrieval evidence-detection leg (#411, the deferred second leg of #346): an OFFLINE leave-one-out k-NN over the committed SPECTER vectors that flags a record evidence-bearing when its title+abstract neighbours are predominantly evidence-bearing, combined as keyword OR MeSH OR embedding (reuses retag_gold_set._predict/_binary for the keyword+MeSH legs). Evaluated on the BALANCED v2 gold set (270 records, 32% positive, NOT v1's 87% which degenerates a majority-vote k-NN to always-positive): recovers 19/28 of the MeSH-residual false-negatives, lifting binary recall 67.8%→89.7% with NO precision drop (35.3%→39.8% on v2; the embedding's own precision exceeds the keyword baseline's). v2 recall reproduces the documented v1 55.2%/67.8% (shared positives); v2's low absolute precision is from its hard negatives, not the manuscript's v1 96%. Binary detection only (not a level); off-by-default/gold-set-validated (production recall unchanged). analysis/evidence-gold-embedding-eval.{md,json}
scripts/validate_rd_vs_biofvm.py               reaction-diffusion external cross-check vs BioFVM (#408, the deferred PhysiCell leg of #343): runs the SAME matched scenario (Dirichlet vessel sources c=1, no-flux, uniform decay, λ=√(D/k)) in the Rust `reaction_diffusion` finite-volume SOR solver AND in BioFVM (PhysiCell's independent LOD/Thomas microenvironment solver, run to steady state), comparing field shape, decay length, and magnitude. Result: the solvers AGREE, identical shape (log-field Pearson r 0.9997→1.0 single + 1.0 two-source), identical effective decay length (55.1µm single / 59.0µm two-source, both solvers; shorter than planar λ=100 because a 3D point source is the Yukawa exp(-r/λ)/r, reproduced by BOTH so it is real geometry not a Rust artifact), magnitude ratio converging to 1 as BioFVM's dt→0 (2.82→1.42→1.08 at dt 0.02→0.005→0.001, the residual being BioFVM's LOD operator-splitting error, not a disagreement). LOCAL tool (needs a compiled BioFVM driver, source + build at simulations/calibration/biofvm/; Rust side is the `rd_field_dump` example); CI reads only the committed analysis/calibration/rd-biofvm-crosscheck.{md,json}. Upgrades the RD module from analytical-self-consistency-only to externally cross-checked
simulations/calibration/biofvm/                BioFVM cross-check driver (#408): biofvm_rd_check.cpp (the matched-scenario driver) + README.md (build incantation, incl. the macOS Apple-clang workarounds: empty omp.h stub since BioFVM calls no omp_ functions, + explicit -isysroot/-isystem for libc++). BioFVM itself is not vendored (own license); the README documents cloning it
scripts/dashboard.py                           interactive Streamlit dashboard (#354): Corpus tab (sidebar filters on mechanism/cancer/evidence/year, summary metrics, mechanism + evidence-tier bar charts, per-year trend, top-10×10 mechanism×cancer matrix heatmap, filtered article table) + Simulation-sweep tab (live `ferroptosis_core.sim_batch` death-rate sweep over a chosen PRCC rate constant × phenotype × treatment when the extension is built, else degrades to the committed prior-predictive intervals, the issue's "committed outputs first, live runs optional"). Launch: `pip install -r requirements-dashboard.txt && streamlit run scripts/dashboard.py`. Streamlit/pandas are UI-only deps NOT in requirements-lock; the offline core is untouched
scripts/dashboard_data.py                      pure data layer for the dashboard (#354): stdlib-only load_index / value_counts / year_histogram / mechanism_cancer_matrix / filter_records / summary_stats over corpus/INDEX.jsonl, factored out of the Streamlit app so the logic is unit-tested in CI WITHOUT streamlit (tests/test_dashboard_data.py). requirements-dashboard.txt holds the optional UI deps
simulations/calibration/CALIBRATION_STATUS.md per-layer calibration tiers + roadmap (the honest sim accounting)
MODEL_CARD.md                                model card: intended use, out-of-scope, assumptions/scope checklist, per-layer calibration/validation status (consolidates CALIBRATION_STATUS for external readers; #352)
corpus/INDEX.jsonl                           master index
scripts/                                     Python pipeline
simulations/                                 Rust simulation work
simulations/ferroptosis-python/              Python bindings (PyO3)
simulations/ferroptosis-ffi/                 C FFI bindings (PhysiCell integration)
tags/                                        precomputed tag indexes
article/book-outline.md                      frozen book outline and chapter contracts
article/AUTHORING.md                         writing rules and heading conventions
news/                                        news source scaffolding (issue #99)
PROVENANCE.yaml                              content provenance and redistribution rights
CONTRIBUTING.md                              contributor setup, testing, and PR guide
CITATION.cff                                 citation metadata (renders GitHub "Cite" button)
requirements-lock.txt                        pinned Python dependency versions
FIGURES.yaml                                 figure-to-script traceability index (28 figures)
tests/test_pdf_extraction.py                 PDF text-extraction drift gate: `fetch_articles.py` + `recover_fulltext.py` turn PDFs into corpus text (and `search_books.py` reads them the same way), all via `page.get_text("text")`, and NONE had coverage -- so a PyMuPDF bump could change the corpus silently (nothing raises when whitespace/ligatures/reading order shift; the corpus just becomes heterogeneous). Found during the 1.27.2.3 -> 1.28.0 bump (MuPDF 1.27.2 -> 1.29.0) and MEASURED rather than left as a worry: the REAL `extract_text_from_pdf_bytes` returns byte-identical text under both versions in isolated venvs (1,522 chars, same SHA-256), and the suite passes under both. TWO VACUITY TRAPS THE MUTATION CHECK CAUGHT, both worth knowing: (1) the first fixture was 241 chars, UNDER the extractors' own `>= 1000` floor, so production code discarded it and the tests were asserting against a REIMPLEMENTATION of the call sites -- deleting `if text:` from the real fetch_articles left every test green; (2) with the fixture lengthened, the empty page was LAST, where the skip is undetectable because the trailing separator is absorbed by the extractor's own closing `.strip()` -- same mutation, still green. Only with the empty page in the MIDDLE do 4 guards fire. Fixture is authored byte-by-byte from stdlib (scripts/make_extraction_fixture.py, registered in PROVENANCE.yaml) and deliberately NOT built with PyMuPDF, since a change affecting both writing and reading could cancel. Pins the fixture SHA (so 'regenerate until green' is blocked), the real extractors' agreement with each other, and an INVERSE coverage check (every fitz-importing module must be accounted for, so a new extractor cannot appear unguarded). NOTABLE PROPERTY PINNED: extraction follows CONTENT-STREAM order, not visual reading order (causally confirmed: flipping only the draw order at identical coordinates flips the output, and sort=True gives the opposite). SCOPE LIMIT: base-14 Helvetica ASCII only -- no embedded/subset fonts, ligatures, CID encodings, columns or tables, which is where real-paper risk concentrates, so green here does NOT license 'this bump is safe for the corpus'
tests/test_no_vacuous_assertions.py          META-GATE (narrow, blocking): no test may assert something built only from literal constants in its own file, which compares the test to itself. AST scan allowing str/list methods ON those constants, since the shape is `CONST.count(x) == 1` and a naive no-calls scan misses it. MEASURED: ZERO instances -- and review established the motivating line was written in a DRAFT this session and replaced before commit, so this class was never merged here; the docstring says so rather than implying repo history. The detector VALIDATES ITSELF against planted samples, because a scan returning zero because it is BROKEN looks identical to a clean suite. Review also caught that its allowlist test was DORMANT (ALLOW is empty, so the loop ran nothing and passed for that reason) -- an empty-loop test being exactly this file's subject; the checker is now a function exercised against synthetic entries
scripts/audit_heading_only_assertions.py   ADVISORY (non-blocking) sibling that finds the shape which ACTUALLY recurred: `assert "<literal>" in <doc>` where the literal appears ONLY on a heading line, so the generator's unconditional title satisfies a guard meant to check the section's content. 13 hits, precision about half -- the rest are legitimate structural checks or string functions over synthetic input -- so it PRINTS rather than blocks, because a gate at 50% precision trains people to add exemptions. Four genuinely weak guards were strengthened from it (news-verification residual, replication censored series, authority-result cost, rebuild-compare clean-A/B answer), each verified to fail on an emptied section. The live case worth remembering: `assert "did NOT support" in md` matched only a heading whose whole section sits behind an `if`, and the test survived solely on its sibling assertions
tests/test_calibration_regression.py         calibration-regression gate (#499, runs in Python CI): re-asserts the data-anchored legs' committed held-out metrics (kill-switch ML162/ML210 RMSE #330, tumor-PK Kp #334, trigger-wave 5.52 µm/min #482 live-recomputed, penetration #335, spheroid geometry #333, PDT threshold #464) so a change cannot silently move a calibrated leg off its anchor; guards the science (the byte-identity SHA gate only guards the software)
.github/workflows/python-test.yml            Python CI (Linux PR/push, macOS weekly)
.github/workflows/cargo-test.yml             Rust CI (cargo test + cargo fmt --check gate)
.github/workflows/sim-tme-3d-regression.yml  sim-tme-3d production byte-identity regression (weekly + manual)
.github/workflows/living-review.yml          living-review (PRISMA-LSR) monthly incremental corpus update (#349): re-runs queries.txt vs PubMed, uploads a dated delta as an artifact (never commits)
scripts/living_review_triage.py           READS a living-review delta and says what a human should look at, because the workflow uploads one every month, never commits by design, and NOBODY HAD OPENED ONE -- two windows had accumulated, 1,855 new records in the August one with 24 landmark detections. A monitoring mechanism whose output nobody opens is not monitoring anything. THE CONFOUND IT EXISTS TO PREVENT FIRED ON THE FIRST DELTA TRIAGED: comparing the window's mechanism mix against the frozen corpus reports `cuproptosis` at 0.0% frozen vs 4.0% in the window, which reads as explosive emergence and is a TAGGING-VINTAGE ARTIFACT -- the frozen INDEX was tagged with an older mechanism vocabulary, and 38 of its records already name cuproptosis (33 under `pathway_targets`), 87 for disulfidptosis. The corpus RECOGNISED the concepts; only the `mechanisms` list is stale (#347 added them as a scaffold with frozen results deliberately untouched). So every mechanism is classified COMPARABLE / NOT-COMPARABLE against the frozen TAG VOCABULARY before any share is compared, and the not-comparable ones are never reported as movement. SECOND TRAP, in the triage itself: the frozen index carries NO abstract and NO mesh_terms, so an initial search of `abstract` returned 5 mentions instead of 38 -- a missing field reading as a small count. It searches the tag axes that actually hold the evidence. Reports high-evidence records separately (a maturity shift beats a volume shift) and states plainly that a five-week window against a decade is not a base rate
scripts/living_review_update.py               living-review delta (#349): re-runs the committed mechanism queries (queries.txt) over a trailing PubMed window, diffs vs the FROZEN corpus, writes a dated incremental index (corpus/living/<date>/) + delta changelog (analysis/living-review/<date>.md) with mechanism-tagged new records + landmark detections; pure delta/changelog logic is unit-tested, frozen corpus never mutated; corpus/living/README.md documents the frozen-vs-living split + cadence
.zenodo.json                                 Zenodo deposit metadata template
scripts/generate_release_manifest.py         SHA256 manifest + filtered archive builder
```

## Search Conventions

Prefer fast repo-native inspection first:

```bash
rg "term" scripts analysis article
rg --files corpus/by-pmid | head
sed -n '1,120p' analysis/evidence-coverage-audit.md
```

## Writing Conventions

- every strong claim should be traceable to the corpus, analysis outputs, or external verification
- separate generated outputs from handwritten interpretation notes
- use coverage-aware language such as `not detected in the local keyword-derived analysis` where appropriate
- if a known taxonomy artifact or corpus gap applies, mention it directly rather than burying it
- keep the repo open to thesis revision rather than optimizing for rhetorical neatness
