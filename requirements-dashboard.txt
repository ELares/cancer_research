# Optional UI dependencies for the interactive dashboard (#354), scripts/dashboard.py.
# These are NOT part of the default core/dev environment and are NOT used by CI.
# The canonical dependency metadata now lives in `pyproject.toml`; once `uv.lock`
# has been refreshed, regenerate this compatibility export with:
#     make export-python
#
# Install them only to run the dashboard locally:
#     uv lock
#     uv sync --frozen --group dashboard
#     streamlit run scripts/dashboard.py
# The corpus tab needs only the committed corpus/INDEX.jsonl. The simulation sweep
# additionally needs the compiled ferroptosis_core extension (see
# simulations/ferroptosis-python/); without it that tab degrades to read-only.
streamlit>=1.58.0
pandas>=3.0.3
