# Prior-predictive uncertainty: drug-penetration headline (#332)

Generated by `scripts/headline_uncertainty.py --headline penetration`. Propagates the same joint UNIFORM prior over the 11 PRCC rate constants through the tissue-specific vessel-wall RSL3 kill (`sim-tissue-pk`, the manuscript's 40% in 2D culture dropping to 12.1% / 2.6% / 1.8% at the vessel wall across three tissue scenarios). Each radial-bin kill is `sim_cell` under RSL3 with GPX4 inhibition scaled by the local Krogh concentration, so the 11 biochem rates drive the kill while the transport params (penetration length, vascular permeability, ECM tortuosity) are held at their FIXED, uncalibrated per-tissue presets.

## Method

- 300 Monte-Carlo draws from the joint uniform prior (seed 332, deterministic).
- The interval here is the BIOCHEM-rate uncertainty at each tissue's fixed transport scenario; the transport parameters themselves are uncalibrated placeholders (their own prior is NOT swept — there is no PRCC-style range table for them — so the across-tissue ORDERING, not the absolute per-tissue magnitude, is the robust reading).
- **Prior-predictive (forward) uncertainty over UNIFORM priors — NOT a data-conditioned posterior** (blocked on the #330 GDSC data); PARAMETER, not STRUCTURAL, uncertainty.

## Vessel-wall RSL3 death rate, by tissue

| tissue | default point | median | 95% prior-predictive interval | full range |
|---|---|---|---|---|
| Well-vascularized epithelial | 0.121 | 0.231 | [0.002, 0.926] | [0.000, 1.000] |
| Poorly-vascularized (pancreatic) | 0.026 | 0.076 | [0.000, 0.826] | [0.000, 0.975] |
| Neuroectodermal (CNS / BBB) | 0.018 | 0.043 | [0.000, 0.770] | [0.000, 0.962] |

## Scope

Completes the prior-predictive propagation of #332's headline claims over the 11 PRCC biochem rate constants (single-cell kill switch, Bliss synergy, the two sim-tme headlines, and now penetration). The remaining piece is the data-conditioned ABC posterior, blocked on the external GDSC/DepMap/PRISM drug-response data (#330); turning these uniform priors into a posterior, and sweeping the transport parameters' own (currently undocumented) ranges, are the open extensions.
