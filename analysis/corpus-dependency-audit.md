# What still needs the retrieved corpus

Generated by `scripts/corpus_dependency_audit.py`. 41 scripts read `corpus/INDEX.jsonl`, `corpus/by-pmid/` or `corpus/abstracts/`.

The rule is applied per FIELD, not per script: a consumer needs the corpus only where it reads something the census cannot supply. The census record carries `abstract`, `adjacent_ui`, `cancer_basis`, `cancer_ui`, `doi`, `journal`, `mesh`, `mesh_major`, `mesh_ui`, `pmcid`, `pmid`, `pub_types`, `title`, `year`; the open-access layer supplies full text for 1,116,481 records; and NLM supplies publication types and check tags.

| class | scripts |
|---|--:|
| needs project annotations | 19 |
| full text, available at census scale | 17 |
| census-supplied fields only | 4 |
| no record fields resolved | 1 |

**19 of 41 are genuinely corpus-bound**, and all for the same reason: they read annotations only this project produces. Everything else reads fields the census carries, or full text the census's own open-access layer carries in far greater quantity.

That is the finding worth acting on. "Needs full text" stopped meaning "needs the corpus" when the open-access layer landed, and several consumers in the middle class were kept on the corpus for a reason that expired.

## needs project annotations

- `analyze_corpus.py` — reads `cancer_types`, `evidence_level`, `mechanisms`, `pathway_targets`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `atlas_coverage.py` — reads `mechanisms`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `atlas_evidence_check.py` — reads `evidence_level`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `atlas_landscape.py` — reads `mechanisms`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `atlas_untagged_partner.py` — reads `mechanisms`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `build_evidence_v2_index.py` — reads `evidence_level`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `build_index.py` — reads `mechanisms`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `dashboard_data.py` — reads `evidence_level`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `diagnostic_therapy_audit.py` — reads `diagnostic_therapy_links`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `generate_figures.py` — reads `cancer_types`, `evidence_level`, `mechanisms`, `pathway_targets`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `living_review_triage.py` — reads `mechanisms`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `living_review_update.py` — reads `evidence_level`, `mechanisms`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `mechanism_precision.py` — reads `mechanisms`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `mechanism_recall.py` — reads `mechanisms`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `oa_bias_analysis.py` — reads `mechanisms`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `sample_evidence_gold_set.py` — reads `evidence_level`, `mechanisms`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `scope_audit.py` — reads `mechanisms`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `tag_articles.py` — reads `biology_processes`, `cancer_types`, `diagnostic_therapy_links`, `evidence_level`, `mechanisms`, `pathway_targets`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags
- `taxonomy_coverage.py` — reads `mechanisms`, which the census does not carry: NLM assigns descriptors and publication types, not this project's mechanism, cancer-type or evidence tags

## full text, available at census scale

- `build_news_index.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `census_diagnostic_chains.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `embed_evidence_leg.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `embed_gold_set.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `enrich_metadata.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `evaluate_evidence_v2.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `fetch_articles.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `fetch_semantic_scholar.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `provenance.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `rank_collaborator_candidates.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `recover_fulltext.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `retag_gold_set.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `score_news.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `split_abstract_only.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `strip_non_oa_fulltext.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `verify_news_claims.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid
- `verify_references.py` — reads corpus full text, which the open-access layer supplies for 1,116,481 census records -- roughly 230 times more than the corpus holds, keyed by the same pmid

## census-supplied fields only

- `detect_corpus_duplicates.py` — reads only `journal`, `pmid`, `title`, `year`, every one of which the census record carries
- `generate_release_manifest.py` — reads only `pmid`, every one of which the census record carries
- `landmark_coverage.py` — reads only `abstract`, `doi`, `journal`, `mesh`, `pmid`, `title`, every one of which the census record carries
- `news_verification_audit.py` — reads only `title`, every one of which the census record carries

## no record fields resolved

- `dashboard.py` — touches the corpus but no record field was resolved statically; read it before drawing a conclusion

## Named the corpus without reading it

3 file(s) mention a corpus path only in a docstring, a comment, or as a path DEFINITION, and are not consumers: `atlas_baseline.py`, `config.py`, `corpus_dependency_audit.py`. A first version of this audit counted them, which put `atlas_baseline.py` in the table on the strength of a line promising it leaves the corpus alone -- and put this file in it too, since it names every marker as a constant.

## What this does not decide

Nothing here says an analysis should be kept or cut. A dependency measurement that also issued verdicts would be making an editorial decision under cover of an arithmetic one. For several corpus-bound consumers the real question is whether the ANALYSIS is still wanted -- a gold-set evaluation of a tagger no manuscript figure now depends on is a different case from one that still calibrates something -- and that question is not answerable by reading imports.

The static read is also over-inclusive by design: it collects every string used as a subscript or `.get()` key and intersects against known field sets, so a field accessed through a variable is missed. Missing one UNDERSTATES a dependency, which is the safer direction for a document that could be used to justify deleting something.
